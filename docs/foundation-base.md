<!-- AUTO-MAINTAINED-BY: syncing-roadmap-to-docs -->
# Foundation Base — Implementation Docs

**Module:** `foundation.base` (`src/ede/foundation/base/`)
**Roadmap:** [roadmap/foundation/base/README.md](../roadmap/foundation/base/README.md)
**Status:** ✅ Delivered — Phase 1 ✅ Delivered (baseline, pre-roadmap); [Phase 2 — Model & View Extension SDK](../roadmap/foundation/base/phase-2/README.md) ✅ Delivered 2026-05-18
**Layer:** Foundation engine — platform substrate

> Source of truth is the roadmap. This doc reflects the *current built state* — what is shipped, what is partial, what gaps remain, what configuration it introduces, and how a developer or end user interacts with it. Auto-maintained by the `syncing-roadmap-to-docs` skill.

---

## 1. Functional Understanding

### What it is
<!-- SYNC-BLOCK: what -->
`foundation.base` is the platform substrate of the EDE framework. It ships three layers of capability that every other module relies on:

- **`res.*` cross-domain business masters** — geography, time, units of measure, parties, currencies, organizations, users, languages. Any model in any domain may reference these directly.
- **`ir.*` platform metadata** — menus, actions, sequences, runtime config store, model registry, RBAC permissions and roles, audit trail, data references, notification opt-ins.
- **Framework health-check** — `ping` model + `ping.listener` used by smoke tests and uptime probes.
<!-- /SYNC-BLOCK -->

### Why it exists
<!-- SYNC-BLOCK: why -->
Cross-domain primitives — countries, currencies, parties, units of measure, RBAC, sequences, menus, audit — are needed by every foundation engine and every business domain in the platform. Building them once here means no consuming module re-implements them, and no domain ends up depending on another domain just to share a master.
<!-- /SYNC-BLOCK -->

### How a user / consumer interacts with it
<!-- SYNC-BLOCK: how -->
- **End users** — masters surface under the Settings app (Geography, Currencies, Units, Partners, Organizations, Users, Languages, Menus, Sequences, RBAC roles, Audit). Health-check `ping` is internal-only.
- **Other modules** — reference `res.*` / `ir.*` model keys directly in `Reference` fields, `One2Many`, `Many2Many`, and via `env.models["res.partner"]` lookups. Manifest must list `foundation.base` in `depends` (in practice it is the root of the dependency graph so every app inherits the dependency).
- **Integration boundary** — produces cross-domain masters and metadata; consumes nothing — `foundation.base` is the root of the manifest dependency graph and has no `depends` of its own.
<!-- /SYNC-BLOCK -->

## 2. Technical Implementation

### Architecture at a glance
<!-- SYNC-BLOCK: architecture -->
```
┌──────────────────────────────────────────────────────────────┐
│  Every other foundation engine and every domain module       │
│  (foundation.auth, foundation.communication, logistics.*,    │
│   crm.*, hr.*, …)                                            │
└────────────────────────────┬─────────────────────────────────┘
                             │ depends on
                             ▼
┌──────────────────────────────────────────────────────────────┐
│  foundation.base                                             │
│  ┌──────────────────────┐  ┌──────────────────────────────┐  │
│  │ res.* business       │  │ ir.* platform metadata       │  │
│  │  masters             │  │                              │  │
│  │  country / state /   │  │  menu / action / sequence    │  │
│  │  city / timezone     │  │  config + config.log         │  │
│  │  uom / uom.category  │  │  model + model.property      │  │
│  │  partner / role /    │  │  rbac.permission / role /    │  │
│  │  address             │  │  role.binding / access.grant │  │
│  │  currency /          │  │  audit.*                     │  │
│  │  exchange.rate       │  │  data.reference /            │  │
│  │  organization / user │  │  data.cleanup.log            │  │
│  │  org.unit / language │  │  notification.setting        │  │
│  └──────────────────────┘  └──────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐│
│  │ Framework health-check — ping / ping.listener            ││
│  └──────────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────┘
```
<!-- /SYNC-BLOCK -->

### Models
<!-- SYNC-BLOCK: models -->
| Model Key | Purpose | Source File |
|---|---|---|
| `res.country` | ISO countries with code, numeric code, timezone link | [models/country.py](../src/ede/foundation/base/models/country.py) |
| `res.state` | State / province under country | [models/geography.py](../src/ede/foundation/base/models/geography.py) |
| `res.city` | City under country + state | [models/geography.py](../src/ede/foundation/base/models/geography.py) |
| `res.timezone` | IANA timezone with UTC offset | [models/geography.py](../src/ede/foundation/base/models/geography.py) |
| `res.uom.category` | UOM category (weight, volume, dimension, quantity, time) | [models/uom.py](../src/ede/foundation/base/models/uom.py) |
| `res.uom` | Unit of measure with conversion factor | [models/uom.py](../src/ede/foundation/base/models/uom.py) |
| `res.partner` | Universal party master (customer, vendor, carrier, contact) | [models/partner.py](../src/ede/foundation/base/models/partner.py) |
| `res.partner.role.master` | Role definitions assigned to partners | [models/partner.py](../src/ede/foundation/base/models/partner.py) |
| `res.partner.role` | Partner ↔ role join | [models/partner.py](../src/ede/foundation/base/models/partner.py) |
| `res.partner.address` | Address records per partner (billing, pickup, delivery, …) | [models/partner.py](../src/ede/foundation/base/models/partner.py) |
| `res.currency` | ISO 4217 currencies | [models/currency.py](../src/ede/foundation/base/models/currency.py) |
| `res.exchange.rate` | FX rates with rate-type vocabulary | [models/exchange_rate.py](../src/ede/foundation/base/models/exchange_rate.py) |
| `res.organization` | Legal entity / company | [models/organization.py](../src/ede/foundation/base/models/organization.py) |
| `res.user` | System users | [models/user.py](../src/ede/foundation/base/models/user.py) |
| `res.user.password.reset` | Transient admin password-reset wizard (Enh 10) — `__ede_transient__`, auto-GC'd by PC-11 | [models/user_password_reset.py](../src/ede/foundation/base/models/user_password_reset.py) |
| `org.unit` | Organizational unit (branch, department) | [models/org_unit.py](../src/ede/foundation/base/models/org_unit.py) |
| `res.language` | Supported languages | [models/language.py](../src/ede/foundation/base/models/language.py) |
| `ir.menu` | Web-client menu nodes | [models/menu.py](../src/ede/foundation/base/models/menu.py) |
| `ir.action` | Bound actions (window, server, URL) | [models/action.py](../src/ede/foundation/base/models/action.py) |
| `ir.sequence` | Code/number generators (`HO-YYYY-NNNNNN` style) | [models/sequence.py](../src/ede/foundation/base/models/sequence.py) |
| `ir.config` | Runtime config store | [models/ir_config.py](../src/ede/foundation/base/models/ir_config.py) |
| `ir.config.log` | Audit log of config changes | [models/ir_config_log.py](../src/ede/foundation/base/models/ir_config_log.py) |
| `ir.model` | Reflective model registry | [models/ir_model.py](../src/ede/foundation/base/models/ir_model.py) |
| `ir.model.property` | Per-model property metadata | [models/ir_model_property.py](../src/ede/foundation/base/models/ir_model_property.py) |
| `ir.rbac.permission` | Declared permission rows per module | [models/permission.py](../src/ede/foundation/base/models/permission.py) |
| `ir.role` | RBAC role definitions | [models/role.py](../src/ede/foundation/base/models/role.py) |
| `ir.role.binding` | User ↔ role binding | [models/role_binding.py](../src/ede/foundation/base/models/role_binding.py) |
| `ir.access.grant` | Resolved access grants | [models/access_grant.py](../src/ede/foundation/base/models/access_grant.py) |
| `ir.audit.*` | Audit trail | [models/audit.py](../src/ede/foundation/base/models/audit.py) |
| `ir.data.reference` | Tracking for declaratively loaded data | [models/data_reference.py](../src/ede/foundation/base/models/data_reference.py) |
| `ir.data.cleanup.log` | Audit of orphan-data cleanup runs | [models/data_cleanup_log.py](../src/ede/foundation/base/models/data_cleanup_log.py) |
| `ir.notification.setting` | Per-user notification opt-ins | [models/notification_setting.py](../src/ede/foundation/base/models/notification_setting.py) |
| `ping` | Framework health-check model | [models/ping.py](../src/ede/foundation/base/models/ping.py) |
| `ping.listener` | Health-check listener | [models/ping_listener.py](../src/ede/foundation/base/models/ping_listener.py) |
<!-- /SYNC-BLOCK -->

### Services & key code paths
<!-- SYNC-BLOCK: services -->
| Service / Class | Responsibility | Source File |
|---|---|---|
| _none — `foundation.base` ships models + metadata; services that operate on these masters live in sibling foundation engines (auth, security, communication, …)_ | | |
<!-- /SYNC-BLOCK -->

### Commands
<!-- SYNC-BLOCK: commands -->
| Command | Trigger | Effect |
|---|---|---|
| `ede.create` / `ede.read_one` / `ede.update` / `ede.delete` / `ede.search` / `ede.count` / `ede.read_group` | Generic CRUD from `CrudKernel` for every `res.*` / `ir.*` master | Standard CRUD |
| `res.organization.deactivate` | Programmatic deactivation of a company | Sets `active=False` (soft-archive) |
| `res.organization.change_country` | Programmatic country reassignment | Updates `country_id` reference |
| `res.user.register` / `res.user.set_password` / `res.user.activate` / `res.user.deactivate` | User lifecycle | Creates / updates `res.user` with password hash |
<!-- /SYNC-BLOCK -->

### Events emitted
<!-- SYNC-BLOCK: events -->
| Event | When fired | Typical subscribers |
|---|---|---|
| `ede.record.created` / `ede.record.updated` / `ede.record.deleted` | CRUD on any `res.*` / `ir.*` master | Audit (`ir.audit.*`), communication (chatter follow-up), domain modules tracking master changes |
<!-- /SYNC-BLOCK -->

### REST endpoints
<!-- SYNC-BLOCK: rest -->
| Method + Path | Purpose | Controller |
|---|---|---|
| Generic CRUD under `/api/{model_key}/...` | Standard CRUD for every registered master | `RouteController` subclasses generated by the platform |
| `GET /ping` | Framework health-check | `ping` controller |
<!-- /SYNC-BLOCK -->

### Lifecycle hooks
<!-- SYNC-BLOCK: hooks -->
| Hook key | Behavior |
|---|---|
| `pre.res.organization.delete` | `Organization.restrict_delete()` blocks hard deletes — orgs must be deactivated, not removed |
<!-- /SYNC-BLOCK -->

### State machine
<!-- SYNC-BLOCK: state-machine -->
_none — `foundation.base` masters are reference data; no per-record state machine. Lifecycle is `active=True ↔ active=False` via the soft-archive auto-filter._
<!-- /SYNC-BLOCK -->

## 3. Configuration Introduced

> Captures every knob this module adds. Two parallel tracks — foundation `pydantic-settings` (env vars / `ede.conf`) vs `ir.config` runtime store. Empty rows are fine when the module introduces no config of that kind. Missing sections are an audit failure.

### Activation
<!-- SYNC-BLOCK: activation -->
- `ACTIVE_MODULES` (in `src/ede/foundation/settings.py`): `base` — first entry; every other foundation app and every domain depends on it.
- `ACTIVE_DOMAINS`: _not applicable — foundation app_
- Manifest `depends`: _none — root of the dependency graph_
<!-- /SYNC-BLOCK -->

### Foundation-level settings (`FoundationSettings` / env vars)
<!-- SYNC-BLOCK: foundation-settings -->
| Setting Key | Type | Default | Env Var | Purpose |
|---|---|---|---|---|
| `DEFAULT_TENANT_ID` | `str` | `"system"` | `EDE_DEFAULT_TENANT_ID` | Tenant id used when no explicit tenant is bound on the request. |
| `DEBUG` | `bool` | `False` | `EDE_DEBUG` | Exposes full tracebacks on error responses; never enable in production. |
| `ENABLE_API_DOCS` | `bool` | `False` | `EDE_ENABLE_API_DOCS` | Exposes `/docs` and `/redoc` OpenAPI surfaces. |
<!-- /SYNC-BLOCK -->

### Runtime config (`ir.config` keys)
<!-- SYNC-BLOCK: ir-config -->
| Config Key | Scope | Type | Default | Purpose |
|---|---|---|---|---|
| _none_ | | | | |
<!-- /SYNC-BLOCK -->

### Declarative settings (XML `<settings>` panels)
<!-- SYNC-BLOCK: xml-settings -->
| Panel | File | Fields |
|---|---|---|
| _none yet — future enhancement_ | | |
<!-- /SYNC-BLOCK -->

### Seed data shipped
<!-- SYNC-BLOCK: seed-data -->
| Data file | What it seeds |
|---|---|
| [data/res.country.csv](../src/ede/foundation/base/data/res.country.csv) | ISO countries. |
| [data/res.country.timezones.xml](../src/ede/foundation/base/data/res.country.timezones.xml) | Country ↔ timezone associations. |
| [data/res.timezone.csv](../src/ede/foundation/base/data/res.timezone.csv) | IANA timezones with UTC offset. |
| [data/res.currency.csv](../src/ede/foundation/base/data/res.currency.csv) | ISO 4217 currencies. |
| [data/res.exchange.rate.type.csv](../src/ede/foundation/base/data/res.exchange.rate.type.csv) | Exchange-rate type vocabulary. |
| [data/res.uom.category.csv](../src/ede/foundation/base/data/res.uom.category.csv) | UOM categories (weight, volume, dimension, quantity, time). |
| [data/res.uom.xml](../src/ede/foundation/base/data/res.uom.xml) | Units of measure with conversion factors. |
| [data/res.language.csv](../src/ede/foundation/base/data/res.language.csv) | Supported languages. |
| [data/base_menus.xml](../src/ede/foundation/base/data/base_menus.xml) | Settings menu skeleton. |
| [data/res_partner_menus.xml](../src/ede/foundation/base/data/res_partner_menus.xml) | Partner-related menu nodes. |
| [data/customization_menus.xml](../src/ede/foundation/base/data/customization_menus.xml) | Customization sub-menu skeleton. |
| [data/base_permissions.xml](../src/ede/foundation/base/data/base_permissions.xml) | Initial RBAC permission rows for masters. |
| [data/ir.rbac.permission.csv](../src/ede/foundation/base/data/ir.rbac.permission.csv) | Bulk permission seeds. |
| [data/rbac_roles.xml](../src/ede/foundation/base/data/rbac_roles.xml) | Standard role definitions. |
| [data/rbac_seed.xml](../src/ede/foundation/base/data/rbac_seed.xml) | Default role bindings + access grants. |
| [data/base_setup.xml](../src/ede/foundation/base/data/base_setup.xml) | Bootstrap rows for system tenant. |
| [data/user_management_action.xml](../src/ede/foundation/base/data/user_management_action.xml) | Enh 14 — `base.action_user_management_360` client action (the "Manage access" console). |
| [demo/demo_user_access.xml](../src/ede/foundation/base/demo/demo_user_access.xml) | **Demo (opt-in)** — Enh 14: demo user Priya with a branch org, Global/Org/Branch role bindings, a time-limited grant, a team seat, and an out-of-office window, so the User Management 360 console is non-empty on a `--with-demo=foundation.base` tenant. |
<!-- /SYNC-BLOCK -->

## 4. Developer & User Notes

### Status Snapshot
<!-- SYNC-BLOCK: status-snapshot -->
| Phase | Title | Status | Roadmap |
|---|---|---|---|
| Phase 1 — Baseline | Platform-master + `ir.*` + health-check substrate | ✅ Delivered (baseline) | [roadmap/foundation/base/README.md](../roadmap/foundation/base/README.md) |
| [Phase 2](../roadmap/foundation/base/phase-2/README.md) — Model & View Extension SDK | `@api.extend_model` decorator + view inheritance + registry merge + boot validator + `ir.model.extension` mirror + admin UI + 40 pytest cases + developer guide | ✅ Delivered 2026-05-18 | [phase-2/README.md](../roadmap/foundation/base/phase-2/README.md) |

_Additional `res.*` / `ir.*` business-master models continue to be added under consumer roadmaps when they request them. Phase 2 is the exception — it's a kernel-level capability that every consumer needs, so it belongs here._
<!-- /SYNC-BLOCK -->

### Built Capabilities
> Populated only when a feature reaches ✅ Delivered in the roadmap.

<!-- SYNC-BLOCK: built -->
| Feature | Model Keys | Key Files | Roadmap Source |
|---|---|---|---|
| Geography & Time | `res.country`, `res.state`, `res.city`, `res.timezone` | [models/country.py](../src/ede/foundation/base/models/country.py), [models/geography.py](../src/ede/foundation/base/models/geography.py) | [README.md](../roadmap/foundation/base/README.md) |
| Units of Measure | `res.uom.category`, `res.uom` | [models/uom.py](../src/ede/foundation/base/models/uom.py) | [README.md](../roadmap/foundation/base/README.md) |
| Parties | `res.partner`, `res.partner.role.master`, `res.partner.role`, `res.partner.address` | [models/partner.py](../src/ede/foundation/base/models/partner.py) | [README.md](../roadmap/foundation/base/README.md) |
| Money | `res.currency`, `res.exchange.rate` | [models/currency.py](../src/ede/foundation/base/models/currency.py), [models/exchange_rate.py](../src/ede/foundation/base/models/exchange_rate.py) | [README.md](../roadmap/foundation/base/README.md) |
| Organizations & Users | `res.organization` (recursive tree via `parent_id` + `child_ids`), `res.user`, `res.language` | [models/organization.py](../src/ede/foundation/base/models/organization.py), [models/user.py](../src/ede/foundation/base/models/user.py), [models/language.py](../src/ede/foundation/base/models/language.py) | [README.md](../roadmap/foundation/base/README.md) |
| Menus & Actions | `ir.menu`, `ir.action` | [models/menu.py](../src/ede/foundation/base/models/menu.py), [models/action.py](../src/ede/foundation/base/models/action.py) | [README.md](../roadmap/foundation/base/README.md) |
| Sequences | `ir.sequence` | [models/sequence.py](../src/ede/foundation/base/models/sequence.py) | [README.md](../roadmap/foundation/base/README.md) |
| Config store | `ir.config`, `ir.config.log` | [models/ir_config.py](../src/ede/foundation/base/models/ir_config.py), [models/ir_config_log.py](../src/ede/foundation/base/models/ir_config_log.py) | [README.md](../roadmap/foundation/base/README.md) |
| Model registry | `ir.model`, `ir.model.property` | [models/ir_model.py](../src/ede/foundation/base/models/ir_model.py), [models/ir_model_property.py](../src/ede/foundation/base/models/ir_model_property.py) | [README.md](../roadmap/foundation/base/README.md) |
| RBAC & Roles | `ir.rbac.permission`, `ir.role`, `ir.role.binding`, `ir.access.grant` | [models/permission.py](../src/ede/foundation/base/models/permission.py), [models/role.py](../src/ede/foundation/base/models/role.py), [models/role_binding.py](../src/ede/foundation/base/models/role_binding.py), [models/access_grant.py](../src/ede/foundation/base/models/access_grant.py) | [README.md](../roadmap/foundation/base/README.md) |
| Audit | `ir.audit.*` | [models/audit.py](../src/ede/foundation/base/models/audit.py) | [README.md](../roadmap/foundation/base/README.md) |
| Data references | `ir.data.reference`, `ir.data.cleanup.log` | [models/data_reference.py](../src/ede/foundation/base/models/data_reference.py), [models/data_cleanup_log.py](../src/ede/foundation/base/models/data_cleanup_log.py) | [README.md](../roadmap/foundation/base/README.md) |
| Notifications opt-in | `ir.notification.setting` | [models/notification_setting.py](../src/ede/foundation/base/models/notification_setting.py) | [README.md](../roadmap/foundation/base/README.md) |
| Health-check | `ping`, `ping.listener` | [models/ping.py](../src/ede/foundation/base/models/ping.py), [models/ping_listener.py](../src/ede/foundation/base/models/ping_listener.py) | [README.md](../roadmap/foundation/base/README.md) |
| Phase 2 — Model & View Extension SDK | `ir.model.extension` | [kernel/extensions.py](../src/ede/core/kernel/extensions.py), [registry.py](../src/ede/core/registry.py) (`register_extension` / `_merge_extension_into_base` / `validate_extensions` / `list_extensions`), [metadata_builder.py](../src/ede/core/adapters/persistence/sqlalchemy/metadata_builder.py) (soft-FK degradation), [dsl/parser.py](../src/ede/core/services/presentation/dsl/parser.py) (`<extend>` element), [view_registry.py](../src/ede/core/services/presentation/view_registry.py) (`compose_view_xml`), [ir_model_extension.py](../src/ede/foundation/base/models/ir_model_extension.py), [docs/foundation-base-extensions.md](foundation-base-extensions.md) | [Phase 2 README](../roadmap/foundation/base/phase-2/README.md) |
| Enhancement 05 — `res.user` Org Assignment Fields + Cascade Validator (org-only after Enh 07 Slice 2) — 2 surviving fields `allowed_organization_ids` M2M + `default_organization_id` Reference + `UserOrgUnitAssignmentError` with stable `code` strings + `default_org_not_in_allowed` cascade invariant on pre.create / pre.update hooks + legacy `organization_ids` deprecation shim + 5 pytest cases. Unit-related fields (`allowed_unit_ids` / `default_unit_id`) and their 3 cascade invariants retired by Enh 07 Slice 2. Shipped with the framework `fields.Many2Many(rel=, column_left=, column_right=)` enhancement that unblocks the join-table-name collision between the legacy `organization_ids` M2M and the new `allowed_organization_ids` M2M. | declarative on `res.user` | [models/user.py](../src/ede/foundation/base/models/user.py) (2 fields + 2 hooks + `_validate_user_org_unit_assignment` + `UserOrgUnitAssignmentError`) · [kernel/fields.py](../src/ede/core/kernel/fields.py) (`Many2Many(rel=, column_left=, column_right=)`) · [orm/relational.py](../src/ede/core/orm/relational.py) (`m2m_join_info(..., field_spec=)`) · [kernel/schema.py](../src/ede/core/kernel/schema.py) (build-loop override) · Alembic [c3e8f2b1a479_user_org_unit_assignment.py](../src/ede/foundation/base/migrations/versions/c3e8f2b1a479_user_org_unit_assignment.py) · [tests/foundation/test_user_org_unit_assignment.py](../src/tests/foundation/test_user_org_unit_assignment.py) | [enhancements/05-user-org-unit-assignment.md](../roadmap/foundation/base/enhancements/05-user-org-unit-assignment.md) ✅ 2026-05-25 (unit fields retired by Enh 07 Slice 2 on 2026-05-26) |
| Enhancement 07 — Organization Hierarchy (Collapse Org + Unit into one recursive `res.organization` tree) ✅ — `res.organization.parent_id` + inverse `child_ids` + `SettingsService.get_value` cascade walking `parent_id` with 3-state scope label `System` / `Organization` / `Inherited` + hierarchical `OrganizationSwitcher` as the single switcher + `/api/auth/me` `parent_id` per org. **Slice 2 (Complete Removal)** drops `res.organization.unit` table + `res_user_allowed_unit_rel` M2M + `res_user.default_unit_id` + `res_partner.unit_id` + `ir.session.active_unit_id` column + `active_unit_id` JWT claim + `Env.active_unit_id` slot + `with_active_unit_id` / `with_unit_test` + `POST /api/auth/switch-unit` + `SessionService.switch_active_unit` + `_assert_unit_allowed` + `_allowed_unit_ids` + `_resolve_initial_unit_for_login` + me.py unit fallback + `@api.model(unit_scope=...)` decorator + `apply_unit_filter` + `augment_with_unit_filter` + `_UNIT_SCOPE_BYPASS_MODELS` + `unit_scope_hooks` module + bootstrap registration + `base.allow_multiple_units` ir.config toggle + `base.default_unit_name_template` ir.config key + 3 multi-unit guard hooks + auto-stamp hook + `SECURITY_REQUIRE_ACTIVE_UNIT` setting + `BranchSwitcher.tsx` + 12 vitest + 3 E2E + 17 unit_scope pytest + 7 unit-invariant pytest. Repoints 11 FK constraints (`res_team.org_unit_id` + 10 logistics `branch_id`/`entity_id` fields) from `res_organization_unit` to `res_organization`. Ships `res.user.grant_all_branches` admin command + form-header button as the UX shortcut at grant-creation time. | `res.organization` (tree shape) | [models/organization.py](../src/ede/foundation/base/models/organization.py) (`parent_id` + `child_ids`) · [models/user.py](../src/ede/foundation/base/models/user.py) (`grant_all_branches` command) · [views/res_user_views.xml](../src/ede/foundation/base/views/res_user_views.xml) (button + Access section) · Alembic [e4f8b2c91a37_enh07_slice2_drop_org_units.py](../src/ede/foundation/base/migrations/versions/e4f8b2c91a37_enh07_slice2_drop_org_units.py) + [f3a7e1c298b4_enh07_slice2_drop_session_active_unit.py](../src/ede/foundation/auth/migrations/versions/f3a7e1c298b4_enh07_slice2_drop_session_active_unit.py) · [tests/foundation/test_grant_all_branches.py](../src/tests/foundation/test_grant_all_branches.py) | [enhancements/07-organization-hierarchy.md](../roadmap/foundation/base/enhancements/07-organization-hierarchy.md) ✅ 2026-05-26 (both slices) |
| Enhancement 10 — Admin Password Reset (transient wizard) — first consumer of [Platform Change 11](platform-11-autovacuum-and-transient-models.md) transient models + [presentation Enh 10](foundation-presentation.md) record-bound actions. `res.user.password.reset` `TransientDataModel`; pre-create hook gates `system_admin`, applies via `res.user.set_password`, strips the clear password before storing; surfaced as a **record-bound `Change Password` action in the `res.user` Actions ▾** (pre-fills `user_id` from the open record), not a global menu; tests. | `res.user.password.reset` | [models/user_password_reset.py](../src/ede/foundation/base/models/user_password_reset.py) · [views/res_user_password_reset_views.xml](../src/ede/foundation/base/views/res_user_password_reset_views.xml) · [data/res_user_password_reset.xml](../src/ede/foundation/base/data/res_user_password_reset.xml) · Alembic [cf1ec4d1d4aa](../src/ede/foundation/base/migrations/versions/cf1ec4d1d4aa_res_user_password_reset_transient_wizard.py) · [tests/foundation/test_password_reset.py](../src/tests/foundation/test_password_reset.py) | [enhancements/10-admin-password-reset.md](../roadmap/foundation/base/enhancements/10-admin-password-reset.md) ✅ 2026-06-06 |
| Enhancement 14 — **User Management 360 Console** — a per-user access dashboard opened from the Users list via **Manage access** (`base.action_user_management_360` client action). One aggregate read (`GET /api/user-management/users/{id}/overview`, gated on the caller, read under `env.sudo()`) backs a full-width Jira-clean dashboard: role bindings + direct grants + collapsible effective-access preview (reuses `PrincipalEnricher`), separate **Recent Activity** (record audit) + **Change Logs** (`ir.rbac.binding.change.log`) cards, Organizations (searchable grant/revoke **hierarchy**, default locked, non-cascading), Teams, Availability (OOO), Sessions, and a **⚙ Roles & types** slide-over that edits RBAC roles / permissions / record rules / team roles / team types from the same screen (reference inputs reuse `Many2OneField` over `ir.model` / `ir.rbac.role` / `res.organization` — no new widgets). **Model change:** `ir.rbac.role.binding.scope_type` collapsed to **`GLOBAL / ORG / BRANCH`** (dropped `TENANT` + `DEPARTMENT`; zero-schema-change — Text-backed enum), where Organization = a parent-less `res.organization` and Branch = one with a parent. Ships `demo/demo_user_access.xml`. | `ir.rbac.role.binding` (scope_type), `ir.rbac.access.grant`, `ir.user.out.of.office`, `res.team.*` (surfaced) | [api/user_management_routes.py](../src/ede/foundation/base/api/user_management_routes.py) · [models/role_binding.py](../src/ede/foundation/base/models/role_binding.py) (scope enum + change-log) · [services/principal_enricher.py](../src/ede/foundation/base/services/principal_enricher.py) · [frontend/components/UserManagement360.tsx](../src/ede/foundation/base/frontend/components/UserManagement360.tsx) · [frontend/components/MastersPanel.tsx](../src/ede/foundation/base/frontend/components/MastersPanel.tsx) · [frontend/client-actions/user-management-360.tsx](../src/ede/foundation/base/frontend/client-actions/user-management-360.tsx) · [data/user_management_action.xml](../src/ede/foundation/base/data/user_management_action.xml) · [views/res_user_views.xml](../src/ede/foundation/base/views/res_user_views.xml) (Manage-access button + Teams column) · [demo/demo_user_access.xml](../src/ede/foundation/base/demo/demo_user_access.xml) · [tests/foundation/test_user_management_360.py](../src/tests/foundation/test_user_management_360.py) | [enhancements/14-user-management-360-console.md](../roadmap/foundation/base/enhancements/14-user-management-360-console.md) ✅ 2026-07-07 |
<!-- /SYNC-BLOCK -->

### Known Gaps
> Mirrored from gap tables in the roadmap (🔴/🟠/🟡 entries).

<!-- SYNC-BLOCK: gaps -->
| Gap | Severity | Roadmap Reference |
|---|---|---|
| **Field-Change Audit Log (`ir.field.change.log`)** — generic append-only audit primitive; `__ede_audit_fields__` opt-in; `<audit-trail/>` DSL element; retention sweep through `foundation.jobs`. **Hard prereq for `logistics.sales-crm` Phase 2 · 03 slice 1.** First consumer is sales-crm; every domain that needs commercial / regulatory / compliance audit gets one primitive instead of N hand-rolled tables. | 🔴 Not Started (drafted 2026-05-25) | [enhancements/01-field-change-audit.md](../roadmap/foundation/base/enhancements/01-field-change-audit.md) |
| **Tenant Base Currency + Dual-Currency Storage** — one base currency per tenant; auto-stamped `_base_currency` sibling Decimal on every opt-in monetary field. Surfaced by sales-crm Phase 2 · 03 but cross-cutting (every domain with a money field). | 🔴 Not Started — design stub (drafted 2026-05-25) | [enhancements/02-res-currency-base-currency.md](../roadmap/foundation/base/enhancements/02-res-currency-base-currency.md) |
| **`res.partner` Duplicate Merge Governance** — `res.partner.merge` command + append-only audit + `data_steward` role + preview endpoint + cross-model FK discovery via `ir.model.field` registry. Surfaced by sales-crm Phase 2 · 03 but cross-cutting (every domain with `partner_id`). | 🔴 Not Started — design stub (drafted 2026-05-25) | [enhancements/03-res-partner-merge.md](../roadmap/foundation/base/enhancements/03-res-partner-merge.md) |
| **`res.organization.unit` Model + Per-Org Multi-Branch Toggle** ✅ — typed `res.organization.unit` model FK'd to `res.organization` (replaces polymorphic `ir.org.unit` — retired in Slice 4); auto-stamps one unit on org creation via `base.default_unit_name_template`; `base.allow_multiple_units` org-scoped `ir.config` toggle surfaced in General Settings; pre-create cap hook + pre-set toggle-down hooks; `Settings → Organization & Units → Organizations / Units` menu group. **All 4 slices delivered 2026-05-25**: (S1) typed model + admin views + menu + 4 RBAC rows + Alembic `b1d8c5e2a479` + 11 tests. (S2) auto-stamp hook + 9 tests. (S3) `ConfigurationError` + cap + toggle-down hooks + 10 tests. (S4) legacy retirement — Alembic `f7a2c91e8b46` backfills `ir_org_unit` → `res_organization_unit` preserving `record_uuid` + rewires 11 FK constraints (1 foundation `res_team.org_unit_id` + 10 logistics: `crm_inquiry/lead/opportunity/quote/handover/organization_profile.branch_id` + `pricing_rate.branch_id/entity_id` + `pricing_margin_rule.branch_id/entity_id`) with per-table `existing_tables` probe so logistics-less tenants upgrade cleanly + drops `ir_org_unit` table. Python source repointed (`res.team.org_unit_id` + 10 logistics `fields.Reference` declarations). Deletions: legacy model + view file + 4 RBAC rows + admin action + menu leaf + 5 bypass-list entries (`domain_filter` × 2, `company_scope_hooks`, `unit_scope_hooks`, `record_rule_engine`). `rbac_routes.py` `/org-units` rewired to dispatch against `res.organization.unit` (URL preserved for back-compat). **2830 pytest + 574 vitest** full suite green; zero production data loss; downstream consumers (Enh 05 + foundation.security Enh 01 BranchSwitcher) target the typed model. | ✅ Delivered 2026-05-25 (all 4 slices) | [enhancements/04-organization-units-model.md](../roadmap/foundation/base/enhancements/04-organization-units-model.md) |
| **`res.user` Org-Unit Assignment Fields + Cascade Validators** ✅ — 4 new fields on `res.user` (`allowed_organization_ids` M2M, `default_organization_id` Reference, `allowed_unit_ids` M2M, `default_unit_id` Reference) + `UserOrgUnitAssignmentError` with stable `code` strings + 4 cascade invariants enforced via `pre.res.user.create`/`pre.res.user.update` hooks (default_org_not_in_allowed / unit_org_not_in_allowed / default_unit_not_in_allowed / default_unit_org_mismatch) + single-unit auto-resolution + Alembic migration `c3e8f2b1a479` + 12 pytest cases. Shipped alongside a 1-file framework enhancement: `fields.Many2Many(target, *, rel=None, column_left=None, column_right=None)` accepting optional join-table + column-name overrides (used by `allowed_organization_ids` to avoid the collision with the legacy `organization_ids` M2M). Legacy `organization_ids` kept as one-release deprecation shim. Unblocks `foundation.security` Enh 01 BranchSwitcher (uses these fields for the default-org / default-unit auto-selection at login). | ✅ Delivered 2026-05-25 | [enhancements/05-user-org-unit-assignment.md](../roadmap/foundation/base/enhancements/05-user-org-unit-assignment.md) |
| **Team Substrate (`res.team` + `res.team.type` + `ir.team.role` + `res.team.role.assignment` + `TeamRoleService`)** — ✅ Delivered 2026-05-25. Generic team primitive distinct from legal org-unit hierarchy; team-functional roles decoupled from RBAC `ir.role`; sequence-aware role assignment for escalation; shared `TeamRoleService` consumed by both `foundation.approval` and `foundation.workflow`. Adds `res.user.team_ids` M2M + `primary_team_id` Reference. New `Settings → Teams & Roles` menu group with 4 child leaves + 16 RBAC permission rows. Bundled platform fix: M2M soft-FK degradation in `metadata_builder._build_join_tables`. 20 new pytest cases · 2617 pytest + 564 vitest green · `bun run build` clean. Developer guide: [docs/foundation-base-team-substrate.md](foundation-base-team-substrate.md). **Unblocks foundation.approval Enh 01, foundation.workflow Enh 01, logistics.sales-crm Enh 09, and eventual sales-crm Phase 2 · 02 Pricing Approval.** | ✅ Delivered 2026-05-25 | [enhancements/06-team-substrate.md](../roadmap/foundation/base/enhancements/06-team-substrate.md) |
| **Organization Hierarchy — Collapse Org + Unit into one Recursive Model** ✅ Delivered 2026-05-26 (both slices). See Built Capabilities row above for the full surface inventory. | ✅ Delivered (both slices) | [enhancements/07-organization-hierarchy.md](../roadmap/foundation/base/enhancements/07-organization-hierarchy.md) |
| **Delegation Inheritance Kernel + Org/User Partner Shadow** — Drafted 2026-05-26. Adds `@api.model(delegated={"parent.model": "fk_field"})` decorator kwarg as a declarative replacement for hand-wired `_ensure_partner_for_user`-style auto-create + sync hooks. **Motivating gap**: `res.organization` today has no `partner_id` shadow, so an org has no contact identity to anchor outbound email senders / notification "from" addresses / per-branch defaults on — asymmetric with the existing `res.user.partner_id` automation. **Kernel behavior**: decorator splits create/update payloads by field ownership, creates parent row first, stamps FK, then self. Field reads stay **explicit** (no `__getattr__` magic per design choice) — consumers write `org.partner_id.name`. **Scope**: only `name` field delegates initially (minimal blast radius); `code` / `is_active` / `timezone_id` / `language_id` stay org-specific. `on_delete="set null"` on the FK — partners survive org delete; orgs survive partner delete via NULL slot. **Three slices**: **(S1) Kernel feature** — decorator + boot validation (FK exists / targets right parent / no field-name collision) + CrudKernel create/update split + ≥15 pytest cases. **(S2) `res.organization` adoption** — add `partner_id` Reference + delegation declaration + backfill shadow partners for every existing org + drop `res_organization.name` column + repoint readers across views/controllers/tests. **(S3) `res.user` migration** — drops `_ensure_partner_for_user` helper + `_sync_partner_from_user` pre.update hook + `res_user.name` column; delegation handles it declaratively. Each slice independently shippable. **Composes with Enh 07** — every node in the org tree (parent or branch) gets its own shadow partner via auto-create, exactly the per-branch outbound-email-sender scenario from the connectors discussion. Reusable kernel feature for any future shadow-bearing model with a single decorator line. | ✅ Delivered 2026-05-26 (all 3 slices) | [enhancements/08-organization-partner-shadow-via-delegation.md](../roadmap/foundation/base/enhancements/08-organization-partner-shadow-via-delegation.md) |
| **Settings App as a Client Action (Multi-Panel Shell)** — port the legacy `src/frontend/src/workspace/settings/` Settings shell verbatim onto `src/frontend` as a menu-attached client action. `foundation.base.api.settings_routes` already exposes the backend (`GET /api/settings` lists modules, `GET /api/settings/{module}` returns sections + fields, `PATCH /api/settings/{module}` writes values); the new workspace client has zero Settings UI today (menu routes to a stub). **One menu-attached client action `open_settings`** mounts via `ClientActionPage` (URL-pushed at `/wc/<org>/settings`, deep-linkable via `?module=<key>`). Layout XML carries the static shell chrome (Save / Discard / Refresh buttons + status hint); SettingsShell component owns the dynamic content (left-rail SettingsNav + right-pane SettingsPane). Component decomposition ports verbatim from legacy: `SettingsShell` → `SettingsNav` + `SettingsPane` → `SettingsSection` → `SettingsField` + per-type renderer (`fields/{Boolean,Integer,Char,Selection,Many2One}Field.tsx`). Many2One renderer reuses `ReferenceCombobox` from WS 4.3. **Dirty-state machine** matches legacy verbatim (`isDirty` ref, confirm-on-nav, confirm-on-close, error-keeps-dirty, save-returns-full-panel). **Backend additions** (small): wire `depends_on` predicate-gated field visibility (the type slot exists in legacy; implementation defers to Enhancement 04's safe-eval substrate); add `@api.rbac_required("settings.write")` decorator to the PATCH route + seed the new permission row against `system_admin`; document panel ordering convention. New `ISettingsService` interface in `ServiceRegistry`. Tests planned: 47 cases (17 pytest covering `depends_on` filter + RBAC + round-trip; 30 vitest covering shell composition + dirty state + per-type field rendering + service HTTP shape). **Depends on**: foundation.presentation Enhancement 08 (Client-Action Lifecycle + Registry) — the substrate this enhancement is the canonical menu-attached adopter of (first dialog-only adopter is `equipment_3d_viewer`; first dual-surface adopter is `storage_drive`; this is the menu-only adopter). New developer guide planned at `docs/foundation-settings-developer-guide.md`. | 🔴 Not Started (drafted 2026-06-04) | [enhancements/09-settings-app-client-action.md](../roadmap/foundation/base/enhancements/09-settings-app-client-action.md) |
<!-- /SYNC-BLOCK -->

### Things developers commonly get wrong
<!-- HAND-AUTHORED — preserved across syncs -->
- Putting a domain-specific master in `foundation.base` to "share later". If only one domain uses it, it belongs in that domain. The bar for `res.*` is **two or more ERP domains would reference it**.
- Duplicating an existing platform master. Before creating a model, search `foundation.base/models/` — the master almost certainly exists.
- Referencing a `res.*` master by `dbid` instead of `record_uuid`. All FKs across the platform point at `record_uuid`.
- Hard-deleting an organization. `pre.res.organization.delete` vetoes the operation — soft-archive via `res.organization.deactivate` instead.
- Declaring a permission row in `foundation.base` for a domain feature. Each module ships its own permission rows in its own `data/`; `foundation.base` only ships the engine and the platform-wide permissions.

### Migration / upgrade notes
<!-- SYNC-BLOCK: migration -->
- This module is the migration root — its initial Alembic revision is the parent of every other foundation/domain migration tree.
- `res.partner.address` was added when `logistics.masters` Phase 1 needed it — additions to `foundation.base` are driven by the first consumer that needs them, not by speculative design.
<!-- /SYNC-BLOCK -->

### Permissions / RBAC
<!-- SYNC-BLOCK: rbac -->
| Role | Permissions |
|---|---|
| Seeded standard roles | Defined in [data/rbac_roles.xml](../src/ede/foundation/base/data/rbac_roles.xml); bound via [data/rbac_seed.xml](../src/ede/foundation/base/data/rbac_seed.xml). Permission rows for masters live in [data/ir.rbac.permission.csv](../src/ede/foundation/base/data/ir.rbac.permission.csv) and [data/base_permissions.xml](../src/ede/foundation/base/data/base_permissions.xml). |
<!-- /SYNC-BLOCK -->

### Related modules
<!-- SYNC-BLOCK: related -->
- [Platform Execution Rules](../roadmap/platform/00-execution-rules.md)
- [Roadmap Tracker](../roadmap/roadmap-tracker.md)
- [Foundation Model Naming](foundation-model-naming.md)
- Every other foundation engine — `foundation.auth`, `foundation.communication`, `foundation.notifications`, `foundation.approval`, `foundation.presentation`, `foundation.security`, `foundation.workflow`, `foundation.i18n`, `foundation.l10n`, … — and every domain module depend on this one.
<!-- /SYNC-BLOCK -->

---

*Last sync: 2026-07-07 (foundation.base Enhancement 14 — **User Management 360 Console ✅ Delivered**. Per-user access dashboard opened from the Users list via **Manage access** (`base.action_user_management_360` client action): one aggregate read endpoint (`GET /api/user-management/users/{id}/overview`) backs a full-width Jira-clean console — role bindings + direct grants + effective-access preview (reuses `PrincipalEnricher`), separate Recent Activity + Change Logs cards, Organizations (searchable grant/revoke hierarchy), Teams, Availability (OOO), Sessions, and a ⚙ Roles & types masters slide-over. Reference inputs reuse `Many2OneField` (`ir.model` / `ir.rbac.role` / `res.organization`). **Model change:** `ir.rbac.role.binding.scope_type` collapsed to `GLOBAL / ORG / BRANCH` (zero-schema-change Text-backed enum). Ships `demo/demo_user_access.xml`. `./run_tests.sh` 5296 passed + 626 vitest + `bun run build` clean (branch `main-fond_user_management-dharmang`, rebased onto current `main`; post-rebase drift reconciled — `granted_by`→`granted_by_id`, `ir.user.out_of_office`→`ir.user.out.of.office`). Built Capabilities row 14 + Seed-data rows (action + demo) added; README row 14 + enhancement file flipped ✅. Runtime demo smoke blocked on sqlite by the unrelated communication migration `846a5dca05e4`; UI golden-path e2e via `ede e2e record` is a tracked follow-up. Previous sync: 2026-06-06 (foundation.base Enhancement 10 — **Admin Password Reset (transient wizard) ✅ Delivered** — first consumer of [Platform Change 11](platform-11-autovacuum-and-transient-models.md) (`@api.autovacuum` + `__ede_transient__`). New `res.user.password.reset` `__ede_transient__` model whose pre-create hook gates `system_admin`, applies the reset via `res.user.set_password`, and strips the clear-text password before storing; reachable at Settings → Users & Access → Change User Password (`target=dialog`). Alembic `cf1ec4d1d4aa`; 8 tests; 3615 pytest + 392 vitest green; UI wired, browser-walkthrough pending. Built Capabilities + Models blocks updated; README row 10 added. Previous sync: 2026-06-04 (foundation.base Enhancement 09 — **Settings App as a Client Action (Multi-Panel Shell) — 🔴 Not Started** authored at [`roadmap/foundation/base/enhancements/09-settings-app-client-action.md`](../roadmap/foundation/base/enhancements/09-settings-app-client-action.md). Closes UI revamp WS 6.2 (Settings app) by porting the legacy `src/frontend/src/workspace/settings/` shell verbatim onto `src/frontend` as the canonical menu-attached client action — the third adopter of [foundation.presentation Enhancement 08's](../roadmap/foundation/presentation/enhancements/08-client-action-lifecycle-and-registry.md) client-action substrate (first dialog-only adopter is `equipment_3d_viewer`; first dual-surface adopter is `storage_drive` ([Storage Enhancement 01](../roadmap/foundation/storage/enhancements/01-frontend-drive-client-action.md)); this is the canonical menu-only adopter — full-page, URL-pushed, no record context). **Backend already complete** — `foundation.base.api.settings_routes` exposes `GET /api/settings` (list modules), `GET /api/settings/{module}` (sections + fields), `PATCH /api/settings/{module}` (write values); this enhancement is purely a frontend port plus a few small backend additions: `depends_on` predicate-gated field visibility (wires through to Enhancement 04's safe-eval substrate), `@api.rbac_required("settings.write")` decorator on the PATCH route + new `settings.write` permission row against `system_admin`, return-full-panel-on-write contract documented. **One client action `open_settings`** mounts via `ClientActionPage` host at `/wc/<org>/settings` with deep-link via `?module=<key>`. Layout XML carries the static shell chrome (Save / Discard / Refresh buttons + status hint); `SettingsShell` component owns the dynamic content (left-rail `SettingsNav` + right-pane `SettingsPane`). **Component decomposition ports verbatim from legacy** — `SettingsShell` → `SettingsNav` + `SettingsPane` → `SettingsSection` → `SettingsField` + per-type renderers (`fields/{Boolean,Integer,Char,Selection,Many2One}Field.tsx`); `Many2OneField` reuses `ReferenceCombobox` from WS 4.3. **Dirty-state machine** matches legacy 1:1 (`isDirty` ref, confirm-on-nav, confirm-on-close, error-keeps-dirty, save-returns-full-panel). New `ISettingsService` interface in `ServiceRegistry`. Tests planned: 47 cases (17 pytest covering `depends_on` filter + RBAC + round-trip; 30 vitest covering shell composition + dirty state + per-type field rendering + service HTTP shape). New developer guide planned at `docs/foundation-settings-developer-guide.md`. Known Gaps gains the matching 🔴 row. Previous sync: 2026-05-26 (Enhancement 07 — Organization Hierarchy ✅ **Delivered (both slices)** — Slice 2 Complete Removal landed. Drops the entire unit axis: `res.organization.unit` table + `res_user_allowed_unit_rel` join + `res_user.default_unit_id` + `res_partner.unit_id` + `ir.session.active_unit_id` column + JWT claim + `Env.active_unit_id` slot + `with_active_unit_id` / `with_unit_test` clone methods + `POST /api/auth/switch-unit` controller + `SessionService.switch_active_unit` + `_assert_unit_allowed` + `_allowed_unit_ids` + `_resolve_initial_unit_for_login` + me.py unit fallback fork + `@api.model(unit_scope=...)` decorator + `apply_unit_filter` + `augment_with_unit_filter` + `_UNIT_SCOPE_BYPASS_MODELS` + `unit_scope_hooks` module + bootstrap registration + `base.allow_multiple_units` ir.config toggle + `base.default_unit_name_template` ir.config key + 3 multi-unit guard hooks + auto-stamp hook + `SECURITY_REQUIRE_ACTIVE_UNIT` setting + `BranchSwitcher.tsx` + WorkspaceContext unit slots + URL `?u=` reconciliation + `authService.switchUnit` + 12 vitest cases + 3 E2E cases + 17 unit_scope pytest + 7 unit-invariant pytest. Repoints 11 FK constraints (`res_team.org_unit_id` + 8 logistics `branch_id` columns + 2 logistics `entity_id` columns) from `res_organization_unit` to `res_organization` via Alembic `e4f8b2c91a37` + auth migration `f3a7e1c298b4` drops the session column. Ships `res.user.grant_all_branches` admin command + form-header button as the UX shortcut at grant-creation time (idempotent — re-clicking unions the descendant set into `allowed_organization_ids` without duplicating rows) + 3 new pytest cases. **Verification**: 2825 pytest + 1 skipped + 573 vitest green; `bun run build` clean. **Status sites updated in lockstep across 3 sites**: enhancement file 07 (🟡 In Progress (Slice 2) → ✅ Delivered (both slices)), foundation/base README row, roadmap-tracker row. **Net architectural simplification** — one scoping axis instead of two; ~1,400 LOC of recently-shipped code removed; one mental model for developers. Enh 04 (org-units model) + Enh 05's unit fields + foundation.security Enh 01 surface all retired in lockstep. Prior sync: 2026-05-26 (Enhancement 08 — Delegation Inheritance Kernel + Org/User Partner Shadow 🔴 **Not Started — drafted**. Adds `@api.model(delegated={"parent.model": "fk_field"})` decorator kwarg as a declarative replacement for hand-wired `_ensure_partner_for_user`-style auto-create + sync hooks. **Motivating gap**: `res.organization` today has no `partner_id` shadow, so an org has no contact identity to anchor outbound email / notification defaults on — asymmetric with the existing `res.user.partner_id` automation. Kernel splits create/update payloads by field ownership, creates parent row first, stamps FK, then self. Field reads stay explicit (no `__getattr__` magic per design choice) — consumers write `org.partner_id.name`. Only `name` field delegates initially (minimal blast radius). `on_delete="set null"`. Three slices: S1 Kernel feature, S2 res.organization adoption, S3 res.user migration. Composes with Enh 07 — every node in the org tree gets its own shadow partner. **Sync propagation**: docs/foundation-base.md Known Gaps row added for Enh 08. No status flips on existing ✅ Delivered features. Implementation work awaits HARD GATE approval per the roadmap-driven-delivery skill before any `src/` edits.) (previous: 2026-05-26 (Enhancement 07 — Organization Hierarchy 🔴 **Not Started — drafted**. Collapse `res.organization` + `res.organization.unit` into one recursive `res.organization` tree (`parent_id` + `child_ids`). **Supersedes** Enh 04 (org-units model retired in Slice 2) + Enh 05's unit fields + `foundation.security` Enh 01 (BranchSwitcher + `active_unit_id` JWT slot + `unit_scope` ORM all retired). Six design questions settled with the user: **explicit-only grants** (no auto-cascade), **settings cascade via `parent_id`** with 3-state scope label `System` / `Organization` / `Inherited`, **hierarchical OrganizationSwitcher as the single switcher**, **no `type` discriminator**, **two-slice sequencing** (Shadow Mode → Complete Removal), **demo data modified** with new approach (parent-org + child-orgs replaces org + units). **Sync propagation**: docs/foundation-base.md Known Gaps row added for Enh 07; docs/foundation-security.md Known Gaps row added cross-referencing the planned retirement of Enh 01's surface in Slice 2 (BranchSwitcher.tsx + `ir.session.active_unit_id` + JWT claim + `Env.active_unit_id` + `switch-unit` endpoint + `unit_scope` decorator + `apply_unit_filter` + `unit_scope_hooks`). No status flips on existing ✅ Delivered features — Slice 1 (Shadow Mode) is additive; Slice 2 (Complete Removal) is the breaking change. Implementation work awaits HARD GATE approval per the roadmap-driven-delivery skill before any `src/` edits.) (previous: 2026-05-25 (Enhancement 04 ✅ **Delivered — Slice 4 landed**, completing the legacy `ir.org.unit` retirement. **Slice 4** ships Alembic migration `f7a2c91e8b46_retire_ir_org_unit.py` at `src/ede/foundation/base/migrations/versions/`: backfills every `ir_org_unit` row into `res_organization_unit` **preserving `record_uuid`** so existing FK references and `ir.rbac.role.binding.scope_id` BRANCH-scoped rows continue resolving without a data-rewrite step (the model_key changes; the UUID does not); rewires **11 FK constraints** in lockstep before the DROP TABLE — 1 foundation (`res_team.org_unit_id` → `res.organization.unit`) + 10 logistics (`crm_inquiry.branch_id`, `crm_lead.branch_id`, `crm_opportunity.branch_id`, `crm_quote.branch_id`, `crm_handover.branch_id`, `crm_organization_profile.branch_id`, `pricing_rate.branch_id`, `pricing_rate.entity_id`, `pricing_margin_rule.branch_id`, `pricing_margin_rule.entity_id`) — with per-table `existing_tables` probe via `sa.inspect(bind)` so tenants without logistics installed upgrade cleanly; finally drops the `ir_org_unit` table. Python source repointed in lockstep: `res.team.org_unit_id` FK target + 10 logistics `fields.Reference("ir.org.unit", ...)` declarations → `"res.organization.unit"`. **Deletions**: legacy `OrgUnit` model class at `src/ede/foundation/base/models/org_unit.py` (+ `from . import org_unit` line removed from `models/__init__.py`); legacy admin views file `src/ede/foundation/base/views/ir_org_unit_views.xml` (+ removed from `__manifest__.py` data list); 4 RBAC permission rows (`ir.org.unit.{read,create,update,delete}`) from `ir.rbac.permission.csv`; the `base.action_ir_org_unit` action + `base.menu_settings_org_units` leaf from `base_menus.xml`; 5 bypass-list entries (`ir.org.unit` removed from `_COMPANY_SCOPE_BYPASS_MODELS` in `domain_filter.py`, `_UNIT_SCOPE_BYPASS_MODELS` in the same file, `_BYPASS_MODELS` in `company_scope_hooks.py`, `_BYPASS_MODELS` in `unit_scope_hooks.py`, `_BYPASS_MODELS` in `record_rule_engine.py`). **Rewires**: `rbac_routes.py` `/org-units` GET + POST endpoints now dispatch against `res.organization.unit` (URL preserved for back-compat with any pinned consumer). **Test imports migrated**: `test_team_substrate.py` + `test_rbac.py` swap from `OrgUnit` → `OrganizationUnit`. **Verification**: 2830 pytest + 574 vitest full suite green; zero regressions. **Production impact**: zero rows lost — every legacy `ir_org_unit` row lands in `res_organization_unit` as a branch under its organization; the `unit_type` HQ/DIV/BRANCH enum collapses since the new model treats branches as units with non-null `parent_unit_id`; the `notes` Text column has no destination and is dropped; downgrade synthesizes back `unit_type='BRANCH'` + `notes=NULL`. **Webclient walkthrough**: the duplicate "Units" admin paths from Slice 1 collapse — the legacy `Settings → Users & Access → Org Units` leaf is gone; only the typed `Settings → Organization & Units → Units` leaf remains. **Status sites updated in lockstep across 4 sites**: enhancement file 04 (🟡 → ✅ Delivered all 4 slices), foundation/base README row, roadmap-tracker Last-refreshed block, docs/foundation-base.md Known Gaps row updated to ✅ Delivered + Last sync footer updated. **Branch-switching design complete end-to-end** — foundation.base Enh 04 (✅ typed model + multi-unit toggle + legacy retired) + foundation.base Enh 05 (✅ user assignment + framework Many2Many(rel=) unblocker) + foundation.security Enh 01 (✅ active-unit propagation + ORM scope + BranchSwitcher). Prior sync: Enhancement 05 ✅ Delivered same-day — framework unblocker + atomic slice both landed. **Framework** (1-file diff at `src/ede/core/kernel/fields.py`): extended `fields.Many2Many(target, *, rel=None, column_left=None, column_right=None)` accepting optional join-table + column-name overrides; threaded overrides into `relation` dict via `to_spec`; extended `m2m_join_info(model_key, field_name, target_model_key, field_spec=None)` at `src/ede/core/orm/relational.py` to honor `relation.rel` / `column_left` / `column_right` when supplied (falls through to auto-generated convention otherwise); parallel honoring in the schema-side build loop at `src/ede/core/kernel/schema.py`; threaded `field_spec=` argument through 3 existing callers (`_process_m2m` in relational.py, `_get_many2many` in recordset.py, `_resolve_m2m_fields` in presentations.py). Backward-compatible — callers that don't pass `field_spec` keep original behavior. **Enh 05** (atomic slice): 4 new fields on `res.user` (`allowed_organization_ids` M2M with explicit `rel="res_user_allowed_organization_rel"` to avoid colliding with the legacy `organization_ids` M2M; `default_organization_id` Reference set-null; `allowed_unit_ids` M2M with `rel="res_user_allowed_unit_rel"` + `column_right="res_organization_unit_id"`; `default_unit_id` Reference set-null) + legacy `organization_ids` kept as one-release deprecation shim with updated label/help. New `UserOrgUnitAssignmentError(ValueError)` exception carrying stable `code` strings + `_validate_user_org_unit_assignment(env, cmd, *, existing_record=None)` module-level helper enforcing the 4 cascade invariants (default_org_not_in_allowed / unit_org_not_in_allowed / default_unit_not_in_allowed / default_unit_org_mismatch) + `_resolve_single_unit_for_org(env, organization_id)` auto-resolution helper (auto-fills `allowed_unit_ids` + `default_unit_id` when chosen default org has exactly 1 active unit) + `_fetch_unit_org_map(env, unit_ids)` cross-check helper + `_normalize_m2m_value` accepting bare-UUID-list / per-row LINK tuple / SET-replace-all tuple wire forms. 2 new `@api.on_hook("pre.res.user.create" / "pre.res.user.update")` methods firing the validator (the update path reads existing values off `cmd.record` for fields not present in the payload). Alembic migration `c3e8f2b1a479_user_org_unit_assignment.py` adds 2 scalar columns + FKs + indexes via `batch_alter_table` (SQLite-compatible) + creates 2 new join tables with cascade FKs + composite PKs; downgrade reverses cleanly. **12 new pytest cases** at `src/tests/foundation/test_user_org_unit_assignment.py` across 4 TestClass blocks: TestFrameworkOverride × 3 (metadata builds without collision; m2m_join_info honors rel override; unit field honors column_right override), TestCascadeInvariants × 5 (one per invariant + 1 happy path), TestAutoResolution × 2 (single-unit auto-fills + multi-unit does NOT auto-fill), TestUpdateInvariants × 2 (update rejects + update passes). **TDD discipline**: 2 fixes during red-green cycle — (a) M2M values must arrive in the SET command-tuple form `[[6, 0, [uuid, ...]]]` not bare UUID lists; added a `_m2m_set` helper to tests + extended `_normalize_m2m_value` in the validator to handle all 3 wire shapes; (b) auto-resolution helper had to write `values["allowed_unit_ids"]` in the SET tuple form too (the ORM relational engine expects it). **Verification**: 12/12 new pytest green; full repo suite **2813 pytest + 564 vitest** clean; zero regressions across the codebase despite the framework field-API change. **Status sites updated in lockstep across 4 sites**: enhancement file 05 (🟠 Blocked on Framework → ✅ Delivered), foundation/base README row, this Last-refreshed block, docs/foundation-base.md Built Capabilities row added + Known Gaps row replaced with the ✅ Delivered framing. **Unblocks**: `foundation.security` Enh 01 BranchSwitcher (uses these fields for default-org / default-unit auto-selection at login + the per-user allowed-units list that powers the BranchSwitcher dropdown). Prior sync: Enhancement 04 Slice 3 of 4 ✅ landed — `base.allow_multiple_units` org-scoped `ir.config` key (boolean, default `false`) declared in `views/base_settings.xml` under the existing "Organization Units" section + new `ConfigurationError` exception type at `src/ede/foundation/base/models/organization_unit.py` (carries stable `code` string the frontend can branch on without string-matching the message) + `pre.res.organization.unit.create` cap hook `enforce_multi_unit_cap` rejecting the 2nd+ active unit with code `organization_not_multi_unit_enabled` unless the toggle is ON (bypasses: orgs with 0 units → auto-stamp path; `is_system` principals → migrations / demo loaders; absent principals → boot / migration / seed / test contexts; `_skip_multi_unit_cap=True` payload flag → explicit per-call escape hatch) + `pre.ir.config.create` + `pre.ir.config.update` toggle-down hooks on `IrConfig` rejecting `base.allow_multiple_units=false` writes when the org has >1 active unit with code `multi_unit_disable_requires_single_unit` (admin must archive extras first — never destroys user-created config silently). 10 new pytest cases at `src/tests/foundation/test_organization_unit_multi_cap.py` (TestMultiUnitCapHook × 5: first-unit-allowed / second-blocked / system-bypass / explicit-skip-payload-bypass + TestToggleDownHook × 5: false-allowed-with-one-unit / false-rejected-with-multiple / true-always-allowed / unrelated-key-no-trigger / update-path-blocked). **Verification**: 10/10 new pytest green; **2801 pytest + 564 vitest** full suite clean; zero regressions despite the new cap hook firing on every unit create across the entire test base (absent-principal bypass holds for every existing fixture). Slice 4 still 🔴 (data migration off `ir.org.unit` — backfill HQ/DIV/BRANCH rows → new units, rewrite `ir.rbac.role.binding.scope_id` for BRANCH-scoped rows, drop legacy table + model + views + menu leaf). Webclient walkthrough still deferred to Slice 4. Prior sync this session: Slice 2 of 4 ✅ — `base.default_unit_name_template` `ir.config` key (system scope, type `char`, default `"{org_name}'s Unit"`) declared in `views/base_settings.xml` under a new "Organization Units" section + `post.res.organization.create` auto-stamp hook on `Organization` that creates exactly one `res.organization.unit` per new org via the configured name template. Hook is idempotent (bypasses the soft-archive auto-filter so archived units block re-stamping), gracefully degrades to no-op when `res.organization.unit` isn't registered (back-compat for fixtures registering only Organization), and falls back to the safe default on malformed templates. 9 new pytest cases under `src/tests/foundation/test_organization_unit_autostamp.py` covering default template applied / each org gets its own unit / active=True / idempotent on re-fire / archived unit blocks re-stamp / custom template via ir.config row / malformed template fallback / graceful degrade when model absent. **Verification**: 9/9 new pytest green; **2680 pytest + 564 vitest** full suite clean (zero regressions despite the post.create hook now firing on every org create across the entire test base — graceful-degrade path holds). Slices 3-4 + Enh 05 + foundation.security Enh 01 still 🔴. Prior sync this session: Enhancement 04 status flipped 🔴 → 🟡 — **Slice 1 of 4 landed**: typed `res.organization.unit` model (5 fields: `organization_id` / `parent_unit_id` self-ref / `name` / `code` / `active`) + list + form + search admin views + new `Settings → Organization & Units` parent menu (Organizations reparented under it; Units leaf added) + 4 RBAC permission rows + Alembic migration `b1d8c5e2a479` (forward-only — adds the new table, leaves `ir.org.unit` running) + 11 pytest cases covering registration / CRUD / FK required / parent-unit self-ref / name+code search / soft-archive auto-filter / explicit `active=False` reachability. The legacy `ir.org.unit` table + admin leaf + model file all stay live through Slice 3; Slice 4 will backfill HQ/DIV/BRANCH rows into the new table, rewrite `ir.rbac.role.binding.scope_id` for BRANCH-scoped rows, then drop the legacy table. Webclient walkthrough not yet performed — backend verification only. Slices 2-4 + Enh 05 + foundation.security Enh 01 still 🔴. Prior sync earlier today: Enhancement 06 Team Substrate ✅ **Delivered same-day** — flipped 🔴 → ✅ across 4 sites. Ships 4 new platform models (`res.team`, `res.team.type`, `ir.team.role` decoupled from RBAC `ir.role`, `res.team.role.assignment` with sequence) + 2 new `res.user` fields (`team_ids`, `primary_team_id`) + shared `TeamRoleService.resolve/is_assigned/all_assignments_for_user` consumed by foundation.approval Enh 01 and foundation.workflow Enh 01 (siblings). New `Settings → Teams & Roles` menu group with 4 child leaves + 16 RBAC permission rows + admin views for all 4 models. Foundation ships zero seed rows for types/roles — consumers seed their own. Alembic migration `a2e7f4b3c819` (4 new tables + `res_user.primary_team_id` additive column + `res_user_res_team_rel` M2M join). **Bundled platform fix**: M2M soft-FK degradation in `metadata_builder._build_join_tables` (skip join table when either FK target absent from build batch — same contract as Reference-field soft-degrade shipped by Phase 2 SDK). 20 new pytest cases under `src/tests/foundation/test_team_substrate.py`. **2617 pytest passed + 564 vitest passed + `bun run build` clean**. Developer guide: [foundation-base-team-substrate.md](foundation-base-team-substrate.md). **Unblocks foundation.approval Enh 01, foundation.workflow Enh 01, logistics.sales-crm Enh 09, and eventual sales-crm Phase 2 · 02 Pricing Approval feature.** Prior sync earlier today: Enhancement 06 stub added to Known Gaps. Prior sync this session: Enhancement 05 `res.user` Org-Unit Assignment Fields + Cascade Validators stub added to Known Gaps — 4 new fields on `res.user` for explicit org/unit assignment + 4 cascade validators + auto-resolution for single-unit orgs + reactive admin form + `PrincipalEnricher` direct-read of `allowed_organization_ids` + one-release deprecation shim for legacy `organization_ids` M2M. Depends on Enhancement 04. Prior sync this session: Enhancement 04 added — `res.organization.unit` model + per-org `base.allow_multiple_units` toggle + auto-stamp + migration off `ir.org.unit`. Together enhancements 04 + 05 form the foundation half of the branch-switching design — `foundation.security/enhancements/01` carries the auth + runtime + BranchSwitcher half.) To refresh, invoke the `syncing-roadmap-to-docs` skill.*
