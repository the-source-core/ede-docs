<!-- AUTO-MAINTAINED-BY: syncing-roadmap-to-docs -->
# Document Report Engine (DRE) — Implementation Docs

**Module:** `foundation.document` (`src/ede/core/engines/document/` + `src/ede/foundation/document/`)
**Roadmap:** [roadmap/foundation/document/](../roadmap/foundation/document/README.md)
**Status:** ✅ Phase 1 Fully Delivered — **Slice L — Frontend Print Menu (WS-Y1) ✅ Delivered 2026-05-27** (the final Phase 1 piece. Wires the existing Print menu button in `WorkspaceActionControlPanel` to the platform `/api/reports/*` HTTP surface, dynamically populating menu items from `useReportActions` based on view context (model + record + mode), dispatching the chosen action via `runReport`, and delivering the result via format-aware client-side routing. Scope per user direction: Print menu shows on **list + form views only** (never kanban — kanban dispatches via the `ede.report.run` Command bus directly when needed). Files shipped: `src/frontend/src/workspace/services/ReportService.ts` (NEW · `listReportActions` + `runReport` + discriminated `InlineReportResult` | `PersistedReportResult` union), `src/frontend/src/workspace/components/action/useReportActions.ts` (NEW · React hook auto-fetching the print-menu lister, cancellation-safe, short-circuits when `disabled`), `src/frontend/src/workspace/components/action/ReportDelivery.ts` (NEW · `base64ToBlob` + `defaultFilename` slugifier + `deliverInlineReport` format-switch (pdf/html → new tab via Blob URL, docx/xlsx → anchor download with slugified filename) + `deliverReport` top-level dispatcher discriminating `persisted`), `src/frontend/src/workspace/components/action/WorkspaceActionControlPanel.tsx` (modified · replaces hardcoded placeholder Print items at lines 233+334 with dynamic items from `useReportActions`; `printMenuApplicable` guard prevents unnecessary API fetches; list-mode adds `viewMode === "list"` guard so kanban never shows the menu; both buttons wrap in `{printItems.length > 0 && (...)}` matching the "empty state hides dropdown" requirement; new `dispatchReport(descriptor, targetUuids)` helper calls `runReport` + `deliverReport`), `src/frontend/src/workspace/components/action/WorkspaceActionController.tsx` (modified · passes `selectedIds` to the control panel — was only passing count). 2 new vitest files with 24 cases: `src/frontend/src/__tests__/services/ReportService.test.ts` (12 cases) + `src/frontend/src/__tests__/services/ReportDelivery.test.ts` (12 cases — uses manual mock replacement via `installBrowserMocks()` instead of `vi.spyOn` to avoid TS strict-mode generic-typing issues; `vi.useFakeTimers()` so the `setTimeout` for URL.revokeObjectURL doesn't fire after test restore). **Design notes (load-bearing)**: (1) Print menu is the user-facing UI surface, NOT the universal report-trigger — kanban/automation/batch dispatch via `ede.report.run` Command bus directly. (2) Engine-agnostic frontend — never sees `engine_key="dre"` specifics. (3) Format-to-delivery routing is client-side only — no server-side Content-Disposition needed. (4) `useReportActions` short-circuits when not applicable (no useless `/api/reports/_list` fetches in kanban / list-with-no-selection states). (5) Manual mock replacement (not `vi.spyOn`) in vitest under TS strict — generic `MockInstance<...>` can't accept specific spy types. (6) Render Preview + Inspect Resolved XML admin-form buttons (WS-Y1 design specified them) deferred to Phase 2 polish. (7) No E2E Cypress tests this slice — defer to broader `foundation.qa-automation` E2E test bed. **Verification**: `cd src/frontend && bun run build` exits 0 (tsc + vite clean); `bun run test` exits 0 (597 vitest cases including the 24 new); full repo `./run_tests.sh` exits 0 (3055 pytest + 597 vitest = 3652 total green). **Phase 1 fully delivered.** End-to-end Print Quote story works: user lands on `crm.quote` form view → Print menu shows "Print Quote" from `action_print_quote` → DRE cascade resolves to `sale_crm.quote.standard` → click dispatches via `/api/reports/sales_crm.action_print_quote/run` → `BindChromeResolver` resolves chrome → engine produces PDF bytes → client opens inline in new tab; multi-record list dispatch works identically via the template's `<for-each items="records" as="quote">` block.); **Slice K2 — Quote Document (Domain Seed in `logistics.sale_crm`) (WS-X20 part 2) ✅ Delivered 2026-05-27** (final WS-X20 commit. Ships the Quote-specific seeds in `logistics.sale_crm` that plug into the K1 engine substrate. With K2 done, the platform has a complete "Print Quote against a `crm.quote`" path: `action_print_quote` is registered against `crm.quote`; the cascade resolves to `sale_crm.quote.standard`; `BindChromeResolver` injects the org's chosen header/footer; the standard template's body is the Quote-specific xpath-patched content; the engine produces PDF bytes. Files shipped: `src/domains/logistics/sales_crm/data/quote_doc_type.xml` (NEW · 1 `ir.document.type` row with `key=quote`, `name=Sales Quotation`, `category=trade` — lives in sales_crm/data/ because "quote" is sales_crm's domain concept, not foundation.document's), `src/domains/logistics/sales_crm/data/quote_template.xml` (NEW · `sale_crm.quote.standard` `ir.document.template` row with `inherit_mode=copy` of `foundation.document.standard`, `doc_type_id` xml-id-ref to `sales_crm.doc_type_quote`, `priority=10`, `active=True`; **`dml` body** is a single `<xpath expr="//page[@id='standard-document-body']" position="replace">` patch that replaces the placeholder body slot in the parent's resolved DML with a `<for-each items="records" as="quote">` block; inside the for-each: two pages per quote — cover (quote number / dates / customer reference / payment terms / prepared-by line) + notes-and-terms (notes + status); multi-record-aware via the K1 contract — single quote → 2 pages, 5-quote batch → 10 pages), `src/domains/logistics/sales_crm/data/quote_action_report.xml` (NEW · 1 `ir.action.report` row with `key=sales_crm.action_print_quote`, `name="Print Quote"`, `engine_key=dre`, `model_id` xml-id-ref to `sales_crm.model_crm_quote` — the `ir.model` row for `crm.quote` auto-stamped by `RegistrySync` at boot — `target_model_key=ir.document.type`, `target_record_uuid` xml-id-ref to `sales_crm.doc_type_quote` — the data loader's `ref=` resolves the xml_id to the doc-type's `record_uuid` string and writes it into the Char column — `available_output_formats=pdf`, `priority=10`, `active=True`). `src/domains/logistics/sales_crm/__manifest__.py` (modified — adds `foundation.document` to `depends` so DRE loads before sales_crm seeds need to resolve the `parent_id="foundation.document.standard"` xml-id-ref; appends the 3 new data files in load order: doc-type → template → action.report — each depends on the previous via xml-id-ref). **Design notes (load-bearing)**: (1) Quote concepts live with the quote model, not in the engine — `quote` doc-type + `sale_crm.quote.standard` template + `action_print_quote` action all in `logistics.sales_crm/data/`; other domains shipping printable documents replicate this pattern in their own modules; `foundation.document/data/` only contains engine-level seeds (standard skeleton + chrome fragments + default config). (2) `ref=` on Char fields resolves xml_id → record_uuid string — standard EDE data-loader behavior; useful for the polymorphic `target_record_uuid` Char field on `ir.action.report` that intentionally avoids being a hard FK (different action types point at different target tables per engine). (3) `inherit_mode=copy` xpath-patches the parent's resolved DML — the patch swaps the parent's placeholder body slot with the Quote's `<for-each>` block; the parent's chrome (resolved via `<bind-chrome>` at render time) and page geometry (A4 portrait, default styles) inherit automatically. (4) No quote-lines table in K2's template body — the roadmap originally specified `<rows datasource="quote.lines">` for line items but that requires registering a `quote.lines` metric in `foundation.dataset` (out of scope for K2); the Phase 1 K2 template ships a 2-page-per-quote layout using only direct field substitution from the K1 render context contract; line-items table is a follow-up enhancement. (5) Reference field display-name resolution deferred — `{quote.partner_id}` substitutes to the partner's `record_uuid` (raw stored value) rather than the partner's display name; Phase 1 placeholder binder doesn't auto-expand Reference fields; either pass partner-name strings explicitly into params from the controller, or extend the K1 `_fetch_records` helper to optionally expand Reference fields — follow-up enhancement. Verification: full repo `./run_tests.sh` exits 0 (3055 pytest + 573 vitest = 3628 total green); the new K2 data files pass the data-loader validator at test-suite boot (all 3 XML files parse + all `ref=` xml-id lookups resolve via the existing `ir.data.reference` machinery seeded by RegistrySync). Dedicated K2 dispatch-integration tests deferred to follow-up — would require heavy test env registering Quote model plus all dependencies (Chatterable / workflow / approval / partner / etc.); the engine path is already well-covered by Slice J + K1 integration tests using synthetic records. **WS-X20 fully delivered** across K1 + K1b + K2; Phase 1 Print Quote against `crm.quote` story is now end-to-end-wired except for the React frontend in Slice L.); **Slice K1b — Admin Views + Settings Menu (WS-X20 part 1b) ✅ Delivered 2026-05-27** (follow-up to K1's engine substrate. Lands the admin UX for editing every WS-X20-introduced record via the React webclient — with K1b done, the document-engine surface is fully manageable from the UI without touching XML data files. Files shipped: `src/ede/foundation/document/views/ir_document_template_views.xml` (NEW · list ordered by `key asc` showing key/name/doc_type_id/parent_id/inherit_mode/organization_id/priority/active + notebook-tab form with 4 tabs — General/DML Body/Inheritance/Applicability — + search view with named filters bases/extensions/copies + groupbys), `src/ede/foundation/document/views/ir_document_type_views.xml` (NEW · simple list + single-tab form + search), `src/ede/foundation/document/views/ir_document_config_views.xml` (NEW · list with "System default (no organization)" named filter + form with Scope + Chrome Templates sections, both Reference dropdowns wired to `ir.document.template`), `src/ede/foundation/base/views/ir_action_report_views.xml` (NEW · lives in foundation.base because the model lives there — engine-agnostic; notebook tabs General/Target/Output/Scope + DRE filter + groupbys on engine_key/model_id/organization_id), `src/ede/foundation/document/data/document_admin_menus.xml` (NEW · Settings → Documents subtree under `base.menu_settings_root` at sequence 50 with 4 leaves — Document Settings/Document Templates/Document Types/Report Actions; sequence ordering puts the most-likely-edited row "which chrome does this org use?" at the top). Manifest updates: `foundation.document/__manifest__.py` appends 3 view files + the menus file (view files load before menus so action_id refs resolve); `foundation.base/__manifest__.py` appends `views/ir_action_report_views.xml` between team-substrate views and the Settings DSL panel. Design notes (load-bearing): (1) Settings DSL panel deferred — `ir.document.config` admin form already provides the per-org chrome-picker UX via Settings → Documents → Document Settings; the Settings DSL is designed for flat `ir.config` key-value writes and doesn't naturally surface record-aware forms with org-scope + Reference dropdowns + display name. (2) Menus live in `foundation.document` while the view for `ir.action.report` lives in `foundation.base` — the model lives where the action-record substrate lives (foundation.base, Slice H decision); the view file is adjacent to the model; the menu + action that exposes the view to users lives in `foundation.document` because DRE is the marquee engine consumer in Phase 1. (3) Settings → Documents sequence is 50 — falls after Teams & Roles + Customization, before any future entries. (4) Form-field placement matches mental model — Inheritance + Applicability are separate tabs on the template form so the user authoring a base template doesn't see Inheritance fields until they're cloning; `ir.action.report`'s polymorphic Target pair gets its own tab to isolate the most error-prone concept. Verification: 0 new tests (views are declarative XML validated at parse time by the existing presentation DSL validator + data-file validator); full repo `./run_tests.sh` exits 0 (3055 pytest + 573 vitest = 3628 total green); all 4 view files validate against the presentation RNG; all 5 menu entries + 4 action entries load without xml_id-ref resolution errors.); **Slice K1 — Engine Substrate (`<bind-chrome>` DSL + Pages-Level `<for-each>` + `ir.document.config` Model + Reworked Render Context) (WS-X20 part 1) ✅ Delivered 2026-05-27** (first commit of the WS-X20 K split — lands the engine substrate every domain document module copy-inherits before authoring its own template. K2 will ship the Quote-specific seeds in `logistics.sale_crm` on top. New `<bind-chrome from-config="<field>"/>` DSL element + `BindChromeResolver` pipeline step (runs between WS-X3 inheritance and WS-X4 includes; reads the active `ir.document.config` row for the current org (cascade: org-scoped → NULL-org default), fetches the bound template by uuid via `_fetch_chrome()`, replaces the marker with the bound template's top-level `<header>` / `<footer>` elements; idempotent on DML without `<bind-chrome>`). Three error classes: `BindChromeError` umbrella + `BindChromeConfigNotFoundError` + `BindChromeTemplateNotFoundError`. Pages-level `<for-each items="records" as="r">` wired through `PageExpander` (IR/parser/schema shipped in WS-X1 Slice B; `PageExpander` was crashing on `ForEachNode` siblings — closed with new `_expand_pages_for_each` helper that unrolls the wrapper, clones inner pages per iteration, stamps loop metadata + `params_overlay` for nested datasources). New model `ir.document.config` (5 fields: `organization_id` Reference→`res.organization` cascade nullable + `default_header_template_id` Reference→`ir.document.template` restrict + `default_footer_template_id` Reference→`ir.document.template` restrict + `display_name` Char 200 + `active` Boolean) + Alembic migration `d7dc5275ec8d` chained off `c7d3a8e2f5b1` with all FK + index names wrapped in `op.f(...)`. **`DocumentReportEngine.dispatch` context contract reworked** — Slice J placeholder shape (`target_record_uuid` + `target_record_uuids` in params) dropped; new contract guarantees `records: list[dict]` (always a list, even single-record dispatches return list of 1), `organization: dict` (resolved from `env.active_organization_id`; `{}` when no active org), `user: dict` (resolved from `env.principal["user_id"]`; `{}` when no principal / system principal). Caller params merge on top of these. Template authors always reach data via `records[N].field` or `<for-each items="records" as="r">` — single vs multi-record dispatch is transparent. New helpers: `_resolve_source_model_key`, `_fetch_records` (preserves input order, drops missing uuids silently), `_fetch_organization`, `_fetch_user` — all best-effort returning `{}` on miss paths so template authors don't need null guards. Data seeds: 6 chrome fragment templates (`foundation.document.header_minimal` / `header_formal` / `header_branded` / `footer_minimal` / `footer_formal` / `footer_branded`) + `foundation.document.standard` base template (canonical A4 portrait page geometry, 4 default styles, two `<bind-chrome>` markers, placeholder `<page id="standard-document-body"/>` body slot for copies to xpath-patch) + 1 NULL-org `ir.document.config` row pointing at `header_minimal` + `footer_minimal` via xml_id refs. **Design notes (load-bearing)**: (1) `<bind-chrome>` is string-level not IR-level — runs before parsing so substituted chrome's own `<include>` / `<var>` / `<rows>` flow through the rest of the pipeline. (2) Identifier shape is `record_uuid` only — config FKs store uuids whether the row came from a data file (xml_id resolved through `ir.data.reference` at load time) or a runtime designer (uuid direct from UI); end state on disk is identical. (3) Two separate `organization_id` semantics now: `ir.document.config.organization_id` is the chrome-selection knob (per-org config row choosing the active header/footer); `ir.document.template.organization_id` is the template visibility scope. (4) Pages-level `<for-each>` IR was ready in WS-X1; only `PageExpander` wiring was missing. (5) Render context is multi-record-mode-only — engine handles single→multi scaling transparently. (6) `_fetch_organization` / `_fetch_user` are best-effort — `{}` on every miss path. (7) K1 vs K1b vs K2 split — K1 (this slice) ships substrate + data seeds, fully functional on the system-default config row; K1b adds admin UI (deferred); K2 ships Quote-specific seeds in `logistics.sale_crm`. 17 new tests across 5 classes (TestBindChromeResolverNoOp × 1, TestBindChromeResolverHappyPath × 2, TestBindChromeResolverOrgCascade × 2, TestBindChromeResolverErrors × 5, TestPagesLevelForEach × 7, TestDocumentConfigModel × 4). Slice J's `test_calls_render_by_key_with_template_key` assertion updated for new context shape; cascade tests get `model_id` added to action dicts so `_resolve_source_model_key` succeeds. Full repo `./run_tests.sh` exits 0 (3055 pytest + 573 vitest = 3628 total green); ruff clean. Files shipped: 4 new (bind_chrome.py, document_config.py, migration, test_k1_substrate.py) + 4 modified (document_service.py pipeline wiring, page_expander.py for-each unrolling, document_report_engine.py context contract, models/__init__.py registration) + 3 new data files (chrome fragments, standard template, default config row) + manifest update.); **Platform Layer Slice J — `DocumentReportEngine` Implements Platform Protocol (WS-X19) ✅ Delivered 2026-05-27** (fourth + final slice of the platform-layer 4-slice sequence G→J. Registers DRE as `engine_key="dre"` against the platform `ReportEngine` Protocol shipped in Slice G. With Slice J done, the platform layer is fully delivered: DRE is now a regular registered engine like any future ERE / native-PostScript / EPUB engine would be — the platform dispatcher routes `ir.action.report` rows with `engine_key="dre"` to `DocumentReportEngine.dispatch()`, which resolves `(target_model_key, target_record_uuid)` to a specific `ir.document.template` (via direct uuid lookup or doc-type cascade) and delegates to the existing `DocumentService.render_by_key()`. New `src/ede/foundation/document/services/document_report_engine.py` (~370 LoC) — `@api.report_engine`-decorated `DocumentReportEngine` class with `engine_key="dre"`, `display_name="Document Report Engine"`, `supported_output_formats=["pdf"]` (widens to `["pdf","docx","html"]` in Phase 2). Three Protocol classmethods: `list_actions_for_context(env, model_key, target_record_uuid, is_multi_record)` resolves `model_key` → `ir.model.record_uuid` first, then filters `ir.action.report` rows where `engine_key="dre" AND active=True AND model_id=<uuid>`, applies org-filter in Python (`active_org=None` → only NULL-org rows; `active_org=X` → NULL-org OR matching X); in form mode (`target_record_uuid` present + not multi) loads the target record and evaluates each action's `domain` against it via `DomainEvaluator.matches()`; in list mode bypasses domain. Returns list of `ActionDescriptor` from `_to_descriptor()`. `dispatch(env, action, target_record_uuids, params, output_format)` resolves target via `_resolve_template()`: when `target_model_key="ir.document.template"` does direct uuid lookup; when `target_model_key="ir.document.type"` runs the cascade — `_select_template_for_doc_type()` filters non-`extend` templates with matching `doc_type_id`, prefers `organization_id=active_org` over NULL-org, sorts by `priority asc`, takes first. Auto-augments `params` with `target_record_uuid` (first of the list) + `target_record_uuids` so DML datasources (Slice F WS-X6) can scope queries to the action's target row(s). Calls `DocumentService.render_by_key(env, key=template["key"], params=augmented_params, format=output_format, target_record_uuid=first_uuid)` and returns the raw bytes. `validate_action(action)` returns `list[ValidationError]`: rejects `target_model_key` not in `("ir.document.template", "ir.document.type")` with the offending value in the error; rejects empty `target_record_uuid`; when env is reachable on the action (via `getattr(action, "env", None)`), additionally verifies template uuid points at an active row, or doc-type cascade resolves to ≥ 1 non-`extend` template; without env (offline admin validation) skips DB-touching checks — the lifecycle hook on `ir.action.report` (Slice H) re-runs validation once the row hits the DB. Module wired into the app's import chain: `src/ede/foundation/document/services/__init__.py` adds `from . import document_report_engine` (side-effect import — `@api.report_engine` registers at module-import time); `src/ede/foundation/document/__init__.py` prepends `from . import services` so the engine module is imported when the app loads. **Design choices (load-bearing)**: (1) **Two distinct target shapes — `ir.document.template` (admin/debug, direct uuid) vs `ir.document.type` (production, cascade)**. The admin form binds a specific template by uuid for "test this template now" actions; the production action binds the abstract doc-type and lets the cascade pick the right variant per org/priority/active flag. (2) **`extend`-mode templates are filtered from the cascade** — they layer onto a parent and never render alone. `DocumentService._is_extension()` rejects direct rendering of an extend template; the cascade pre-filter here matches that contract. (3) **Org filter — prefer org-matched over NULL-org, sort by priority within bucket**. A more-specific template (org-scoped) beats a less-specific one (global) even when the global one has a lower priority number. This matches the records-as-data inheritance precedence (Slice 4 WS-X3) where org-scoped extensions layer onto a parent over global. (4) **Auto-augment `params` with `target_record_uuid` + `target_record_uuids`** lets DML datasources (Slice F WS-X6) reference the action's target row(s) without callers having to wire those into params manually — keeps the action contract minimal (`payload={"action_key": ..., "target_record_uuids": [...]}` is enough). (5) **`validate_action` skips DB checks when no env** — offline admin validators (form-render checks before save) can call `validate_action(action)` from a UI context without a database, get back shape errors, and not crash on missing env. The full check re-runs at write-time via the `ir.action.report` lifecycle hook (Slice H) so the contract is enforced at the boundary. (6) **Format-list cross-check at two more layers** beyond the existing two from Slice H + I: `validate_action` is a third checkpoint (admin form), and `dispatch` adds an explicit `output_format not in supported_output_formats` raise — total of four redundant checks across the write/read path; the redundancy is the contract enforcement, and each layer has different lifecycle visibility. 14 new tests across 6 classes (TestEngineRegistration × 2, TestValidateAction × 7, TestDispatchDirectTemplate × 3, TestDispatchDocTypeCascade × 4, TestListActionsForContext × 7, TestReportDispatcherIntegration × 1). `unittest.mock.patch` used to stub `DocumentService.render_by_key` so tests don't need the full DML→PDF pipeline; `_AttrView` test helper bridges dict → attr access for `validate_action` calls. Full repo `./run_tests.sh` exits 0 (3032 pytest + 573 vitest green); ruff clean on the new file); **Platform Layer Slice I — `ReportDispatcher` + `ede.report.run` command + `/api/reports/*` (WS-X0c) ✅ Delivered 2026-05-26** (third slice of the platform-layer 4-slice sequence G→J. Closes the engine-agnostic dispatch path that every consumer — HTTP routes, command handlers, automation hooks, email-attachment flows, EDI exporters — uses to render any registered engine's output. With Slice I done, the only remaining piece is Slice J: register `DocumentReportEngine` as `engine_key="dre"` against this surface. New `src/ede/foundation/base/services/report_dispatcher.py` (~200 LoC) with `ReportDispatcher` class + 2 public classmethods (`list_for_context` aggregates `engine.list_actions_for_context(...)` across all registered engines and sorts by `(priority ASC, name ASC)` for deterministic display; `dispatch` looks up `ir.action.report` by `action_key`, validates format at two layers (action's available list + engine's supported list — catches admin-downgrade race), calls `engine.dispatch(env, action, target_record_uuids, params, output_format) → bytes`, optionally writes to `storage.document` when `persist=True`). Three new error classes: `ReportDispatcherError(ValueError)` umbrella + `ReportActionNotFoundError` (no active row with that key) + `ReportFormatNotAvailableError` (covers both action+engine layer mismatches with the same exception class so callers handle them identically) + `ReportEngineNotRegisteredError` (wraps `ReportEngineNotRegistered` from kernel). `src/ede/foundation/base/models/ir_action_report.py` gains `@api.on_command("ede.report.run")` method `handle_run(cmd)` — reads action_key / target_record_uuids / params / format / persist from `cmd.payload`, delegates to `ReportDispatcher.dispatch()`. Mirrors the `ede.document.render` pattern. Module-top import of `ReportDispatcher` is safe (service imports only kernel modules — no cycle). New `src/ede/foundation/base/api/reports_routes.py` (~130 LoC) `ReportsController` with `GET /api/reports/_list?model=&record_id=&mode=` (delegates to `list_for_context()`, serializes `ActionDescriptor` to JSON via `_descriptor_to_json()`) + `POST /api/reports/{action_key}/run?format=&persist=` (delegates to `dispatch()`; when `persist=True` returns JSON envelope with storage UUID + byte_count; when `persist=False` (default) returns base64-wrapped bytes envelope with content_type + byte_count — base64 envelope on persist=False is the Phase 1 contract, makes the bytes transport-safe through JSON at ~33% size overhead, eliminates Content-Type negotiation, makes responses trivially loggable; streaming optimization is a Phase 2 polish concern). Both routes are `auth="user"` (JWT required). `src/ede/foundation/base/api/__init__.py` — appended `from . import reports_routes`. Design choices (load-bearing): (1) `ede.report.run` is the canonical platform dispatch entry point — production callers use it; `ede.document.render` (Slice 3 DRE Foundation Shell) remains as an internal entry point that `DocumentReportEngine.dispatch()` (Slice J) will call via the action's resolution cascade. (2) Format cross-check at two layers — model-layer write-time (Slice H's `_validate_action`) + dispatcher-layer runtime (this slice) — catches the admin-downgrade race where `engine.supported_output_formats` narrows after action rows are saved. (3) `storage.document` persistence is best-effort, not transactional — if persistence fails the caller still gets the bytes back; logging + retry live higher up the stack. (4) Lister returns one flat list across engines — UI doesn't see "DRE actions" vs "ERE actions", just one Print menu; engine_key in the descriptor lets the client route back to `POST /{action_key}/run` but UX is engine-agnostic. 12 new tests across 5 classes (TestDispatchHappyPath × 3, TestDispatchErrors × 3, TestListForContext × 3 with `_StubEngineA` + `_StubEngineB` proving multi-engine aggregation + sort, TestPersist × 1, TestEdeReportRunCommand × 2). Full repo `./run_tests.sh` exits 0; ruff clean on the 4 new files); **Platform Layer Slice H — `ir.action.report` Model in `foundation.base` (WS-X0b) ✅ Delivered 2026-05-26** (second slice of the platform-layer 4-slice sequence G→J. Lands the engine-agnostic report-action descriptor. New `@api.model("ir.action.report")` on `IrActionReport(DomainModel)` at `src/ede/foundation/base/models/ir_action_report.py` with 11 business fields: `name` (Char 128, required) · `key` (Char 128, required + unique + index) · `engine_key` (Char 64, required, index) · `model_id` (Reference→`ir.model`, restrict, required, index) · `target_model_key` + `target_record_uuid` (the polymorphic discriminator pair pointing at the engine's resolution target — DRE: `ir.document.type` or `ir.document.template`; ERE: `ir.excel.template`) · `available_output_formats` (Char 128, required, comma-separated subset of `engine.supported_output_formats`) · `domain` (Char multi-line, nullable EDE search-domain) · `organization_id` (Reference→`res.organization`, restrict, nullable — NULL = global, populated = hard-scoped) · `priority` (Integer default 10) · `active` (Boolean default True). Two lifecycle hooks `pre.ir.action.report.create` + `pre.ir.action.report.update` call `_validate_action(record)` which (a) resolves `engine_key` via `ReportEngineRegistry.resolve()` raising `ValueError("references unregistered engine_key=...")` on miss, (b) cross-checks every requested format in `available_output_formats` against `engine.supported_output_formats`, (c) delegates to `engine.validate_action(record)` and aggregates returned `ValidationError` list into one raised `ValueError`. The update hook uses a duck-typed `_MergedView(cmd.record, cmd.payload["values"])` so validation sees the POST-update state (catches "update changes engine_key to something not registered"). New Alembic migration `5fa2a5742d5a_add_ir_action_report.py` chained off `b8e1f4a92c37` (latest foundation.base head, PR #2 `res_organization.name` nullable fix); declares 2 hard FKs inline + 2 unique constraints + 3 single-column indexes + 2 composite indexes (`(model_id, organization_id, active)` for the lister's form/list-mode lookup + `(engine_key, active)` for engine-scoped queries) + 4 audit indexes; all names wrapped in `op.f(...)` per the `writing-alembic-migrations` skill — every name verified ≤ 52 chars under PG's 63-char limit. Design choices (load-bearing): hard FKs at DB layer for `model_id` + `organization_id` (stable platform tables, restrict-on-delete is enforceable at migration time) + soft `engine_key` validation at model layer (engines register at boot via `@api.report_engine` decorators — a DB FK would be circular); merged-view on pre-update is load-bearing (without it, an update changing `engine_key` to invalid would slip through because `cmd.record` is the pre-update DB row); format cross-check at write-time means seed loaders + admin form saves + import workflows all surface format/engine mismatches at the same place; `target_model_key` is intentionally a plain Char not a Reference (polymorphic discriminator points at different tables per engine — a hard FK would defeat polymorphism); two composite indexes match the two lister query shapes. 11 new tests across 4 classes (TestCRUD × 3, TestUniqueKey × 1, TestEngineValidation × 4, TestRequiredFieldsAndDefaults × 3); full repo `./run_tests.sh` exits 0; ruff clean on new files); **Platform Layer Slice G — `ReportEngine` Protocol + Registry + `@api.report_engine` decorator (WS-X0a) ✅ Delivered 2026-05-26** (first slice of the platform-layer 4-slice sequence G→J. Kernel-only substrate — no DB models, no HTTP routes, no foundation.document changes. New `src/ede/core/engines/reporting/` package: `protocol.py` ships the structural `ReportEngine` Protocol (3 classmethods — `list_actions_for_context` / `dispatch` / `validate_action` — plus 3 class attrs — `engine_key` / `display_name` / `supported_output_formats`) + two frozen dataclasses (`ActionDescriptor` lister return shape + `ValidationError` engine-side action validation); `registry.py` ships `ReportEngineRegistry` (process-local class-only registry with `register` / `resolve` / `registered_engines` / `is_registered` / `clear` class methods, case-insensitive engine_key lookup normalized to lowercase, idempotent re-registration of the same class, `DuplicateReportEngine` for class-clash, alphabetical engine-key ordering for deterministic multi-engine listers); `errors.py` umbrella `ReportEngineError → ValueError` with `DuplicateReportEngine` + `ReportEngineNotRegistered`. New `src/ede/core/kernel/report_engine_decorator.py` ships `@api.report_engine` — side-effecting at module-import time, calls `ReportEngineRegistry.register(cls)`, returns the class unchanged (transparent — no metaclass, no class mutation). `src/ede/core/api.py` re-exports `report_engine` in `__all__`. Design choices (load-bearing): structural Protocol (not abstract base class) — engines don't inherit, they just match the shape; idempotent re-registration of the same class is a no-op (test fixture reloads safe); engine_key without a non-empty class attribute raises at registration. 14 new tests across 4 classes; full repo `./run_tests.sh` exits 0; ruff clean); **Stage 1 fully ✅ complete (A+B+C+D+E+F all delivered 2026-05-22 → 2026-05-26)** · **Stage 1 Polish Slice F — Datasource Binding (WS-X6) ✅ Delivered 2026-05-26** (sixth + final slice of the "Stage 1 fully" sequence; new `<rows datasource= as= key=>` block-level iterator fetches data from the platform metric registry via `run_metric(env, key, params)` at render time; new `RowsBlock` IR class + `dml.rng` `rows-block` define under `block-child` choice (`PageChild` union now 14 alternatives); new `DataSourceResolver` IR transform at `src/ede/core/engines/document/dml/datasources.py` runs between page-filter and placeholder-binder; `PageExpander._resolve_block_children` return widened from `list[Paragraph]` to `list[PageChild]` so RowsBlock survives expansion; new `Page.params_overlay: Optional[Dict[str, Any]]` field carries per-iteration loop binding so `<rows>` inside `<page for=>` sees the current iteration item in its metric params; final pipeline order is now `inheritance → includes → variables → parser → page-expansion → page-filter → DATASOURCE RESOLVER → placeholder-binder → emitter → Stage 2`; no caching in Phase 1 (per-render memoization deferred to Phase 2); four error classes — `DataSourceError` umbrella + `DataSourceNotFoundError` + `DataSourceExecutionError` + `DataSourceNonTableModeError`; row-context substitution via `_substitute_row_refs` — only refs whose head is in row context substitute, global params + render-time tokens pass through; 18 new tests across 6 classes; 332/332 DRE suite); **Stage 1 Polish Slice E — Formula-Form Variables (WS-X5 second pass) ✅ Delivered 2026-05-26** (lights up `<var name= type= formula="expr"/>` alongside the existing value form; consumes the safe AST evaluator at `src/ede/core/engines/formula/safe_eval.py` already shipped by `foundation.dataset` Phase 2; function whitelist = `full` (math + aggregate + logic + date + format helpers); mutual-exclusivity check — declaring both `value=` and `formula=`, or neither, raises `VariableSyntaxError`; cross-form dependency single graph — formula→value via bare name, value→formula via `$ref` substitution; AST-derived dep edges filter call-targets / whitelisted function names / render-param names / undeclared names so cycle detection stays precise; render-time params first-class — `VariableResolver.resolve(dml, params=…)` threads the render param map directly into the safe_eval scope, declared-var name beats param key on clash; typed parallel store — `resolved_str` for XML substitution + `typed_values` for evaluator scope; lenient formula-result coercion — INTEGER accepts whole floats, BOOLEAN accepts bool/int/string, DATE accepts Python date/datetime/ISO-strings; new `VariableFormulaError` class wraps `FormulaEvalError`; 39 new tests; 314/314 DRE suite); **Stage 1 Polish Slice D — Full `<field>` Element + Strict-Mode Flag (WS-X10) ✅ Delivered 2026-05-26** (rich placeholder `<field name= format= default=/>` + closed-vocab `FormatSpec` enum (11 values: string/integer/decimal/currency/percent/date/datetime/boolean/upper/lower/title) + format-arg parsing (decimal:N, currency:CCY, date:fmt, boolean:Y|N) + opt-in strict-mode flag raising `MissingPlaceholderError`. PlaceholderBinder full rewrite walks the entire Phase 1 IR vocabulary (recursive into Group/Section/Component/Table/Header/Footer + text substitution on Paragraph/TextNode/SpanNode/HeadingNode/LabelNode/DetailsNode). FieldNode flattens to Paragraph at bind time — same lifecycle as IfBlock/ForEachBlock; resolved-XML schema unchanged. Two-form coexistence: string `{field}` is always lenient; element `<field>` is strict-aware. YYYY/MM/DD → strftime locale-naïve (Babel/CLDR defers to Phase 3). 43 new tests; 275/275 DRE suite); **Stage 1 Polish Slice C — Full Emitter + Resolved-Parser Mirror (WS-X11B + WS-X13) ✅ Delivered 2026-05-22** (symmetric mirror of Slice B; every IR visual node emits to resolved XML + parses back; resolved.rng + `ResolvedXmlEmitter` + `ResolvedXmlParser` all expanded to cover 11 visual node types + top-level `<styles>` + `<headers>` + `<footers>` chrome + `<margins>`; full pipeline DML → IR → resolved XML → ResolvedTree symmetric roundtrip proven; resolution-time constructs (`<var>` / `<if>` / `<for-each>` / `<override>` / `<include>` / `<page for=>` / `<rows datasource=>`) remain schema-excluded; 20 new tests; 232/232 DRE suite; first slice authored in `/tmp/ede-doc-engine` worktree on `document-engine-phases` branch forked from main); **Stage 1 Polish Slice B — Full IR + Parser + RelaxNG (WS-X1 + WS-X2) ✅ Delivered 2026-05-22** (full Phase 1 DML grammar lands in IR + parser + schema; ~14 new IR classes spanning chrome / inline + block content / table family / control / page chrome; `PageChild` union widened to 14 types; parser threads Slice A enums via `_opt_enum` helper; 27 new tests; 1 obsolete hello-world test patched); **Stage 1 Polish Slice A — Full Enum Kernel (WS-X12) ✅ Delivered 2026-05-22** (12 new closed-vocab enums — `PageType` (9) + `PageVisibility` (5) + `TextAlign` + `VerticalAlign` + `FontWeight` + `FontStyle` + `TextDecoration` + `TextTransform` + `BorderStyle` + `ImagePosition` + `OverrideMode` + `VarScope`; `ALL_ENUMS` = 15; stable-value contract via per-enum value-tuple tests; 18 enum tests; 184/184 DRE suite); **Full Schema Slice (WS-X18) ✅ Delivered 2026-05-22** (new models `ir.document.type` + `ir.document.render.audit`; `doc_type_id` Reference on `ir.document.template`; third migration; render audit rows on every render; 7 tests; 170/170 DRE suite); **Two-Pass Executor Slice (WS-X16) ✅ Delivered 2026-05-22** (`TwoPassExecutor` substitutes render-time tokens at Stage 2; full vocabulary now resolves: `{PageNumber}` / `{TotalPages}` / `{LogicalPageId}` / `{LogicalPageIndex}` / `{PageInLogical}` / `{LogicalPageCount}` / `{LoopIndex}` / `{LoopCount}` / `{LoopKey}`; Page IR + resolved-XML schema gain loop-metadata attributes; PageExpander filter-then-index dense indexing; 10 tests; 163/163 DRE suite); **Within-Page Conditionals & Loops Slice (WS-X9) ✅ Delivered 2026-05-22** (`<if test="expr">` + `<for-each items="key" as="x">` as page-children; `IfBlock` + `ForEachBlock` IR nodes flattened by `PageExpander` before emitter; recursive `_resolve_block_children` supports nested combinations; within-page blocks see outer page-level loop context; 13 integration tests; 153/153 DRE suite); **Page Conditional Slice (WS-X8) ✅ Delivered 2026-05-22** (`<page if="expr">` + `PredicateEvaluator` + `PageFilter`; truthy / `==` / `!=` with type coercion; combines with WS-X7 per-iteration filtering; 21 integration tests; 140/140 DRE suite); **Page Expansion Slice (WS-X7) ✅ Delivered 2026-05-22** (`<page for= as= key=>` + `PageExpander` IR transform produces one physical page per loop iteration; auto-key + 1-based LoopIndex; render-time tokens preserved; 13 integration tests; 119/119 DRE suite); **Variable Evaluator Slice (WS-X5 — value form) ✅ Delivered 2026-05-22** (`<var name= type= value=>` declarations + `$varName` / `${varName}` substitution; closed-vocab `VarType` enum (10 values); DFS cycle detection; 10 type validators; pipeline `inheritance → includes → variables → engine`; 20 integration tests; 106/106 DRE suite); **`<include>` Directive Slice (WS-X4) ✅ Delivered 2026-05-22** (`IncludeResolver` inlines `<include src="key"/>` directives — max recursion depth 5, per-chain cycle detection; composes with WS-X3 inheritance; 13 integration tests; 86/86 DRE suite); **Records-as-Data Inheritance Slice (WS-X3) ✅ Delivered 2026-05-22** (`parent_id` + `inherit_mode` + `domain` + `priority` + `organization_id` on `ir.document.template`; `XPathMerger` + `DomainEvaluator`; 12 inheritance tests; 73/73 DRE suite); **Foundation Shell Slice ✅ Delivered 2026-05-22** (`ir.document.template` + migration + `ede.document.render` command via `env.dispatch`); **Hello-World Engine Slice ✅ Delivered 2026-05-22** (DML → PDF pipeline; 50 pytest cases); **WS-X11 Slice A ✅ Delivered 2026-05-22** (kernel data-loader extension); roadmap drafted 2026-05-12; architecture revised 2026-05-21; print-engine dropped + platform `ReportEngine` abstraction + `ir.document.type` + `ir.action.report` integration locked 2026-05-22
**Layer:** Foundation engine — consumes `foundation.dataset` substrate; **implements the platform `ReportEngine` Protocol** declared at the kernel layer (`src/ede/core/engines/reporting/`)

> Source of truth is the roadmap. This doc reflects the *current built state*. Auto-maintained by the `syncing-roadmap-to-docs` skill.

---

## 1. Functional Understanding

### What it is
<!-- SYNC-BLOCK: what -->
The **Document Report Engine (DRE)** is a **two-stage engine** glued by a serializable intermediate XML.

- **Stage 1** — takes a `.dml` template + caller params + registered datasets/metrics and produces a **resolved `.xml`** with all inheritance, includes, page expansion (`<page for= as=>`), conditionals (`<page if=>`), variable evaluation, datasource binding, and placeholder substitution complete. **No logic constructs remain.** The resolved XML is a first-class artifact — persisted in `ir.document.resolution`, hashable, replayable, format-independent.
- **Stage 2** — three **independent output engines** read that same resolved XML and emit format-specific bytes: PDF (Phase 1, pixel-perfect via ReportLab Canvas — *not* HTML-to-PDF), HTML (Phase 2, standalone + email modes), DOCX (Phase 2, OOXML via python-docx). **No print engine** — print-to-OS is explicitly out of scope (future report-viewer concern). Engines share zero rendering code — only the resolved-XML schema.

Beyond Stage 1/Stage 2 internals, DRE **implements the platform `ReportEngine` Protocol** (declared at the kernel layer in `src/ede/core/engines/reporting/`). The user-facing print menu lists `ir.action.report` records (lives in `foundation.base`) — engine-agnostic; DRE is one of N registered engines. ERE (Excel Report Engine) and other future engines register alongside via `@api.report_engine` without touching DRE.

The grammar is **page-centric**: `<pages><page>` are explicit, authored units (with `for=` / `as=` / `if=` / `key=` for loops + conditionals). Pagination — i.e. one logical `<page>` overflowing to multiple physical pages — is computed by each Stage 2 engine independently via a contracted two-pass cycle (Pass 1 measure → Pass 2 emit with `{PageNumber}` / `{TotalPages}` / `{LoopIndex}` substituted).

Templates are first-class records (`ir.document.template`) with a `doc_type_id` Reference to the new `ir.document.type` taxonomy (HBL / Quote / Invoice / etc. — replaces the prior `doc_type` `Char` field). Resolved per `(doc_type_id, organization_id, domain, priority)` — so jurisdictional + variant templates (HBL FCL/LCL/AIR, India HSN-layered Quote) compose via inheritance + domain-filtered cascade without forking.
<!-- /SYNC-BLOCK -->

### Why it exists
<!-- SYNC-BLOCK: why -->
Every domain that ships paper or PDFs to customers needs the same primitive. Without a shared engine, each domain reinvents — typically badly. Quote PDFs, shipping documents, invoices, certificates, contracts — all consume one engine.

Three load-bearing design choices drive the architecture:

1. **Two-stage architecture with a serializable resolved XML.** One Stage 1 run feeds N Stage 2 engines without recomputing data binding. The resolved XML is the perfect audit artifact (customer disputes an invoice → load the resolved XML and see exactly which data was rendered), the natural cache boundary, and the testing-isolation seam (Stage 1 tests need no PDF library; Stage 2 tests need no database).

2. **Page-centric grammar, not body-flow.** Pages are explicit, authored units — `<page id="cover">`, `<page id="content-page" for="$customers" as="customer">`, `<page id="extras" if="$matched">`. Authors retain layout intent; the renderer handles only the physical-pagination overflow within each logical page.

3. **Native pixel-perfect PDF, not HTML-to-PDF.** PDF engine uses ReportLab Canvas at the coordinate level — glyph at (x, y) at exact size. Regulatory documents (customs declarations, certified invoices, government forms) require this fidelity. No browser engine, no HTML/CSS rasterization, no WeasyPrint dependency.
<!-- /SYNC-BLOCK -->

### How a user / consumer interacts with it
<!-- SYNC-BLOCK: how -->
- **End-user UX** — Settings → Technical → Document Templates: form with notebook tabs (General / DML Body / Inheritance / Resolution / Audit). `[Render Preview]` button generates an inline PDF in <2 seconds. `[Inspect Resolved XML]` shows the Stage 1 output — critical for debugging. Phase 3 ships a **React drag-drop document designer**: pick a business model or dataset, see available fields, drag onto pages.
- **Authoring** — Authors write `.dml` files (DML XML), upload via the admin form, or ship under `<app>/demo/templates/`. Phase 3 designer emits DML.
- **Programmatic entry points:**
  - **Primary (platform-level)**: `env.dispatch(Command("ede.report.run", payload={"action_key": "action_print_hbl", "target_record_uuids": [...], "format": "pdf", "params": {...}, "persist": False}))` — engine-agnostic. Platform dispatcher looks up `ir.action.report.engine_key`, routes to the matching engine. This is what UI clicks / workflow actions / email-attachment / EDI all go through.
  - HTTP (platform): `POST /api/reports/<action_uuid>/run?format=pdf` · `GET /api/reports/_list?model=...&record_id=...&mode=form|list` (print-menu lister).
  - **Internal / debug (DRE-specific)**: `env.dispatch(Command("ede.document.render", payload={"key": ..., "params": {...}, "format": "pdf"}))` — render a specific template by key, bypasses the action/cascade. Used by the admin form's preview button + test fixtures.
  - `env.dispatch(Command("ede.document.resolve", payload={"key": ..., "params": {...}}))` — Stage 1 only; returns resolved XML (debugging).
  - `env.dispatch(Command("ede.document.batch_render", payload={"key": ..., "params": {...}, "formats": [...]}))` — Phase 3; one Stage 1 → parallel Stage 2 fanout.
  - `@api.on_event("ede.document.rendered")` — react to render completion (delivery dispatcher, audit logger).
  - HTTP (DRE internal): `POST /api/document/<key>/render` · `POST /api/document/<key>/resolve` · `POST /api/document/<key>/preview` · `POST /api/document/<key>/batch_render` (Phase 3).
- **Integration boundary** — PRODUCES rendered bytes + `ir.document.resolution` (cached resolved XML) + `ir.document.render.audit` rows + `ede.document.rendered` events. CONSUMES `foundation.dataset` (datasource binding + shared safe AST evaluator for every dynamic attribute), `foundation.storage` (template body files + rendered output persistence), `foundation.base` (res.organization / res.country / res.user.language_id for resolution + variable scope), `foundation.l10n` (Phase 3 — Babel/CLDR locale resolution).
<!-- /SYNC-BLOCK -->

### Design choices — load-bearing decisions
<!-- SYNC-BLOCK: design-choices -->
DRE makes a small set of deliberate choices that show up everywhere in the engine. (Full reasoning in [roadmap/foundation/document/README.md → Design Choices](../roadmap/foundation/document/README.md#design-choices--load-bearing-decisions).)

1. **Platform `ReportEngine` Protocol — DRE is one of N engines.** Print menu, dispatch path, and automation hooks are platform-level abstractions over a registry. DRE registers as `engine_key="dre"`; ERE and others register identically. Consumers always go through `ir.action.report` + `ede.report.run` — never directly to DRE.
2. **`ir.document.type` first-class taxonomy.** HBL / Quote / Invoice are records — surfaced in the print menu. Internally DRE resolves a doc-type click to a specific template via the `(doc_type_id, organization_id, domain, priority)` cascade. HBL FCL/LCL/AIR are three templates pointing at one doc_type; user clicks one menu item, engine picks variant per shipment mode.
3. **Two-stage architecture with serializable resolved XML.** Stage 1 emits `.xml`; Stage 2 consumes it. Resolved XML is a first-class artifact (persisted, hashable, cacheable, replayable).
4. **Records-as-data inheritance.** Templates are `ir.document.template` records; inheritance via `parent_id` + `inherit_mode` (`extend` / `copy`); applicability via `domain` field (EDE search-domain DSL). XPath merger reused from `foundation.presentation` view inheritance — one implementation, two consumers. Same pattern as `@api.extend_model` for models and `<extend ref=>` for views.
5. **Page-centric grammar (`<pages><page>`).** Pages are explicit, authored units. `<page for= as=>` loops; `<page if=>` conditionals; `<for-each>` wrappers for multi-page-per-iteration.
6. **Pixel-perfect PDF via native writer.** ReportLab Canvas at the coordinate level — no HTML-to-PDF intermediary.
7. **Three independent output engines — PDF / HTML / DOCX. No print engine.** PDF, HTML, DOCX share no rendering code. Adding EPUB / RTF later means writing a new engine, never touching Stage 1. Print-to-OS is **explicitly out of scope** (future report-viewer concern).
8. **One expression language across the engine.** Variable formulas, `<if test=>` predicates, `<page if=>` conditionals, `<cell style_if=>`, chart axis bounds, `<barcode value=>` formula values all route through the same safe AST evaluator (`src/ede/core/engines/formula/safe_eval.py`).
9. **Two-pass pagination per Stage 2 engine.** Each engine implements its own Pass 1 (measure) → Pass 2 (emit with render-time tokens substituted).
10. **Auto-key from dict / index.** `<page for= as=>` and `<for-each items= as=>` auto-derive `key` and `{LoopIndex}`: dict iteration → dict key + 1-based positional index; list iteration → 0-based key + 1-based positional index. Explicit `key="$expr"` overrides.
11. **Explicit cycle detection.** Variable + style inheritance graphs validated via Tarjan's algorithm at Stage 1 parse time.
12. **Datasource binding routes through the dataset/metric registry.** No local lookup table.
13. **Babel/CLDR locale (Phase 3) — not hardcoded tables.**
14. **Plugin architecture for custom DML nodes (Phase 3).** `@api.dml_node` + per-engine renderer hooks. Registry-open, grammar-closed.
15. **Authoring-time `<requires>` validation (Phase 3).** Lock-time fail, not render-time.
16. **React drag-drop designer (Phase 3), not a code editor.** Pick model/dataset, drag fields onto pages, emit DML XML. Non-developer authoring.
17. **`ir.document.resolution` caching keyed by `(template_id, template_version, matched_extensions_hash, params_hash, data_hash)`.** Different applied-extension sets get separate cache rows; cache invalidates naturally when any extension's `write_date` changes. Multi-format batch is N parallel Stage 2 reads against one cached resolution.
18. **`<include>` recursion bounded at depth 5.**
19. **`active` flag on `ir.document.template`.** Standard EDE soft-archive.
<!-- /SYNC-BLOCK -->

## 2. Technical Implementation

### Architecture at a glance
<!-- SYNC-BLOCK: architecture -->
```
                  ╔════════════════════════════════════════════════════════════╗
                  ║                                                            ║
   .dml template  ║   Stage 1: Resolution                                      ║
       +          ║   ─────────────────────                                    ║
   params (data)  ║   - Inheritance + <override>                               ║
       +          ║   - <include> directives (max depth 5)                     ║
   datasets       ║   - Variable evaluation (cycle detection)                  ║
   (registry)     ║   - Datasource binding (foundation.dataset registry)       ║
       │          ║   - Page expansion: <page for= as=> loops + <page if=>     ║
       ▼          ║   - Within-page <if> / <for-each> preprocessing            ║
   ┌──────────┐   ║   - Placeholder substitution ({field}, <field name=>)      ║
   │ DML      │──►║   - Render-time tokens left UNRESOLVED:                    ║
   │ parser + │   ║       {PageNumber}, {TotalPages},                          ║
   │ resolver │   ║       {LogicalPageId}, {LogicalPageIndex},                 ║
   └──────────┘   ║       {PageInLogical}, {LogicalPageCount},                 ║
                  ║       {LoopIndex}, {LoopCount}, {LoopKey}                  ║
                  ║                            │                               ║
                  ║                            ▼                               ║
                  ║                    resolved .xml                           ║
                  ║   (cached in ir.document.resolution, audited, replayable)  ║
                  ║                            │                               ║
                  ╠════════════════════════════│═══════════════════════════════╣
                  ║   Stage 2: Output engines  │   ── each engine independent  ║
                  ║   ───────────────────────  │                               ║
                  ║                            ▼                               ║
                  ║              ┌─────────────┼─────────────┬───────────────┐ ║
                  ║              ▼             ▼             ▼               ▼ ║
                  ║       ┌──────────┐  ┌──────────┐  ┌──────────┐    ┌──────┐ ║
                  ║       │ PDF      │  │ HTML     │  │ DOCX     │    │Print │ ║
                  ║       │ (Phase 1)│  │ (Phase 2)│  │ (Phase 2)│    │(Ph.3)│ ║
                  ║       │          │  │          │  │          │    │      │ ║
                  ║       │ ReportLab│  │ Semantic │  │ OOXML    │    │ lp / │ ║
                  ║       │ Canvas — │  │ HTML +   │  │ via      │    │ Win32│ ║
                  ║       │ coordinate│  │ inline   │  │ python-  │    │ from │ ║
                  ║       │ -level   │  │ CSS      │  │ docx     │    │ PDF  │ ║
                  ║       │          │  │          │  │          │    │ bytes│ ║
                  ║       │ Two-pass │  │ Two-pass │  │ Two-pass │    │      │ ║
                  ║       │ paginatn │  │ paginatn │  │ paginatn │    │      │ ║
                  ║       │ + token  │  │ + token  │  │ + token  │    │      │ ║
                  ║       │ subst    │  │ subst    │  │ subst    │    │      │ ║
                  ║       └─────┬────┘  └─────┬────┘  └─────┬────┘    └───┬──┘ ║
                  ║             ▼             ▼             ▼             ▼    ║
                  ║          .pdf          .html         .docx         to lp   ║
                  ╚════════════════════════════════════════════════════════════╝

Core engine layout:
src/ede/core/engines/document/
├── dml/                 # Stage 1: DML → resolved XML
├── resolved/            # Resolved-XML schema (Stage 1 ↔ Stage 2 contract)
└── output/              # Stage 2: per-format engines
    ├── primitives/      #   shared measurement + two-pass scaffold
    ├── pdf/             #   ReportLab Canvas (Phase 1)
    ├── html/            #   Semantic HTML (Phase 2)
    ├── docx/            #   python-docx OOXML (Phase 2)
    └── print/           #   POSIX lp + Win32 (Phase 3 — consumes PDF)

Foundation shell:
src/ede/foundation/document/
├── models/              # ir.document.template + ir.document.resolution
│                        # + ir.document.render.audit + ir.document.post_process (Ph.2)
├── controllers.py       # HTTP routes
├── services.py          # Render orchestrator (Stage 1 + Stage 2)
├── views/               # Admin form + React designer (Phase 3)
├── data/                # RBAC + menus
└── demo/                # Demo templates (.dml)
```
<!-- /SYNC-BLOCK -->

### Document IR catalog
<!-- SYNC-BLOCK: ir-catalog -->
DML parses into a strongly-typed `DocumentIR` tree (Pydantic). Every supported tag maps to one IR node. Mirrored from [roadmap/foundation/document/README.md](../roadmap/foundation/document/README.md#stage-1--dml-node-catalog-template-side).

**Phase 1 — DML nodes (template-side):**
- Document structure: `Document`, `Meta`, `PaperFormat`, `Margins`, `StyleDef`, `VarDef`
- Page-centric: `PagesNode`, `PageNode` (with `for=`, `as=`, `if=`, `key=` for loops + conditionals), `HeaderNode`, `FooterNode`, `ForEachNode` (page-level wrapper), `IfNode`
- Content: `TextNode`, `FieldNode`, `SpanNode`, `ParagraphNode`, `HeadingNode`, `ImageNode`, `BlockNode` (+ `LabelNode` + `DetailsNode`), `GroupNode`, `SectionNode`, `ComponentNode` (with `keep_together` + scoped `variables`), `TableNode` family (+ `style_if`), `OverrideNode`, `IncludeNode`, `PageBreakNode`

**Phase 2 — DML language additions:**
- `GridLayoutNode` + `GridCellNode`
- `ChartNode` family (+ `AxisNode` + `DataSeriesNode` + `SlicesNode` + `LegendNode` + `DataLabelsNode` + `ColorsNode` + `GridNode`)
- `BarcodeNode`, `QRCodeNode`

**Resolved XML schema (Stage 1 output):** strictly smaller grammar. `<resolved>` root with `<meta>`, `<styles>` (flattened), `<headers>`, `<pages>` (expanded — no `<for-each>`, no `if=` left), `<footers>`. No `<override>`, no `<include>`, no `<if>`, no `<for-each>`, no `<var>`, no `<rows datasource=>`. Logic gone; data + layout intent remain. Render-time tokens (`{PageNumber}`, `{TotalPages}`, `{LoopIndex}`, etc.) survive as literal strings for Stage 2 to substitute.
<!-- /SYNC-BLOCK -->

### Render-time token vocabulary
<!-- SYNC-BLOCK: render-tokens -->
Tokens NOT substituted at Stage 1 — survive as literal text in resolved XML, filled in by Stage 2 Pass 2.

| Token | Meaning |
|---|---|
| `{PageNumber}` | Current physical page (1-based) |
| `{TotalPages}` | Total physical pages (from Pass 1 measurement) |
| `{LogicalPageId}` | `id=` of the current logical `<page>` |
| `{LogicalPageIndex}` | 1-based index of current logical page in surviving `<pages>` |
| `{PageInLogical}` | Current physical page within the current logical page (1..K when overflowing) |
| `{LogicalPageCount}` | Total surviving logical pages (after `if=` filtering) |
| `{LoopIndex}` | Iteration index (1-based) within the current loop |
| `{LoopCount}` | Total iterations of the current loop |
| `{LoopKey}` | The `key=` value — dict key (dict iteration) or 0-based index (list); explicit `key="$expr"` overrides |

`key` and `{LoopIndex}` auto-resolve: dict iteration → dict key; list iteration → 0-based index. `{LoopIndex}` is always 1-based for printed output.
<!-- /SYNC-BLOCK -->

### Closed vocabularies
<!-- SYNC-BLOCK: closed-vocabularies -->
Every dynamic attribute enforces a closed taxonomy — authoring outside the set is a `DmlValidationError` at parse time. Mirrored from [roadmap/foundation/document/README.md](../roadmap/foundation/document/README.md#closed-vocabularies).

| Vocabulary | Values | Phase |
|---|---|---|
| `Orientation` | `portrait` · `landscape` | 1 |
| `PageType` (header/footer on physical pages) | `all` · `first` · `rest` · `last` · `odd` · `even` · `except-first` · `except-last` · `first-and-last` (9) | 1 |
| `PageVisibility` (within overflowing logical page) | `all` · `first` · `last` · `odd` · `even` (5) | 1 |
| `TextAlign` · `VerticalAlign` · `FontWeight` · `FontStyle` · `TextDecoration` · `TextTransform` · `BorderStyle` · `ImagePosition` | (see roadmap) | 1 |
| `OverrideMode` | `replace` (default) · `append` · `prepend` · `remove` | 1 |
| `VarType` | `string` · `number` · `integer` · `decimal` · `date` · `datetime` · `boolean` · `color` · `currency` · `list` (10) | 1 |
| `VarScope` | `document` (default) · `component` | 1 |
| `OutputFormat` | `pdf` (Phase 1) · `html` · `docx` (Phase 2) · `print` (Phase 3) | 1+ |
| `ChartType` | `bar` · `column` · `line` · `area` · `pie` · `doughnut` · `scatter` · `radar` · `stacked-bar` · `stacked-column` (10) | 2 |
| `ChartTheme` | `default` · `minimal` · `corporate` · `print` · `mono` (5) | 2 |
| `AxisType` · `LegendPosition` | (see roadmap) | 2 |
| `BarcodeType` | `code128` · `code39` · `ean13` · `ean8` · `upc-a` · `upc-e` · `itf` · `codabar` · `pdf417` · `datamatrix` (10) | 2 |
| `QRCodeErrorCorrection` | `L` (~7%) · `M` (~15%) · `Q` (~25%) · `H` (~30%) | 2 |
<!-- /SYNC-BLOCK -->

### Inheritance Model — Records-as-Data
<!-- SYNC-BLOCK: inheritance-model -->
Templates inherit via record-relationship — no DML-side `extends=` attribute. The full design lives in [README.md → Inheritance Model](../roadmap/foundation/document/README.md#inheritance-model--records-as-data--xpath); this is the brief mirror.

**Two modes:**

| `inherit_mode` | Semantics | Result template `key` | Original affected? |
|---|---|---|---|
| `extend` | Layer onto parent when `domain` matches | (extensions invisible in print menu — they layer on their parent) | Yes — anyone rendering parent's key sees the layered version when applicable |
| `copy` | Separate template starting from parent's resolved DML | Different from parent | No — original remains intact |

**Domain semantics by mode + view context:**

| Case | Behaviour |
|---|---|
| `domain = NULL` | Global — always applies |
| `extend` + domain set | Applies to parent only when domain matches current target record (or current `res.organization` fallback) |
| `copy` + domain set, **form view** | Copy shown in print menu only when domain matches the form's current record |
| `copy` + domain set, **list / bulk-print** | Domain bypassed — copy always shown if `model_id` + `organization_id` match |

**Two resolver flows:**

1. **Render flow** — `resolve_template_for_render(key, env, target_record)` loads the target by unique key, recursively resolves parent for `copy` mode, discovers `extend`-mode extensions via `parent_id` lookup, filters by domain, applies in priority order via XPath merger.
2. **Print-menu flow** — `list_templates_for_print(model_key, env, target_record, is_multi_record)` returns visible templates: extensions excluded; bases + copies filtered by domain in form view; domain bypassed in list view.

**XPath patch grammar** — reuses `foundation.presentation` view-inheritance machinery. Standard `position=` values: `inside` (default), `after`, `before`, `replace`, `attributes`.

**Lock-time XPath validation** — every `<xpath expr=>` in an extension/copy is verified against the parent's current DML at lock time; parents being re-locked re-validate all active extensions and auto-revert orphans to draft.
<!-- /SYNC-BLOCK -->

### Models
<!-- SYNC-BLOCK: models -->
| Model Key | Purpose | Source File |
|---|---|---|
| `ir.attachment` *(Enhancement 01 ✅ 2026-05-27)* | **Polymorphic attachment row** — sibling to `storage.document`. Shared `StorageRouter` for backend routing. Carries `(attached_to_model_key, attached_to_record_uuid, attached_to_field_name)` polymorphic back-ref + `usage_kind` discriminator (`field_image` / `field_binary` / `record_attachment`) + file metadata (`name`, `mimetype`, `size_bytes`, `checksum_sha256`) + storage pointer (`storage_backend`, `connector_id`, `storage_key`, `storage_object_id`). `pre.ir.attachment.delete` cascades to backend bytes via the router. | `src/ede/foundation/document/models/attachment.py` |
| _other models_ | **Phase 1 (platform):** `ir.action.report` — universal "printable thing" record (lives in `foundation.base`, NOT DRE; engine-agnostic; polymorphic via `target_model_key` + `target_record_uuid`; consumed by UI print menu + automation + email-attachment + EDI). **Phase 1 (DRE):** `ir.document.type` — **NEW first-class taxonomy** (HBL / Quote / Invoice as records; replaces the prior `doc_type` `Char` discriminator); `ir.document.template` — **records-as-data inheritance**: unique `key`, `doc_type_id` Reference, `parent_id` + `inherit_mode` (`extend` / `copy`), `domain` (EDE search-domain DSL), `priority`, `organization_id` hard scope, `model_id` binding (designer field discovery), `dml` field (loaded via `type="xml"` — full body for base, `<xpath>` patches for extensions/copies), draft/locked state, active flag; `ir.document.resolution` (cached resolved XML keyed by `(template_id, template_version, matched_extensions_hash, params_hash, data_hash)`); `ir.document.render.audit` (append-only — `cache_hit`, `applied_extension_keys`, `target_record_uuid`, output_format, output_size, success/error; **Enhancement 01 adds `output_attachment_id` Reference to `ir.attachment`** stamped on saves). **Phase 2:** `ir.document.post_process` (signing/watermark/encrypt/compress chain — PDF-specific). | `src/ede/foundation/base/models/` (action) + `src/ede/foundation/document/models/` (DRE) |
<!-- /SYNC-BLOCK -->

### Services & key code paths
<!-- SYNC-BLOCK: services -->
| Service / Class | Responsibility | Source File |
|---|---|---|
| _none yet_ | **Platform layer (Phase 1):** `ReportEngine` Protocol + `ReportEngineRegistry` + `@api.report_engine` decorator (NEW — at `src/ede/core/engines/reporting/`); `ReportDispatcher` (NEW — at `src/ede/foundation/base/services/`) implements `ede.report.run` + `GET /api/reports/_list` + `POST /api/reports/<action>/run`. **Stage 1 (Phase 1):** `DmlParser` (XML → Pydantic IR), `RelaxNgValidator`, `TemplateResolver` (records-as-data: discovers applicable extensions via `parent_id` + `inherit_mode` + `domain` matching; layers via XPath merger in priority order), `XPathMerger` (**thin wrapper reusing `foundation.presentation`'s view-inheritance machinery — one impl, two consumers**), `DomainEvaluator` (wraps EDE's standard search-domain evaluator with current-target-record + org-fallback context), `IncludeResolver` (max depth 5), `VariableEvaluator` (value form + scope + `$varName` substitution + Tarjan cycle detection), `StyleCascadeResolver` (inherit + cache), `DataSourceBinder` (routes through `foundation.dataset` registry), `PageExpander` (`<page for= as= key=>` + `<for-each>` wrapper + auto-key), `Preprocessor` (`<if test=>` + content-level `<for-each>` via shared safe AST), `PlaceholderBinder` (non-system tokens), `ResolvedXmlEmitter` (validated against `resolved.rng`), `EnumKernel`. **Stage 2 PDF (Phase 1):** `ResolvedXmlParser`, `UnitResolver`, `PaperSize`, `PdfLayoutEngine` (flow + overflow + headers/footers per `pagetype`), `TwoPassExecutor` (Pass 1 measure → Pass 2 emit with token subst), `PdfEngine` + `PdfWriter` (ReportLab Canvas, coordinate-level). **DRE engine impl (Phase 1):** `DocumentReportEngine` (registers as `engine_key="dre"` via `@api.report_engine`; `list_actions_for_context()` returns engine-specific actions; `dispatch()` runs doc-type → template cascade then Stage 1 + Stage 2; `validate_action()` for lock-time checks). **Phase 2:** `VariableEvaluator` formula-form (substrate AST), `GridIR` parser, `ChartParser` + `ChartSvgEmitter` (10 types · 5 themes), `BarcodeImageEmitter` (10 symbologies + 4 QR EC), `HtmlEngine` (standalone + email), `DocxEngine` (python-docx OOXML), `PostProcessor` chain (`Sign` → `Watermark` → `Encrypt` → `Compress` — PDF-only). **Phase 3:** `BatchExecutor` (parallel Stage 2 fanout), `DocumentLocaleResolver` (Babel/CLDR), `DmlNodePluginRegistry` (`@api.dml_node`), `RequiresValidator` (lock-time contract), React `DocumentDesigner` (drag-drop authoring → DML XML). **No print engine** — out of scope. | (planned) `src/ede/core/engines/reporting/` (platform) + `src/ede/core/engines/document/` (DRE core) + `src/ede/foundation/document/` (DRE shell) |
<!-- /SYNC-BLOCK -->

### Commands
<!-- SYNC-BLOCK: commands -->
| Command | Trigger | Effect |
|---|---|---|
| `ede.report.run` *(platform — primary)* | HTTP `/api/reports/<action_uuid>/run`, workflow actions, scheduled jobs, email-attachment, EDI emitters | Looks up `ir.action.report` by key; routes to `ReportEngineRegistry.resolve(action.engine_key).dispatch(...)`; returns bytes (and optional `storage.document` UUID when `persist=True`). Engine-agnostic — DRE is one of N. |
| `ede.document.render` *(DRE internal)* | HTTP `/api/document/<key>/render`, admin preview button, test fixtures | Resolves template directly by key (bypasses `ir.action.report` cascade) → Stage 1 → Stage 2 (single format) → audit row → emit event → return bytes. Used by admin form only; production callers go through `ede.report.run`. |
| `ede.document.resolve` *(DRE internal)* | HTTP `/api/document/<key>/resolve`, debugging | Stage 1 only → return resolved XML string |
| `ede.document.preview` *(DRE internal)* | In-browser designer live preview | Same pipeline as `render` but skips audit row + uses draft body |
| `ede.document.batch_render` *(DRE — Phase 3)* | HTTP `/api/document/<key>/batch_render` | One Stage 1 → parallel Stage 2 fanout → return `{format: bytes}` dict |
| `ede.create`/`ede.update`/`ede.delete` on `ir.document.template` / `ir.document.type` | Admin form / React designer | CRUD; locked templates block updates via hook; `<requires>` validation runs at lock-time (Phase 3) |
| `ede.create`/`ede.update`/`ede.delete` on `ir.action.report` | Admin form (in `foundation.base`) | CRUD; on lock transition, `ReportEngineRegistry.resolve(engine_key).validate_action()` is called |
<!-- /SYNC-BLOCK -->

### Events emitted
<!-- SYNC-BLOCK: events -->
| Event | When fired | Typical subscribers |
|---|---|---|
| `ede.document.rendered` | After successful render (Stage 2 complete) | Delivery dispatcher (e.g. email attachment); reporting Phase 3 (snapshot capture) |
| `ede.document.resolved` | After successful Stage 1 (resolution cached) | Audit log; cache-warmth indicators |
| `ede.document.render.failed` | After exception during render | Audit log; alerting |
<!-- /SYNC-BLOCK -->

### REST endpoints
<!-- SYNC-BLOCK: rest -->
| Method + Path | Purpose | Controller |
|---|---|---|
| `GET /api/reports/_list?model=<model_key>&record_id=<uuid\|null>&mode=form\|list` *(platform)* | **Universal print-menu lister** — iterates all registered engines (DRE + ERE + future engines), applies each action's `domain` per form/list mode, returns flat list of visible `ir.action.report` records. Extensions never appear (they layer on their parent). | `src/ede/foundation/base/controllers/reports_controller.py` |
| `POST /api/reports/<action_uuid>/run?format=<fmt>&persist=<bool>` *(platform)* | **Universal dispatcher** — looks up `ir.action.report.engine_key`, routes to the matching engine via `ReportEngineRegistry.resolve(engine_key).dispatch(...)`, returns bytes (or JSON envelope with `storage_document_uuid` when `persist=true`) | same |
| `POST /api/document/<key>/render` *(DRE internal)* | Render specific template by key, single format — used by admin preview + tests | `src/ede/foundation/document/controllers.py` |
| `POST /api/document/<key>/resolve` *(DRE internal)* | Stage 1 only → resolved XML string | same |
| `POST /api/document/<key>/preview` *(DRE internal)* | Render a draft body without audit (designer live preview) | same |
| `POST /api/document/<key>/batch_render` *(DRE — Phase 3)* | Multi-format from one Stage 1 → dict of format → bytes | same |
| `GET /api/document/data_sources` *(Phase 3)* | List registered business models + metrics + datasets for the React designer | same |
| `GET /api/document/data_sources/<key>` *(Phase 3)* | Schema for a specific data source | same |
| `POST /api/document/attachments` *(Enhancement 01 ✅)* | Upload bytes (multipart or JSON+base64) — required params: `model_key`, `record_uuid`, `field_name`, `usage_kind` | `src/ede/foundation/document/api/attachments.py` (AttachmentController) |
| `GET /api/document/attachments?model_key=&record_uuid=...` *(Enh 01 ✅)* | List attachments for a record, optional narrowing by `field_name` / `usage_kind` | same |
| `GET /api/document/attachments/{uuid}/download` *(Enh 01 ✅)* | Streams bytes with `Content-Disposition: attachment` | same |
| `GET /api/document/attachments/{uuid}/preview` *(Enh 01 ✅)* | Streams bytes with `Content-Disposition: inline` (for `<img src>`) | same |
| `DELETE /api/document/attachments/{uuid}` *(Enh 01 ✅)* | Delete row + cascade backend bytes via `pre.ir.attachment.delete` | same |
| `GET /api/document/dms/virtual/categories` *(Enh 01 ✅)* | Documents-app virtual-folder tree: list visible categories (Attachable consumers with `_attachment_document_visible=True`) | `src/ede/foundation/document/api/virtual_tree.py` (VirtualTreeController) |
| `GET /api/document/dms/virtual/category/{model_key}/records` *(Enh 01 ✅)* | Records with ≥ 1 attachment in this category, labeled per the model's `_attachment_document_record_label_field` | same |
| `GET /api/document/dms/virtual/category/{model_key}/record/{uuid}` *(Enh 01 ✅)* | Files for one record (filtered to `record_attachment`) | same |
| `GET /api/document/dms/search?q=&category=` *(Enh 01 ✅)* | Unified search across `storage.document` ∪ `ir.attachment` — each hit carries `source = "document" \| "attachment"` discriminator | `src/ede/foundation/document/api/unified_search.py` (UnifiedSearchController) |
<!-- /SYNC-BLOCK -->

### Lifecycle hooks
<!-- SYNC-BLOCK: hooks -->
| Hook key | Behavior |
|---|---|
| `pre.ir.document.template.update` | Blocks updates when `state="locked"`; on lock transition, validates every `<xpath expr=>` in the template's `dml` resolves against the parent (for extensions / copies) |
| `post.ir.document.template.update` | When a base template's `dml` changes, re-validate every active extension's xpath expressions against the new parent DML; auto-revert orphaned extensions to `state="draft"` with an audit log entry |
| `pre.ir.document.template.update` (Phase 3) | On `state → "locked"`, runs `RequiresValidator` — rejects if `<requires>` block is incomplete relative to template body |
| `pre.ir.document.render.audit.update` | Always raises — append-only |
| `pre.ir.document.render.audit.delete` | Always raises — append-only |
| `pre.ir.attachment.delete` *(Enhancement 01 ✅)* | Resolves backend via `StorageRouter.get_backend_for_version` + `backend.delete(storage_key)` before the DB row is removed. Non-vetoing — log + continue on backend failure (orphan blob recoverable; orphan DB row is not). |
| `pre.*.delete` cascade *(Enhancement 01 ✅)* | Wildcard hook in `foundation.document.hooks.attachment_cascade` — fires on every model's delete. Fast-path on `__ede_field_specs__` skips models with no `image` / `binary` / `attachments` field types. Otherwise routes through `AttachmentService.cascade_delete_for_record(env, model_key, record_uuid)` to remove every owned `ir.attachment` (any `usage_kind`, any `field_name`). `ir.attachment` itself excluded to prevent recursion. |
<!-- /SYNC-BLOCK -->

### State machine
<!-- SYNC-BLOCK: state-machine -->
`ir.document.template.state`:

```
draft  ──(action_lock; template must parse + validate cleanly)──►  locked
locked ──(action_unlock; bumps revision)─────────────────────────►  draft
```

Locked templates still render — locking only blocks edits. Phase 3 adds `<requires>`-based contract validation at the lock transition; a template referencing undeclared fields cannot lock.
<!-- /SYNC-BLOCK -->

## 3. Configuration Introduced

### Activation
<!-- SYNC-BLOCK: activation -->
- `ACTIVE_MODULES`: `"document"` (added in Phase 1)
- Manifest `depends`: `["base", "presentation", "dataset"]` — `storage` added in Phase 2 (post-processing); `l10n` added in Phase 3 (Babel/CLDR locale resolution)
<!-- /SYNC-BLOCK -->

### Foundation-level settings
<!-- SYNC-BLOCK: foundation-settings -->
| Setting Key | Type | Default | Env Var | Purpose |
|---|---|---|---|---|
| `DOCUMENT_DEFAULT_PAPER_SIZE` | str | `"A4"` | `DOCUMENT_DEFAULT_PAPER_SIZE` | Fallback when template omits `<paperformat>`. |
| `DOCUMENT_DEFAULT_LOCALE` | str | `"en"` | `DOCUMENT_DEFAULT_LOCALE` | Locale fallback for number/date formats. Babel/CLDR integration ships in Phase 3. |
| `DOCUMENT_RENDER_TIMEOUT_SECONDS` | int | `60` | `DOCUMENT_RENDER_TIMEOUT_SECONDS` | Hard timeout per render. |
| `DOCUMENT_STRICT_BINDING_DEFAULT` | bool | `False` | `DOCUMENT_STRICT_BINDING_DEFAULT` | When True, missing placeholders raise instead of rendering empty. |
| `DOCUMENT_RESOLUTION_CACHE_TTL_SECONDS` | int | `3600` | `DOCUMENT_RESOLUTION_CACHE_TTL_SECONDS` | TTL for `ir.document.resolution` rows. |
| `DOCUMENT_OUTPUT_ENGINES_ENABLED` | str | `"pdf"` (P1) · `"pdf,html,docx"` (P2) · `"pdf,html,docx,print"` (P3) | `DOCUMENT_OUTPUT_ENGINES_ENABLED` | Comma-separated enabled engines. |
| `DOCUMENT_PDF_SIGNING_KEY_PATH` (Phase 2) | str | `""` | `DOCUMENT_PDF_SIGNING_KEY_PATH` | Default signing key path for PDF post-processing. |
| `DOCUMENT_CHART_THEME_DEFAULT` (Phase 2) | str | `"default"` | `DOCUMENT_CHART_THEME_DEFAULT` | Chart theme fallback. |
| `DOCUMENT_BARCODE_QUIET_ZONE_MM` (Phase 2) | float | `2.0` | `DOCUMENT_BARCODE_QUIET_ZONE_MM` | Default white margin around barcodes. |
| `DOCUMENT_QR_ERROR_CORRECTION_DEFAULT` (Phase 2) | str | `"M"` | `DOCUMENT_QR_ERROR_CORRECTION_DEFAULT` | QR EC level default. |
| `DOCUMENT_HTML_EMAIL_LITMUS_STRICT` (Phase 2) | bool | `True` | `DOCUMENT_HTML_EMAIL_LITMUS_STRICT` | HTML email mode fails fast on disallowed constructs. |
| `DOCUMENT_DEFAULT_PRINTER` (Phase 3) | str | `""` | `DOCUMENT_DEFAULT_PRINTER` | Default printer name. |
| `DOCUMENT_BABEL_CACHE_SIZE` (Phase 3) | int | `128` | `DOCUMENT_BABEL_CACHE_SIZE` | LRU cache for Babel locale lookups. |
| `DOCUMENT_PLUGIN_PACKS_ENABLED` (Phase 3) | bool | `True` | `DOCUMENT_PLUGIN_PACKS_ENABLED` | Master switch for third-party `@api.dml_node` plugins. |
| `DOCUMENT_DESIGNER_PREVIEW_DEBOUNCE_MS` (Phase 3) | int | `500` | `DOCUMENT_DESIGNER_PREVIEW_DEBOUNCE_MS` | React designer live-preview debounce. |
<!-- /SYNC-BLOCK -->

### Runtime config (`ir.config` keys)
<!-- SYNC-BLOCK: ir-config -->
| Config Key | Scope | Type | Default | Purpose |
|---|---|---|---|---|
| _none_ | | | | |
<!-- /SYNC-BLOCK -->

### Declarative settings (XML `<settings>` panels)
<!-- SYNC-BLOCK: xml-settings -->
| Panel | File | Fields |
|---|---|---|
| _none_ | | |
<!-- /SYNC-BLOCK -->

### Seed data shipped
<!-- SYNC-BLOCK: seed-data -->
| Data file | What it seeds |
|---|---|
| `data/document_rbac.csv` | 3 RBAC roles — `ir.document.viewer`, `ir.document.author`, `ir.document.admin` |
| `data/document_menus.xml` | `Settings → Technical → Document Templates` |
| `demo/demo_document_templates.xml` | 4 Phase 1 demo templates (Default Quote / India Quote variant / Rate Sheet / Sales Acknowledgement) |
| `demo/templates/*.dml` | 4 Phase 1 DML template body files |
| `demo/templates/bill_of_lading.dml` (Phase 2) | Bill of Lading with chart + grid + barcode + QR + post-processing demo |
| `demo/demo_document_post_process.xml` (Phase 2) | Watermark + encrypt chain for B/L |
| `demo/templates/marketplace/*.dml` (Phase 3) | 8 commercial templates |
| `demo/demo_template_localization.xml` (Phase 3) | French / Spanish / German variants of default quote |
| `demo/templates/commercial_invoice.dml` (Phase 3) | reference `<requires>` block example |
<!-- /SYNC-BLOCK -->

## 4. Developer & User Notes

### Status Snapshot
<!-- SYNC-BLOCK: status-snapshot -->
| Phase | Title | Status | Roadmap |
|---|---|---|---|
| Phase 1 | Stage 1 Resolution + Stage 2 PDF Engine + **Platform `ReportEngine` Integration** (24 workstreams — **platform layer**: `ReportEngine` Protocol + `ReportEngineRegistry` + `@api.report_engine` decorator at `src/ede/core/engines/reporting/` + `ir.action.report` model in `foundation.base` + `ede.report.run` command + `/api/reports/*` endpoints; **Stage 1**: DML parser, RelaxNG, records-as-data template inheritance, `<include>`, variables + style cascade + cycle detection, datasource binding, page expansion with `<page for= as= if= key=>` + `<for-each>` wrapper + auto-key, within-page preprocessor, placeholder/`<field>` binding, resolved-XML emitter + schema, **data-loader `type="xml"` + `eval=` attribute support**, closed-vocab enums; **Stage 2 PDF**: resolved-XML parser, measurement primitives, layout engine, two-pass executor, native pixel-perfect PDF via ReportLab Canvas; **DRE engine impl**: `DocumentReportEngine` registers as `engine_key="dre"`; **Models**: `ir.document.type` (NEW taxonomy) + `ir.document.template` (with `doc_type_id` Reference) + `ir.document.resolution` + `ir.document.render.audit`; **Frontend WS-Y1**: React print-menu wiring consuming `/api/reports/*` + render-result delivery + admin-form modal components + bun build/test/Cypress; **Demo**: 4 base templates + l10n_in HSN extension + VIP copy + **Quotation FCL/LCL/AIR three-template demo (renamed from HBL demo 2026-05-26 — no shipment substrate yet; quotation reuse proves the doc-type→domain-based selection contract on real records via `logistics.sale.crm`)** proving doc-type → domain-based selection + paired `ir.action.report` rows + `ir.document.type` seed) | ✅ Delivered 2026-05-27 — **Phase 1 fully delivered**: 25 code-shipping slices — Stage 1 (A–F) + platform layer (G `ReportEngine` Protocol/registry/`@api.report_engine` · H `ir.action.report` · I `ReportDispatcher` + `ede.report.run` + `/api/reports/*` · J `DocumentReportEngine` `engine_key="dre"`) + Quotation seed (K1 `<bind-chrome>` + `ir.document.config` · K1b admin views + Settings menu · K2 Quote doc-type/template/action seeds) + L Frontend Print Menu. End-to-end Print Quote flow validated; full `./run_tests.sh` green (3357 pytest + 597 vitest); `bun run build` clean. · **Stage 1 fully ✅ complete (A+B+C+D+E+F)** · **Stage 1 Polish Slice F — Datasource Binding (WS-X6) ✅ Delivered 2026-05-26** (sixth + final slice; new `<rows datasource= as= key=>` block-level iterator fetches from the platform metric registry via `run_metric` at render time; new `RowsBlock` IR + `dml.rng` `rows-block` define; new `DataSourceResolver` runs between page-filter and placeholder-binder; PageExpander `_resolve_block_children` widened to `list[PageChild]` so RowsBlock survives expansion; new `Page.params_overlay` field carries per-iteration loop binding into nested `<rows>` metric calls; final pipeline `inheritance → includes → variables → parser → page-expansion → page-filter → datasource-resolver → placeholder-binder → emitter → Stage 2`; no caching in Phase 1; four error classes — `DataSourceError` umbrella + `DataSourceNotFoundError` + `DataSourceExecutionError` + `DataSourceNonTableModeError`; row-context substitution preserves global params + render-time tokens; 18 new tests; 332/332 DRE suite). **Stage 1 Polish Slice E — Formula-Form Variables (WS-X5 second pass) ✅ Delivered 2026-05-26** (fifth of the 6-slice "Stage 1 fully" sequence; lights up `<var name= type= formula="expr"/>` alongside the existing value form; consumes the safe AST evaluator at `src/ede/core/engines/formula/safe_eval.py` shipped by `foundation.dataset` Phase 2; function whitelist = `full` (math + aggregate + logic + date + format helpers); mutual-exclusivity check; cross-form dependency single graph; AST-derived dep edges filter call-targets + whitelisted function names + render-params + undeclared names; render-time params first-class via `VariableResolver.resolve(dml, params=…)`; typed parallel store — `resolved_str` for XML substitution + `typed_values` for evaluator scope; lenient formula-result coercion (INTEGER accepts whole floats, BOOLEAN accepts bool/int/string, DATE accepts date/datetime/ISO-strings); new `VariableFormulaError` class wraps `FormulaEvalError`; 39 new tests; 314/314 DRE suite); **Stage 1 Polish Slice D — Full `<field>` Element + Strict-Mode Flag (WS-X10) ✅ Delivered 2026-05-26** (rich placeholder element `<field name= format= default=/>` + closed-vocab `FormatSpec` enum (11 values) + per-spec format-arg parsing + opt-in strict mode. New `FormatSpec` enum in `enums.py` covers string/integer/decimal/currency/percent/date/datetime/boolean/upper/lower/title; registered in `ALL_ENUMS` (now 16 classes). New `FieldNode(name, format_spec, format_arg, default)` Pydantic class in `ir.py` appended to PageChild union. Parser's `_parse_field()` splits format= at first colon, validates head against FormatSpec, preserves arg tail. RelaxNG schema adds `field-node` define under block-child. PlaceholderBinder is fully rewritten: takes `strict: bool` kwarg, recursively walks the entire Phase 1 IR vocabulary (Group/Section/Component/Table/Header/Footer + every text-bearing leaf), text-substitutes via existing `{field}` regex, and flattens FieldNode → Paragraph with formatted text via new `_resolve_field()` + `_format_value()` helpers. `MissingPlaceholderError` raised in strict mode for unbound FieldNode without default; lenient mode leaves literal `{name}` token (visible debug hint). `_to_strftime()` helper translates YYYY/MM/DD/HH/mm/ss tokens longest-first. `DocumentService.render_by_key(... strict=False)` threads the flag through. **Two-form coexistence (load-bearing)**: `{field}` string form is always lenient; `<field>` element form is strict-aware. **FieldNode flattens at bind time** — same lifecycle as IfBlock/ForEachBlock; resolved-XML schema (Slice C) unchanged. 43 new tests across 7 classes; 275/275 DRE suite). **Stage 1 Polish Slice C — Full Emitter + Resolved-Parser Mirror (WS-X11B + WS-X13) ✅ Delivered 2026-05-22** (symmetric mirror of Slice B; every IR visual node emits to resolved XML + parses back. `src/ede/core/engines/document/resolved/schemas/resolved.rng` full rewrite — top-level structure permits optional `<styles>` + `<headers>` + `<footers>` siblings alongside `<meta>` + `<pages>`; `<meta>` widens to allow `<margins>`; `<block-child>` `<choice>` covers 11 visual node types (paragraph / text / span / heading / image / block-with-label-and-details-halves / group / section / component-with-keep_together / table-family / page-break). `src/ede/core/engines/document/resolved/parser.py` full rewrite — new Resolved* dataclasses mirror IR 1:1; ResolvedTree gains styles/headers/footers lists; ResolvedBlockChild union covers 11 visual node types; ResolvedPage.children widened to List[ResolvedBlockChild]; recursive _parse_block_children walks 11 tag branches; _parse_table handles colgroup + rows + cells. `src/ede/core/engines/document/dml/emitter.py` full rewrite — _emit_meta (with margins), _emit_styles, _emit_headers, _emit_footers, _emit_pages, _emit_one_page (preserves Slice 10 loop metadata), _emit_block_children dispatching on Python type via isinstance, per-node emitters for each of 11 visual types, _emit_table preserves colgroup + cell attrs + recursive cell contents; new _set_if helper avoids None/empty attribute artifacts. Symmetry contract enforced by tests: every IR node from Slice B has an exactly-matching tag in resolved.rng + a Resolved* dataclass — new IR nodes added without resolved-side support fail round-trip tests. Stage 2 readiness: PdfEngine remains minimum-subset; resolved XML now carries the full vocabulary so Stage 2 polish can add typography/table-layout/image-embedding without touching Stage 1 again — Stage 1 → Stage 2 contract complete for Phase 1. 20 new tests across 6 classes; 232/232 DRE suite. **First slice authored in `/tmp/ede-doc-engine` worktree on `document-engine-phases` branch forked from main — primary repo branch undisturbed**). **Stage 1 Polish Slice B — Full IR + Parser + RelaxNG (WS-X1 + WS-X2) ✅ Delivered 2026-05-22** (full Phase 1 DML grammar lands in IR + parser + schema. `src/ede/core/engines/document/dml/ir.py` adds ~14 new Pydantic classes: chrome (`Margins`, `StyleDef`, `VarDef`), inline + block content (`TextNode`, `SpanNode`, `HeadingNode`, `ImageNode`, `BlockNode` + `LabelNode` + `DetailsNode`, `GroupNode`, `SectionNode`, `ComponentNode` with `keep_together` + scoped variables), table family (Table/Row/Cell/ColGroup/Col with align/valign/colspan/rowspan/border-style/style_if), control (`OverrideNode` deprecated but parsed; `PageBreakNode`), page chrome (`HeaderNode`/`FooterNode` with `pagetype`), and `ForEachNode` page-level wrapper for multi-page-per-iteration. `PageChild` union widened to 14 alternatives; `Pages.children` accepts `Union[Page, ForEachNode]`; `Document` gains top-level `styles`/`variables`/`headers`/`footers` lists. Forward references resolved via explicit `model_rebuild()` chain. RelaxNG schema mirrors the IR — `<block-child>` choice has 15 alternatives. Parser threads Slice A closed-vocab enums via `_opt_enum` helper that raises `DmlValidationError` on unknown membership. 27 new tests across 6 classes; 1 obsolete hello-world test patched (Slice-2 `<heading>` rejection now tests `<bogus-tag>` for the unknown-tag invariant). User-attested: 211 pytest passed / 1 obsolete-assertion failure fixed by Slice B itself). **Stage 1 Polish Slice A — Full Enum Kernel (WS-X12) ✅ Delivered 2026-05-22** (12 new closed-vocab enums added to `enums.py`: `PageType` (9 vals) + `PageVisibility` (5) + `TextAlign` + `VerticalAlign` + `FontWeight` + `FontStyle` + `TextDecoration` + `TextTransform` + `BorderStyle` + `ImagePosition` + `OverrideMode` + `VarScope`. `ALL_ENUMS` registry contains 15 classes (Phase 2 chart/barcode/QR enums register here later). Test file rewritten with 3 classes — TestExistingEnums × 2 + TestNewStage1Enums × 13 + TestRegistry × 3 — locking the canonical value tuples as the stable contract for downstream RelaxNG fragment generators in Slice B/C. 18 enum tests; 184/184 DRE suite green. First of a 6-slice "Stage 1 fully" sequence A–F). **Full Schema Slice (WS-X18) ✅ Delivered 2026-05-22** (new `ir.document.type` taxonomy model + `ir.document.render.audit` append-only audit model + `doc_type_id` Reference on `ir.document.template`. Third migration `c7d3a8e2f5b1`. `DocumentService.render_by_key()` wraps engine in try/except writing audit rows on every render (success + failure-after-lookup). `_resolve_template_dml` captures `applied_extension_keys` for forensic reproducibility. `params_keys` records top-level keys only (no PII). `target_record_uuid` parameter threaded through for Slice 12 cascade integration. `ir.document.resolution` cache deferred to Phase 2. 7 integration tests; 170/170 DRE suite green). **Two-Pass Executor Slice (WS-X16) ✅ Delivered 2026-05-22** (`TwoPassExecutor` runs as the first step of `PdfEngine.render()`; Pass 1 measures pages, Pass 2 substitutes render-time tokens — `{PageNumber}` / `{TotalPages}` / `{LogicalPageId}` / `{LogicalPageIndex}` / `{PageInLogical}` / `{LogicalPageCount}` / `{LoopIndex}` / `{LoopCount}` / `{LoopKey}` all resolve. Page IR + ResolvedPage + resolved.rng gain `loop_index` / `loop_count` / `loop_key` for tagging loop-generated pages. PageExpander filter-then-index for dense LoopCount. PlaceholderBinder preserves loop metadata. 10 tests; 163/163 DRE suite). **Within-Page Conditionals & Loops Slice (WS-X9) ✅ Delivered 2026-05-22** (`<if test="expr">` + `<for-each items="key" as="x">` as page-children; `IfBlock` + `ForEachBlock` Pydantic IR nodes added, `Page.children` widened to `List[PageChild]` union with explicit `model_rebuild()`; parser's recursive `_parse_block_children` walks paragraph/if/for-each in document order; RelaxNG schema's `<page>` switches to a `block-child` choice; `PageExpander._resolve_block_children` recursively flattens to bare Paragraphs against the active context (within-page blocks see outer page-level loop variables); old per-as-name substitution helper removed in favor of unified context-based `_substitute_context_refs`; emitter unchanged. 13 integration tests; 153/153 DRE suite). **Page Conditional Slice (WS-X8) ✅ Delivered 2026-05-22** (`<page if="expr">` page-level conditional + `PredicateEvaluator` + `PageFilter` IR transform. Predicate grammar: truthy-of-dotted-path / `path == literal` / `path != literal` with type coercion. Combines with WS-X7 — when a loop-page has both `for=` and `if=`, the predicate evaluates against `{**params, as_name: item}` per iteration. IR `Page` adds `if_expr` field; RelaxNG schema allows the new attribute; `_render()` pipeline inserts `PageFilter` between expansion and placeholder-binding. 21 integration tests across 3 classes; 140/140 DRE suite). **Page Expansion Slice (WS-X7) ✅ Delivered 2026-05-22** (`<page for= as= key=>` attribute set + `PageExpander` IR transform materializes one physical page per loop iteration; list/tuple/dict source types; auto-key + 1-based `LoopIndex`; per-iteration `{as_name.field}` substitution via dotted-path lookup; render-time tokens `{LoopIndex}`/`{LoopCount}`/`{LoopKey}` survive untouched into resolved XML; IR `Page` extended with `for_expr`/`as_name`/`key_expr` optional fields; parser defaults `as_name="item"` when only `for=` set; RelaxNG schema allows three optional attrs; `_render()` pipeline inserts page expansion between parser and placeholder-binder; composes with WS-X3 inheritance (extension xpath injects loop-page — proven); 13 integration tests; 119/119 DRE suite green). **Variable Evaluator Slice (WS-X5 — value form) ✅ Delivered 2026-05-22** (`<var>` declarations + `$varName` / `${varName}` substitution; `VariableResolver` (new module) walks DML, extracts declarations, resolves dependency graph with DFS cycle detection (subset of Tarjan's SCC), removes `<var>` elements, then substitutes references in text + attributes; `VarType` enum (10 closed values) + per-type literal-pattern validators; pipeline order is now `inheritance → includes → variables → engine`; running variables AFTER includes is load-bearing — host's `<var>` declarations resolve inside content inlined from included templates; 20 integration tests; 106/106 DRE suite green). **`<include>` Directive Slice (WS-X4) ✅ Delivered 2026-05-22** (`IncludeResolver` inlines `<include src="key"/>` by walking DML + resolving by key from `ir.document.template` + replacing with source's `<page>` children; max recursion depth 5 via per-chain length check; per-chain cycle detection via tuple-typed `_chain`; composes cleanly with WS-X3 inheritance — extensions can inject `<include>`; `DocumentService.render_by_key()` calls `IncludeResolver.resolve()` between the inheritance pass and the engine pipeline; demo `demo.common.terms` + `demo.invoice.basic`; 13 integration tests; 86/86 DRE suite green). **Records-as-Data Inheritance Slice (WS-X3) ✅ Delivered 2026-05-22** (`parent_id` + `inherit_mode` + `domain` + `priority` + `organization_id` on `ir.document.template`; second Alembic migration `b5e2c8f491a0`; `XPathMerger` (Odoo-standard `inside`/`replace`/`before`/`after` semantics) + `DomainEvaluator` (safe `ast.literal_eval`-based EDE search-domain DSL); `DocumentService._resolve_template_dml` extends + copy modes; 3 demo templates — base + India-HSN extension + VIP copy; 12 integration tests; 73/73 DRE suite green; 4-variant PDF smoke). **Foundation Shell Slice ✅ Delivered 2026-05-22** (`ir.document.template` minimal model + Alembic migration + `DocumentService` + `ede.document.render` command + `foundation.document` manifest + `document` in ACTIVE_MODULES + demo template via `<field type="xml">` + 10 integration tests; `env.dispatch` end-to-end path online; full suite 2487 green). **Hello-World Engine Slice ✅ Delivered 2026-05-22** (DML → PDF pipeline via minimal subsets of 9 WS; 50 pytest cases). **WS-X11 Slice A ✅ Delivered 2026-05-22** (kernel data-loader `type="xml"` + `eval=` attribute support; 22 pytest cases) | [phase-1-implementation.md](../roadmap/foundation/document/phase-1-implementation.md) |
| Phase 2 | HTML + DOCX engines + Charts + Barcodes + Grid + Post-Processing (9 workstreams — formula-form `<var>`, `<grid columns=>`, 10 chart types × 5 themes (SVG), 10 barcode symbologies + 4 QR EC levels with formula-bindable values, **HTML output engine** (standalone + email), **DOCX output engine** (OOXML via python-docx), PDF chart/barcode/grid integration, post-processing (sign/watermark/encrypt/compress — PDF-only), Bill-of-Lading demo across all 3 formats; `DocumentReportEngine.supported_output_formats` widens to `["pdf","docx","html"]`) | 🔴 Not Started | [phase-2-implementation.md](../roadmap/foundation/document/phase-2-implementation.md) |
| Phase 3 | Multi-Format Batch + Localization + React Designer + Plugins + Validation (**8 workstreams — no print engine**; multi-format batch (one Stage 1 → parallel Stage 2 fanout), template localization (language_id FK + cascade), **React drag-drop document designer** (visual authoring — pick model/dataset, drop fields onto pages, emits DML), Babel/CLDR locale, `@api.dml_node` plugin architecture with per-engine hooks, `<requires>` authoring-time validation, 8-template marketplace seed) | 🔴 Not Started | [phase-3-implementation.md](../roadmap/foundation/document/phase-3-implementation.md) |
<!-- /SYNC-BLOCK -->

### Built Capabilities
<!-- SYNC-BLOCK: built -->
| Feature | Model Keys | Key Files | Roadmap Source |
|---|---|---|---|
| **Enhancement 01 — Attachment Substrate ✅ Delivered 2026-05-27** — polymorphic `ir.attachment` model + kernel `fields.Image` / `fields.Binary` / `fields.Attachments` + `Attachable` mixin (sibling to `Chatterable`) + `AttachableRegistry` + DRE `save_output=True` flow with SHA-256 dedup + auto-chatter-post + audit-row `output_attachment_id` FK + Documents-app virtual-folder tree projection + unified DMS search + `image_avatar` on res.partner/res.organization/res.user. First consumer: `crm.quote` extends `(Chatterable, Attachable)` with `_attachment_document_visible=True` and surfaces as the "Quotations" virtual category. 13 workstreams (WS-ATT-1 → WS-ATT-13); 15 commits on branch `worktree-document-attachment-substrate-enh01`. Suite **3357 pytest passed** (+97 vs 3260 baseline), frontend `bun run build` clean. Playwright e2e for UI slices deferred per user direction (code-only delivery for WS-ATT-11/12/13). | `ir.attachment` (new, foundation.document); `image_avatar` field on `res.partner` + `res.organization` + `res.user` (foundation.base, soft-FK); `output_attachment_id` field on `ir.document.render.audit`; `attachment_ids` M2M on `communication.message` (via `@api.extend_model` from foundation.document). | Backend: `src/ede/foundation/document/models/attachment.py` · `models/attachable.py` · `models/message_attachment_ext.py` · `services/attachment_service.py` · `services/attachable_registry.py` · `services/document_service.py` (save_output flow) · `api/attachments.py` · `api/virtual_tree.py` · `api/unified_search.py` · `hooks/attachment_cascade.py` · Kernel: `src/ede/core/kernel/fields.py` (Image / Binary / Attachments) · `src/ede/core/engines/document/output/pdf/engine.py` (`<image src="storage://<uuid>"/>` resolver via `AttachmentService.get_bytes`). Frontend: `src/frontend/src/workspace/services/VirtualTreeService.ts` · `services/UnifiedSearchService.ts` · `services/ReportService.ts` (added `runReportWithSaveToHistory`) · `components/dms/VirtualCategoryTree.tsx`. Migrations: `foundation.document` × 3 (`e3f9a5c2b817` ir_attachment + `e4f8c2a9b637` message↔attachment M2M + `f7a3c8e1b294` audit output_attachment_id); `foundation.base` × 1 (`c8e2a1b945f3` image_avatar on partner/org/user). Domain consumer: `src/domains/logistics/sales_crm/models/quote.py` extends (Chatterable, Attachable). | [Enhancement 01](../roadmap/foundation/document/enhancements/01-attachment-substrate.md) |
| **Slice L — Frontend Print Menu (WS-Y1)** — The final Phase 1 piece. Wires the existing Print menu button in `WorkspaceActionControlPanel` to the platform `/api/reports/*` HTTP surface; dynamically populates menu items from `useReportActions` per view context (model + record + mode); dispatches the chosen action via `runReport`; delivers the result via format-aware client-side routing (pdf/html → new tab via Blob URL, docx/xlsx → anchor download). Scope per user direction: Print menu on **list + form views only**, never kanban (kanban / automation / batch dispatch via the `ede.report.run` Command bus directly). New TypeScript modules: `ReportService` (typed wrappers around the Slice I HTTP surface, returning discriminated `InlineReportResult` | `PersistedReportResult` union) · `useReportActions` (React hook, auto-fetches per dependency change, cancellation-safe, short-circuits when `disabled`) · `ReportDelivery` (`base64ToBlob` + `defaultFilename` slugifier + format-aware `deliverInlineReport` + top-level `deliverReport` dispatcher). 2 new vitest files with 24 cases (12 ReportService + 12 ReportDelivery; uses manual mock replacement instead of `vi.spyOn` to avoid TS strict-mode generic-typing issues; `vi.useFakeTimers()` so the `setTimeout(URL.revokeObjectURL)` in `downloadBlob` doesn't fire after test restore). **Design notes (load-bearing)**: Print menu is the user-facing UI surface only — universal report triggering is the Command bus's job; engine-agnostic frontend (no `engine_key` specifics); format-to-delivery routing client-side only; `useReportActions` short-circuits to skip useless API calls; manual mocks in vitest under TS strict; Render Preview + Inspect Resolved XML admin-form buttons deferred to Phase 2; no E2E Cypress tests this slice. Verification: frontend `bun run build` + `bun run test` clean; full repo `./run_tests.sh` exits 0 (3055 pytest + 597 vitest = 3652 total green). **Phase 1 fully delivered** — end-to-end Print Quote flow works against the K2-seeded Quote document. — ✅ Delivered 2026-05-27 | (no new model — UI consuming Slice I `/api/reports/*` endpoints + Slice J/K1/K2 backend wiring) | [src/frontend/src/workspace/services/ReportService.ts](../src/frontend/src/workspace/services/ReportService.ts) (NEW · `listReportActions` + `runReport` + types) · [src/frontend/src/workspace/components/action/useReportActions.ts](../src/frontend/src/workspace/components/action/useReportActions.ts) (NEW · React hook) · [src/frontend/src/workspace/components/action/ReportDelivery.ts](../src/frontend/src/workspace/components/action/ReportDelivery.ts) (NEW · client-side delivery dispatch) · [src/frontend/src/workspace/components/action/WorkspaceActionControlPanel.tsx](../src/frontend/src/workspace/components/action/WorkspaceActionControlPanel.tsx) (modified · wires dynamic Print items + viewMode='list' guard) · [src/frontend/src/workspace/components/action/WorkspaceActionController.tsx](../src/frontend/src/workspace/components/action/WorkspaceActionController.tsx) (passes selectedIds to panel) · [ReportService.test.ts](../src/frontend/src/__tests__/services/ReportService.test.ts) (NEW · 12 cases) · [ReportDelivery.test.ts](../src/frontend/src/__tests__/services/ReportDelivery.test.ts) (NEW · 12 cases) | [phase-1-implementation.md Slice L](../roadmap/foundation/document/phase-1-implementation.md#slice-l--frontend-print-menu-ws-y1--delivered-2026-05-27) |
| **Slice K2 — Quote Document (Domain Seed in `logistics.sale_crm`) (WS-X20 part 2)** — Final WS-X20 commit. Ships the Quote-specific seeds in `logistics.sale_crm` that plug into the K1 engine substrate. With K2 done, the platform has a complete "Print Quote against `crm.quote`" path: `action_print_quote` registered against `crm.quote` → cascade resolves to `sale_crm.quote.standard` → `BindChromeResolver` injects the org's chosen header/footer → standard template's body is the Quote-specific xpath-patched content → engine produces PDF bytes. 3 new data files: `quote_doc_type.xml` (1 `ir.document.type` row, `key=quote`) · `quote_template.xml` (1 `ir.document.template` row, `inherit_mode=copy` of `foundation.document.standard`, body is a single `<xpath expr="//page[@id='standard-document-body']" position="replace">` patch wrapping `<for-each items="records" as="quote">` with cover + notes-and-terms pages per quote; multi-record-aware via K1 contract — single quote → 2 pages, 5-quote batch → 10 pages) · `quote_action_report.xml` (1 `ir.action.report` row, `key=sales_crm.action_print_quote`, `engine_key=dre`, `model_id` xml-id-ref to `sales_crm.model_crm_quote`, `target_record_uuid` xml-id-ref to the doc-type via `ref=` on Char field). Manifest adds `foundation.document` to `depends` + 3 data file entries in load order. **Design notes (load-bearing)**: Quote concepts live with the quote model (not in the engine); `ref=` on Char fields resolves xml_id → record_uuid string; `inherit_mode=copy` xpath-patches the parent's resolved DML; no quote-lines table in K2 (deferred, needs `quote.lines` metric in foundation.dataset); Reference field display-name resolution deferred to a follow-up. Verification: full repo `./run_tests.sh` exits 0 (3055 pytest + 573 vitest = 3628 total green). **WS-X20 fully delivered** across K1 + K1b + K2; Phase 1 Print Quote story is now end-to-end-wired except for the React frontend in Slice L. — ✅ Delivered 2026-05-27 | New: `ir.document.type#sales_crm.doc_type_quote` + `ir.document.template#sale_crm.quote.standard` + `ir.action.report#sales_crm.action_print_quote` (3 seed rows, no new model classes) | [src/domains/logistics/sales_crm/data/quote_doc_type.xml](../src/domains/logistics/sales_crm/data/quote_doc_type.xml) (NEW · quote doc-type seed) · [src/domains/logistics/sales_crm/data/quote_template.xml](../src/domains/logistics/sales_crm/data/quote_template.xml) (NEW · sale_crm.quote.standard template — inherit_mode=copy of foundation.document.standard, xpath patch replacing body placeholder with `<for-each items="records" as="quote">`) · [src/domains/logistics/sales_crm/data/quote_action_report.xml](../src/domains/logistics/sales_crm/data/quote_action_report.xml) (NEW · action_print_quote binding crm.quote → quote doc-type) · [src/domains/logistics/sales_crm/__manifest__.py](../src/domains/logistics/sales_crm/__manifest__.py) (added foundation.document to depends + 3 data files in load order) | [phase-1-implementation.md Slice K2](../roadmap/foundation/document/phase-1-implementation.md#slice-k2--quote-document-domain-seed-in-logisticssale_crm-ws-x20-part-2--delivered-2026-05-27) |
| **Slice K1b — Admin Views + Settings Menu (WS-X20 part 1b)** — Follow-up to K1's engine substrate. Lands the admin UX for editing every WS-X20-introduced record via the React webclient. With K1b done, the document-engine surface is fully manageable from the UI: tenants pick their default header/footer chrome by editing an `ir.document.config` row, browse / edit / create `ir.document.template` records, manage `ir.document.type` taxonomy, and configure `ir.action.report` rows — all without touching XML data files. **4 admin view files** (one per model — template form has notebook with 4 tabs General/DML Body/Inheritance/Applicability; doc-type form is single-tab; config form has Scope + Chrome Templates sections with Reference dropdowns; action.report form has notebook with 4 tabs General/Target/Output/Scope). **Settings → Documents menu subtree** at sequence 50 under `base.menu_settings_root` with 4 leaves: Document Settings (the chrome-picker UX, sequence 10) · Document Templates (sequence 20) · Document Types (sequence 30) · Report Actions (sequence 40). The Settings DSL panel was deferred — the `ir.document.config` admin form already provides the per-org chrome-picker UX via Settings → Documents → Document Settings; the Settings DSL is shaped for flat `ir.config` key-value writes and doesn't naturally surface a record-aware form with org-scope + Reference dropdowns. Menus live in `foundation.document` while the view file for `ir.action.report` lives in `foundation.base` — the view file is adjacent to the model (foundation.base, Slice H decision) while the menu + action exposing the view is in `foundation.document` because DRE is the marquee engine consumer in Phase 1. 0 new tests (views are declarative XML validated at parse time by the existing presentation DSL validator + data-file validator); full repo `./run_tests.sh` exits 0 (3055 pytest + 573 vitest = 3628 total green). — ✅ Delivered 2026-05-27 | (no new model — admin views surface the 4 existing models: `ir.document.template` / `ir.document.type` / `ir.document.config` / `ir.action.report`) | [src/ede/foundation/document/views/ir_document_template_views.xml](../src/ede/foundation/document/views/ir_document_template_views.xml) (NEW · 4-tab form) · [src/ede/foundation/document/views/ir_document_type_views.xml](../src/ede/foundation/document/views/ir_document_type_views.xml) (NEW · simple form) · [src/ede/foundation/document/views/ir_document_config_views.xml](../src/ede/foundation/document/views/ir_document_config_views.xml) (NEW · chrome-picker form) · [src/ede/foundation/base/views/ir_action_report_views.xml](../src/ede/foundation/base/views/ir_action_report_views.xml) (NEW · 4-tab form in foundation.base) · [src/ede/foundation/document/data/document_admin_menus.xml](../src/ede/foundation/document/data/document_admin_menus.xml) (NEW · Settings → Documents subtree with 4 leaves) | [phase-1-implementation.md Slice K1b](../roadmap/foundation/document/phase-1-implementation.md#slice-k1b--admin-views--settings-menu-ws-x20-part-1b--delivered-2026-05-27) |
| **Slice K1 — Engine Substrate: `<bind-chrome>` DSL + Pages-Level `<for-each>` + `ir.document.config` + Reworked Render Context (WS-X20 part 1)** — First commit of the WS-X20 K split. Lands the engine substrate every domain document module copy-inherits before authoring its own template. New `<bind-chrome from-config="<field>"/>` DSL element resolves at render time by reading the active `ir.document.config` row (per-org → NULL-org cascade) and inlining the bound template's `<header>` / `<footer>` chrome. New `BindChromeResolver` pipeline step runs between WS-X3 inheritance and WS-X4 includes so bound chrome bodies participate in the rest of the pipeline (their own includes, variables, datasources, placeholders all run). Pages-level `<for-each items="records" as="r"><page/><page/></for-each>` finally wired through `PageExpander` (IR class + parser + schema shipped in WS-X1; expander was crashing on `ForEachNode` siblings; new `_expand_pages_for_each` helper unrolls the wrapper, clones inner pages per iteration, stamps loop metadata + `params_overlay`). New `ir.document.config` model (5 fields — `organization_id`, `default_header_template_id`, `default_footer_template_id`, `display_name`, `active`) + Alembic migration. **`DocumentReportEngine.dispatch` context contract reworked** — Slice J placeholder shape dropped; engine now guarantees three standard keys in every render's params: `records: list[dict]` (always a list, single → list of 1), `organization: dict` (from `env.active_organization_id`, `{}` on miss), `user: dict` (from `env.principal["user_id"]`, `{}` on miss). Caller params merge on top. Template authors reach data via `records[N].field` or `<for-each items="records">` — multi-record-mode-only contract. Data seeds: `foundation.document.standard` base template (A4 portrait, 4 default styles, two `<bind-chrome>` markers, placeholder body slot) + 6 chrome fragments (`header_minimal/formal/branded`, `footer_minimal/formal/branded`) + NULL-org system-default `ir.document.config` row pointing at minimal fragments. 17 new tests across 5 classes; full repo `./run_tests.sh` exits 0 (3055 pytest + 573 vitest = 3628 green). Admin views (`ir.document.template`, `ir.document.type`, `ir.document.config`, `ir.action.report` forms) + Settings panel under General Settings > Documents are deferred to follow-up K1b. — ✅ Delivered 2026-05-27 | New: `ir.document.config` · Updated context contract on existing DRE engine (no new model for the engine itself — augments existing `DocumentReportEngine` from Slice J) | [src/ede/core/engines/document/dml/bind_chrome.py](../src/ede/core/engines/document/dml/bind_chrome.py) (NEW · `BindChromeResolver` + 3 error classes) · [src/ede/core/engines/document/dml/page_expander.py](../src/ede/core/engines/document/dml/page_expander.py) (extended `_expand_pages_for_each` for `ForEachNode` unrolling) · [src/ede/foundation/document/models/document_config.py](../src/ede/foundation/document/models/document_config.py) (NEW · `IrDocumentConfig` model) · [src/ede/foundation/document/migrations/versions/d7dc5275ec8d_add_ir_document_config.py](../src/ede/foundation/document/migrations/versions/d7dc5275ec8d_add_ir_document_config.py) (NEW migration) · [src/ede/foundation/document/services/document_report_engine.py](../src/ede/foundation/document/services/document_report_engine.py) (reworked `dispatch` context contract + 4 new helpers) · [src/ede/foundation/document/services/document_service.py](../src/ede/foundation/document/services/document_service.py) (`BindChromeResolver.resolve` wired into Stage 1 pipeline) · [src/ede/foundation/document/data/document_chrome_fragments.xml](../src/ede/foundation/document/data/document_chrome_fragments.xml) (NEW · 6 fragments) · [src/ede/foundation/document/data/document_standard.xml](../src/ede/foundation/document/data/document_standard.xml) (NEW · base skeleton) · [src/ede/foundation/document/data/document_config_default.xml](../src/ede/foundation/document/data/document_config_default.xml) (NEW · NULL-org config row) · [test_k1_substrate.py](../src/tests/foundation/document/test_k1_substrate.py) (NEW · 17 cases across 5 classes) | [phase-1-implementation.md Slice K1](../roadmap/foundation/document/phase-1-implementation.md#slice-k1--engine-substrate--bind-chrome--pages-level-for-each--irdocumentconfig--reworked-render-context-ws-x20-part-1--delivered-2026-05-27) |
| **Platform Layer Slice J — `DocumentReportEngine` Implements Platform Protocol (WS-X19)** — Fourth + final slice of the platform-layer 4-slice sequence G→J. Registers DRE as `engine_key="dre"` against the platform `ReportEngine` Protocol shipped in Slice G. With Slice J done, the platform layer is fully delivered: DRE is now a regular registered engine. The platform `ReportDispatcher` routes `ir.action.report` rows with `engine_key="dre"` to `DocumentReportEngine.dispatch()`, which resolves `(target_model_key, target_record_uuid)` to a specific `ir.document.template` (direct uuid lookup or doc-type cascade) and delegates to `DocumentService.render_by_key()`. New `@api.report_engine`-decorated **`DocumentReportEngine`** class at `src/ede/foundation/document/services/document_report_engine.py` (~370 LoC) with `engine_key="dre"`, `display_name="Document Report Engine"`, `supported_output_formats=["pdf"]` (widens to `["pdf","docx","html"]` in Phase 2). Three Protocol classmethods: **`list_actions_for_context(env, model_key, target_record_uuid, is_multi_record)`** resolves `model_key` → `ir.model.record_uuid` first (returns [] if model not in `ir.model`), then filters `ir.action.report` rows where `engine_key="dre" AND active=True AND model_id=<uuid>`, applies org-filter in Python (`active_org=None` → only NULL-org rows; `active_org=X` → NULL-org OR matching X); in form mode (`target_record_uuid` present + `is_multi_record=False`) loads the target record and evaluates each action's `domain` against it via `DomainEvaluator.matches()`; in list mode bypasses domain. Malformed-domain rows are defensively excluded (the action's lifecycle hook surfaces the syntax error on next edit). Returns list of `ActionDescriptor` from `_to_descriptor()` which CSV-parses `available_output_formats` and unpacks priority/target fields. **`dispatch(env, action, target_record_uuids, params, output_format)`** verifies `output_format` is in `supported_output_formats` (raises `ValueError("not in supported_output_formats=...")`), resolves target via `_resolve_template()`: when `target_model_key="ir.document.template"` calls `_find_template_by_uuid()`; when `target_model_key="ir.document.type"` runs `_select_template_for_doc_type()` which filters non-`extend` templates with matching `doc_type_id`, partitions into org-matched bucket + NULL-org bucket, sorts each by `priority asc`, takes first of the org bucket if any else first of NULL bucket. Auto-augments `params` with `target_record_uuid` (first of the list) + `target_record_uuids` so DML datasources (Slice F WS-X6) can scope queries to the action's target row(s). Calls `DocumentService.render_by_key(env, key=template["key"], params=augmented_params, format=output_format, target_record_uuid=first_uuid)` and returns the raw bytes. **`validate_action(action)`** returns `list[ValidationError]`: rejects `target_model_key` not in `("ir.document.template", "ir.document.type")` with the offending value in the error; rejects empty `target_record_uuid`; when env is reachable on the action (via `getattr(action, "env", None)`), additionally verifies template uuid points at an active row, or doc-type cascade resolves to ≥ 1 non-`extend` template; without env (offline admin validation) skips DB-touching checks — the lifecycle hook on `ir.action.report` (Slice H) re-runs validation once the row hits the DB. Module wired into the app's import chain: `src/ede/foundation/document/services/__init__.py` adds `from . import document_report_engine` (side-effect import — `@api.report_engine` registers at module-import time); `src/ede/foundation/document/__init__.py` prepends `from . import services` so the engine module is imported when the app loads. **Design notes (load-bearing)**: (1) Two distinct target shapes — `ir.document.template` (admin/debug, direct uuid) vs `ir.document.type` (production, cascade). The admin form binds a specific template by uuid for "test this template now" actions; the production action binds the abstract doc-type and lets the cascade pick the right variant per org/priority/active flag. (2) `extend`-mode templates are filtered from the cascade — they layer onto a parent and never render alone; `DocumentService._is_extension()` rejects direct rendering of an extend template, and the cascade pre-filter here matches that contract. (3) Org filter — prefer org-matched over NULL-org, sort by priority within bucket. A more-specific template (org-scoped) beats a less-specific one (global) even when the global one has a lower priority number. This matches the records-as-data inheritance precedence (Slice 4 WS-X3) where org-scoped extensions layer onto a parent. (4) Auto-augment `params` with `target_record_uuid` + `target_record_uuids` lets DML datasources reference the action's target row(s) without callers having to wire those in. (5) `validate_action` skips DB checks when no env — offline admin validators (form-render checks before save) can call `validate_action(action)` from a UI context without a database, get back shape errors, and not crash on missing env. The full check re-runs at write-time via the `ir.action.report` lifecycle hook. (6) Format-list cross-check at four redundant layers (action write-time + dispatcher runtime + engine validate_action + engine dispatch) — each layer has different lifecycle visibility; redundancy is the contract enforcement. 14 new tests across 6 classes: TestEngineRegistration × 2 (engine self-registered via decorator + class-attribute presence), TestValidateAction × 7 (returns [] for valid template / returns [] for valid doc-type / rejects unsupported `target_model_key` like `res.partner` / rejects missing `target_record_uuid` / rejects template uuid pointing at nonexistent row / rejects doc-type with no cascade candidates / skips DB checks without env), TestDispatchDirectTemplate × 3 (calls `render_by_key` with template key + auto-augmented params + first uuid as target_record_uuid via `unittest.mock.patch` stub / raises when target template missing / raises when format unsupported), TestDispatchDocTypeCascade × 4 (picks lowest priority when only NULL-org templates exist / org-specific beats NULL-org even at higher priority / excludes `extend`-mode templates from cascade / raises when cascade resolves to nothing), TestListActionsForContext × 7 (DRE-only rows, returns [] for model not in `ir.model`, filters inactive via soft-archive, filters by model_id, org filter NULL-only when no active org, org filter includes matching + NULL when active org, form-mode evaluates domain against a real `res.organization` record, list-mode bypasses domain), TestReportDispatcherIntegration × 1 (end-to-end through `ReportDispatcher.dispatch` → `DocumentReportEngine.dispatch` → patched `DocumentService.render_by_key` returns `{"bytes": b"%PDF-FAKE", "format": "pdf"}`). `_AttrView` test helper bridges dict → attr access for `validate_action` calls. Full repo `./run_tests.sh` exits 0 (3032 pytest + 573 vitest green); ruff clean on the new file. — ✅ Delivered 2026-05-27 | (no new model — the engine consumes `ir.action.report` (Slice H) + `ir.document.template` (Slice 3 / Slice 11) + `ir.document.type` (Slice 11) and delegates rendering to the existing `DocumentService`) | [src/ede/foundation/document/services/document_report_engine.py](../src/ede/foundation/document/services/document_report_engine.py) (NEW · `DocumentReportEngine` class with 3 Protocol classmethods + 6 query helpers) · [src/ede/foundation/document/services/__init__.py](../src/ede/foundation/document/services/__init__.py) (`from . import document_report_engine` — registration side effect) · [src/ede/foundation/document/__init__.py](../src/ede/foundation/document/__init__.py) (`from . import services` — pulls the engine module into the app's import chain) · [test_document_report_engine.py](../src/tests/foundation/test_document_report_engine.py) (NEW · 14 cases across 6 classes) | [phase-1-implementation.md Platform Layer · Slice J](../roadmap/foundation/document/phase-1-implementation.md#platform-layer--slice-j--documentreportengine-implements-platform-protocol-ws-x19--delivered-2026-05-27) |
| **Platform Layer Slice I — `ReportDispatcher` + `ede.report.run` command + `/api/reports/*` (WS-X0c)** — Third slice of the platform-layer 4-slice sequence G→J. Closes the engine-agnostic dispatch path. New **`ReportDispatcher` service** at `src/ede/foundation/base/services/report_dispatcher.py` with two public classmethods: `list_for_context(env, model_key, target_record_uuid, is_multi_record=False) → list[ActionDescriptor]` aggregates `engine.list_actions_for_context(...)` across every registered engine and sorts by `(priority ASC, name ASC)` for deterministic display; `dispatch(env, action_key, target_record_uuids, params=None, output_format="pdf", persist=False) → dict` looks up `ir.action.report` by key (filtered by `key == action_key AND active == True`), validates format at two layers (action's `available_output_formats` + engine's `supported_output_formats` — catches admin-downgrade race), resolves the engine via `ReportEngineRegistry.resolve()`, calls `engine.dispatch(env, action, target_record_uuids, params, output_format) → bytes`, optionally writes to `storage.document` via `_persist_to_storage()` when `persist=True`. Returns `{"bytes": <raw>, "format": <fmt>, "storage_document_uuid": <uuid|None>}`. Three new error classes: `ReportDispatcherError(ValueError)` umbrella + `ReportActionNotFoundError` (no active row with that key) + `ReportFormatNotAvailableError` (covers both action-layer and engine-layer format mismatches — same exception class because the user-facing fix is identical) + `ReportEngineNotRegisteredError` (wraps `ReportEngineNotRegistered` from `ede.core.engines.reporting.errors`). Engine.dispatch raises propagate as-is — engines own their own error story. New **`ede.report.run` command** as `@api.on_command` method on `IrActionReport`: reads `action_key` / `target_record_uuids` / `params` / `format` (default `"pdf"`) / `persist` (default False) from `cmd.payload`, delegates to `ReportDispatcher.dispatch()`. Mirrors the `ede.document.render` pattern. Module-top import of `ReportDispatcher` is safe (service imports only kernel modules — no foundation.base.models import → no cycle). New **`ReportsController` HTTP surface** at `src/ede/foundation/base/api/reports_routes.py` with two routes both `auth="user"` (JWT required): `GET /api/reports/_list?model=<key>&record_id=<uuid|null>&mode=form|list` delegates to `ReportDispatcher.list_for_context()` and serializes the `ActionDescriptor` dataclasses to JSON via `_descriptor_to_json()` — mode defaults to `"form"` when `record_id` is present else `"list"`. `POST /api/reports/{action_key}/run?format=<fmt>&persist=<bool>` accepts `target_record_uuids` + `params` in the JSON body, delegates to `ReportDispatcher.dispatch()` — when `persist=True` returns a JSON envelope `{"storage_document_uuid": "...", "format": "pdf", "byte_count": N}`; when `persist=False` (default) returns base64-encoded bytes envelope `{"format": "pdf", "content_type": "application/pdf", "bytes_base64": "...", "byte_count": N}` (base64 wrapping is the Phase 1 contract — ~33% size overhead but eliminates Content-Type negotiation, makes responses trivially loggable, keeps the route shape identical across persist=True/False; streaming optimization is a Phase 2 polish concern). `src/ede/foundation/base/api/__init__.py` — appended `from . import reports_routes`. **Design notes (load-bearing)**: (1) `ede.report.run` is the canonical platform dispatch entry point — production callers go through it; `ede.document.render` (Slice 3 DRE Foundation Shell) remains as an internal entry point that `DocumentReportEngine.dispatch()` (Slice J) will call via the action's resolution cascade; admin debug surfaces (form preview, "inspect resolved XML") may still call it directly. (2) Format cross-check at two layers — model-layer write-time (Slice H's `_validate_action`) ensures saved rows have a sane `(engine_key, available_output_formats)` combo at the moment of write; dispatcher-layer runtime check catches the "admin narrowed `engine.supported_output_formats` after the action was saved" race. Two checks; same `ReportFormatNotAvailableError` so callers handle both identically. (3) `storage.document` persistence is best-effort, not transactional — if persistence fails (model not registered, disk full, cloud backend error), the caller still gets the bytes back; logging persistence errors + retry strategies live higher up the stack (HTTP retry, job retry); the dispatcher doesn't second-guess what the caller wants on failure. (4) Base64 envelope on `persist=False` makes the bytes transport-safe through JSON, costs ~33% size overhead, eliminates Content-Type negotiation, makes the response trivially loggable for audit (no raw-bytes-in-log noise), and keeps the route shape identical across `persist=True/False` paths — a streaming optimization for large reports is a Phase 2 polish concern. (5) Lister returns one flat list across engines — UI doesn't see "DRE actions" vs "ERE actions"; it sees one Print menu. The engine_key is in each descriptor for client-side routing back to `POST /{action_key}/run`, but UX is engine-agnostic. Future engines (ERE, PowerPoint, native PostScript, EPUB) plug into the same lister with zero frontend changes. 12 new tests across 5 classes: TestDispatchHappyPath × 3 (dispatch returns correct bytes+format / calls engine with correct args / multi-record bulk dispatch with 3 record UUIDs), TestDispatchErrors × 3 (unknown action_key raises `ReportActionNotFoundError` / format-not-in-available raises `ReportFormatNotAvailableError` / engine cleared from registry after action created raises `ReportEngineNotRegisteredError`), TestListForContext × 3 (aggregates across 2 stub engines and sorts by priority — `_StubEngineB` priority 10 before `_StubEngineA` priority 20 / same priority secondary-sorts by name ASC / no engines registered returns `[]`), TestPersist × 1 (`persist=True` returns `storage_document_uuid=None` when `storage.document` not in `env.models` — best-effort path; bytes still returned), TestEdeReportRunCommand × 2 (`env.dispatch(Command("ede.report.run", ...))` returns the same envelope as direct `ReportDispatcher.dispatch()` / missing `action_key` raises `ValueError("missing 'action_key' in payload")`). Stub-engine pattern: two stub classes (`_StubEngineA` engine_key=`"stub_a"` formats=`["pdf","docx"]` + `_StubEngineB` engine_key=`"stub_b"` formats=`["html"]`) share a `_BaseStub` parent with class-level `last_dispatch_args` + `listed_descriptors` slots that tests pre-populate or read back; `_BaseStub.reset()` clears between tests; the `env` fixture clears + re-registers `_StubEngineA` between tests. — ✅ Delivered 2026-05-26 | (no new model — adds `@api.on_command("ede.report.run")` method on the existing `ir.action.report` model from Slice H) | [src/ede/foundation/base/services/report_dispatcher.py](../src/ede/foundation/base/services/report_dispatcher.py) (NEW · `ReportDispatcher.list_for_context()` + `ReportDispatcher.dispatch()` + 3 error classes) · [src/ede/foundation/base/models/ir_action_report.py](../src/ede/foundation/base/models/ir_action_report.py) (`@api.on_command("ede.report.run")` `handle_run()`) · [src/ede/foundation/base/api/reports_routes.py](../src/ede/foundation/base/api/reports_routes.py) (NEW · `ReportsController` with `/api/reports/_list` GET + `/api/reports/{action_key}/run` POST) · [src/ede/foundation/base/api/__init__.py](../src/ede/foundation/base/api/__init__.py) (controller registration) · [test_report_dispatcher.py](../src/tests/foundation/test_report_dispatcher.py) (12 cases across 5 classes) | [phase-1-implementation.md Platform Layer · Slice I](../roadmap/foundation/document/phase-1-implementation.md#platform-layer--slice-i--reportdispatcher--edereportrun-command--apireports-ws-x0c--delivered-2026-05-26) |
| **Platform Layer Slice H — `ir.action.report` Model in `foundation.base` (WS-X0b)** — Second slice of the platform-layer 4-slice sequence G→J. Lands the engine-agnostic report-action descriptor — one row per printable / exportable action surfaced in the React webclient's Print menu. Polymorphic across engines via `(target_model_key, target_record_uuid)`: DRE rows point at `ir.document.type` (cascade resolves to template) or `ir.document.template` (admin/debug specific); future ERE rows point at `ir.excel.template`. New `@api.model("ir.action.report", default_order="priority asc, name asc")` on `IrActionReport(DomainModel)` at `src/ede/foundation/base/models/ir_action_report.py` with 11 business fields: `name` (Char 128, required, menu label) · `key` (Char 128, required + unique + index, stable seed-data identifier like `action_print_quote_standard`) · `engine_key` (Char 64, required, index, validated against `ReportEngineRegistry` at write-time) · `model_id` (Reference→`ir.model`, restrict, required, index, the business model this prints for) · `target_model_key` (Char 64, required, polymorphic discriminator) · `target_record_uuid` (Char 36, required) · `available_output_formats` (Char 128, required, comma-separated subset of `engine.supported_output_formats`) · `domain` (Char multi-line, nullable EDE search-domain — lister evaluates against the current record in form mode, bypassed in list mode) · `organization_id` (Reference→`res.organization`, restrict, nullable — NULL = global, populated = hard-scoped) · `priority` (Integer default 10, ASCending menu order) · `active` (Boolean default True, soft-archive). **Two lifecycle hooks** `pre.ir.action.report.create` + `pre.ir.action.report.update` call `_validate_action(record)` which: (a) resolves `engine_key` via `ReportEngineRegistry.resolve()` and raises `ValueError("references unregistered engine_key=...")` on miss (catches typos + boot-order mistakes — Slice J registers DRE; before that, only test stubs are registered); (b) cross-checks every requested format in `available_output_formats` against `engine.supported_output_formats` (raises with the unsupported-format list so seed loaders surface the problem at write-time rather than at runtime dispatch); (c) delegates to `engine.validate_action(record)` and aggregates returned `ValidationError` list (one item per problem) into a single raised `ValueError`. **The update hook uses a duck-typed `_MergedView(cmd.record, cmd.payload["values"])` overlay** so validation sees the POST-update state — without this, an update changing `engine_key` to `"nonexistent"` would slip through because `cmd.record` is the OLD DB row. New Alembic migration `5fa2a5742d5a_add_ir_action_report.py` chained off `b8e1f4a92c37` (latest foundation.base head, PR #2 `res_organization.name` nullable fix); declares 2 hard FKs inline (`fk_ir_action_report_model_id_ir_model` restrict + `fk_ir_action_report_organization_id_res_organization` restrict) + 2 unique constraints (`uidx_ir_action_report_record_uuid` audit + `uidx_ir_action_report_key` business) + 3 single-column indexes (engine_key, model_id, organization_id) + **2 composite indexes for the lister's hot paths** (`idx_ir_action_report_model_org_active` = `(model_id, organization_id, active)` for form/list-mode lookup + `idx_ir_action_report_engine_active` = `(engine_key, active)` for engine-scoped queries) + 4 standard kernel audit indexes (`created_at_utc` / `updated_at_utc` / `created_uid` / `updated_uid`). All constraint names wrapped in `op.f(...)` per the `writing-alembic-migrations` skill; every name verified ≤ 52 chars (well under PG's 63-char NAMEDATALEN-1 limit). New-table migration → no `batch_alter_table` needed for SQLite compat (constraints declared inline in `create_table`). Downgrade drops indexes LIFO then drops the table. **Design choices (load-bearing)**: (1) **Hard FKs at DB layer + soft engine_key validation at model layer** — `model_id` + `organization_id` are stable platform tables that exist before any engine boots, so restrict-on-delete is enforceable at migration time. `engine_key` references the in-process `ReportEngineRegistry` which only populates at boot via `@api.report_engine` — a DB FK would be circular. Lifecycle hook is the correct enforcement point: fires every create/update against the live registry. (2) **Merged-view on pre-update is load-bearing** — without it, an update changing `engine_key` to invalid would slip through because `cmd.record` is the pre-update DB row. The `_MergedView` overlay (a 12-line duck-typed class with `__slots__ = ("_record", "_overlay")` and an overlay-first `__getattr__`) ensures the validator sees the POST-update state. (3) **Format cross-check at write-time** means seed loaders, admin form saves, and import workflows all surface format/engine mismatches at the same place; the runtime dispatcher (Slice I) doesn't need its own check. (4) **`target_model_key` is intentionally a plain Char, not a Reference** — the whole point of the polymorphic discriminator pattern is that ONE column references DIFFERENT tables depending on the engine + use case. A hard FK would force a single target — defeating polymorphism. Validation that the discriminator points at a real row in the right table is the engine's `validate_action()` responsibility. (5) **Two composite indexes match the two lister query shapes** — the print-menu lister (Slice I) makes two main lookups: (model, org, active) for "show me the print actions for this record on this page" and (engine, active) for "show me everything DRE knows how to do". Hot-path indexes for both. 11 new tests across 4 classes: TestCRUD × 3 (create-and-read / update via `Command(model_key=..., model_id=record_uuid, payload={"values":...})` / delete + verify search returns empty), TestUniqueKey × 1 (duplicate-key raises sqlite UNIQUE constraint violation), TestEngineValidation × 4 (unregistered engine_key raises on create / unregistered engine_key raises on update — proves the `_MergedView` plumbing end-to-end / unsupported format raises with the offending format in the error message / engine-side `validate_action` errors raise as aggregated message via second `_StubEngineWithError` registered alongside the happy-path `_StubEngine`), TestRequiredFieldsAndDefaults × 3 (priority default 10 / active default True / empty `available_output_formats` rejected with required-non-empty message). Stub-engine pattern: `autouse=True` `clean_registry` fixture clears + re-registers `_StubEngine` between tests so registry state never leaks. — ✅ Delivered 2026-05-26 | `ir.action.report` (11 business fields + 7 kernel audit columns; 2 hard FKs to `ir.model` + `res.organization`; 2 unique constraints + 5 single-column + 2 composite indexes) | [src/ede/foundation/base/models/ir_action_report.py](../src/ede/foundation/base/models/ir_action_report.py) · [migrations/versions/5fa2a5742d5a_add_ir_action_report.py](../src/ede/foundation/base/migrations/versions/5fa2a5742d5a_add_ir_action_report.py) · [models/__init__.py](../src/ede/foundation/base/models/__init__.py) (registration) · [test_ir_action_report.py](../src/tests/foundation/test_ir_action_report.py) (11 cases) | [phase-1-implementation.md Platform Layer · Slice H](../roadmap/foundation/document/phase-1-implementation.md#platform-layer--slice-h--iractionreport-model-in-foundationbase-ws-x0b--delivered-2026-05-26) |
| **Platform Layer Slice G — `ReportEngine` Protocol + Registry + `@api.report_engine` (WS-X0a)** — First slice of the platform-layer 4-slice sequence G→J (G WS-X0a → H WS-X0b → I WS-X0c → J WS-X19). Kernel-only substrate. Lands the engine-agnostic dispatch surface that every report-producing engine (DRE for documents, future ERE for Excel, …) plugs into. Pure kernel — no DB models, no HTTP routes, no foundation.document changes. New `src/ede/core/engines/reporting/` package with four files. **`protocol.py`** ships the structural `ReportEngine` Protocol (`@runtime_checkable` so `isinstance()` works on engine instances when needed) with three classmethods (`list_actions_for_context(env, model_key, target_record_uuid, is_multi_record) → list[ActionDescriptor]` · `dispatch(env, action, target_record_uuids, params, output_format) → bytes` · `validate_action(action) → list[ValidationError]`) plus three class attributes (`engine_key: ClassVar[str]` · `display_name: ClassVar[str]` · `supported_output_formats: ClassVar[list[str]]`). Two frozen dataclasses on the same module: `ActionDescriptor(action_key, name, engine_key, available_formats, priority, target_model_key, target_record_uuid, organization_id)` — the print-menu lister return shape, JSON-serializable; `ValidationError(field, message, severity="error")` — what `engine.validate_action()` returns per problem, aggregated across engines. **`registry.py`** ships `ReportEngineRegistry` — process-local class-only registry (`_engines: Dict[str, Type[ReportEngine]]` keyed by lowercase engine_key) with class-level static methods `register(engine_class)` · `resolve(engine_key) → Type[ReportEngine]` · `registered_engines() → list[Type[ReportEngine]]` (alphabetical by key for deterministic multi-engine listers) · `is_registered(engine_key) → bool` · `clear()` (test isolation). Case-insensitive lookup normalized to lowercase at registration time. Idempotent re-registration of the SAME class is allowed (test fixture reloads safe); two DIFFERENT classes claiming the same key → `DuplicateReportEngine`. Missing/empty `engine_key` class attribute → `DuplicateReportEngine` at registration. **`errors.py`** ships `ReportEngineError → ValueError` umbrella + `DuplicateReportEngine` + `ReportEngineNotRegistered`. **`__init__.py`** re-exports the public surface (`ActionDescriptor`, `DuplicateReportEngine`, `ReportEngine`, `ReportEngineNotRegistered`, `ReportEngineRegistry`, `ValidationError`). **`src/ede/core/kernel/report_engine_decorator.py`** ships `@api.report_engine` — side-effecting at module-import time, calls `ReportEngineRegistry.register(cls)`, returns the class unchanged (transparent — no metaclass, no class mutation; mirrors how `@api.metric` works). **`src/ede/core/api.py`** re-exports `report_engine` and adds it to `__all__`. **Design choices (load-bearing)**: (1) **Structural Protocol, not abstract base class** — engines don't inherit `ReportEngine`; they just define the right shape. This keeps engines free to live in their own modules with no kernel dependency beyond the decorator import. (2) **Case-insensitive engine_key** — authoring + admin surfaces will mix cases ("DRE" in display, "dre" in CSV seed). Registry normalizes once at registration; stored canonical form is lowercase. (3) **Idempotent re-registration** — module re-imports (test fixture reload, hot-reload during dev) re-execute decorators. Same-class no-op; different-class clash raises. (4) **Three classmethods** — lister contributes to the print-menu, dispatch renders bytes, validate flags author errors. The platform layer (Slice I `ReportDispatcher`) calls these classmethods polymorphically — no engine-specific code in the dispatcher. (5) **`ActionDescriptor` is the lister's wire format** — frozen dataclass, JSON-serializable shape, will appear in the `GET /api/reports/_list` response (Slice I). 14 new tests across 4 classes (TestRegistration × 5 — register-first-class / duplicate-key-raises / idempotent-same-class / missing-engine_key-attr-raises / empty-engine_key-raises; TestResolution × 4 — unknown-engine-raises-NotRegistered / case-insensitive-lookup / mixed-case-engine_key-normalized / registered_engines-alphabetical; TestDecorator × 2 — registers-at-class-definition / returns-class-unchanged; TestProtocolShape × 3 — engine-conforms-to-runtime-protocol / ActionDescriptor-is-frozen / ValidationError-carries-field+message+default-severity). Full repo `./run_tests.sh` exits 0; ruff clean. — ✅ Delivered 2026-05-26 | (kernel — no models; pure Protocol + Registry + decorator) | [src/ede/core/engines/reporting/protocol.py](../src/ede/core/engines/reporting/protocol.py) · [reporting/registry.py](../src/ede/core/engines/reporting/registry.py) · [reporting/errors.py](../src/ede/core/engines/reporting/errors.py) · [reporting/__init__.py](../src/ede/core/engines/reporting/__init__.py) · [src/ede/core/kernel/report_engine_decorator.py](../src/ede/core/kernel/report_engine_decorator.py) · [src/ede/core/api.py](../src/ede/core/api.py) (re-export) · [test_protocol_registry.py](../src/tests/core/engines/reporting/test_protocol_registry.py) (14 cases) | [phase-1-implementation.md Platform Layer · Slice G](../roadmap/foundation/document/phase-1-implementation.md#platform-layer--slice-g--reportengine-protocol--registry--apireport_engine-ws-x0a--delivered-2026-05-26) |
| **Stage 1 Polish Slice F — Datasource Binding (WS-X6)** — Sixth + final slice of the 6-slice "Stage 1 fully" sequence (A → F). Closes the per-record rendering surface that demo templates need — templates declare what data they want via `<rows datasource= as= key=>`, and the engine fetches it via the platform metric registry at render time. New `<rows datasource= as= key=>` element with required `datasource=` (the metric key looked up via `run_metric` from `ede.core.engines.integration.runner`), optional `as=` (default `"row"`), optional `key=` (reserved for explicit-key extraction). New `RowsBlock(datasource, as_name, key_expr, children)` Pydantic IR class appended to `PageChild` union (now 14 alternatives). RelaxNG `dml.rng` adds `rows-block` define under `block-child` choice. Resolution-time construct — remains EXCLUDED from `resolved.rng` (Slice C symmetry contract preserved). Parser branch in `_parse_block_children` mirrors `<for-each>` parsing; empty `datasource=""` raises `DmlValidationError`. **New `DataSourceResolver` module** at `src/ede/core/engines/document/dml/datasources.py` — IR transform walking each page's children, calling `run_metric(env, key, params)` per RowsBlock, flattening one paragraph-set per fetched row with `as_name` bound to the row dict. Returns a new `Document` (functional, doesn't mutate input). **Pipeline placement (load-bearing)**: runs AFTER `PageExpander.expand` + `PageFilter.filter`, BEFORE `PlaceholderBinder.substitute`. Final Stage 1 pipeline is `inheritance → includes → variables → parser → page-expansion → page-filter → datasource-resolver → placeholder-binder → emitter → Stage 2`. Env access is needed only by the datasource fetch — PageExpander stays env-free + pure-IR. **`PageExpander._resolve_block_children` widened return type** from `list[Paragraph]` to `list[PageChild]` so RowsBlock survives expansion intact; PageExpander recursively pre-substitutes within-block children's context refs (so within-page `<for-each>` loop vars resolve correctly inside a `<rows>` body) but preserves the RowsBlock wrapper for the post-pass. **New `Page.params_overlay: Optional[Dict[str, Any]]` field** — IR-internal (not emitted to resolved XML); PageExpander writes `{as_name: item}` into `params_overlay` when cloning a loop page; DataSourceResolver merges `page.params_overlay` into the metric call's params so a `<rows>` block inside a `<page for="invoices" as="inv">` sees the current `inv` and can scope on it. **No caching in Phase 1** — each `<rows>` block fires its own metric call; per-render memo deferred to Phase 2. **Four error classes — single `DataSourceError` umbrella ValueError subclass**: `DataSourceNotFoundError` (wraps `DatasetSpecError` from registry lookup), `DataSourceExecutionError` (wraps any non-spec exception with `{type}: {message}`), `DataSourceNonTableModeError` (scalar mode or non-list `rows`). **Row-context substitution** via `_substitute_row_refs(text, row_context)` — matches `\{([A-Za-z_][A-Za-z_0-9]*(?:\.[A-Za-z_][A-Za-z_0-9]*)*)\}` and substitutes only references whose head is in row context; global params + render-time tokens (`{PageNumber}`, etc.) pass through unchanged. Dotted-path lookup supports nested dicts + attribute fallback. **DocumentService threading**: `_render(...)` gains `env: Any` kwarg; `DataSourceResolver.resolve(...)` inserted between page-filter and placeholder-binder; caller site updated to pass `env=env` directly. 18 new tests across 6 classes (TestParser × 3 minimal/custom-as-name/missing-datasource-raises, TestPureResolver × 7 single-row/multiple-rows/multiple-children-per-row/empty-rows/unbound-placeholder-pass-through/nested-dict-dotted-path/global-params-survive, TestErrors × 5 unknown-metric/execution-error-wraps/scalar-mode/non-list-rows/hierarchy, TestParamsThreading × 1, TestCompositionWithPageExpander × 1 proving `params_overlay` plumbing end-to-end, TestServiceIntegration × 1 via env.dispatch + pdftotext with monkeypatched run_metric). All 314 prior DRE tests preserved (PageExpander widening was non-breaking — every consumer reads `page.children` polymorphically). Two pre-existing F401/F841 warnings in `page_expander.py` (unused `deepcopy` + unused `is_dict` from Slice 7) fixed as Slice F hygiene. — ✅ Delivered 2026-05-26 | (engine — no model changes; new IR class + parser branch + schema define + new resolver module + pipeline plumbing) | [src/ede/core/engines/document/dml/ir.py](../src/ede/core/engines/document/dml/ir.py) (RowsBlock + Page.params_overlay) · [dml/parser.py](../src/ede/core/engines/document/dml/parser.py) (`<rows>` branch) · [dml/schemas/dml.rng](../src/ede/core/engines/document/dml/schemas/dml.rng) (rows-block define) · [dml/page_expander.py](../src/ede/core/engines/document/dml/page_expander.py) (RowsBlock preservation + params_overlay writes + hygiene fixes) · [dml/datasources.py](../src/ede/core/engines/document/dml/datasources.py) (NEW · ~210 LoC) · [src/ede/foundation/document/services/document_service.py](../src/ede/foundation/document/services/document_service.py) (env threading) · [test_datasources.py](../src/tests/foundation/document/test_datasources.py) (18 cases) | [phase-1-implementation.md Stage 1 Polish · Slice F](../roadmap/foundation/document/phase-1-implementation.md#stage-1-polish--slice-f--datasource-binding-ws-x6--delivered-2026-05-26) |
| **Stage 1 Polish Slice E — Formula-Form Variables (WS-X5 second pass)** — Fifth of the 6-slice "Stage 1 fully" sequence (A → F). Lights up `<var name= type= formula="expr"/>` alongside the existing `<var name= type= value="…"/>` value form. Formulas consume the safe AST evaluator at `src/ede/core/engines/formula/safe_eval.py` already shipped by `foundation.dataset` Phase 2 (2026-05-13), using the `full` function set (math + aggregate + logic from `numeric` + date helpers `today`/`add_days`/`add_months`/`days_between` + format helpers `currency`/`percent`). **Two declaration forms, mutually exclusive** — each `<var>` must declare exactly one of `value=` or `formula=`; both → `VariableSyntaxError("declares both")`, neither → `VariableSyntaxError("missing both")`. **Cross-form dependency, single graph** — a formula-form var can reference value-form vars by bare name (no `$`); a value-form var can reference formula-form vars via `$ref` substitution. Both forms share one DFS-on-stack traversal in `_resolve_graph` (the visit function dispatches to `_resolve_value` or `_resolve_formula` based on which attribute is present), so a cycle that crosses forms (`value → formula → value → …`) is detected the same as any other cycle. **AST-derived dependency edges** — formula-form vars expose their dependency set via an `ast.walk` over `Name` nodes; the `_collect_var_deps` helper filters out (1) call targets (`min`, `today`, …) — those are looked up in the function whitelist, not scope; (2) whitelisted function names appearing as bare names; (3) render-param names — they're external inputs and cannot close a cycle; (4) undeclared names — left for the safe-eval to flag at eval time. **Render-time params first-class** — `VariableResolver.resolve(dml_str, params=None)` accepts the render parameter map; `DocumentService.render_by_key` threads its `params_map` directly into the call. Inside formulas, identifiers resolve against `{**params, **typed_values}` — declared vars take precedence on key clash (resolved AFTER the params merge). **Typed parallel store** — `_resolve_graph` maintains two parallel maps: `resolved_str: dict[name → canonical str]` for XML substitution (unchanged from Slice 6) + a new `typed_values: dict[name → Any]` for the safe-eval scope (value-form vars convert via `_typed_from_canonical(name, var_type, str)` — INTEGER → `int(s)`, DECIMAL → `float(s)`, BOOLEAN → `s.lower() == "true"`, DATE → `date.fromisoformat(s)`; formula-form vars get their typed form directly from the evaluator result). **Lenient formula-result coercion** — `_coerce_formula_result(name, var_type, result)` accepts wider input than `_validate_and_coerce`. INTEGER accepts whole floats (`10.0` → `10`) but rejects fractional (`10.5` → `VariableTypeError`). BOOLEAN accepts Python bool / int (0/non-zero) / `"true"`/`"false"` strings. DATE / DATETIME accept Python date/datetime objects directly + ISO strings. **Canonical string formatting** — `_to_canonical_str` defines XML-visible form (`bool → "true"/"false"`, `float → trimmed decimal` via `_format_number` which strips `.0` for whole-number floats but keeps fractions, `date → "YYYY-MM-DD"`, `datetime → ISO 8601`). **New error class — `VariableFormulaError`** — wraps any `FormulaEvalError` from the safe AST evaluator (disallowed node, unknown function, undefined name, parse error) and re-raises with the offending var's name + formula text. **Per-var dep traversal happens BEFORE evaluation** — inside `_resolve_formula`, after AST-parsing the expression and collecting var-deps, the slice calls `visit(dep)` for each dependency in encounter order; this guarantees `typed_values` contains every name the scope build will need AND surfaces cycles immediately at the recursive `visit` of the problematic dep, not at eval time. 39 new tests in `test_formula_variables.py` across 9 classes (arithmetic × 6 + functions × 7 + cross-form × 4 + render-params × 4 + mixed cycle × 3 + form-syntax × 5 + result-coercion × 6 + function-name-not-a-dep × 2 + service-integration × 2); all 20 prior `test_variables.py` tests preserved (purely additive). 314/314 DRE suite (was 275/275 — +39). Ruff clean (pre-existing E731 lambda warning in `_substitute_in_tree` also fixed as hygiene). — ✅ Delivered 2026-05-26 | (engine — no model changes; pure VariableResolver expansion + one-line DocumentService threading change) | [src/ede/core/engines/document/dml/variables.py](../src/ede/core/engines/document/dml/variables.py) (substantial rewrite — new `_Decl` dataclass + `_resolve_formula` + `_resolve_value` branches + `_collect_var_deps` + `_coerce_formula_result` + `_typed_from_canonical` + `_to_canonical_str` + `_format_number` + `VariableFormulaError`) · [src/ede/foundation/document/services/document_service.py](../src/ede/foundation/document/services/document_service.py) (one-line — `params=params_map` threaded) · [test_formula_variables.py](../src/tests/foundation/document/test_formula_variables.py) (39 cases) | [phase-1-implementation.md Stage 1 Polish · Slice E](../roadmap/foundation/document/phase-1-implementation.md#stage-1-polish--slice-e--formula-form-variables-ws-x5-second-pass--delivered-2026-05-26) |
| **Stage 1 Polish Slice D — Full `<field>` Element + Strict-Mode Flag (WS-X10)** — Fourth of the 6-slice "Stage 1 fully" sequence. Adds the rich placeholder element `<field name= format= default=/>` + closed-vocab `FormatSpec` enum (11 values: `string`, `integer`, `decimal`, `currency`, `percent`, `date`, `datetime`, `boolean`, `upper`, `lower`, `title`) registered in `ALL_ENUMS` (now 16 classes). Per-spec format-arg parsing splits the colon-suffix (`decimal:N` precision, `currency:CCY` ISO 4217 code, `date:fmt` + `datetime:fmt` YYYY-MM-DD-style templates, `boolean:Y|N` pipe-split labels) — parser raises `DmlValidationError` with allowed values on unknown spec. New `FieldNode(name, format_spec, format_arg, default)` Pydantic class appended to `PageChild` union; new `field-node` define under RelaxNG `block-child` choice. **PlaceholderBinder full rewrite** — `substitute(document, params, *, strict=False)` adds the strict kwarg AND recursively walks the entire Phase 1 IR vocabulary (closes a Slice-B-era latent bug where only Paragraph children were touched); every text-bearing leaf (Paragraph / TextNode / SpanNode / HeadingNode / LabelNode / DetailsNode) gets string-form `{field}` substitution; every container (GroupNode / SectionNode / ComponentNode / TableNode → RowNode → CellNode / HeaderNode / FooterNode) is walked recursively; FieldNode is flattened to `Paragraph(text=formatted_value)` via new `_resolve_field()` + `_format_value()` helpers using Python stdlib (`int()`, `float()`, `datetime.strftime`, `f"{x:,.2f}"`). New `_to_strftime()` helper translates `YYYY`/`YY`/`MMMM`/`MMM`/`MM`/`DDDD`/`DDD`/`DD`/`HH`/`hh`/`mm`/`ss`/`AA` tokens → strftime directives (longest-token-first replacement). New `MissingPlaceholderError` exception raised in strict mode for unbound `<field>` without `default=`; lenient mode leaves literal `{name}` token in output for visible debug. `DocumentService.render_by_key(... strict: bool = False)` parameter threaded through to `_render()` and `PlaceholderBinder.substitute()`. **Two-form coexistence (load-bearing)**: `{field}` string form is ALWAYS lenient (backwards-compat preserved); `<field>` element form is strict-aware. Authors choose per-call. **FieldNode flattens at bind time** — same lifecycle as IfBlock/ForEachBlock from Slices 8-9; resolved-XML schema (Slice C) UNCHANGED — no schema/parser/emitter changes on the resolved side. **Locale formatting is Phase 3** — Phase 1 ships Python-stdlib-driven formatters (locale-naïve currency / date / number). 43 new tests across 7 classes (TestFieldParser × 7, TestFieldFlattening × 4, TestAllFormatSpecs × 17 covering every FormatSpec value + edge cases, TestStrictMode × 4 including string-form-stays-lenient invariant, TestRecursiveWalk × 9 proving PlaceholderBinder walks Slice B vocabulary, TestPipelineIntegration × 2 end-to-end Invoice template); 1 enum-count test updated (`test_all_15_enums_registered` → `test_all_16_enums_registered`). — ✅ Delivered 2026-05-26 | (engine — no model changes; closed-vocab + IR + parser + schema + binder + service-surface extension) | [src/ede/core/engines/document/dml/enums.py](../src/ede/core/engines/document/dml/enums.py) (FormatSpec) · [dml/ir.py](../src/ede/core/engines/document/dml/ir.py) (FieldNode) · [dml/parser.py](../src/ede/core/engines/document/dml/parser.py) (_parse_field) · [dml/schemas/dml.rng](../src/ede/core/engines/document/dml/schemas/dml.rng) (field-node) · [dml/placeholders.py](../src/ede/core/engines/document/dml/placeholders.py) (rewrite) · [src/ede/foundation/document/services/document_service.py](../src/ede/foundation/document/services/document_service.py) (strict kwarg) · [test_field_node.py](../src/tests/foundation/document/test_field_node.py) (43 cases) | [phase-1-implementation.md Stage 1 Polish · Slice D](../roadmap/foundation/document/phase-1-implementation.md#stage-1-polish--slice-d--full-field-element--strict-mode-flag-ws-x10--delivered-2026-05-26) |
| **Stage 1 Polish Slice C — Full Emitter + Resolved-Parser Mirror (WS-X11B + WS-X13)** — Third of the 6-slice "Stage 1 fully" sequence; symmetric mirror of Slice B. Every IR visual node added in Slice B now emits to resolved XML + parses back via the Stage 2 entry parser. Three-part rewrite: (1) `src/ede/core/engines/document/resolved/schemas/resolved.rng` — top-level structure permits optional `<styles>` + `<headers>` + `<footers>` siblings alongside `<meta>` + `<pages>`; `<meta>` widens to allow `<margins>`; new `<block-child>` `<choice>` covers 11 visual node types (paragraph / text / span / heading / image / block-with-label-and-details-halves / group / section / component-with-keep_together-hint / table-family (Table/Row/Cell/ColGroup/Col) / page-break); all resolution-time constructs (`<var>` / `<if>` / `<for-each>` / `<override>` / `<include>` / `<page for=>` / `<rows datasource=>`) remain EXCLUDED. (2) `src/ede/core/engines/document/resolved/parser.py` — new Resolved* dataclasses mirror IR 1:1 (`ResolvedMargins`, `ResolvedStyleDef`, `ResolvedText`, `ResolvedSpan`, `ResolvedHeading`, `ResolvedImage`, `ResolvedLabel`, `ResolvedDetails`, `ResolvedBlock`, `ResolvedGroup`, `ResolvedSection`, `ResolvedComponent` with `keep_together` bool, `ResolvedCol`, `ResolvedColGroup`, `ResolvedCell`, `ResolvedRow`, `ResolvedTable`, `ResolvedPageBreak`, `ResolvedHeader`, `ResolvedFooter`); `ResolvedTree` gains `styles`/`headers`/`footers` lists; `ResolvedBlockChild` union covers 11 visual node types; `ResolvedPage.children` widened to `List[ResolvedBlockChild]`; recursive `_parse_block_children` walks 11 tag branches; `_parse_table` handles colgroup + rows + cells with all attrs. (3) `src/ede/core/engines/document/dml/emitter.py` — new `_emit_meta` (with margins), `_emit_styles`, `_emit_headers`, `_emit_footers`, `_emit_pages`, `_emit_one_page` (preserves Slice 10 loop metadata), `_emit_block_children` dispatching on Python type via `isinstance` checks, per-node emitters for each of the 11 visual types, `_emit_table` preserves colgroup + cell attrs + recursive cell contents; new `_set_if(elem, attr, value)` helper avoids `attr=""` artifacts. Schema validation runs as the final step before returning so any emitter bug surfaces as `ResolvedSchemaError`. **Symmetry contract (load-bearing)**: every IR node from Slice B has an exactly-matching tag in `resolved.rng` and a Resolved* dataclass — tests enforce 1:1 correspondence so adding an IR node without resolved-side support fails the round-trip tests. **Stage 2 readiness**: Stage 2 PdfEngine remains minimum-subset (single-font Helvetica + Paragraph rendering only); resolved XML now carries the full vocabulary so Stage 2 polish slices can add typography / table layout / image embedding without touching Stage 1 again — the Stage 1 → Stage 2 contract is now complete for Phase 1. 20 new tests across 6 classes (TestEmitterCoversNewNodes × 9, TestTableRoundtrip × 2, TestChromeRoundtrip × 4, TestNestedContainers × 2, TestFullPipelineRoundtrip × 1 (DML XML → DmlParser → IR → ResolvedXmlEmitter → resolved XML string → ResolvedXmlParser → ResolvedTree), TestBackwardsCompatible × 2 (hello-world still emits / loop metadata still emits — Slice 10 forward-compat lock)). **First slice authored in `/tmp/ede-doc-engine` worktree on `document-engine-phases` branch forked from main — primary repo branch undisturbed**. — ✅ Delivered 2026-05-22 | (engine — no model changes; pure resolved-XML schema + parser + emitter expansion) | [src/ede/core/engines/document/resolved/schemas/resolved.rng](../src/ede/core/engines/document/resolved/schemas/resolved.rng) · [src/ede/core/engines/document/resolved/parser.py](../src/ede/core/engines/document/resolved/parser.py) · [src/ede/core/engines/document/dml/emitter.py](../src/ede/core/engines/document/dml/emitter.py) · [test_full_emitter_resolved.py](../src/tests/foundation/document/test_full_emitter_resolved.py) (20 cases) | [phase-1-implementation.md Stage 1 Polish · Slice C](../roadmap/foundation/document/phase-1-implementation.md#stage-1-polish--slice-c--full-emitter--resolved-parser-mirror-ws-x11b--ws-x13--delivered-2026-05-22) |
| **Stage 1 Polish Slice B — Full IR + Parser + RelaxNG (WS-X1 + WS-X2)** — Second of the 6-slice "Stage 1 fully" sequence; biggest by raw scope. Full Phase 1 DML grammar lands in IR + parser + schema. New IR classes (~14): chrome (`Margins`, `StyleDef`, `VarDef`), inline + block content (`TextNode`, `SpanNode`, `HeadingNode`, `ImageNode`, `BlockNode` + `LabelNode` + `DetailsNode`, `GroupNode`, `SectionNode`, `ComponentNode` with `keep_together` + scoped variables), table family (`TableNode`/`RowNode`/`CellNode`/`ColGroupNode`/`ColNode` with align/valign/colspan/rowspan/border-style/style_if), control (`OverrideNode` deprecated but parsed for legacy-import compat; `PageBreakNode`), page chrome (`HeaderNode`/`FooterNode` with `pagetype`), and `ForEachNode` page-level wrapper for multi-page-per-iteration (distinct from `<page for=>` single-page-per-iteration). `PageChild` union widened to 14 alternatives; `Pages.children` accepts `Union[Page, ForEachNode]`; `Document` gains top-level `styles: list[StyleDef]` / `variables: list[VarDef]` / `headers: list[HeaderNode]` / `footers: list[FooterNode]`. Forward references resolved via explicit `model_rebuild()` chain at module bottom (recursive containers cross-reference `PageChild`). RelaxNG schema mirrors the IR with new `<define>` entries per node; `<block-child>` `<choice>` widened to 15 alternatives; pages content model becomes `<choice>` of `<page>` or `<for-each>`; top-level grammar permits `<meta>` + `<style>*` + `<var>*` + `<header>*` + `<footer>*` + `<pages>`. Parser threads Slice A's closed-vocab enums via `_opt_enum(raw, enum_cls)` helper that raises `DmlValidationError` with offending value + allowed-values list on unknown membership. Heading-level out-of-range (`< 1` or `> 6`) raises. Required attributes (`image@src`, `section@name`, `component@name`, `override@target`) checked + raise on missing. `<include>` schema-accepted as block-child (resolver consumes it BEFORE parsing in production; tests can parse with `<include>` present). 27 new tests across 6 classes (DocumentChrome × 5, ContentNodes × 9, Table × 4, OverrideAndControl × 4, PageLevelForEach × 1, SchemaValidation × 2); 1 obsolete hello-world test patched (Slice-2 era `test_unknown_tag_inside_page_raises` asserted `<heading>` is rejected — swapped to `<bogus-tag>` since Slice B grammar now allows `<heading>`). — ✅ Delivered 2026-05-22 | (engine — no model changes; pure IR + parser + schema expansion) | [src/ede/core/engines/document/dml/ir.py](../src/ede/core/engines/document/dml/ir.py) · [dml/parser.py](../src/ede/core/engines/document/dml/parser.py) · [dml/schemas/dml.rng](../src/ede/core/engines/document/dml/schemas/dml.rng) · [test_full_grammar.py](../src/tests/foundation/document/test_full_grammar.py) (27 cases) · [test_dml_parser.py](../src/tests/foundation/document/test_dml_parser.py) (1 obsolete test patched) | [phase-1-implementation.md Stage 1 Polish · Slice B](../roadmap/foundation/document/phase-1-implementation.md#stage-1-polish--slice-b--full-ir--parser--relaxng-ws-x1--ws-x2--delivered-2026-05-22) |
| **Stage 1 Polish Slice A — Full Enum Kernel (WS-X12)** — First of a 6-slice "Stage 1 fully" sequence (A→F). Closes the WS-X12 closed-vocabulary registry by adding 12 new `(str, Enum)` classes to `src/ede/core/engines/document/dml/enums.py`: `PageType` (9 vals — `all`/`first`/`rest`/`last`/`odd`/`even`/`except-first`/`except-last`/`first-and-last` for header/footer placement on physical pages), `PageVisibility` (5 — `all`/`first`/`last`/`odd`/`even` for visibility within overflowing logical pages, active when WS-X15 multi-page ships), `TextAlign` (`left`/`center`/`right`/`justify`), `VerticalAlign` (`top`/`middle`/`bottom`), `FontWeight` (`normal`/`bold`), `FontStyle` (`normal`/`italic`), `TextDecoration` (`none`/`underline`/`line-through`/`overline`), `TextTransform` (`none`/`uppercase`/`lowercase`/`capitalize`), `BorderStyle` (`none`/`solid`/`dashed`/`dotted`/`double`), `ImagePosition` (`inline`/`left`/`right`/`center`), `OverrideMode` (`replace`/`append`/`prepend`/`remove` — deprecated `<override>` mechanism kept for grammar completeness + legacy import compatibility), `VarScope` (`document`/`component`). `VarType` (10 values) carried forward from Slice 6. `ALL_ENUMS` registry now contains 15 enum classes total (Phase 2 chart/barcode/QR enums register here later). Test file rewritten with three classes: `TestExistingEnums` × 2 (Orientation + OutputFormat unchanged), `TestNewStage1Enums` × 13 (one per added enum asserting the canonical value tuple), `TestRegistry` × 3 (all-15-registered drift check + lossless `enum_values()` helper + no-duplicate-classes invariant). **Stable-value contract:** every test asserts the canonical value tuple by ORDERING — downstream RelaxNG fragment generators in Slice B/C will iterate the enum and emit `<value>...</value>` choices verbatim, so a values-order change would silently break those consumers. Tests lock the ordering as the contract. — ✅ Delivered 2026-05-22 | (engine — no model changes; closed-vocab registry only) | [src/ede/core/engines/document/dml/enums.py](../src/ede/core/engines/document/dml/enums.py) · [test_enums.py](../src/tests/foundation/document/test_enums.py) (18 cases) | [phase-1-implementation.md Stage 1 Polish Slice A — Full Enum Kernel](../roadmap/foundation/document/phase-1-implementation.md#stage-1-polish--slice-a--full-enum-kernel-ws-x12--delivered-2026-05-22) |
| **Full Schema Slice (WS-X18)** — Adds the doc-type taxonomy + render audit log to foundation.document. New models: `ir.document.type` (`key`/`name`/`description`/`active` — first-class taxonomy for HBL / Quote / Invoice / etc., referenced by templates via `doc_type_id`) and `ir.document.render.audit` (append-only — `template_uuid`/`template_key`/`output_format`/`output_size_bytes`/`success`/`error_message`/`target_record_uuid`/`params_keys` (JSON top-level only)/`applied_extension_keys` (JSON)). `ir.document.template` adds `doc_type_id = fields.Reference("ir.document.type", on_delete="restrict")`. Third migration `c7d3a8e2f5b1` chained off `b5e2c8f491a0` creates the new tables + adds the column + FK + index. `DocumentService.render_by_key()` extended: new `target_record_uuid: str | None` parameter (Slice 12 cascade integration); engine pipeline wrapped in try/except so every render writes an audit row via best-effort `_write_audit()` (success or failure-after-lookup). `_resolve_template_dml` accepts optional `applied_out: list` and appends each successfully-applied extension so the audit row captures `applied_extension_keys` for forensic reproducibility. Audit writes are best-effort — table missing in some test fixtures shouldn't fail the render. `ir.document.resolution` cache deferred to Phase 2 (pure optimization). 7 integration tests across 2 classes (DocumentType CRUD × 2; RenderAudit × 5 — success-row / no-row-on-template-not-found / failure-after-lookup-row / applied-extensions-capture / target-record-uuid-passthrough). — ✅ Delivered 2026-05-22 | `ir.document.type` (key/name/description/active) + `ir.document.template.doc_type_id` Reference + `ir.document.render.audit` append-only (full schema above) | [src/ede/foundation/document/models/document_type.py](../src/ede/foundation/document/models/document_type.py) · [models/render_audit.py](../src/ede/foundation/document/models/render_audit.py) · [models/document_template.py](../src/ede/foundation/document/models/document_template.py) · [migrations/versions/c7d3a8e2f5b1_full_schema.py](../src/ede/foundation/document/migrations/versions/c7d3a8e2f5b1_full_schema.py) · [services/document_service.py](../src/ede/foundation/document/services/document_service.py) · [test_full_schema.py](../src/tests/foundation/document/test_full_schema.py) (7 cases) | [phase-1-implementation.md Full Schema Slice](../roadmap/foundation/document/phase-1-implementation.md#full-schema-slice-ws-x18--delivered-2026-05-22) |
| **Two-Pass Executor Slice (WS-X16)** — Stage 2 render-time-token substitution. New `TwoPassExecutor.execute(resolved: ResolvedTree) → ResolvedTree` at `src/ede/core/engines/document/output/pdf/two_pass.py`. Pass 1 measures (`physical_total = len(resolved.pages)`, 1:1 logical-physical for Phase 1; overflow → multi-physical-per-logical lands in WS-X15 full). Pass 2 substitutes the full token vocabulary via regex `\{([A-Za-z_]\w*)\}` against a per-page context: `{PageNumber}` (1-based physical), `{TotalPages}`, `{LogicalPageId}` (= `page.id`), `{LogicalPageIndex}` (= PageNumber until overflow), `{PageInLogical}` (= 1 today), `{LogicalPageCount}` (= TotalPages until overflow), `{LoopIndex}` / `{LoopCount}` / `{LoopKey}` (only set when page carries loop metadata from PageExpander). Unknown tokens + unbound loop tokens on non-loop pages pass through as literals — authoring mistakes surface as visible debug hints. Page IR gains three optional fields `loop_index` / `loop_count` / `loop_key`; emitter writes them as `<page loop-index= loop-count= loop-key=>` attributes; resolved.rng allows the optional attributes; ResolvedXmlParser reads them. PageExpander rewritten with filter-then-index two-phase pattern: filter first against `<page if=>`, THEN enumerate survivors with 1-based `loop_index` — so `{LoopCount}` reflects survivors (dense indexing 1, 2, 3 rather than sparse 1, 3, 5). PlaceholderBinder.substitute updated to preserve loop metadata when rebuilding `Page` (previously lost between expansion and emission — discovered + fixed during this slice). `PdfEngine.render()` calls `TwoPassExecutor.execute(resolved)` as the first step before any canvas drawing. 10 integration tests across 3 classes (pure executor × 7, resolved-XML roundtrip × 1, service integration × 2); 1 Slice-3 test updated. — ✅ Delivered 2026-05-22 | (engine — IR + parser + emitter + schema enhancements; no model changes) | [src/ede/core/engines/document/output/pdf/two_pass.py](../src/ede/core/engines/document/output/pdf/two_pass.py) · [output/pdf/engine.py](../src/ede/core/engines/document/output/pdf/engine.py) · [dml/ir.py](../src/ede/core/engines/document/dml/ir.py) · [dml/page_expander.py](../src/ede/core/engines/document/dml/page_expander.py) · [dml/placeholders.py](../src/ede/core/engines/document/dml/placeholders.py) · [dml/emitter.py](../src/ede/core/engines/document/dml/emitter.py) · [resolved/parser.py](../src/ede/core/engines/document/resolved/parser.py) · [resolved/schemas/resolved.rng](../src/ede/core/engines/document/resolved/schemas/resolved.rng) · [test_two_pass.py](../src/tests/foundation/document/test_two_pass.py) (10 cases) | [phase-1-implementation.md Two-Pass Executor Slice](../roadmap/foundation/document/phase-1-implementation.md#two-pass-executor-slice-ws-x16--delivered-2026-05-22) |
| **Within-Page Conditionals & Loops Slice (WS-X9)** — `<if test="expr">` and `<for-each items="key" as="x">` as page-children (paragraph-level granularity, NOT page-level — those are WS-X7/X8). Reuses `PredicateEvaluator` from Slice 8 for the test expression. New IR nodes added: `IfBlock(test, children)` and `ForEachBlock(items, as_name, key_expr, children)` Pydantic models. `Page.children` type widened from `List[Paragraph]` to `List[PageChild]` where `PageChild = Union[Paragraph, IfBlock, ForEachBlock]`. Forward references resolved via explicit `model_rebuild()` calls. Parser's `_parse_page` calls new recursive `_parse_block_children` walking `<paragraph>` / `<if>` / `<for-each>` children in document order. RelaxNG schema's `<page>` definition switches its content model to `<ref name="block-child"/>` — a `<choice>` of paragraph / if-block / for-each-block, both control nodes recursive. `PageExpander.expand()` now walks every page (not just loop-pages); new `_resolve_block_children(children, context) → list[Paragraph]` recursively flattens `IfBlock`s (kept/dropped by predicate) and `ForEachBlock`s (one set of resolved children per item). Within-page substitution uses new `_substitute_context_refs` that only substitutes refs whose head is in context — global params + render-time tokens pass through unchanged. Within-page blocks see outer page-level loop context — `<page for="companies" as="c">…<for-each items="c.depts">` works. Emitter remains unchanged — `PageExpander` always flattens to `List[Paragraph]` before the emitter sees the IR. Old per-as-name `_substitute_loop_refs` removed in favor of unified context-based approach. 13 integration tests across 3 classes (pure × 7, errors × 3, service integration × 3). — ✅ Delivered 2026-05-22 | (engine — IR + parser + schema enhancements; no model changes) | [src/ede/core/engines/document/dml/ir.py](../src/ede/core/engines/document/dml/ir.py) (IfBlock + ForEachBlock + PageChild union) · [dml/parser.py](../src/ede/core/engines/document/dml/parser.py) (recursive block-children parsing) · [dml/page_expander.py](../src/ede/core/engines/document/dml/page_expander.py) (recursive _resolve_block_children) · [dml/schemas/dml.rng](../src/ede/core/engines/document/dml/schemas/dml.rng) (block-child choice) · [test_within_page.py](../src/tests/foundation/document/test_within_page.py) (13 cases) | [phase-1-implementation.md Within-Page Conditionals & Loops Slice](../roadmap/foundation/document/phase-1-implementation.md#within-page-conditionals--loops-slice-ws-x9--delivered-2026-05-22) |
| **Page Conditional Slice (WS-X8)** — `PredicateEvaluator.evaluate(expr, context) → bool` + `PageFilter.filter(document, params) → new Document`. Predicate grammar (Phase 1 minimal): `path` (truthy check on dotted-path lookup) / `path == literal` (equality with type coercion — `"5" == 5` is True) / `path != literal` (inequality). Literals: `'string'` / `"string"` / `123` / `1.23` / `True` / `False` / `None`. `PageExpander` extended — when a loop-page has both `for=` and `if=`, the predicate evaluates per iteration against `{**params, as_name: item}` and iterations where the predicate is falsy are skipped (no clone produced). `PageFilter` runs as an IR transform after page expansion, dropping global non-loop pages whose predicate is falsy against `params`. IR `Page` model adds `if_expr` optional field; parser reads `if=` attribute (empty `if=""` raises `DmlValidationError`); RelaxNG schema's `<page>` definition adds the optional `if` attribute. `_render()` pipeline inserts `PageFilter.filter(expanded, params)` between `PageExpander.expand(...)` and `PlaceholderBinder.substitute(...)`. Final pipeline order: `inheritance → includes → variables → parser → page-expansion (with per-iter if) → page-filter (global if) → placeholder-binder → emitter → Stage 2`. Errors: `PredicateError` umbrella + `PredicateSyntaxError`. The full safe-AST form (with `&&`, `||`, `!`, arithmetic) lights up when `foundation.dataset` Phase 2 substrate is consumed. 21 integration tests across 3 classes (predicate evaluator × 14, page filter × 3, service integration × 4). — ✅ Delivered 2026-05-22 | (engine — IR + parser + schema enhancements; no model changes) | [src/ede/core/engines/document/dml/conditionals.py](../src/ede/core/engines/document/dml/conditionals.py) · [dml/ir.py](../src/ede/core/engines/document/dml/ir.py) · [dml/page_expander.py](../src/ede/core/engines/document/dml/page_expander.py) (per-iter if) · [dml/parser.py](../src/ede/core/engines/document/dml/parser.py) · [dml/schemas/dml.rng](../src/ede/core/engines/document/dml/schemas/dml.rng) · [src/ede/foundation/document/services/document_service.py](../src/ede/foundation/document/services/document_service.py) · [test_conditionals.py](../src/tests/foundation/document/test_conditionals.py) (21 cases) | [phase-1-implementation.md Page Conditional Slice](../roadmap/foundation/document/phase-1-implementation.md#page-conditional-slice-ws-x8--delivered-2026-05-22) |
| **Page Expansion Slice (WS-X7)** — `PageExpander.expand(document, params) → new Document` materializes one physical page per loop iteration. Authored as `<page id="ship" for="shipments" as="s"><paragraph>BL {s.bl_number}</paragraph></page>`. List/tuple sources yield 0-based key + 1-based `LoopIndex`; dict sources yield dict key + 1-based `LoopIndex`. Per-iteration `{as_name}` and `{as_name.field.subfield}` placeholders substitute against the current item via dotted-path lookup (`Mapping` keys + object attribute fallback). Generated page id: `{original_id}-{key_slug}` where slug sanitizes the key to `[A-Za-z0-9_-]+`. Render-time tokens (`{LoopIndex}`, `{LoopCount}`, `{LoopKey}`, etc.) are NOT substituted — they survive into resolved XML for Stage 2's two-pass executor (WS-X16). IR `Page` model extended with `for_expr` / `as_name` / `key_expr` optional fields; parser defaults `as_name="item"` when only `for=` is set; RelaxNG schema allows three optional `<page>` attributes (`for` / `as` / `key`); `_render()` pipeline inserts page expansion between parser and placeholder-binder (final order: `inheritance → includes → variables → parser → page-expansion → placeholder-binder → emitter → Stage 2`). Composes cleanly with WS-X3 inheritance — extension xpath patches can inject loop-pages into a base's `<pages>` block (proven by `test_loop_inside_extension_via_xpath`). Errors: `PageExpansionError` umbrella + `LoopSourceNotFoundError` (key absent from params) + `LoopSourceNotIterableError` (value isn't list/tuple/dict). Empty `for=""` raises `DmlValidationError` at parse time. 13 integration tests across 4 classes (pure expander × 7, error paths × 3, empty iteration × 1, service integration × 2). — ✅ Delivered 2026-05-22 | (engine — IR + parser + schema enhancements; no model changes) | [src/ede/core/engines/document/dml/page_expander.py](../src/ede/core/engines/document/dml/page_expander.py) · [dml/ir.py](../src/ede/core/engines/document/dml/ir.py) · [dml/parser.py](../src/ede/core/engines/document/dml/parser.py) · [dml/schemas/dml.rng](../src/ede/core/engines/document/dml/schemas/dml.rng) · [src/ede/foundation/document/services/document_service.py](../src/ede/foundation/document/services/document_service.py) · [test_page_expansion.py](../src/tests/foundation/document/test_page_expansion.py) (13 cases) | [phase-1-implementation.md Page Expansion Slice](../roadmap/foundation/document/phase-1-implementation.md#page-expansion-slice-ws-x7--delivered-2026-05-22) |
| **Variable Evaluator Slice (WS-X5 — value form)** — `VariableResolver.resolve(dml_str)` is a pure DML transform (no env / no I/O) with a 4-step pipeline: (1) walk `xpath("//var")` and extract declarations into `dict[name → (VarType, raw_value)]`; (2) resolve the dependency graph via DFS-with-on-stack (a subset of Tarjan's SCC algorithm — finds *any* cycle and reports the path that closes it, sufficient because variable references form a tree-of-deps); (3) remove every `<var>` element from the tree (parser never sees them); (4) substitute `$varName` and `${varName}` occurrences in text content + attribute values throughout the tree via regex `\$\{([a-zA-Z_]\w*)\}|\$([a-zA-Z_]\w*)`. Closed-vocabulary `VarType` enum (10 values: `string`, `number`, `integer`, `decimal`, `date`, `datetime`, `boolean`, `color`, `currency`, `list`) with per-type literal-pattern validators (`integer` requires Python `int()`-parseable; `boolean` accepts only `true`/`false`; `color` requires `#rgb`/`#rrggbb`/`#rrggbbaa` hex; `currency` requires 3-letter ISO 4217; `date` requires `YYYY-MM-DD`; `datetime` requires `YYYY-MM-DD[T ]HH:MM[:SS]`). Pipeline order is now `inheritance → includes → variables → engine` — running variables AFTER includes is load-bearing because a host template's `<var>` declarations resolve inside content inlined from included templates. Errors: `VariableError` umbrella + `VariableSyntaxError` (malformed declaration) / `VariableTypeError` / `VariableCycleError` / `UndefinedVariableError`. Style cascade defers — DML has no `<style>` element yet. 20 integration tests across 5 classes (pure resolver × 5, cycle detection × 3, type validation × 6, syntax errors × 4, service integration × 2). — ✅ Delivered 2026-05-22 | (engine — no model changes) | [src/ede/core/engines/document/dml/variables.py](../src/ede/core/engines/document/dml/variables.py) · [dml/enums.py](../src/ede/core/engines/document/dml/enums.py) (added `VarType`) · [src/ede/foundation/document/services/document_service.py](../src/ede/foundation/document/services/document_service.py) · [test_variables.py](../src/tests/foundation/document/test_variables.py) (20 cases) | [phase-1-implementation.md Variable Evaluator Slice](../roadmap/foundation/document/phase-1-implementation.md#variable-evaluator-slice-ws-x5--value-form--delivered-2026-05-22) |
| **`<include>` Directive Slice (WS-X4)** — `IncludeResolver` walks merged DML (post-inheritance) and inlines every `<include src="key"/>` directive by looking up the source template in `ir.document.template` (active only), recursively resolving nested includes within the source, then replacing the `<include>` element in-place with the source's `//pages/page` children. Bounded by `MAX_INCLUDE_DEPTH = 5` enforced at every recursive call (`IncludeDepthExceededError`). Cycle-detected via a per-chain `_chain` tuple parameter (A → B → A raises `IncludeCycleError` on the second visit). Composes with WS-X3 inheritance: extension xpath patches can inject `<include>` elements and the resolver picks them up. `DocumentService.render_by_key()` extended to call `IncludeResolver.resolve(merged_dml, env)` after the WS-X3 pass and before the engine pipeline — final pipeline order is `inheritance → includes → engine`. Errors: `IncludeError` umbrella + `IncludeNotFoundError`, `IncludeCycleError`, `IncludeDepthExceededError`, `IncludeSyntaxError`. Demo: `demo.common.terms` (reusable T&C block) + `demo.invoice.basic` (uses `<include src="demo.common.terms"/>`). 13 integration tests across 5 classes (happy path × 4, cycle detection × 2, depth limit × 2, error paths × 4, composition with inheritance × 1). — ✅ Delivered 2026-05-22 | (engine — no model changes) | [src/ede/core/engines/document/dml/includes.py](../src/ede/core/engines/document/dml/includes.py) · [src/ede/foundation/document/services/document_service.py](../src/ede/foundation/document/services/document_service.py) · [demo/demo_document_templates.xml](../src/ede/foundation/document/demo/demo_document_templates.xml) · [test_includes.py](../src/tests/foundation/document/test_includes.py) (13 cases) | [phase-1-implementation.md `<include>` Directive Slice](../roadmap/foundation/document/phase-1-implementation.md#include-directive-slice-ws-x4--delivered-2026-05-22) |
| **Records-as-Data Inheritance Slice (WS-X3)** — `ir.document.template` extended with `parent_id` + `inherit_mode` (`extend` / `copy`) + `domain` (EDE search-domain DSL) + `priority` + `organization_id` fields. `XPathMerger` applies extension patches against base DML using Odoo-standard `position=` semantics (`inside` appends children · `replace` swaps whole target with patch's children · `before`/`after` insert siblings; **intentionally diverges from `foundation.presentation`'s view-inheritance variant** — DRE's pattern follows ERP-author muscle memory). `DomainEvaluator` parses domain strings via `ast.literal_eval` (safe — no `eval()`); supports `=`/`!=`/`>`/`<`/`>=`/`<=`/`in`/`not in`; AND-only flat list; dot-path field lookup. `DocumentService._resolve_template_dml` (a) rejects direct render of `extend`-mode templates with `InvalidTemplateError`, (b) recursively resolves parent for `copy`-mode and applies its xpath patches, (c) discovers `extend` extensions via `parent_id` search filtered by `inherit_mode == "extend"` + `active == True`, (d) further filters via `DomainEvaluator.matches(ext.domain, params)`, (e) layers patches in `priority asc` order. Demo records: base quote (`demo.quote.standard`) + India HSN extension (`demo.quote.l10n_in`) layering HSN paragraph + GST page when `params['country'] == 'IN'` + VIP copy (`demo.quote.vip`) with own xpath patches. 4-variant smoke verified: US base (no extension), India base+extension (HSN + GST), VIP copy (priority paragraph), VIP+India (transitive inheritance — copy inherits parent's active extensions). 12 inheritance tests across 4 classes; 73/73 DRE suite + 2500/1 full repo. — ✅ Delivered 2026-05-22 | `ir.document.template` now carries `parent_id` + `inherit_mode` + `domain` + `priority` + `organization_id`; `default_order` flipped to `"priority asc, key asc"` | [src/ede/core/engines/document/dml/merger.py](../src/ede/core/engines/document/dml/merger.py) · [dml/domain.py](../src/ede/core/engines/document/dml/domain.py) · [src/ede/foundation/document/models/document_template.py](../src/ede/foundation/document/models/document_template.py) · [services/document_service.py](../src/ede/foundation/document/services/document_service.py) · [migrations/versions/b5e2c8f491a0_inheritance_fields.py](../src/ede/foundation/document/migrations/versions/b5e2c8f491a0_inheritance_fields.py) · [demo/demo_document_templates.xml](../src/ede/foundation/document/demo/demo_document_templates.xml) · [test_inheritance.py](../src/tests/foundation/document/test_inheritance.py) (12 cases) | [phase-1-implementation.md Records-as-Data Inheritance Slice](../roadmap/foundation/document/phase-1-implementation.md#records-as-data-inheritance-slice-ws-x3--delivered-2026-05-22) |
| **WS-X11 Slice A** — Kernel data-loader `<field type="xml">` + `<field eval="...">` attribute support (kernel-shared, not DRE-specific; benefits any module that needs inline XML field bodies or `ast.literal_eval`-style Python literals in seed data) — ✅ Delivered 2026-05-22 | (kernel — no models) | [src/ede/core/services/data_loader/xml_parser.py](../src/ede/core/services/data_loader/xml_parser.py) · [schemas/data.rng](../src/ede/core/services/data_loader/schemas/data.rng) · [loader.py](../src/ede/core/services/data_loader/loader.py) · [csv_parser.py](../src/ede/core/services/data_loader/csv_parser.py) · [test_xml_eval_attrs.py](../src/tests/data_loader/test_xml_eval_attrs.py) | [phase-1-implementation.md WS-X11](../roadmap/foundation/document/phase-1-implementation.md) |
| **Hello-World Engine Slice** — DML → PDF end-to-end pipeline (engine pieces only — foundation shell deferred). Minimal subsets of 9 workstreams: WS-X1 DML parser, WS-X2 RelaxNG, WS-X10 placeholders (with render-time-token preservation), WS-X11 Slice B resolved-XML emitter + schema, WS-X12 enum kernel (`Orientation`, `OutputFormat`), WS-X13 resolved parser, WS-X14 units + paper, WS-X15 single-page layout, WS-X17 ReportLab Canvas writer. Produces a real `%PDF-1.3` from a trivial template; render-time tokens (`{PageNumber}`/`{TotalPages}`/`{LoopIndex}` etc.) survive end-to-end to be substituted by Pass 2 in a later WS. — ✅ Delivered 2026-05-22 | (engine — no models yet) | [src/ede/core/engines/document/dml/parser.py](../src/ede/core/engines/document/dml/parser.py) · [dml/ir.py](../src/ede/core/engines/document/dml/ir.py) · [dml/enums.py](../src/ede/core/engines/document/dml/enums.py) · [dml/placeholders.py](../src/ede/core/engines/document/dml/placeholders.py) · [dml/emitter.py](../src/ede/core/engines/document/dml/emitter.py) · [dml/schemas/dml.rng](../src/ede/core/engines/document/dml/schemas/dml.rng) · [resolved/parser.py](../src/ede/core/engines/document/resolved/parser.py) · [resolved/schemas/resolved.rng](../src/ede/core/engines/document/resolved/schemas/resolved.rng) · [output/pdf/engine.py](../src/ede/core/engines/document/output/pdf/engine.py) · [output/pdf/units.py](../src/ede/core/engines/document/output/pdf/units.py) · [src/tests/foundation/document/](../src/tests/foundation/document/) (6 test files, 50 cases) | [phase-1-implementation.md Hello-World Engine Slice](../roadmap/foundation/document/phase-1-implementation.md#hello-world-engine-slice--delivered-2026-05-22) |
| **Foundation Shell Slice** — `foundation.document` app online via `env.dispatch`. `ir.document.template` minimal model (`key`/`name`/`dml`/`active`) + Alembic migration (chained off `c4f8a9d2e1b3` foundation.base ir_model_extension head) + `DocumentService.render_by_key()` orchestrator wrapping the engine pipeline + `@api.on_command("ede.document.render")` handler reading `cmd.payload` + `foundation.document` `__manifest__.py` (depends on `foundation.base`) + `document` added to `ACTIVE_MODULES` (last position) + demo template loaded via `<record id="demo_hello"><field name="dml" type="xml">…</field></record>` (powered by WS-X11 Slice A) + 10 integration tests through the full command-bus path verifying PDF magic bytes + text extraction via `pdftotext` + render-time-token preservation + error paths (template-not-found, inactive-template, unsupported-format, missing-key). — ✅ Delivered 2026-05-22 | `ir.document.template` (minimal — `key`, `name`, `dml`, `active`; full schema with `doc_type_id`/`parent_id`/`inherit_mode`/`domain`/etc. ships in subsequent slices) | [src/ede/foundation/document/__manifest__.py](../src/ede/foundation/document/__manifest__.py) · [models/document_template.py](../src/ede/foundation/document/models/document_template.py) · [services/document_service.py](../src/ede/foundation/document/services/document_service.py) · [migrations/versions/a3f7e2c91d04_document_init.py](../src/ede/foundation/document/migrations/versions/a3f7e2c91d04_document_init.py) · [demo/demo_document_templates.xml](../src/ede/foundation/document/demo/demo_document_templates.xml) · [src/ede/foundation/settings.py](../src/ede/foundation/settings.py) (ACTIVE_MODULES) · [src/tests/foundation/document/test_render_integration.py](../src/tests/foundation/document/test_render_integration.py) (10 cases) | [phase-1-implementation.md Foundation Shell Slice](../roadmap/foundation/document/phase-1-implementation.md#foundation-shell-slice--delivered-2026-05-22) |
<!-- /SYNC-BLOCK -->

### Known Gaps
<!-- SYNC-BLOCK: gaps -->
| Gap | Severity | Roadmap Reference |
|---|---|---|
| Entire module not yet built | 🔴 Not Started | [roadmap/foundation/document/](../roadmap/foundation/document/README.md) |
<!-- /SYNC-BLOCK -->

### Things developers commonly get wrong
<!-- HAND-AUTHORED — preserved across syncs -->

- **Chatter `<Attachments/>` tab vs `Attachable` mixin are gated differently.** The
  chatter Attachments tab renders whenever the form view's `<activity>` block
  declares `<Attachments/>` — it talks to `GET /api/document/attachments` which
  accepts any `(model_key, record_uuid)`. There is *no* Attachable check on
  upload. The Documents-app virtual tree, in contrast, *does* gate visibility:
  `AttachableRegistry.categories()` filters by
  `issubclass(cls, Attachable) and _attachment_document_visible == True`. Net
  effect: a record can collect chatter uploads even when its model isn't formally
  Attachable — those files just won't surface in the DMS tree. To make them
  visible, the consumer must inherit `Attachable` and set the three class-level
  attributes (visible flag + category label + record-label field).
- **Image / Binary widgets need a saved record.** The polymorphic back-reference
  (`attached_to_model_key`, `attached_to_record_uuid`, `attached_to_field_name`)
  is written at INSERT time, so uploads on an unsaved draft cannot be addressed.
  Both widgets show a "Save first to upload" hint until the record has a
  `record_uuid`. Save once, then the field's pencil-overlay button becomes
  active.
- **Reference-shape on read.** `image_avatar` / any Reference field comes back
  from the API as `{id, record_uuid, display_name, …}`, not as a bare UUID
  string. The widgets normalize this via an `extractUuid()` helper before
  rendering; downstream consumers (e.g. DRE `<image src="storage://{...}"/>`)
  should do the same.

### Attachable consumers (as of 2026-05-28)

These models inherit the `Attachable` mixin with
`_attachment_document_visible = True`, so the Documents-app virtual tree shows
a category for each:

| Model | Category | Record label field |
|---|---|---|
| `crm.lead` | Leads | `lead_number` |
| `crm.inquiry` | Inquiries | `inquiry_number` |
| `crm.opportunity` | Opportunities | `opportunity_number` |
| `crm.quote` | Quotations | `quote_number` |
| `crm.handover` | Handovers | `handover_number` |

Adding a new consumer is two attributes + an MRO line — see
`src/domains/logistics/sales_crm/models/quote.py` for the canonical example.

### Migration / upgrade notes
<!-- SYNC-BLOCK: migration -->
**Enhancement 01 — Attachment Substrate (✅ 2026-05-27):**

- `foundation.document/migrations/versions/e3f9a5c2b817_add_ir_attachment.py` — creates `ir_attachment` table with all back-ref + storage-pointer columns + 5 indexes (model_key, record_uuid, field_name, usage_kind, checksum_sha256). All FK + index names wrapped in `op.f()` per the `writing-alembic-migrations` skill. Chains off `d7dc5275ec8d` (Slice K1).
- `foundation.document/migrations/versions/e4f8c2a9b637_add_message_attachment_m2m.py` — creates `communication_message_ir_attachment_rel` join table linking chatter messages to their attachments. Migration lives in foundation.document (consuming module owner of `@api.extend_model`-contributed field per CLAUDE.md). Chains off `e3f9a5c2b817`.
- `foundation.document/migrations/versions/f7a3c8e1b294_audit_output_attachment_id.py` — adds `output_attachment_id` Reference column on `ir_document_render_audit`. `batch_alter_table` for SQLite compatibility. Chains off `e4f8c2a9b637`.
- `foundation.base/migrations/versions/c8e2a1b945f3_res_partner_image_avatar.py` — adds `image_avatar VARCHAR(36)` column + index to `res_partner` + `res_organization` + `res_user` (delegation pattern — all three carry the column; partner is source of truth via delegation kernel). **No hard FK to `ir_attachment`** — foundation.base migrations run before foundation.document's, soft-FK is the EDE convention for Reference fields. Chains off `5fa2a5742d5a`.
<!-- /SYNC-BLOCK -->

### Permissions / RBAC
<!-- SYNC-BLOCK: rbac -->
| Role | Permissions |
|---|---|
| `ir.document.viewer` | Read `ir.document.template`; call `POST /api/document/<key>/render` |
| `ir.document.author` | Viewer + CRUD on draft templates + use React designer (Phase 3) |
| `ir.document.admin` | Author + lock/unlock + manage post-processing pipelines (Phase 2) + manage marketplace seed (Phase 3) + register plugin packs (Phase 3) |
<!-- /SYNC-BLOCK -->

### Related modules
<!-- SYNC-BLOCK: related -->
- [`foundation.dataset`](./foundation-dataset.md) — datasource binding through metric/dataset registry; shared safe AST evaluator (variables + `<if test=>` + `<page if=>` + `style_if` + chart-axis expressions + barcode formula values — **one expression language for the whole engine**).
- [`foundation.storage`](./17-storage-module.md) — Phase 2 persists rendered output + template body files.
- [`foundation.reporting`](./foundation-reporting.md) — Phase 3 of reporting consumes DRE for PDF snapshots.
- [`foundation.email`](./foundation-jobs.md) — Phase 2 HTML engine output consumable as email body.
- [`foundation.presentation`](./foundation-presentation.md) — admin form for managing templates; React designer (Phase 3) is a new view kind that emits DML.
- [`foundation.base`](./foundation-customization.md) — `res.organization` / `res.country` / `res.user.language_id` for resolution + variable scope.
- [`foundation.l10n`](./foundation-l10n.md) — **Phase 3** locale resolution chain (template `<meta language>` → caller params → principal → tenant default → setting); Babel/CLDR formatting.
<!-- /SYNC-BLOCK -->

---

*Last sync: 2026-05-27 (**Enhancement 01 — Attachment Substrate ✅ Delivered** — additive substrate on top of Phase 1. All 13 workstreams (WS-ATT-1 → WS-ATT-13) shipped in one session as 15 commits on branch `worktree-document-attachment-substrate-enh01`. New polymorphic `ir.attachment` model sibling to `storage.document` (shared `StorageRouter` for backend routing); kernel field types `fields.Image` / `fields.Binary` / `fields.Attachments`; new `Attachable` mixin (parallels `Chatterable`) + `AttachableRegistry` boot-time catalog of `_attachment_document_visible=True` consumers; HTTP surface at `/api/document/attachments/*` (upload/download/preview/delete/list) + `/api/document/dms/virtual/*` (Documents-app virtual folder tree with three layers — categories → records → files) + `/api/document/dms/search` (unified search across `storage.document` ∪ `ir.attachment`); wildcard `pre.*.delete` cascade hook with O(1) fast-path on `__ede_field_specs__` for models without attachment fields; DRE PdfEngine extended with `<image src="storage://<uuid>"/>` resolver via `AttachmentService.get_bytes`; `DocumentService.render_by_key(..., save_output=True, save_output_message="...")` flow with SHA-256 dedup + auto-chatter-post + audit-row `output_attachment_id` FK (return shape additive — bytes by default, dict when save_output=True); `mail.message.attachment_ids` M2M via `@api.extend_model("communication.message")` from foundation.document (consuming-module ownership per CLAUDE.md); `image_avatar` declared directly on `res.partner` + `res.organization` + `res.user` (delegation kernel + soft-FK convention since foundation.base migrations run before foundation.document); frontend service `VirtualTreeService.ts` + lazy 3-level component `VirtualCategoryTree.tsx` + service `UnifiedSearchService.ts` + helper `runReportWithSaveToHistory()`; first consumer `crm.quote` extends `(Chatterable, Attachable)` with `_attachment_document_visible=True` so prints land in the "Quotations" virtual folder. 4 migrations (3 in foundation.document, 1 in foundation.base; all `op.f()`-wrapped per the writing-alembic-migrations skill). 95 new dedicated unit tests + 2 pre-existing tests broken by the refactor of audit-row write surface fixed in-place. Verification: full Python suite 3357 passed (+97 vs 3260 baseline), 1 skipped; frontend `bun run build` exits 0 (3034 modules transformed, no TS errors). Playwright e2e for the 3 UI slices (WS-ATT-11/12/13) deferred per user direction — code-only delivery. Design simplification mid-build: original 4-value `usage_kind` enum (`field_image` / `field_binary` / `field_collection` / `chatter`) collapsed to 3 values (`field_image` / `field_binary` / `record_attachment`); chatter file-drops are unified as `record_attachment` rows linked from `mail.message.attachment_ids` rather than carrying a separate `chatter` kind. Privacy contract preserved: `field_image` + `field_binary` are NEVER surfaced in chatter or the Documents-app virtual tree — they remain private to their form widget. Sync sites updated. Previous sync was 2026-05-27 (**Slice L — Frontend Print Menu (WS-Y1) ✅ Delivered** — twenty-fifth code-shipping slice; the final Phase 1 piece. Wires the existing Print menu button in `WorkspaceActionControlPanel` to the platform `/api/reports/*` HTTP surface (Slice I), dynamically populating menu items from `useReportActions` per view context (model + record + mode), dispatching the chosen action via `runReport`, and delivering the result via format-aware client-side routing (pdf/html → new tab via Blob URL, docx/xlsx → anchor download with slugified filename). Print menu visible on list + form views only (kanban excluded per user direction; non-UI dispatch paths use the `ede.report.run` Command bus directly). 3 new frontend modules (ReportService 110 LoC + useReportActions 70 LoC + ReportDelivery 110 LoC) + 2 modified (WorkspaceActionControlPanel + Controller for selectedIds passthrough) + 2 new vitest files with 24 cases. Design choices: print menu is user-facing UI only (not universal report-triggering); engine-agnostic frontend; format-to-delivery routing client-side only; useReportActions short-circuits to skip useless API calls when not applicable; manual mock replacement (not vi.spyOn) for TS-strict friendliness; vi.useFakeTimers for the setTimeout-URL.revokeObjectURL deferred cleanup. Render Preview + Inspect Resolved XML admin-form buttons (originally in WS-Y1 spec) deferred to Phase 2 polish; no E2E Cypress tests (defer to broader foundation.qa-automation E2E test bed). Verification: `cd src/frontend && bun run build` exits 0 (tsc + vite clean); `bun run test` exits 0 (597 vitest cases including the 24 new); full repo `./run_tests.sh` exits 0 (3055 pytest + 597 vitest = 3652 total green). **Phase 1 fully delivered.** End-to-end Print Quote story works: form view → "Print Quote" menu item → cascade resolves to `sale_crm.quote.standard` → `BindChromeResolver` resolves chrome from active `ir.document.config` → engine produces PDF bytes → client opens inline in new tab. Multi-record list dispatch works identically via the template's `<for-each items="records" as="quote">` block. Phase 2 next (HTML + DOCX engines, charts, barcodes, grid, post-processing — 9 WS). Sync sites updated to mirror new state. Previous sync was 2026-05-27 (**Slice K2 — Quote Document (Domain Seed in `logistics.sale_crm`) (WS-X20 part 2) ✅ Delivered** — twenty-fourth code-shipping slice; final WS-X20 commit. Ships the Quote-specific seeds in `logistics.sale_crm` that plug into the K1 engine substrate. With K2 done, the platform has a complete "Print Quote against `crm.quote`" path end-to-end — `action_print_quote` registered against `crm.quote` → cascade resolves to `sale_crm.quote.standard` → `BindChromeResolver` injects org's chrome → engine produces PDF bytes. 3 new data files in `src/domains/logistics/sales_crm/data/` (quote_doc_type.xml + quote_template.xml + quote_action_report.xml — all using xml-id refs to chain dependencies across files: doc-type → template via `parent_id` + `doc_type_id` refs; doc-type → action.report via `target_record_uuid` `ref=` on Char field that resolves to record_uuid string). Quote template uses `inherit_mode=copy` of `foundation.document.standard` with a single `<xpath expr="//page[@id='standard-document-body']" position="replace">` patch wrapping `<for-each items="records" as="quote">` (multi-record-aware per K1 contract). 1 manifest update adding `foundation.document` to `depends` + the 3 data file entries in dependency load order. Design choices: Quote concepts live with the quote model (sales_crm/data/, not foundation.document/data/); `ref=` on Char fields resolves xml_id → record_uuid (standard EDE data-loader behavior, useful for the polymorphic `target_record_uuid`); copy mode xpath-patches the parent's resolved DML; no quote-lines table (deferred — needs `quote.lines` metric in foundation.dataset); Reference field display-name resolution deferred. 0 new tests (data files validated by the data-loader at boot time); full repo `./run_tests.sh` exits 0 (3055 pytest + 573 vitest = 3628 total green). **WS-X20 fully delivered** across K1 (engine substrate) + K1b (admin views) + K2 (Quote seeds). Phase 1 Print Quote story is now end-to-end-wired except for Slice L (React frontend). Sync sites updated to mirror new state. Previous sync was 2026-05-27 (**Slice K1b — Admin Views + Settings Menu (WS-X20 part 1b) ✅ Delivered** — twenty-third code-shipping slice; follow-up to K1's engine substrate. Lands the admin UX for editing every WS-X20-introduced record via the React webclient — with K1b done, the document-engine surface is fully manageable from the UI without touching XML data files. 4 new admin view files (one per model) + 1 new menus file declaring Settings → Documents subtree under `base.menu_settings_root` (sequence 50, icon file-text) with 4 leaves (Document Settings sequence 10 — the chrome-picker UX · Document Templates 20 · Document Types 30 · Report Actions 40). The `ir.document.template` form is the most complex with notebook tabs General/DML Body/Inheritance/Applicability; the others are simpler. The Settings DSL panel was deferred — `ir.document.config` admin form already provides the per-org chrome-picker UX via Settings → Documents → Document Settings; the Settings DSL is shaped for flat `ir.config` key-value writes and doesn't naturally surface record-aware forms with org-scope + Reference dropdowns. Menus live in `foundation.document` while the view for `ir.action.report` lives in `foundation.base` — the view is adjacent to the model (foundation.base, Slice H decision); the menu + action exposing the view to users is in `foundation.document` because DRE is the marquee engine consumer in Phase 1. 0 new tests (views are declarative XML validated at parse time by the existing presentation DSL validator + data-file validator); full repo `./run_tests.sh` exits 0 (3055 pytest + 573 vitest = 3628 total green); all 4 view files validate against the presentation RNG; all 5 menu entries + 4 action entries load without xml_id-ref resolution errors. Sync sites updated to mirror the new state. Previous sync was 2026-05-27 (**Slice K1 — Engine Substrate (`<bind-chrome>` DSL + Pages-Level `<for-each>` + `ir.document.config` + Reworked Render Context) (WS-X20 part 1) ✅ Delivered** — twenty-second code-shipping slice; first commit of the WS-X20 K split. Lands the engine substrate every domain document module copy-inherits before authoring its own template. New `<bind-chrome from-config="<field>"/>` DSL element + `BindChromeResolver` pipeline step (runs between WS-X3 inheritance and WS-X4 includes; cascade lookup of active `ir.document.config` row; per-org → NULL-org default fallback; idempotent on DML without `<bind-chrome>`). New model `ir.document.config` (5 fields) + Alembic migration `d7dc5275ec8d` chained off `c7d3a8e2f5b1`. Pages-level `<for-each items="records" as="r"><page/></for-each>` finally wired through `PageExpander` (IR + parser + schema shipped in WS-X1; expander was crashing on `ForEachNode` siblings — closed with new `_expand_pages_for_each` helper). `DocumentReportEngine.dispatch` context contract reworked from Slice J's placeholder shape to multi-record-mode-only: every render's params now guarantees `records: list[dict]` (always a list, single-record dispatch returns list of 1), `organization: dict` (from `env.active_organization_id`, `{}` on miss), `user: dict` (from `env.principal["user_id"]`, `{}` on miss). Caller params merge on top. Data seeds: `foundation.document.standard` base template + 6 chrome fragments (header/footer × minimal/formal/branded) + NULL-org default config row pointing at minimal fragments via xml_id refs. Design choices: bind-chrome is string-level (substituted chrome flows through full pipeline); identifier shape is record_uuid only (data-loaded xml_id refs and runtime-designer uuids land at identical column storage); two distinct `organization_id` semantics (`ir.document.config.organization_id` = chrome selection knob, `ir.document.template.organization_id` = visibility scope); pages-level `<for-each>` was IR-ready since WS-X1, only expander wiring was missing; render context is multi-record-mode-only — engine handles single→multi scaling transparently. 17 new tests across 5 classes (TestBindChromeResolverNoOp × 1, TestBindChromeResolverHappyPath × 2, TestBindChromeResolverOrgCascade × 2, TestBindChromeResolverErrors × 5, TestPagesLevelForEach × 7, TestDocumentConfigModel × 4); Slice J's `test_calls_render_by_key_with_template_key` updated for new context shape; full repo `./run_tests.sh` exits 0 (3055 pytest + 573 vitest green); ruff clean on the 4 new + 5 modified files. Admin views (4 forms) + Settings panel deferred to follow-up K1b; the system-default config row in `data/document_config_default.xml` provides out-of-the-box working chrome until then. Sync sites updated to mirror the new state. Previous sync was 2026-05-27 (**Platform Layer · Slice J — `DocumentReportEngine` Implements Platform Protocol (WS-X19) ✅ Delivered** — twenty-first code-shipping slice; fourth + final slice of the platform-layer 4-slice sequence G→J. Registers DRE as `engine_key="dre"` against the platform `ReportEngine` Protocol; the platform `ReportDispatcher` (Slice I) now routes `ir.action.report` rows with `engine_key="dre"` to `DocumentReportEngine.dispatch()`, which resolves `(target_model_key, target_record_uuid)` to a specific `ir.document.template` (direct uuid lookup or doc-type cascade) and delegates to `DocumentService.render_by_key()`. With Slice J done, the platform layer is fully delivered — DRE is now a regular registered engine like any future ERE/PostScript/EPUB engine. New `src/ede/foundation/document/services/document_report_engine.py` (~370 LoC) ships `@api.report_engine`-decorated `DocumentReportEngine` class (`engine_key="dre"`, `display_name="Document Report Engine"`, `supported_output_formats=["pdf"]`) with three Protocol classmethods: `list_actions_for_context` filters `ir.action.report` rows by engine+model+org+active+(form-mode)domain; `dispatch` resolves target via direct uuid or doc-type cascade (prefers org-matched, sorts by priority, excludes `extend`-mode), auto-augments params with `target_record_uuid` / `target_record_uuids`, calls `DocumentService.render_by_key()`; `validate_action` checks shape + verifies cascade has resolvable candidates when env is reachable. Module wired into the import chain via `services/__init__.py` (side-effect import — `@api.report_engine` registers at module-import time) + `foundation/document/__init__.py` (prepended `from . import services`). Design choices (load-bearing): (1) two distinct target shapes — `ir.document.template` (admin/debug, direct uuid) vs `ir.document.type` (production, cascade); (2) `extend`-mode templates filtered from cascade — they layer onto a parent and never render alone; (3) org filter — prefer org-matched over NULL-org, sort by priority within bucket — a more-specific template beats a less-specific one even when the less-specific has a lower priority number; (4) auto-augment params with target_record_uuid + target_record_uuids lets DML datasources reference the action's target row(s) without callers having to wire those in; (5) validate_action skips DB checks when no env — offline admin validators get back shape errors without crashing on missing env, the full check re-runs at write-time via the `ir.action.report` lifecycle hook; (6) format-list cross-check at four redundant layers (action write-time + dispatcher runtime + engine validate_action + engine dispatch) — redundancy is the contract enforcement. 14 new tests across 6 classes: TestEngineRegistration × 2, TestValidateAction × 7, TestDispatchDirectTemplate × 3, TestDispatchDocTypeCascade × 4, TestListActionsForContext × 7, TestReportDispatcherIntegration × 1; `unittest.mock.patch` stubs `DocumentService.render_by_key` so tests don't need the full DML→PDF pipeline. Full repo `./run_tests.sh` exits 0 (3032 pytest + 573 vitest green); ruff clean. Sync-block bodies updated to mirror the new state. Previous sync was 2026-05-26 (**Platform Layer · Slice I — `ReportDispatcher` + `ede.report.run` command + `/api/reports/*` (WS-X0c) ✅ Delivered** — twentieth code-shipping slice; third of the platform-layer 4-slice sequence G→J. Closes the engine-agnostic dispatch path. New `src/ede/foundation/base/services/report_dispatcher.py` ships `ReportDispatcher` with `list_for_context()` (aggregates `engine.list_actions_for_context(...)` across registered engines, sorts by `(priority ASC, name ASC)`) + `dispatch()` (looks up `ir.action.report` by key, validates format at two layers — action's available list + engine's supported list — catches admin-downgrade race, resolves engine via `ReportEngineRegistry.resolve()`, calls `engine.dispatch(env, action, target_record_uuids, params, output_format) → bytes`, optionally persists to `storage.document` when `persist=True`). Three new error classes (`ReportDispatcherError` umbrella + `ReportActionNotFoundError` + `ReportFormatNotAvailableError` + `ReportEngineNotRegisteredError`). New `@api.on_command("ede.report.run")` method on `IrActionReport` reads `action_key`/`target_record_uuids`/`params`/`format`/`persist` from `cmd.payload`, delegates to `ReportDispatcher.dispatch()`. New `ReportsController` HTTP surface at `src/ede/foundation/base/api/reports_routes.py` with `GET /api/reports/_list?model=&record_id=&mode=` (delegates to lister, serializes descriptors to JSON) + `POST /api/reports/{action_key}/run?format=&persist=` (delegates to dispatcher; persist=True returns JSON envelope with storage UUID; persist=False returns base64-wrapped bytes envelope — Phase 1 contract). `src/ede/foundation/base/api/__init__.py` appended `from . import reports_routes`. Design choices (load-bearing): `ede.report.run` is the canonical platform dispatch entry point — production callers go through it (`ede.document.render` from Slice 3 remains as internal entry point for the engine's own resolution cascade); format cross-check at two layers (model-layer write-time + dispatcher-layer runtime) catches admin-downgrade race with same `ReportFormatNotAvailableError` class; `storage.document` persistence is best-effort not transactional (if persistence fails caller still gets bytes); base64 envelope on persist=False makes bytes transport-safe through JSON with ~33% size overhead but eliminates Content-Type negotiation + makes responses trivially loggable + keeps route shape identical across persist=True/False; lister returns one flat list across engines — UI sees one Print menu, engine_key in descriptor for client routing but UX is engine-agnostic. 12 new tests across 5 classes (TestDispatchHappyPath × 3, TestDispatchErrors × 3, TestListForContext × 3 with `_StubEngineA` + `_StubEngineB` proving multi-engine aggregation + sort, TestPersist × 1, TestEdeReportRunCommand × 2). Full repo `./run_tests.sh` exits 0 (pytest + 573 vitest green); ruff clean on the 4 new files. **Platform Layer · Slice H — `ir.action.report` Model in `foundation.base` (WS-X0b) ✅ Delivered** — nineteenth code-shipping slice; second of the platform-layer 4-slice sequence G→J. Lands the engine-agnostic report-action descriptor — one row per printable / exportable action surfaced in the React webclient's Print menu. New `@api.model("ir.action.report")` on `IrActionReport(DomainModel)` at `src/ede/foundation/base/models/ir_action_report.py` with 11 business fields (`name` / `key` unique-indexed / `engine_key` indexed / `model_id` Reference→`ir.model` restrict required indexed / `target_model_key` + `target_record_uuid` polymorphic discriminator / `available_output_formats` comma-separated subset / `domain` nullable EDE search-domain / `organization_id` Reference→`res.organization` restrict nullable indexed / `priority` Integer default 10 / `active` Boolean default True). Two lifecycle hooks `pre.ir.action.report.create` + `pre.ir.action.report.update` validate via `ReportEngineRegistry.resolve(engine_key)` + cross-check requested formats against `engine.supported_output_formats` + delegate to `engine.validate_action(record)`. The update hook uses a duck-typed `_MergedView(cmd.record, cmd.payload["values"])` overlay so validation sees the POST-update state (catches "update changes engine_key to something not registered"). New Alembic migration `5fa2a5742d5a_add_ir_action_report.py` chained off `b8e1f4a92c37`; declares 2 hard FKs inline + 2 unique constraints + 3 single-column indexes + 2 composite indexes for the lister's hot paths (`(model_id, organization_id, active)` form/list-mode + `(engine_key, active)` engine-scoped) + 4 audit indexes; all names wrapped in `op.f(...)` per the `writing-alembic-migrations` skill; all names ≤ 52 chars (well under PG's 63-char limit); new-table migration so no `batch_alter_table` needed for SQLite compat. Design choices (load-bearing): hard FKs at DB layer + soft `engine_key` validation at model layer (engines register at boot — DB FK would be circular); merged-view on pre-update; format cross-check at write-time; `target_model_key` is intentionally Char not Reference (polymorphic discriminator); two composite indexes match the two lister query shapes. 11 new tests across 4 classes (TestCRUD × 3, TestUniqueKey × 1, TestEngineValidation × 4 proving the `_MergedView` plumbing + format cross-check + engine-side errors path, TestRequiredFieldsAndDefaults × 3). Stub-engine pattern: `autouse=True` `clean_registry` fixture clears + re-registers `_StubEngine` between tests. Full repo `./run_tests.sh` exits 0 (Python pytest + 573 vitest green); ruff clean on the 3 new files. **Platform Layer · Slice G — `ReportEngine` Protocol + Registry + `@api.report_engine` decorator (WS-X0a) ✅ Delivered** — eighteenth code-shipping slice; first of the platform-layer 4-slice sequence G→J. Kernel-only substrate that every report-producing engine (DRE today, ERE / future engines later) plugs into. New `src/ede/core/engines/reporting/` package (4 files): `protocol.py` ships the structural `ReportEngine` Protocol with 3 classmethods (`list_actions_for_context` / `dispatch` / `validate_action`) + 3 class attributes (`engine_key` / `display_name` / `supported_output_formats`) + two frozen dataclasses (`ActionDescriptor` lister return shape + `ValidationError` engine-side action validation); `registry.py` ships `ReportEngineRegistry` (process-local class-only registry with `register` / `resolve` / `registered_engines` / `is_registered` / `clear` class methods, case-insensitive engine_key lookup normalized to lowercase, idempotent re-registration of the same class, `DuplicateReportEngine` for class-clash, alphabetical engine-key ordering for deterministic multi-engine listers); `errors.py` umbrellas `ReportEngineError → ValueError` with `DuplicateReportEngine` + `ReportEngineNotRegistered`; `__init__.py` re-exports the public surface. New `src/ede/core/kernel/report_engine_decorator.py` ships `@api.report_engine` — side-effecting at module-import time, calls `ReportEngineRegistry.register(cls)`, returns the class unchanged (transparent — no metaclass, no class mutation; mirrors `@api.metric`). `src/ede/core/api.py` re-exports `report_engine` in `__all__`. Design choices (load-bearing): structural Protocol (not abstract base class) — engines don't inherit, they just match the shape, keeping engines free to live in their own modules with no kernel dependency beyond the decorator import; case-insensitive engine_key normalized to lowercase at registration; idempotent re-registration of the same class is a no-op (test fixture reloads safe); engine_key without a non-empty class attribute raises at registration time. 14 new tests across 4 classes (TestRegistration × 5, TestResolution × 4, TestDecorator × 2, TestProtocolShape × 3); full repo `./run_tests.sh` exits 0; ruff clean. Authored directly on the primary repo branch `document-engine-platform-layer` (no worktree this cycle — Slice A→F worktree workflow already established the pattern; primary-repo workflow is the next-iteration default). **Stage 1 fully ✅ complete (A+B+C+D+E+F all delivered 2026-05-22 → 2026-05-26)** · **Stage 1 Polish · Slice F — Datasource Binding (WS-X6) ✅ Delivered** — seventeenth code-shipping slice; sixth + final of the 6-slice "Stage 1 fully" sequence A→F. Closes the per-record rendering surface that demo templates need — templates can now declare what data they want via `<rows datasource= as= key=>` and the engine fetches it via the platform metric registry at render time. New `<rows datasource= as= key=>` block-level iterator with required `datasource=` (the metric key looked up via `run_metric(env, key, params)` from `ede.core.engines.integration.runner`), optional `as=` (default `"row"`), optional `key=` (reserved for explicit-key extraction in a later polish slice). New `RowsBlock(datasource, as_name, key_expr, children)` Pydantic IR class appended to `PageChild` union (now 14 alternatives). RelaxNG `dml.rng` adds `rows-block` define under `block-child` choice. Resolution-time construct — remains EXCLUDED from `resolved.rng` (Slice C symmetry contract preserved). Parser branch in `_parse_block_children` mirrors `<for-each>` parsing; empty `datasource=""` raises `DmlValidationError`. New `DataSourceResolver` module at `src/ede/core/engines/document/dml/datasources.py` — IR transform walking each page's children, calling `run_metric(env, key, params)` per RowsBlock, flattening one paragraph-set per fetched row with `as_name` bound to the row dict; returns a new `Document` (functional, no mutation). **Pipeline placement (load-bearing)**: runs AFTER `PageExpander.expand` + `PageFilter.filter`, BEFORE `PlaceholderBinder.substitute`. Final Stage 1 pipeline `inheritance → includes → variables → parser → page-expansion → page-filter → datasource-resolver → placeholder-binder → emitter → Stage 2`. Env access is needed only by the datasource fetch — PageExpander stays env-free + pure-IR. `PageExpander._resolve_block_children` widened return type from `list[Paragraph]` to `list[PageChild]` so RowsBlock survives expansion; recursively pre-substitutes within-block children's context refs (within-page `<for-each>` loop vars resolve correctly inside a `<rows>` body) but preserves the RowsBlock wrapper for the post-pass. New `Page.params_overlay: Optional[Dict[str, Any]]` field — IR-internal (not emitted to resolved XML); PageExpander writes `{as_name: item}` into `params_overlay` when cloning a loop page; DataSourceResolver merges `page.params_overlay` into the metric call's params so a `<rows>` block inside a `<page for="invoices" as="inv">` sees the current `inv` and can scope on it. No caching in Phase 1 — each `<rows>` block fires its own metric call; per-render memo deferred to Phase 2. Four error classes — `DataSourceError` umbrella + `DataSourceNotFoundError` (wraps `DatasetSpecError` from registry lookup) + `DataSourceExecutionError` (wraps any non-spec exception with `{type}: {message}`) + `DataSourceNonTableModeError` (scalar mode or non-list `rows`). Row-context substitution via `_substitute_row_refs(text, row_context)` — matches `\{([A-Za-z_][A-Za-z_0-9]*(?:\.[A-Za-z_][A-Za-z_0-9]*)*)\}` and substitutes only references whose head is in row context; global params + render-time tokens (`{PageNumber}`, etc.) pass through unchanged. Dotted-path lookup supports nested dicts + attribute fallback. DocumentService threading: `_render(...)` gains `env: Any` kwarg; resolver inserted between page-filter and placeholder-binder; caller site updated to pass `env=env` directly. 18 new tests across 6 classes; full DRE suite **332/332** (was 314/314 — +18); ruff clean. Two pre-existing F401/F841 warnings in `page_expander.py` (unused `deepcopy` import + unused `is_dict` var from Slice 7) fixed as Slice F hygiene. **WS-X20 renamed from "HBL demo" to "Quotation demo"** in the same sync — no shipment/HBL records exist in the platform yet; the canonical doc-type → domain-based template selection proof reuses the quotation surface (logistics.sale.crm opportunities ship rate quotes today, so quotation templates have real records to render). HBL demo lives in Phase 2/3 after the shipment substrate is built. Authored in `/tmp/ede-doc-engine` worktree on `document-engine-phases` branch. **Stage 1 fully complete — A+B+C+D+E+F all delivered.** Remaining Phase 1 work: platform layer (WS-X0a/b/c + WS-X19 DocumentReportEngine), Quotation demo (WS-X20), and frontend WS-Y1. **Stage 1 Polish · Slice E — Formula-Form Variables (WS-X5 second pass) ✅ Delivered** — sixteenth code-shipping slice; fifth of the 6-slice "Stage 1 fully" sequence A→F. Lights up `<var name= type= formula="expr"/>` alongside the existing value form. Formulas consume the safe AST evaluator at `src/ede/core/engines/formula/safe_eval.py` already shipped by `foundation.dataset` Phase 2 (2026-05-13), using the `full` function set (math + aggregate + logic from `numeric` + date helpers `today`/`add_days`/`add_months`/`days_between` + format helpers `currency`/`percent`). Mutual-exclusivity check at extract time — declaring both `value=` and `formula=` or neither raises `VariableSyntaxError`. Cross-form dependency single graph — formula→value via bare name, value→formula via `$ref`; both forms share one DFS-on-stack traversal so cross-form cycles surface the same as same-form cycles. AST-derived dep edges (via `ast.walk` over `Name` nodes) filter call-targets / whitelisted function names / render-param names / undeclared names so only declared-var → declared-var edges feed the cycle tracker. Render-time params first-class — `VariableResolver.resolve(dml_str, params=None)` threads the render param map directly into the safe_eval scope; declared-var name beats param key on clash (vars resolve AFTER the params merge in the scope build). Typed parallel store — `resolved_str` for XML substitution + new `typed_values: dict[name → Any]` for the safe-eval scope; value-form vars get their typed form via `_typed_from_canonical(name, var_type, str)` (INTEGER → `int(s)`, DECIMAL → `float(s)`, BOOLEAN → `s.lower() == "true"`, DATE → `date.fromisoformat(s)`, LIST → comma-split). Lenient formula-result coercion — INTEGER accepts whole floats (`10.0` → `10`) but rejects fractional (`10.5` → `VariableTypeError`); BOOLEAN accepts Python bool / int (0/non-zero) / `"true"`/`"false"` strings; DATE / DATETIME accept Python date/datetime objects directly + ISO strings. Canonical string formatting via `_to_canonical_str` (`bool → "true"/"false"`, `float → trimmed decimal` via `_format_number` which strips `.0` for whole-number floats but keeps fractions, `date → "YYYY-MM-DD"`, `datetime → ISO 8601`). New `VariableFormulaError` class wraps any `FormulaEvalError` from the safe AST evaluator (disallowed AST node, unknown function, undefined name, parse error) and re-raises with the offending var's name + formula text. Per-var dep traversal happens BEFORE evaluation — inside `_resolve_formula`, after AST-parsing the expression and collecting var-deps, the slice calls `visit(dep)` for each dependency in encounter order; this guarantees `typed_values` contains every name the scope build will need AND surfaces cycles immediately at the recursive `visit` of the problematic dep, not at eval time. 39 new tests in `test_formula_variables.py` across 9 classes (arithmetic × 6 + functions × 7 + cross-form × 4 + render-params × 4 + mixed cycle × 3 + form-syntax × 5 + result-coercion × 6 + function-name-not-a-dep × 2 + service-integration × 2); all 20 prior `test_variables.py` tests preserved (purely additive). Full DRE suite **314/314** (was 275/275 — +39). Ruff clean. Pre-existing E731 lambda warning in `_substitute_in_tree` fixed as Slice E hygiene. Authored in `/tmp/ede-doc-engine` worktree on `document-engine-phases` branch. **Stage 1 Polish · Slice D — Full `<field>` Element + Strict-Mode Flag (WS-X10) ✅ Delivered** — fifteenth code-shipping slice; fourth of the 6-slice "Stage 1 fully" sequence A→F. Adds the rich placeholder element + format specifier kernel + opt-in strict mode + a recursive PlaceholderBinder rewrite that walks the full Phase 1 IR vocabulary (closes a Slice-B-era latent bug). New `FormatSpec` enum in `enums.py` covers 11 closed values (`string` / `integer` / `decimal` / `currency` / `percent` / `date` / `datetime` / `boolean` / `upper` / `lower` / `title`); each accepts an optional colon-suffix arg (`decimal:N`, `currency:CCY`, `date:fmt`, `boolean:Y|N`). `ALL_ENUMS` registry grows from 15 → 16 classes. New `FieldNode(name, format_spec, format_arg, default)` Pydantic class added to `PageChild` union. Parser's `_parse_field()` splits format= at first colon, validates head against FormatSpec, preserves arg tail. RelaxNG schema adds `field-node` define under `block-child`. **PlaceholderBinder is fully rewritten**: `substitute(document, params, *, strict=False)` signature adds the strict kwarg; new recursive `_substitute_children()` walks every IR container and every text-bearing leaf; new `_resolve_field()` does the FieldNode-flattening (looks up value, applies format via `_format_value()`, falls back to `default=` when value is None; strict mode without default raises `MissingPlaceholderError`; lenient mode without default leaves a literal `{name}` token); `_format_value()` implements all 11 FormatSpec branches with Python stdlib; new `_to_strftime()` translates YYYY/MM/DD/HH/mm/ss/AA tokens longest-first; new `MissingPlaceholderError` exception class. `DocumentService.render_by_key(... strict: bool = False)` parameter threaded through. **Two-form coexistence (load-bearing)**: `{field}` string form (lenient, always) + `<field>` element form (strict-aware) — authors choose per-call. **FieldNode flattens to Paragraph at bind time** — same lifecycle as IfBlock / ForEachBlock; resolved-XML schema (Slice C) UNCHANGED. **Locale-naïve Phase 1** — Babel/CLDR locale formatting defers to Phase 3. 43 new tests across 7 classes (TestFieldParser × 7, TestFieldFlattening × 4, TestAllFormatSpecs × 17, TestStrictMode × 4, TestRecursiveWalk × 9 proving PlaceholderBinder walks the full Slice B vocabulary, TestPipelineIntegration × 2 end-to-end). 1 enum-count test updated. Full DRE suite **275/275** (was 232/232 — +43). Ruff clean. Authored in `/tmp/ede-doc-engine` worktree on `document-engine-phases` branch. **Stage 1 Polish · Slice C — Full Emitter + Resolved-Parser Mirror (WS-X11B + WS-X13) ✅ Delivered** — fourteenth code-shipping slice; third of the 6-slice "Stage 1 fully" sequence; symmetric mirror of Slice B. Every IR visual node added in Slice B now emits to resolved XML + parses back via the Stage 2 entry parser. Three-part rewrite: (1) `src/ede/core/engines/document/resolved/schemas/resolved.rng` — top-level structure permits optional `<styles>` + `<headers>` + `<footers>` siblings alongside `<meta>` + `<pages>`; `<meta>` widens to allow `<margins>`; new `<block-child>` `<choice>` covers 11 visual node types (paragraph / text / span / heading / image / block-with-label-and-details-halves / group / section / component-with-keep_together-hint / table-family (Table/Row/Cell/ColGroup/Col) / page-break); all resolution-time constructs (`<var>` / `<if>` / `<for-each>` / `<override>` / `<include>` / `<page for=>` / `<rows datasource=>`) remain EXCLUDED. (2) `src/ede/core/engines/document/resolved/parser.py` — new Resolved* dataclasses mirror IR 1:1 (`ResolvedMargins`, `ResolvedStyleDef`, `ResolvedText`, `ResolvedSpan`, `ResolvedHeading`, `ResolvedImage`, `ResolvedLabel`, `ResolvedDetails`, `ResolvedBlock`, `ResolvedGroup`, `ResolvedSection`, `ResolvedComponent` with `keep_together` bool, `ResolvedCol`, `ResolvedColGroup`, `ResolvedCell`, `ResolvedRow`, `ResolvedTable`, `ResolvedPageBreak`, `ResolvedHeader`, `ResolvedFooter`); `ResolvedTree` gains `styles`/`headers`/`footers` lists; `ResolvedBlockChild` union covers 11 visual node types; `ResolvedPage.children` widened to `List[ResolvedBlockChild]`; recursive `_parse_block_children` walks 11 tag branches; `_parse_table` handles colgroup + rows + cells with all attrs. (3) `src/ede/core/engines/document/dml/emitter.py` — new `_emit_meta` (with margins), `_emit_styles`, `_emit_headers`, `_emit_footers`, `_emit_pages`, `_emit_one_page` (preserves Slice 10 loop metadata), `_emit_block_children` dispatching on Python type via `isinstance` checks, per-node emitters for each of 11 visual types, `_emit_table` preserves colgroup + cell attrs + recursive cell contents; new `_set_if(elem, attr, value)` helper avoids `attr=""` artifacts; schema validation runs as final step before returning. **Symmetry contract (load-bearing)**: every IR node from Slice B has an exactly-matching tag in `resolved.rng` + a Resolved* dataclass in `resolved/parser.py` — tests enforce 1:1 correspondence so a new IR node added in the future without resolved-side support fails the round-trip tests. **Stage 2 readiness**: Stage 2 PdfEngine remains minimum-subset (single-font Helvetica + Paragraph rendering only); resolved XML now carries the full vocabulary so Stage 2 polish slices can add typography/table-layout/image-embedding without touching Stage 1 again — Stage 1 → Stage 2 contract complete for Phase 1. 20 new tests across 6 classes (TestEmitterCoversNewNodes × 9, TestTableRoundtrip × 2, TestChromeRoundtrip × 4, TestNestedContainers × 2, TestFullPipelineRoundtrip × 1 (DML XML → DmlParser → IR → ResolvedXmlEmitter → resolved XML string → ResolvedXmlParser → ResolvedTree), TestBackwardsCompatible × 2 (hello-world still emits / loop metadata still emits — Slice 10 forward-compat lock)). **First slice authored in `/tmp/ede-doc-engine` worktree on `document-engine-phases` branch forked from main — primary repo branch undisturbed**. Verification: 20/20 new tests; full DRE suite 232/232 (was 212/212 — +20); ruff clean; tests run with `PYTHONPATH=/tmp/ede-doc-engine/src` to override the editable install's package path so the worktree's source wins over the main repo's `src/ede/` (since the package was `pip install -e .`'d in the primary repo). **Stage 1 Polish · Slice B — Full IR + Parser + RelaxNG (WS-X1 + WS-X2) ✅ Delivered** — thirteenth code-shipping slice, second of the 6-slice "Stage 1 fully" sequence; biggest slice by raw scope. Full Phase 1 DML grammar lands in IR + parser + schema. `src/ede/core/engines/document/dml/ir.py` gains ~14 new Pydantic classes spanning chrome (`Margins`, `StyleDef`, `VarDef`), inline + block content (`TextNode`, `SpanNode`, `HeadingNode`, `ImageNode`, `BlockNode` + `LabelNode` + `DetailsNode`, `GroupNode`, `SectionNode`, `ComponentNode` with `keep_together` + scoped variables), table family (`TableNode`/`RowNode`/`CellNode`/`ColGroupNode`/`ColNode`), control (`OverrideNode` deprecated, `PageBreakNode`), page chrome (`HeaderNode`/`FooterNode` with `pagetype`), and `ForEachNode` page-level wrapper. `PageChild` union widened to 14 alternatives; `Pages.children` accepts `Union[Page, ForEachNode]`; `Document` gains top-level lists for `styles`/`variables`/`headers`/`footers`. RelaxNG schema mirrors with `<block-child>` choice covering 15 alternatives. Parser threads Slice A enums via `_opt_enum` helper. 27 new tests across 6 classes; 1 obsolete hello-world test (Slice-2 `<heading>`-rejection) patched to use `<bogus-tag>` instead. User-attested: 211 pytest passed / 1 obsolete-assertion failure fixed by Slice B itself; net green. **Stage 1 Polish · Slice A — Full Enum Kernel (WS-X12) ✅ Delivered** — twelfth code-shipping slice, first of a 6-slice "Stage 1 fully" sequence (A→F). Adds 12 new `(str, Enum)` classes to `src/ede/core/engines/document/dml/enums.py`: `PageType` (9 vals), `PageVisibility` (5), `TextAlign`, `VerticalAlign`, `FontWeight`, `FontStyle`, `TextDecoration`, `TextTransform`, `BorderStyle`, `ImagePosition`, `OverrideMode`, `VarScope`. `VarType` (10 vals) carried forward from Slice 6. `ALL_ENUMS` registry now contains 15 enum classes (Phase 2 chart/barcode/QR enums register here later). Stable-value contract enforced by per-enum value-tuple tests: downstream RelaxNG fragment generators in Slice B/C iterate enums to emit `<value>...</value>` choices verbatim, so order changes would silently break consumers. Test file rewritten with three classes: `TestExistingEnums` × 2 (Orientation + OutputFormat unchanged), `TestNewStage1Enums` × 13 (one per added enum), `TestRegistry` × 3 (all-15-registered + lossless `enum_values()` + no-duplicate-classes). Verification: 18/18 enum tests green; full DRE suite 184/184 (was 170/170 — +14); full repo suite green modulo pre-existing `KanbanView` failure (unrelated); ruff clean. **Full Schema Slice (WS-X18) ✅ Delivered** — eleventh code-shipping slice. Adds the doc-type taxonomy + render audit log. Two new models in `foundation.document/models/`: `document_type.py` (`ir.document.type` — `key`/`name`/`description`/`active`, first-class taxonomy for HBL / Quote / Invoice / etc.) and `render_audit.py` (`ir.document.render.audit` append-only — `template_uuid` / `template_key` / `output_format` / `output_size_bytes` / `success` / `error_message` / `target_record_uuid` / `params_keys` JSON / `applied_extension_keys` JSON; `default_order` = `created_at_utc desc`). `ir.document.template` adds `doc_type_id = fields.Reference("ir.document.type", on_delete="restrict")`. Third foundation.document migration `c7d3a8e2f5b1_full_schema.py` chained off `b5e2c8f491a0` creates the new tables + adds the column + FK + 3 indexes; `models/__init__.py` registers the new models (document_type imported BEFORE document_template since the Reference points at it). `DocumentService.render_by_key()` extended with `target_record_uuid: str | None` parameter and wraps the engine pipeline in try/except so every render writes an audit row via best-effort `_write_audit()` — success rows include `output_size_bytes`, failure rows include `error_message`; both include `params_keys` (top-level only — cheap fingerprint without PII) and `applied_extension_keys`. `_resolve_template_dml` gains optional `applied_out: list` parameter and appends every successfully-applied extension; failing extension is named in `error_message` instead. Audit-row semantics: written on every render where template lookup succeeded; NOT written when `TemplateNotFoundError` fires (audit is per-template, not per-attempt); `applied_extension_keys` captures only successfully-applied extensions. `ir.document.resolution` cache deferred to Phase 2 (pure optimization concern). 7 integration tests across 2 classes: DocumentType CRUD × 2 (create-and-search / template-references-doc-type), RenderAudit × 5 (audit-row-on-success / no-audit-on-template-not-found / audit-row-on-extension-failure-after-lookup / audit-captures-applied-extensions / audit-target-record-uuid-passthrough). Verification: 7/7 full-schema tests green; full DRE suite 170/170 (was 163/163 — +7); ruff clean. **Two-Pass Executor Slice (WS-X16) ✅ Delivered** — tenth code-shipping slice. Adds Stage 2 render-time-token substitution. New `TwoPassExecutor` at `src/ede/core/engines/document/output/pdf/two_pass.py`. Pass 1 measures (`physical_total = len(resolved.pages)`, 1:1 logical-physical for Phase 1 — multi-physical-per-logical via overflow ships in WS-X15 full). Pass 2 substitutes the full render-time token vocabulary against a per-page context: `{PageNumber}` / `{TotalPages}` / `{LogicalPageId}` / `{LogicalPageIndex}` / `{PageInLogical}` (= 1 today) / `{LogicalPageCount}` / `{LoopIndex}` / `{LoopCount}` / `{LoopKey}` (last three only when page carries loop metadata). Unknown tokens + unbound loop tokens on non-loop pages pass through as literals (visible authoring-mistake feedback). Page IR + `ResolvedPage` gain `loop_index` / `loop_count` / `loop_key` optional fields; emitter writes them as `<page loop-index= loop-count= loop-key=>` attributes; resolved.rng allows the three optional attributes; ResolvedXmlParser reads them. `PageExpander._expand_one` rewritten as filter-then-index two-phase pattern: filter against `<page if=>` first, then enumerate survivors with 1-based `loop_index` — `{LoopCount}` reflects surviving count for dense indexing (1, 2, 3 not sparse 1, 3, 5). `PlaceholderBinder.substitute` updated to preserve loop metadata when rebuilding `Page` instances (a bug found + fixed during this slice — without this preservation step, the loop attributes set by PageExpander were silently lost between expansion and emission). `PdfEngine.render()` calls `TwoPassExecutor.execute(resolved)` as the first step before any canvas drawing. 10 integration tests across 3 classes: pure executor × 7 (page-number-and-total / logical-page-tokens / page-in-logical-is-one / loop-tokens-on-loop-pages / loop-tokens-unbound-on-non-loop / unrecognized-tokens-pass-through / text-without-tokens-unchanged), resolved-XML roundtrip × 1, service integration × 2 (multi-page-renders-actual-totals / loop-pages-get-loop-tokens via env.dispatch + pdftotext). 1 Slice-3 test updated to flip from "literal survival" to "substituted output" (pre-WS-X16 vs post-WS-X16 expectation). Verification: 10/10 two-pass tests green; full DRE suite 163/163 (was 153/153 — +10); full repo suite green modulo pre-existing `KanbanView` failure (unrelated); ruff clean. **Within-Page Conditionals & Loops Slice (WS-X9) ✅ Delivered** — ninth code-shipping slice. Adds `<if test="expr">` and `<for-each items="key" as="x">` as page-children (paragraph-level granularity, NOT page-level — those are WS-X7/X8). Reuses `PredicateEvaluator` from Slice 8 for the test expression. New IR nodes: `IfBlock(test, children)` + `ForEachBlock(items, as_name, key_expr, children)` Pydantic models, with `Page.children` type widened from `List[Paragraph]` to `List[PageChild]` (union of Paragraph / IfBlock / ForEachBlock). Forward references resolved via explicit `model_rebuild()` calls at module bottom. Parser's `_parse_page` calls new recursive `_parse_block_children` that walks `<paragraph>` / `<if>` / `<for-each>` in document order. RelaxNG schema's `<page>` switches content model to `<ref name="block-child"/>` (a `<choice>` of paragraph / if-block / for-each-block — both control nodes recursive). `PageExpander.expand()` now walks EVERY page (not just loop-pages); new `_resolve_block_children(children, context) → list[Paragraph]` recursively flattens `IfBlock`s (kept/dropped by predicate evaluated against `context`) and `ForEachBlock`s (one set of resolved children per item, with `as_name` bound in the iteration context). Within-page substitution uses new `_substitute_context_refs` that only substitutes refs whose head is in context (global params + render-time tokens pass through unchanged for later pipeline stages). Within-page blocks SEE outer page-level loop variables: `<page for="companies" as="c">…<for-each items="c.depts" as="d"><paragraph>{d.title}</paragraph></for-each>…</page>` correctly iterates each company's departments. Old `_substitute_loop_refs` removed in favor of the unified context-based approach. Emitter remains unchanged — `PageExpander` always flattens to `List[Paragraph]` before the emitter sees the IR. 13 integration tests across 3 classes: pure × 7 (if-kept-truthy / if-dropped-falsy / for-each-flat / dict-items / if-inside-for-each / for-each-inside-if / nested-in-outer-loop), errors × 3 (for-each-missing-items-key / if-missing-test-at-parse / for-each-missing-items-at-parse), service integration × 3 (if-and-for-each-render / if-hidden-when-falsy / within-page-references-outer-loop-var). Verification: 13/13 within-page tests green; full DRE suite 153/153 (was 140/140 — +13); full repo suite green modulo pre-existing `KanbanView` failure (unrelated); ruff clean. **Page Conditional Slice (WS-X8) ✅ Delivered** — eighth code-shipping slice. Adds `<page if="expr">` page-level conditional + new `PredicateEvaluator` + `PageFilter` IR transform at `src/ede/core/engines/document/dml/conditionals.py`. Predicate grammar (Phase 1 minimal): `path` (truthy check on dotted-path lookup) / `path == literal` / `path != literal` with type coercion (`"5" == 5` returns True so authors can compare string-typed params against numeric literals). Literals: `'string'` / `"string"` / `123` / `1.23` / `True` / `False` / `None`. `PageExpander` extended — when a loop-page has both `for=` and `if=`, the predicate evaluates against `{**params, as_name: item}` per iteration and iterations where the predicate is falsy are skipped (no clone produced). `PageFilter` runs as an IR transform after page expansion, dropping non-loop pages whose predicate is falsy against `params`. IR `Page` model adds `if_expr` optional field; parser reads `if=` attribute (empty `if=""` raises `DmlValidationError`); RelaxNG schema's `<page>` definition adds the optional `if` attribute. `_render()` pipeline order is now `inheritance → includes → variables → parser → page-expansion (with per-iter if) → page-filter (global if) → placeholder-binder → emitter → Stage 2`. Errors: `PredicateError` umbrella + `PredicateSyntaxError`. 21 integration tests across 3 classes: predicate evaluator × 14 (truthy / falsy / missing / dotted-path / `'str'` equality / `"str"` equality / inequality / int equality / `"5"==5` type coercion / `True` literal / `None` literal / empty-expr-raises / unsupported-expr-raises / unquoted-literal-raises), page filter × 3 (truthy-kept / falsy-dropped / no-if-passes), service integration × 4 (conditional-renders / conditional-hidden / per-iteration-loop-filter / empty-if-rejected-at-parse). Verification: 21/21 conditional tests green; full DRE suite 140/140 (was 119/119 — +21); full repo suite green modulo pre-existing `KanbanView` failure (unrelated); ruff clean. **Page Expansion Slice (WS-X7) ✅ Delivered** — seventh code-shipping slice. Adds the `<page for= as= key=>` loop attribute set + new `PageExpander` IR transform at `src/ede/core/engines/document/dml/page_expander.py`. Operates on the parsed `Document` IR (not a string transform like Slices 4–6 — by this stage the DML has been validated against the RelaxNG schema, so working in typed IR is safer + faster than re-parsing). `PageExpander.expand(document, params) → new Document` clones every loop-bearing page once per iteration item: list/tuple sources yield 0-based key + 1-based `LoopIndex`; dict sources yield dict-key + 1-based `LoopIndex`. Per-iteration `{as_name}` and `{as_name.field.subfield}` substitution against the current item via dotted-path lookup with `Mapping` + attribute fallback; missing fields fall through unchanged (lenient). Generated page id: `{original_id}-{key_slug}` (slug sanitized to `[A-Za-z0-9_-]+`). Render-time tokens (`{LoopIndex}`, `{LoopCount}`, `{LoopKey}`, etc.) NOT substituted — survive into resolved XML for WS-X16's two-pass executor. IR `Page` model extended with three optional fields (`for_expr`, `as_name`, `key_expr`); parser defaults `as_name="item"` when `for=` is set without `as=`; RelaxNG schema's `<page>` definition adds three optional attributes (`for`/`as`/`key`); empty `for=""` raises `DmlValidationError` at parse time. `DocumentService._render()` extended — pipeline order is now `inheritance (WS-X3) → includes (WS-X4) → variables (WS-X5) → parser → page-expansion (WS-X7) → placeholder-binder → emitter → Stage 2`. Composes cleanly with WS-X3 inheritance (extension xpath patches can inject loop-pages into a base's `<pages>` block — proven by `test_loop_inside_extension_via_xpath`). Errors: `PageExpansionError` umbrella + `LoopSourceNotFoundError` + `LoopSourceNotIterableError`. 13 integration tests across 4 classes: pure expander × 7 (no-loop-pass-through / list-clones-per-item / dict-uses-dict-keys / nested-dotted-path / render-time-tokens-preserved / missing-field-lenient / default-as-name-is-item), error paths × 3 (for-key-not-in-params / non-iterable-source / empty-for-attribute-at-parse), empty iteration × 1 (empty-list-produces-zero-pages with non-loop pages surviving), service integration × 2 (loop-renders-three-concrete-pages via env.dispatch + pdftotext / loop-inside-extension-via-xpath). Verification: 13/13 page-expansion tests green; full DRE suite 119/119 (was 106/106 — +13); full repo suite green modulo pre-existing `KanbanView` failure (unrelated); ruff clean. **Variable Evaluator Slice (WS-X5 — value form) ✅ Delivered** — sixth code-shipping slice. Adds the `<var name= type= value=>` declaration mechanism + `$varName` / `${varName}` substitution + closed-vocabulary `VarType` enum (10 values) + DFS cycle detection (subset of Tarjan's SCC algorithm — finds any cycle, sufficient because variable references form a tree-of-deps where the first cycle is the one authors fix). Style cascade deferred — the DML grammar has no `<style>` element yet, so cascade has nothing to act on until WS-X1 expansion ships. New module `src/ede/core/engines/document/dml/variables.py` ships `VariableResolver.resolve(dml_str) → resolved_dml_str` as a pure DML transform (no env / no I/O) with a 4-step pipeline: (1) walk `xpath("//var")`, extract declarations into `dict[name → (VarType, raw_value)]`, validate `name` is a unique identifier + `value` is required + `type` is in `VarType`; (2) resolve the dependency graph via DFS-with-on-stack — each variable's raw `$dep` references are recursively substituted before type-coercion; cycles raise `VariableCycleError` naming the cycle path; (3) remove every `<var>` element from the tree; (4) substitute `$varName` / `${varName}` in text + attribute values via `_VAR_REF_RE = r'\$\{([a-zA-Z_]\w*)\}|\$([a-zA-Z_]\w*)'`. Closed enum `VarType` added to `enums.py` and registered in `ALL_ENUMS`. Per-type literal-pattern validators reject malformed values at resolution time (`integer "abc"` raises; `boolean "yes"` raises; `color "red"` raises — must be hex; etc.). `DocumentService.render_by_key()` pipeline order now `inheritance (WS-X3) → includes (WS-X4) → variables (WS-X5) → engine` — variables run AFTER includes so the host template's `<var>` declarations substitute into content inlined from included templates (proven by `test_var_with_include_resolved_separately`). 20 integration tests across 5 classes: pure resolver × 5 (no-vars-noop / single-var-text / brace-form / var-references-var / var-in-attribute), cycle detection × 3 (self / 2-node / 3-node), type validation × 6 (integer-rejects-garbage / boolean-rejects-yes / color-hex-ok / currency-iso-4217-ok / date-yyyy-mm-dd-ok / unknown-type-rejected), syntax errors × 4 (missing-name / missing-value / duplicate-name / undefined-reference), service integration × 2 (var-in-template-renders-substituted / var-resolves-into-included-content). Verification: 20/20 variable tests green; full DRE suite 106/106 (was 86/86, +20); full repo suite green modulo the pre-existing `KanbanView` failure on clean main (unrelated); ruff clean. **`<include>` Directive Slice (WS-X4) ✅ Delivered** — fifth code-shipping slice. Adds the `<include src="other.template.key"/>` directive — Stage 1 step that inlines a source template's `<page>` children at the include site. New module `src/ede/core/engines/document/dml/includes.py` ships `IncludeResolver.resolve(dml_str, env, *, _chain=())`. Walks the DML for `<include>` elements via `lxml.xpath("//include")`, looks up each `src` key in `ir.document.template` (active only), recursively resolves nested includes within the source (chain extended each level), then replaces `<include>` in-place with the source's `//pages/page` children. Recursion bounded at `MAX_INCLUDE_DEPTH = 5` enforced via `if len(_chain) > MAX_INCLUDE_DEPTH: raise IncludeDepthExceededError`. Cycle detection via per-chain visited tuple — `if src_key in _chain: raise IncludeCycleError`. Errors: `IncludeError` umbrella `ValueError` subclass + `IncludeNotFoundError` / `IncludeCycleError` / `IncludeDepthExceededError` / `IncludeSyntaxError`. `DocumentService.render_by_key()` updated — pipeline order is now `inheritance (WS-X3) → includes (WS-X4) → engine`; the include resolver sees the post-merge DML so any `<include>` injected by an extension xpath patch is caught and resolved. Demo: `demo.common.terms` (reusable T&C block) + `demo.invoice.basic` (uses `<include src="demo.common.terms"/>` as the trailing element under `<pages>`). 13 integration tests across 5 classes: happy path × 4 (single inline / multiple pages inlined / nested 3 levels / multiple includes in same host), cycle detection × 2 (self-loop / indirect A→B→A), depth limit × 2 (exceeding raises / exactly at max-depth succeeds), error paths × 4 (not-found / inactive-treated-as-not-found / missing-src-attr / source-without-pages), composition with inheritance × 1 (extension xpath patch injects `<include>`). Verification: 13/13 include tests green; full DRE suite 86/86 (was 73/73, +13); full repo suite 2513 passed / 1 pre-existing `KanbanView` DSL failure (unrelated, on clean main); ruff clean. **Records-as-Data Inheritance Slice (WS-X3) ✅ Delivered** — fourth code-shipping slice. Adds `parent_id` + `inherit_mode` (`extend` / `copy`) + `domain` + `priority` + `organization_id` to `ir.document.template` via second Alembic migration `b5e2c8f491a0_inheritance_fields.py` (chained off the slice-3 init `a3f7e2c91d04`). New kernel-side modules: `src/ede/core/engines/document/dml/merger.py` (`XPathMerger.apply_patches(base_dml, patches_fragment) → merged_dml` using `lxml.xpath`; **Odoo-standard `position=` semantics — `inside` appends children, `replace` swaps whole target with patch children, `before`/`after` insert siblings; intentionally diverges from `foundation.presentation`'s view-inheritance variant** so ERP-author muscle memory applies; `XPathMergeError` for malformed inputs) and `src/ede/core/engines/document/dml/domain.py` (`DomainEvaluator.matches(domain_string, params) → bool`; parses via `ast.literal_eval` (safe — no `eval()`); supports `=`/`!=`/`>`/`<`/`>=`/`<=`/`in`/`not in`; AND-only flat list; dot-path field lookup; `DomainSyntaxError` for malformed). `DocumentService._resolve_template_dml` rewritten: (a) target with `inherit_mode == "extend"` rejected with `InvalidTemplateError` (extensions never render directly — they layer onto parents); (b) `inherit_mode == "copy"` recursively resolves parent then applies the copy's xpath patches via `XPathMerger`; (c) base templates are the target as-is; (d) applicable `extend`-mode extensions discovered via `env.dispatch(Command("ede.search", ...))` with `parent_id == target.record_uuid` + `inherit_mode == "extend"` + `active == True` ordered by `priority asc`; (e) each extension's `domain` filtered against caller `params` via `DomainEvaluator.matches`; (f) surviving patches layered in priority order. Three new demo records replace the prior single hello: `demo.quote.standard` (canonical base — multi-line quote), `demo.quote.l10n_in` (`extend` from standard with `domain=[("country","=","IN")]` adding HSN paragraph + GST terms page), `demo.quote.vip` (`copy` from standard with own xpath patches injecting a priority-handling paragraph). 12 integration tests in `test_inheritance.py` across 4 classes (TestExtensionLayering × 6, TestCopyMode × 2, TestDirectExtensionRejection × 2, TestErrorPaths × 2) — uses `pdftotext` (poppler-utils) to extract text from rendered PDFs and assert the right markers appear/disappear per scenario. **4-variant PDF smoke** verified the full matrix: `/tmp/dre-slice4-us.pdf` (US base — no extension), `/tmp/dre-slice4-in.pdf` (India base + HSN extension + GST page), `/tmp/dre-slice4-vip.pdf` (VIP copy with priority paragraph + own title), `/tmp/dre-slice4-vip-in.pdf` (VIP + India — proves transitive inheritance: copy inherits the parent's currently-active extensions). 73/73 DRE suite green; full repo 2500/1 (1 pre-existing `KanbanView` failure on clean main, unrelated). Ruff clean. **Foundation Shell Slice ✅ Delivered earlier same day** — `foundation.document` foundation app online: `ir.document.template` minimal model (`key`/`name`/`dml`/`active`) + Alembic migration `a3f7e2c91d04_document_init.py` chained off `c4f8a9d2e1b3` + `DocumentService.render_by_key()` orchestrator + `@api.on_command("ede.document.render")` handler delegating to the service + `__manifest__.py` declaring `depends=["foundation.base"]` + `"document"` added to `ACTIVE_MODULES` (last position, after `"jobs"`) + demo template loaded via `<record id="demo_hello"><field name="dml" type="xml">…</field></record>` (powered by WS-X11 Slice A) + 10 integration tests through full command-bus path. Smoke-tested via `/tmp/dre-slice3.pdf` (2438 bytes, valid `%PDF-1.3`, 2 pages, all placeholders substituted, render-time tokens preserved). Text extraction via `pdftotext` (poppler-utils) is the verification mechanism — PDF content streams are zlib-compressed by ReportLab so raw byte-search isn't reliable. Full repo suite 2487 passed / 1 pre-existing `KanbanView` failure (unrelated). End-to-end `env.dispatch(Command("ede.document.render", payload={"key": ..., "params": ..., "format": "pdf"}))` returns `{"bytes": <PDF>, "format": "pdf"}`. With Slices A + B + C + D in, the closed-vocab registry + full Phase 1 IR + parser + RelaxNG schema + emitter + resolved-XML schema + resolved-XML parser all symmetrically cover the Phase 1 DML vocabulary, AND placeholder substitution covers the full IR with format-aware element-form `<field>` + opt-in strict mode. The remaining 2 slices in the "Stage 1 fully" sequence (E→F): **Slice E** WS-X5 formula-form variables (consumes the safe AST evaluator at `src/ede/core/engines/formula/safe_eval.py` from foundation.dataset Phase 2); **Slice F** WS-X6 datasource binding (`<rows datasource="metric.key">` against the foundation.dataset registry). After Stage 1 fully: platform layer (WS-X0a/b/c + WS-X19), HBL demo (WS-X20), frontend (WS-Y1). **Hello-World Engine Slice ✅ Delivered** — DML → PDF end-to-end pipeline via minimal subsets of 9 workstreams: WS-X1 DML parser, WS-X2 RelaxNG schema, WS-X10 placeholders, WS-X11 Slice B resolved-XML emitter + schema, WS-X12 enum kernel, WS-X13 resolved parser, WS-X14 units + paper, WS-X15 single-page layout, WS-X17 ReportLab Canvas writer. Engine pieces shipped at `src/ede/core/engines/document/`; foundation shell (`ir.document.template` model + migration + service + command + manifest + ACTIVE_MODULES + demo) deferred to next slice. 50 new pytest cases in `src/tests/foundation/document/` covering parser + schema validation + placeholder + render-time-token preservation + emitter + resolved-parser roundtrip + units + paper sizes + PDF magic bytes + multi-page + end-to-end pipeline. Real `%PDF-1.3` rendered from a smoke harness. `reportlab>=4.0,<5` added to `pyproject.toml`. Full repo suite 2477 passed / 1 pre-existing `KanbanView` failure (unrelated). **WS-X11 Slice A ✅ Delivered** earlier same day — kernel data-loader `type="xml"` + `eval=` attribute support at `src/ede/core/services/data_loader/`. Kernel-shared. 22 new pytest cases. `FieldSpec` gained `kind: FieldKind` discriminator with `is_ref` preserved as backwards-compat `@property`. `csv_parser` migrated to the new constructor shape. RelaxNG schema rewritten as a 4-form `<choice>` with a permissive `any-element` grammar. Status: foundation.document Phase 1 🔴 → 🟡 In Progress. **Earlier same-day entries:** platform `ReportEngine` abstraction lock-down — kernel ships `ReportEngine` Protocol + `@api.report_engine` decorator at `src/ede/core/engines/reporting/`; `ir.action.report` model in `foundation.base`; `ede.report.run` command + `/api/reports/*` routes; DRE registers as `engine_key="dre"`. `ir.document.type` first-class taxonomy. Print engine dropped (DRE outputs PDF / DOCX / HTML only). Frontend WS-Y1 added. Phase 1 workstream count 20 → 24; Phase 3 9 → 8; total 33 → 35. **Prior 2026-05-21 entries:** records-as-data inheritance, two-stage pipeline, page-centric grammar, native pixel-perfect PDF via ReportLab Canvas, HTML + DOCX as independent Stage 2 engines, React drag-drop designer in Phase 3.) To refresh, invoke the `syncing-roadmap-to-docs` skill.*
