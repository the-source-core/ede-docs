<!-- AUTO-MAINTAINED-BY: syncing-roadmap-to-docs -->
# Foundation — Presentation (View DSL & Web Client) — Implementation Docs

**Module:** `foundation.presentation` (`src/ede/foundation/presentation/` for the backend DSL element registry; `src/frontend/src/workspace/views/` for the React renderers)
**Roadmap:** [roadmap/foundation/presentation/](../roadmap/foundation/presentation/README.md)
**Status:** 🟡 In Progress — Phase 1 KanbanView MVP ✅ Delivered 2026-05-11 (verified live via user browser walkthrough); Phases 2 & 3 🔴 Not Started. ListView, FormView, SearchView, `<DynamicProperties/>`, `<workflow>` statusbar previously delivered via `foundation.workflow` and `foundation.customization`.
**Layer:** Foundation engine

> Source of truth is the roadmap. This doc reflects the *current built state* — what is shipped, what is partial, what gaps remain, what configuration it introduces, and how a developer or end user interacts with it. Auto-maintained by the `syncing-roadmap-to-docs` skill.

---

## 1. Functional Understanding

### What it is
<!-- SYNC-BLOCK: what -->
The view DSL grammar plus the React renderers that interpret it. Authors declare views as XML; the parser emits a frontend-consumable RenderPlan; the React webclient looks up a renderer by `view_type` and draws it. Today three view types ship (`list`, `form`, `search`); Phase 1 of this module adds the fourth (`kanban`).
<!-- /SYNC-BLOCK -->

### Why it exists
<!-- SYNC-BLOCK: why -->
View-DSL changes have so far ridden on the back of consumer phases — `<statusbar/>` lived in `foundation.workflow` Phase 2; `<DynamicProperties/>` lived in `foundation.customization` Phase 1. That is fine for one-off element additions but creates a maintenance gap when the work is itself a major surface, like adding a wholly new view type. Pulling the presentation module out gives the next view types (kanban now; pivot, calendar, gantt later) a clear owning home, and makes cross-cutting DSL evolution (conditional invisibility, expression-driven attributes, element composition rules) reviewable in one place.
<!-- /SYNC-BLOCK -->

### How a user / consumer interacts with it
<!-- SYNC-BLOCK: how -->
- Module authors declare a view as XML in their `views/*.xml` file, register it via `__manifest__.py` "data", and the renderer surfaces it automatically when an action is invoked.
- The React webclient looks up `view_type` (`list` / `form` / `search` / `kanban`) in `src/frontend/src/workspace/views/viewRegistry.ts` and dispatches to the matching component.
- For kanban (Phase 1), a workflow-aware drag-drop calls `WorkflowEngine.transition_by_command`; a non-workflow drag falls back to `ede.update`.
- Integration boundary — produces: parsed RenderPlans + React renderers. Consumes: kernel field metadata, workflow engine transitions, `ede.search` / `ede.read_group` for data.
<!-- /SYNC-BLOCK -->

## 2. Technical Implementation

### Architecture at a glance
<!-- SYNC-BLOCK: architecture -->
```
[Author]                          [foundation.presentation]                    [React webclient]
─────────                         ─────────────────────────                    ──────────────────
view XML in module's              ViewRegistry.discover()              ──►   GET /api/web/action/:id
data manifest                     reads __manifest__.py "data"                returns { views: [...] }
       │                                  │                                          │
       ▼                                  ▼                                          ▼
DslParser.parse_to_render_plan(xml) ──►  RenderPlan dict (view_type, fields, …)   viewRegistry[view_type]
                                                                                      │
                                                                                      ▼
                                                                              renders the screen
                                                                              (ListView / FormView /
                                                                               SearchView / KanbanView)
```
<!-- /SYNC-BLOCK -->

### Models
<!-- SYNC-BLOCK: models -->
| Model Key | Purpose | Source File |
|---|---|---|
| `presentations.presentation` | Persists registered view DSL XML keyed by `view_id` and `view_type` | `src/ede/foundation/presentation/models/presentations.py` |
| `presentations.data_filter` | User-saved data filter (search domain + group-by) per view | `src/ede/foundation/presentation/models/data_filter.py` |
<!-- /SYNC-BLOCK -->

### Services & key code paths
<!-- SYNC-BLOCK: services -->
| Service / Class | Responsibility | Source File |
|---|---|---|
| `DslParser` | Parses XML DSL into a frontend-consumable RenderPlan; dispatches on first child element (`ListView` / `FormView` / `SearchView` / `KanbanView` Phase 1 / `page` legacy). The form-element walker also recognises `<related>` (Enhancement 06) — emits `{type: "related", items: [...]}` into `header.elements` | `src/ede/core/services/presentation/dsl/parser.py` |
| `DslValidator` | RelaxNG-validates DSL XML before parsing | `src/ede/core/services/presentation/dsl/validator.py` |
| `DslLoader` | Loads view XML files from app `__manifest__.py` "data" entries | `src/ede/core/services/presentation/dsl/loader.py` |
| `ViewRegistry` | Indexes registered views by `(model_key, view_type, priority)`; resolves the highest-priority view at lookup time | `src/ede/core/services/presentation/view_registry.py` |
| `Env.ref(xml_id, *, raise_if_not_found=True)` | Kernel-level Odoo-style external-id resolver — resolves dotted `module.xml_key` via the `ir.data.reference` registry, returns a single-record `RecordSet` (attribute access for fields). Direct ORM read, no dispatcher hop. Raises `XmlIdNotFoundError(reason=...)` by default, returns `None` when `raise_if_not_found=False`. Used by `load_action` / `action_records` / `get_record` / `related_counts` to resolve `<related action="module.xml_key"/>` references | `src/ede/core/env.py` · `src/ede/core/errors.py` (exception) |
| Frontend `viewRegistry.ts` | Per-view-type metadata (`showSearchPanel`, `showListPager`) consumed by the workspace controller | `src/frontend/src/workspace/views/viewRegistry.ts` |
| Frontend `WorkspaceActionController` | Looks up the renderer by `view_type` and forwards the RenderPlan + record set | `src/frontend/src/workspace/components/action/WorkspaceActionController.tsx` |
<!-- /SYNC-BLOCK -->

### Commands
<!-- SYNC-BLOCK: commands -->
| Command | Trigger | Effect |
|---|---|---|
| `presentation.related_counts` | `POST /api/web/_related-counts` (Enhancement 06) | Batch-resolves each `{action_slug, active_id}` query in the payload — `env.ref(action_slug)` to look up the bespoke `ir.action`, substitutes `active_id` into the action's domain template, counts matching records, and returns `[{count, lone_uuid?}]`. count=1 short-circuit included so the form-toolbar `Related ▾` chip can drill straight to the record |
| Other data flows | `ede.search`, `ede.read_group`, `ede.create`, `ede.update`, `ede.delete` + workflow / domain commands | Presentation is otherwise read-mostly and does not register additional commands |
<!-- /SYNC-BLOCK -->

### Events emitted
<!-- SYNC-BLOCK: events -->
| Event | When fired | Typical subscribers |
|---|---|---|
| _none_ | | |
<!-- /SYNC-BLOCK -->

### REST endpoints
<!-- SYNC-BLOCK: rest -->
| Method + Path | Purpose | Controller |
|---|---|---|
| `GET /api/web/bootstrap` | Returns session + apps + menus payload for the React webclient on boot | `src/ede/foundation/presentation/api/web_client.py` |
| `GET /api/web/action/:id` | Returns the action + its views (parsed RenderPlans) for a menu leaf | `src/ede/foundation/presentation/api/web_client.py` |
| `POST /api/web/data_filter` | Saves a user data filter | `src/ede/foundation/presentation/api/controllers.py` |
| `POST /api/web/_related-counts` | Batch endpoint for the form-toolbar `Related ▾` dropdown (Enhancement 06). Body: `[{action_slug, active_id}, …]`; response: `[{count, lone_uuid?}]`. Dispatches `presentation.related_counts` | `src/ede/foundation/presentation/api/controllers.py` |
<!-- /SYNC-BLOCK -->

### Lifecycle hooks
<!-- SYNC-BLOCK: hooks -->
| Hook key | Behavior |
|---|---|
| _none_ | |
<!-- /SYNC-BLOCK -->

### State machine
<!-- SYNC-BLOCK: state-machine -->
_none_ — presentation does not own state; it surfaces records whose state machines live in their owning modules.
<!-- /SYNC-BLOCK -->

## 3. Configuration Introduced

> Captures every knob this module adds. Two parallel tracks — foundation `pydantic-settings` (env vars / `ede.conf`) vs `ir.config` runtime store. Empty rows are fine when the module introduces no config of that kind. Missing sections are an audit failure.

### Activation
<!-- SYNC-BLOCK: activation -->
- `ACTIVE_MODULES` (in `src/ede/foundation/settings.py`): `"presentation"` — already active. No change for the kanban Phase 1 work.
- `ACTIVE_DOMAINS`: n/a (foundation engine).
- Manifest `depends`: `["base", "workflow"]` — workflow is a soft dep (only the workflow drag path calls it) but `__manifest__.py` declares it explicitly so the boot order is deterministic.
<!-- /SYNC-BLOCK -->

### Foundation-level settings (`FoundationSettings` / env vars)
<!-- SYNC-BLOCK: foundation-settings -->
| Setting Key | Type | Default | Env Var | Purpose |
|---|---|---|---|---|
| `PRESENTATION_VIEW_VALIDATION` | `str` | `strict` | `PRESENTATION_VIEW_VALIDATION` | Load-time view referential-validator mode (Enhancement 03 — anchored on Phase 1, 🔴 Not Started). `strict` blocks `ede migrate upgrade` on any view-validation failure (missing `ir.action` for inline relational targets, field-name typos, broken `ref=` lookups). `warn` logs and continues — rollout-friendly default while existing modules clean up violations. `off` skips the validator entirely. |
<!-- /SYNC-BLOCK -->

### Runtime config (`ir.config` keys)
<!-- SYNC-BLOCK: ir-config -->
| Config Key | Scope | Type | Default | Purpose |
|---|---|---|---|---|
| _none_ | | | | |
<!-- /SYNC-BLOCK -->

### Declarative settings (XML `<settings>` panels)
<!-- SYNC-BLOCK: xml-settings -->
| Panel | File | Fields |
|---|---|---|
| _none_ | | |
<!-- /SYNC-BLOCK -->

### Seed data shipped
<!-- SYNC-BLOCK: seed-data -->
| Data file | What it seeds |
|---|---|
| _none_ — all kanban configuration is per-view DSL XML in consumer modules; this module ships no seed rows | |
<!-- /SYNC-BLOCK -->

## 4. Developer & User Notes

### Status Snapshot
<!-- SYNC-BLOCK: status-snapshot -->
| Phase | Title | Status | Roadmap |
|---|---|---|---|
| Phase 1 | KanbanView MVP — DSL + Renderer + Workflow Drag | ✅ Delivered 2026-05-11 | [phase-1-implementation.md](../roadmap/foundation/presentation/phase-1-implementation.md) |
| Phase 2 | Kanban Power Features                            | 🔴 Not Started | _scoped when Phase 1 ships_ |
| Phase 3 | Kanban At Scale + Future View Types              | 🔴 Not Started | _scoped when Phase 2 ships_ |
<!-- /SYNC-BLOCK -->

### UI Revamp (frontend2) Sub-roadmap
<!-- SYNC-BLOCK: ui-revamp-snapshot -->
The `frontend` workspace-client rewrite is tracked under [roadmap/foundation/presentation/frontend-ui-revamp/roadmap.md](../roadmap/foundation/presentation/frontend-ui-revamp/roadmap.md) and lives on branch `ui-revamp` (not yet merged to main). Per-workstream status, latest audit **2026-06-04**:

| Phase | Workstreams (status per WS) |
|---|---|
| Phase 0 — Foundation & scaffold | ✅ Delivered (2026-05-29) |
| Phase 1 — Runtime kernel        | ✅ Delivered (2026-05-29) |
| Phase 2 — Workspace chrome      | ✅ Delivered (2026-05-29) |
| Phase 3 — Action engine & multi-record views | 3.1 🟡 · 3.2 ✅ · 3.3 ✅ · 3.4 ✅ · 3.5 🟡 · 3.6 ✅ · 3.7 ✅ · 3.8 ✅ |
| Phase 4 — FormView & record drill-down       | 4.1 ✅ · 4.2 ✅ · 4.3 ✅ · 4.4 ✅ · 4.5 ✅ · 4.6 🔴 · 4.7 ✅ |
| Phase 5 — Notifications, palette, assistant, reports | 5.1 ✅ · 5.2 ✅ · 5.3 ✅ · 5.4 🟡 · 5.5 ✅ · 5.6 ✅ · 5.7 🟡 |
| Phase 6 — Parity completion & cutover        | 6.1 🟡 · 6.2 🔴 · 6.3 ✅ · 6.4 🔴 · 6.5 🟡 · 6.6 🔴 · 6.7 ✅ · 6.8 ✅ |

Latest push: **Phase 4 🔴 trio closeout — WS 4.1 + 4.2 + 4.3 flipped 🔴 → ✅ (2026-06-04)** — the foundational form-side trio shipped end-to-end. **WS 4.1 `RecordProvider` + `useFormState`** carries the draft / dirty / save state machine with dirty-filtered UPDATE payloads + workflow-field stripping; `key={recordRef ?? "new"}` remounts the provider on URL change so route-scoped state is implicit (no race refs); `initialIsEditing` lands on first render to skip a post-mount toggle flicker; **invisible-aware required validation** (new `formElements` option + `collectInvisibleFieldNames` helper) skips fields hidden via `invisible="..."` so back-refs declared `invisible="1"` don't block save. **WS 4.2 `FormView` + `FormSheet`** walks the parsed sheet recursively — sections, notebook tabs, colspan/nolabel/required, per-section + form-wide label alignment, `record_title` hoisted into the title row, DynamicProperties placeholder. **WS 4.3 edit field widgets**: per-type edit-mode editors (char / text / integer / decimal / date / datetime / enum / boolean); `ReferenceCombobox` (M2O via cmdk + Popover with `displayFormat` + `domainTemplate` substitution including `$active_record.<field>`); `M2MCombobox` (multi-select cmdk + chip rail); **`RelationListView` for O2M / M2M with the full tuple protocol** — embedded inline-list (inline `<ListView>` declaration OR `loadModelView(target, "list")` fallback OR synthetic 2-col `[record_uuid, display_name]`), Radix-Dialog popup form for Add Line / Edit Line with its own RecordProvider + bufferer wiring, baseline ids ref so untouched rows survive the `value → tuple` shape flip after save, parent-context auto-fill of back-references on Add Line (even when the parent itself is a new unsaved record — backend nested-tuple handler injects the real FK at save time). **Enhancement 07 (model-keyed read/search)** shipped: `presentation.read_record_by_model` + `search_records_by_model` commands + `/api/web/_model/{model_key}/records/...` routes so M2O cells in embedded popups render `display_name` instead of raw uuid. **Nested-tuple persistence** fixed in the ORM relational engine — `_process_o2m` CREATE/UPDATE branches now split nested O2M/M2M tuples from scalars before `repo.create`/`repo.update` (which silently dropped non-column keys) and recursively dispatch `process_relational_commands` against the just-created child; CREATE branch also applies scalar defaults (mirrors `_ede_handle_create`) so `Boolean(default=True)` columns like `active` don't end up NULL and get hidden by `apply_active_filter`. **DialogDepthContext** (overlay z-index 200+20N, content 201+20N) gives each nested popup a real backdrop blur over its parent; cmdk popovers bumped to z 9000 so M2O / enum pickers top any nesting depth; outside-click dismissal disabled on `.relation-dialog` so a click inside a nested popup doesn't collapse the parent stack. Form-view field-name walker extended to pick up `<statusbar field="..."/>` so the status field isn't filtered out of the wire payload (was: workflow stepper highlighted nothing). Boolean read render: NULL / undefined → off-toggle (was: muted em-dash). New backend tests: 8 cases in `src/tests/orm/test_relational_nested_tuples.py` covering CREATE-in-CREATE, CREATE-in-UPDATE (the wire shape from the bug report), UPDATE-in-UPDATE, DELETE-in-UPDATE, mixed-commands-in-one, defaults-applied, sanity (existing children preserved on no-tuple update), multiple-creates. 12 pytest cases for `read_record_by_model` + `search_records_by_model`. New vitest: bare-literal rowVisibility (×2), collectInvisibleFieldNames (×5), validateRequired with invisibleNames (×2), buildAddLineDefaults no-uuid path (×2), buildSaveValues shape changes. **240/240 vitest green**; relational + presentation pytest suites green. Phase 4 banner now reads `4.1 ✅ · 4.2 ✅ · 4.3 ✅ · 4.4 ✅ · 4.5 ✅ · 4.6 🔴 · 4.7 ✅` — only **WS 4.6 FormAside (chatter)** remains for full Phase 4 ✅. Previous push: **Phase 4 🟡 trio closeout — WS 4.4 + 4.5 + 4.7 flipped 🟡 → ✅ (2026-06-02)** — five pending pieces shipped end-to-end. **WS 4.5 button attribs**: `confirm=` routes through a themed Radix Dialog ([`ConfirmDialog`](../src/frontend/src/managers/ConfirmDialog.tsx) singleton) on form header verbs, list-row buttons, and the workflow-transition approval gate (replaces `window.confirm()`); `context=` JSON threads into RUN_BUTTON's `context` slot on all dispatch sites; `special=` has a new [`core/command/specials.ts`](../src/frontend/src/core/command/specials.ts) registry with a `file_upload` built-in (native picker → base64+name+mime; user-cancel returns `null` and dispatch is skipped); `label_field=` was already shipped. Result paths: [`interpretServerAction`](../src/frontend/src/core/command/interpretServerAction.ts) recognises the **`ir.action.client`** envelope (routes to OPEN_ACTION via `action_slug`, deep-links via `params.record_id`); RUN_BUTTON post-success handler honours a **`warning`** payload (`{title?, message?}` or bare string) by surfacing it as a warning toast — handlers can return soft failures without throwing. **WS 4.4 stepper**: terminal stages (`is_terminal=true` — Lost / Cancelled / Rejected) now render dim+italic so the visual weight stays on the active path; fixed a wire-mismatch bug where the mapper was reading `is_final` while the backend ships `is_terminal` — mapper now tolerates both keys. **WS 4.4 pager**: `onFormPrev` / `onFormNext` lazy-fetch across pages — step OUT of the in-memory window, bump `offset` by `PAGE_SIZE`, and a pending-target effect dispatches `OPEN_RECORD` against first/last of the new page once it lands; `canPrev/canNext` honor cross-page navigation; `pagerVisible` survives the fetch window so the affordance doesn't flicker. **WS 4.7 closeout**: unified URL grammar `<key>/<viewMode>/[recordRef]` repeating per frame (`ListDrillFrame.recordRef?` added; M2O record drill now emits `/<modelKey>/form/<recordRef>`); drill row-click mutates the top list-drill's recordRef in place via `OPEN_RECORD` handler so the breadcrumb chain survives; drill title resolves to the drilled record's `display_name` via `recordQ` and an extra crumb shows the drilled-list's action name (e.g. `Inquiries / INQ-… / Tags / <tag display_name>`). **Phase 4 status**: 4.4 / 4.5 / 4.7 all ✅; 4.1 / 4.2 / 4.3 / 4.6 remain 🔴 (RecordProvider + form state machine, FormSheet renderer, edit field widgets, FormAside chatter — the foundational form-side trio). **Earlier**: Enhancement 06 (`Related ▾` Dropdown) ✅ Delivered full end-to-end — Mode A (action-less, parser auto-derives target_model_key) + Mode B (action-bound) + `$active_record.<field>` substitution + two-line dropdown UI + drill row-click breadcrumb preservation + unified URL grammar (see prior log). Earlier still: Enhancement 05 — Quick Meta View Popover ✅ Delivered (2026-06-02)** — `(i)` icon next to the record title in [`FormControlPanel.tsx`](../src/frontend/src/managers/FormControlPanel.tsx) opens a Radix DropdownMenu (same chrome as the OrgSwitcher menu — uppercase `METADATA` label + `--shadow-overlay` surface inherited from the shared `.rdx-menu` styles). Body is a 4-row key/value grid: Created on / by + Last modified on / by, with locale-formatted timestamps via the existing `LocaleManager` + `fmtDateTime` helper. "Never edited" UX when `updated_at_utc === created_at_utc` keeps the Last-Modified row honest. A History icon at the END of the title row carries the "View full history" affordance — disabled with a Radix Tooltip ("Audit module not yet available") until `foundation.audit` ships; when an action keyed `audit-history` appears in `boot.menus`, the icon auto-activates and dispatches `OPEN_ACTION` against it. Pure-frontend implementation (no backend touch); all four cells were already in the auto-field wire payload (`created_at_utc`, `updated_at_utc`, `created_uid`, `updated_uid`). New pure helpers [`quickMetaHelpers.ts`](../src/frontend/src/managers/quickMetaHelpers.ts) (`uidLabel`, `isNeverEdited`) with 8 vitest cases. Hidden on draft records (`recordRef === "new"`) and during the initial fetch flicker so the trigger never sits next to "New record" or an empty title. **Earlier**: Enhancement 05 + 06 scoped (2026-06-02) — two new 🔴 design records authored under `roadmap/foundation/presentation/enhancements/` ahead of implementation. **[Enhancement 05 — Quick Meta View Popover](../roadmap/foundation/presentation/enhancements/05-quick-meta-view-popover.md):** `(i)` icon next to the record title opens a Radix popover with Created on / Created by / Last modified on / Last modified by + a "View full history →" link stubbed until `foundation.audit` ships. Pure-frontend, zero backend touch (every cell is already in the auto-field payload). **[Enhancement 06 — `Related ▾` Dropdown](../roadmap/foundation/presentation/enhancements/06-related-records-dropdown.md):** new `<related>` DSL element in `<header>` accepting `<field name="..." action="module.xml_key" [icon="..."] [string="..."] [hide-empty]/>` children. Domain lives on the bespoke `ir.action` (not the field) and gets `active_id` substituted at render-time from the closest upstream RecordFrame in the URL stack. URL grammar extends with list-drill frames (`{actionSlug}/{viewMode}` — disambiguated by the second segment being a known viewMode keyword). count=1 short-circuits straight to a record drill via a new batch endpoint `POST /api/web/_related-counts` that returns `{count, lone_uuid}` in one round trip. `hide-empty="1"` default; cascade-hides the trigger when every chip is empty. Both designs are fully locked (Q1–Q4 + naming + icon resolution + counter strategy answered); implementation is queued. Earlier: **WS 4.7 RecordDrillStack minimum viable shipped (2026-06-01)** — paired with WS 4.5 so the Convert-to-Lead → drill end-to-end actually renders. [`ActionManager.tsx`](../src/frontend/src/managers/ActionManager.tsx) now detects drill frames (`stack.slice(1)`), resolves each drill record's owning action via a new client-side helper [`findActionForModel(boot, modelKey)`](../src/frontend/src/core/navigation/findActionForModel.ts) that walks `boot.menus[*].action.modelKey`, and shadows `slug`/`viewMode`/`recordRef` at the top of the manager so the existing record-load + FormView render path naturally targets the drilled record. Breadcrumb collapses older crumbs into a Radix `…` overflow-dropdown (reuses the existing `.breadcrumbs__dots` styling from `_breadcrumbs.scss`) — only the immediate parent + the current title stay pinned on screen; `…` opens a "jump backwards" menu listing every ancestor. Pointer-driven close uses `onCloseAutoFocus={(e) => e.preventDefault()}` so no focus ring lingers; keyboard tab still surfaces `:focus-visible` for a11y. Shared [`BreadcrumbAncestry`](../src/frontend/src/managers/ActionControlPanel.tsx) sub-component is exported from `ActionControlPanel` and consumed by `FormControlPanel` so multi-record + form modes stay visually identical (single source of truth). Orphan-drill fallback: when a drilled model has no menu-registered action, a clean placeholder + Back-to-root button surfaces instead of poisoning queries with the wrong slug. **Deferred to a follow-up** (WS 4.7 stays 🟡): display_name on intermediate drill frames (currently uuid-prefix labelled), true form-sheet render (still uses the dummy form from WS 4.2 placeholder), and a dedicated `/api/records/*` direct-model endpoint (current implementation routes through `useActionLoad(resolvedSlug)` instead, which works fine for any model that has at least one menu action). New vitest: [`findActionForModel.test.ts`](../src/frontend/src/core/navigation/findActionForModel.test.ts) — 6 cases (flat / nested / first-match / orphan / no-action menu entries / undefined boot). Earlier today: **WS 4.5 DSL button dispatch design signed off → 🔴 to 🟡 (2026-05-31)** — engine-resolution-with-fallback registry approved (per-`command-type` engine probed via `canHandle`; falls back to default service-API engine when the named engine can't resolve a command — so a button never breaks when a `command-type` is mis-typed or its engine is offline). Implementation in-flight: extends platform `RUN_BUTTON` payload with optional `commandType`, registers the handler in `usePlatformCommands` (closing the "RUN_BUTTON owned by later phases" note in [`usePlatformCommands.ts`](../src/frontend/src/react/runtime/usePlatformCommands.ts)), builds `interpretServerAction()` as the single canonical adapter for `ir.action.url|navigate|window` and the assorted handler-return-shape dialects, adds a `pendingTrack` middleware on the existing `CommandBus` so EVERY dispatched command (not just `RUN_BUTTON`) gets per-button disabled + inline spinner uniformly via a new `useCommandDispatch(type, payload)` hook. Wires a new `NotificationCenter` as the single SSE subscriber that fans `web.client.command.done` cross-user mutations into a new `REFETCH_RECORD` platform command (discreet "Updated by …" banner, refetch in place — toasts reserved for errors). DSL parser already accepts `command-type` attribute on `<button>` (form + list). DSL → TS mapping adds `commandType` + `mapCommandType()`. Replaces the parallel `useButtonRunner.ts` (deleted) and the per-view inline `resolveNavigateHint`/toast wrappers that were briefly added earlier today. Design plan: [/tmp/central-command-handler-plan.md](/tmp/central-command-handler-plan.md). Earlier: **WS 3.4 ListView inline-edit + per-row invisible (2026-05-31)** — closed the two remaining gaps on ListView (which was 🟡 only on those items). `WidgetDescriptor` gained an optional `renderEdit(ctx: EditCellContext)`; built-in editors shipped for char / text / integer / decimal / date / datetime / enum. ListView's `editingCell` state machine swaps `render` ↔ `renderEdit` on double-click of editable cells; Enter / blur-with-change commits via `onWriteField` (already wired to `record.update + refetch`), Esc / blur-unchanged exits. `editable="top"|"bottom"` DSL attribute now flows through `mapListPlan` → `ListView.editable` to gate the affordance. **Add-line** affordance deferred to Phase 4.3 form-state machinery. Per-row invisible: new [`rowVisibility.ts`](../src/frontend/src/widgets/rowVisibility.ts) `evalRowInvisible(expr, record)` supports bare identifier (truthy hide), `not`, `==`/`!=`/`<`/`<=`/`>`/`>=`, string/number/bool/null literals, and top-level `and`/`or`; applied to row-button rendering; unrecognised expressions fail-open (visible). 12 vitest cases lock the evaluator. WS 3.4 flips 🟡 → ✅; Phase 3 banner now reads `3.1 🟡 · 3.2 ✅ · 3.3 ✅ · 3.4 ✅ · 3.5 🟡 · 3.6 ✅ · 3.7 ✅ · 3.8 ✅` — 6/8 ✅, only 3.5 (Duplicate awaits backend) and 3.1 (Form integration awaits Phase 4) remain. Earlier today: **WS 3.7 Field plugin registry shipped (2026-05-31)** — central `WidgetRegistry` at [`src/frontend/src/widgets/`](../src/frontend/src/widgets/) with two-axis lookup (`column.widget` key → `field.type` fallback → default render). Builtin registrations cover every type from the legacy `Cell.tsx` type-switch (boolean / integer / decimal / date / datetime / enum / reference / many2many / one2many / uuid / image / binary / char / text / json) plus the previously-hardcoded `active_switch` and `avatar_title` widgets. `WidgetSlot.kind` admits `field` AND `decoration` slots so widgets can contribute multiple table cells (avatar_title → [avatar | title]). Each widget descriptor declares optional `listColWidthHint` (px) + optional `slots(column) → WidgetSlot[]`. `ListView` consults the registry once per column for slot expansion + per-cell render — no more inline `if (widget === "avatar_title")` / `if (name === "active")` branches; `Cell.tsx` reduced to a thin shim that delegates to `widgets.resolve()` so FormView / KanbanView keep rendering through the same surface. Domain modules extend by calling `widgets.registerWidget("my_widget", { ... })` from their own bootstrap. **WS 3.7 unblocks inline-edit** (WS 3.4 remaining sub-feature) since edit-mode renderers can now slot into the same registry alongside view-mode renderers. Earlier today: **Phase 3 Group A close-out (2026-05-31)** — shipped five sub-features that the morning audit flagged: **3.4a column resize** (pointer drag handle on `.list-table__resizer` + per-column width state + `sessionStorage` keyed by `(modelKey)`), **3.4b row buttons** (parser already emitted `row_buttons`; `mapListPlan` now reads them, `ListView` renders a trailing actions cell with one icon button per declared row-button, click dispatches `runtime.services.action.runButton(slug, recordUuid, command)` then invalidates the records query), **3.5b Actions ▾ Archive** (bulk `record.update {active: false}` gated on `hasActive` + custom-action injection prop scaffold; Duplicate still placeholder pending backend bulk-duplicate), **3.6b quick-create** (inline mini-form per kanban column with Enter to submit / Esc to cancel; pre-fills the active groupby axis with the column's group value + the typed name into the `name` field via new `onQuickCreate(groupValue, values)` path; KanbanView's `+` button now opens this in-place instead of dispatching the global CREATE_RECORD), **3.6c dual drag mode** (KanbanView's `onDragEnd` resolves `plan.onDrag` → `field` keeps the plain `record.update` path; `workflow` calls new [`WorkflowService`](../src/frontend/src/core/services/WorkflowService.ts) which does the 2-step `/api/workflow/available` → `/api/workflow/transition` lookup so guards/approvals/lifecycle hooks fire; "auto" conservatively resolves to `field` until field-type metadata flows through `KanbanPlan` in Phase 4.5). New `IWorkflowService` joined `ServiceRegistry` + `buildRuntime`. With dual-drag + quick-create both shipped, **WS 3.6 KanbanView flips ✅** (groupby columns + dual drag + quick-create + column fold all in). 3.4 stays 🟡 — inline-edit awaits WS 3.7 field plugin registry; 3.5 stays 🟡 — Duplicate awaits backend bulk-duplicate. Earlier today: **Phase 3 Group A sub-feature audit (2026-05-31)** — deep cross-check of 8 candidate Group-A sub-features against live source flipped WS 3.2 ✅ (ViewSwitcher lives in section nav with `is-active` highlighting + `OPEN_ACTION` dispatch — placement deliberate per `ActionControlPanel.tsx:24-25` comment; BreadcrumbBar + Pager + ViewSwitcher all in). Audit also confirmed two sub-features already shipped inside still-🟡 WS rows: **3.5a Print ▾ list-mode** (SelectionActions takes `printItems[]` from `/api/reports/_list?mode=list`, dispatches `report.run` with selected uuids + `deliverReport` — symmetric with form-mode) and **3.6a Kanban column fold** (`foldedCols: Set<string>` + `toggleFold` + `.is-collapsed` 44px strip with rotated `writing-mode: vertical-rl` title). Real remaining Group-A gaps (5): 3.4a column resize (🔴 SCSS exists, no JS), 3.4b row buttons (🟡 backend emits `row_buttons`, `mapListPlan` drops them), 3.5b Actions ▾ finish (🟡 Duplicate/Archive disabled placeholders + no manifest custom-action injection), 3.6b quick-create inline mini-form (🟡 `+` button dispatches global CREATE_RECORD with no column-value prefill), 3.6c dual drag mode (🟡 `KanbanPlan.onDrag` metadata mapped from wire but `onDragEnd`/`onDropCard` never consult it — only `record.update` field-write path exists). Earlier today: **WS 3.8 SearchPanel date-bucket group-by → ✅** (shipped end-to-end with multi-select Year › Quarter › Month › Week › Day chain). Prior: **WS 3.8 SearchPanel date-bucket group-by (🔴 Not Started, 2026-05-31)** — new Phase 3 enhancement scoped today. Adds per-chip bucket dropdown (Year / Quarter / Month / Week / Day) on `date`/`datetime` group-by chips with Month as default, mirroring established ERP UX. Wire format = Odoo-style `field:granularity` (e.g. `order_date:month`); backend `read_group` already accepts `year`/`month`/`week`/`day` ([generic_repo.py:202](../src/ede/core/adapters/persistence/sqlalchemy/generic_repo.py)) — adding `quarter` (PostgreSQL native, SQLite via `strftime` math) is the only backend gap. Frontend gets a `GroupByOption.fieldType` slot, a `parseGroupByEntry`/`buildGroupByEntry` codec, a Radix DropdownMenu trigger inside the search-panel chip, and a locale-aware `formatBucket` helper (via `LocaleManager` + `date-fns-tz`) consumed by ListView group headers + KanbanView column headers. Full design notes in [the WS 3.8 section of the sub-roadmap](../roadmap/foundation/presentation/frontend-ui-revamp/roadmap.md). Earlier this day: **WS 5.1 NotificationActionManager, WS 5.2 GeneralNotificationPane, WS 5.3 ApprovalNotificationPane → ✅** on branch (2026-05-31). All three header bells ship as sibling Radix Popovers in `MainMenuManager` (no central wrapper — the three managers are the routing). [`ApprovalNotification`](../src/frontend/src/managers/ApprovalNotification.tsx) is **always visible** (CheckSquare with numeric badge, no zero-pending gate) with quick-Approve + Open inbox via `clientComponent="approval.inbox"` action lookup; [`UserNotification`](../src/frontend/src/managers/UserNotification.tsx) wires `/api/notifications` list / unread-count / mark-read / mark-all-read / dismiss + live SSE via `useEvent("web.client.notification")`. New runtime services: `runtime.services.approval` ([`ApprovalService`](../src/frontend/src/core/services/ApprovalService.ts) — getInbox/getCase/decide/delegate + `slaBucket`/`formatDueAt` helpers) and `runtime.services.notification` ([`NotificationService`](../src/frontend/src/core/services/NotificationService.ts)) registered in `ServiceRegistry` + wired in [`buildRuntime`](../src/frontend/src/react/runtime/buildRuntime.ts). Pane SCSS at [`_notifications.scss`](../src/frontend/src/theme/components/_notifications.scss) uses semantic tokens only (no Tailwind, no hard-coded colors). **WS 5.4 ActivityNotificationPane** remains 🟡 — [`ActivityNotification`](../src/frontend/src/managers/ActivityNotification.tsx) stub ported verbatim but the pane stays empty until a cross-record `/api/chatter` "activities-for-me" endpoint exists (today only per-record `/api/chatter/{model}/{id}/activities` ships). Prior flip: **WS 5.5 Command Palette → 🟡** (commit `5a76bf5`) — pluggable engine + Cmd+K hotkey + canonical `/`-menus + `@`-chat placeholder + per-app coloured icon tiles at `src/frontend/src/command-palette/`; per-model record search still pending backend `B3` (`/api/web/search`). Other 🟡 partials of note: WS 4.4 form-toolbar chrome (Print menu live-wired to `/api/reports/_list`; verb buttons + workflow stepper await backend), WS 4.7 record-drill stack scaffold (pager wired against surrounding rows).

Backend parallel track: B1 🔴, B2 🟡 (SSE event name mismatch), B3 🔴, B4-B6 🟠 deferred.

This snapshot is regenerated on every roadmap-banner update for the UI revamp; the canonical source is the roadmap file above.
<!-- /SYNC-BLOCK -->

### Built Capabilities
> Populated only when a feature reaches ✅ Delivered in the roadmap.

<!-- SYNC-BLOCK: built -->
| Feature | Model Keys | Key Files | Roadmap Source |
|---|---|---|---|
| ListView DSL + React renderer | `presentations.presentation` | `src/ede/core/services/presentation/dsl/parser.py` (`_parse_list_view`) · `src/frontend/src/workspace/views/ListView.tsx` | predates the presentation roadmap |
| FormView DSL + React renderer | `presentations.presentation` | `src/ede/core/services/presentation/dsl/parser.py` (`_parse_form_view`) · `src/frontend/src/workspace/views/FormView.tsx` | predates the presentation roadmap |
| SearchView DSL + React renderer | `presentations.presentation` | `src/ede/core/services/presentation/dsl/parser.py` (`_parse_search_view`) | predates the presentation roadmap |
| `<DynamicProperties/>` element | `ir.model.property.definition` | `parser.py` (`_parse_form_elements`) · `src/frontend/src/workspace/views/PropertiesEditor.tsx` | [foundation/customization Phase 1](../roadmap/foundation/customization/README.md) |
| `<statusbar/>` element + `WorkflowController` HTTP routes | `ir.workflow.*` | `parser.py` Phase 2 work · workflow-routes module | [foundation/workflow Phase 2](../roadmap/foundation/workflow/phase-2-implementation.md) |
| `<KanbanView>` DSL element family + React renderer + workflow drag + legality preview + fold-to-vertical-strip + 3 first adopters | `presentations.presentation` · consumes `ir.workflow.*` for the workflow drag path · `pricing.rate.status`, `crm.lead.status`, `crm.opportunity.status` first adopters | `src/ede/core/services/presentation/dsl/parser.py` (`_parse_kanban_view`) · `src/ede/core/services/presentation/dsl/schemas/view.rng` (KanbanView grammar) · `src/ede/foundation/presentation/models/presentations.py` (`_extract_field_names_from_render_plan` kanban walker) · 19 files under `src/frontend/src/workspace/views/kanban/` · `src/frontend/src/workspace/components/action/WorkspaceActionContent.tsx` (kanban branch + `KanbanWorkspaceSection`) · 3 first-adopter view XMLs · developer guide [docs/foundation-presentation-kanban-guide.md](foundation-presentation-kanban-guide.md) | [foundation/presentation Phase 1](../roadmap/foundation/presentation/phase-1-implementation.md) ✅ 2026-05-11 |
| Enhancement 01 — dynamic field-ref domain on Reference pickers (client-side substitution of bare identifiers in `domain="..."` against current form state; missing field values drop their tuple, loose semantics; literals untouched) | n/a — pure frontend evaluator, no model changes | `src/frontend/src/workspace/views/fields/substituteDomain.ts` (pure utility) · `src/frontend/src/workspace/views/fields/ReferenceField.tsx` (calls `substituteDomain` before `searchRelated()`) · 4 sales-crm consumer forms updated (inquiry / lead / opportunity / quote) + `crm.quote.communication.recipient_contact_id` | [enhancements/01-dynamic-field-ref-domain.md](../roadmap/foundation/presentation/enhancements/01-dynamic-field-ref-domain.md) ✅ 2026-05-12 |
| Enhancement 02 — M2O templated display + multi-field name search (`@api.model(..., display_name_format="[{code}] {name}", name_search_fields=["code","name"])` decorator wiring honored end-to-end through `/related/<field>` + first-class `<field display_format="..."/>` XML override; 6 first-adopter masters opted in declaratively with no view-XML changes) | declarative on every code-bearing master — opted in: `res.currency` · `logistics.incoterm.master` · `crm.quote.stage` · `crm.lost.reason` · `logistics.commodity.master` · `logistics.document.type.master` | `src/ede/core/kernel/decorators.py` (decorator + validation + `_extract_format_keys` + `__ede_display_data_fields__` precompute) · `src/ede/core/services/presentation/display_format.py` (shared `render_with_missing` helper) · `src/ede/foundation/presentation/api/controllers.py` (multi-field OR-domain + per-row `data` payload) · `src/ede/foundation/presentation/models/presentations.py` `_resolve_reference_fields` (chip parity) · `src/ede/core/services/presentation/dsl/parser.py` (`display_format` extraction in form/list/kanban) · `src/ede/core/services/presentation/dsl/schemas/view.rng` (RelaxNG additive) · `src/frontend/src/workspace/views/fields/formatDisplay.ts` (pure helper) · `src/frontend/src/workspace/views/fields/ReferenceField.tsx` (override precedence) | [enhancements/02-m2o-display-format-and-multi-field-search.md](../roadmap/foundation/presentation/enhancements/02-m2o-display-format-and-multi-field-search.md) ✅ 2026-05-12 |
| Enhancement 04 — Predicate-Gated View Rendering (`visible-when="..."` / `hidden-when="..."` attributes on every renderable element + `@api.view_predicate(view_name=, env_method=)` decorator + dedicated safe AST evaluator + form/list/search/kanban strip walkers + admin debug command + 2 FoundationSettings + 1 RBAC permission + developer guide; 3 first-party predicates in foundation.base; 103 new pytest cases) | `foundation.presentation` registry mechanism; `foundation.base/services/predicates/config.py`/`roles.py`/`team_roles.py` first adopters | `src/ede/core/api.py` (`view_predicate` decorator) · `src/ede/core/registry.py` (predicate storage + 4 methods) · `src/ede/core/errors.py` (2 new exception types) · `src/ede/core/env.py` (`__getattr__` verb-prefixed binding) · `src/ede/core/services/presentation/dsl/schemas/view.rng` (shared `<define name="gate-attrs">` + `<ref/>` on 13 gateable elements) · `src/ede/core/services/presentation/dsl/parser.py` (`_extract_gate_attrs` + mutual-exclusion) · `src/ede/core/services/presentation/predicate_expression.py` (AST evaluator + `collect_predicate_names`) · `src/ede/core/services/presentation/strip_pass.py` (form/list/search/kanban walkers + boot validation) · `src/ede/foundation/presentation/models/presentations.py` (wired into 4 parse sites + `presentation.debug_view_strip` command) · `src/ede/foundation/presentation/data/ir.rbac.permission.csv` (`view.debug.read`) · `src/ede/foundation/settings.py` (2 new settings) · `src/ede/foundation/base/services/predicates/*.py` (3 first-party predicates) · developer guide [docs/foundation-presentation-predicate-gates.md](./foundation-presentation-predicate-gates.md) | [enhancements/04-predicate-gated-view-rendering.md](../roadmap/foundation/presentation/enhancements/04-predicate-gated-view-rendering.md) ✅ 2026-05-25 |
| Enhancement 05 — Quick Meta View Popover (`(i)` icon next to record title opens a Radix DropdownMenu with uppercase `METADATA` label + History icon at title-row end + 4-row key/value grid showing created on / by + last modified on / by; locale-formatted timestamps via existing LocaleManager + `fmtDateTime`; "Never edited" UX when `updated_at_utc === created_at_utc`; History icon currently disabled with tooltip until `foundation.audit` ships, then auto-activates on detecting an `audit-history` action in `boot.menus`; 8 vitest cases for pure helpers) | frontend2 — consumes auto-fields on every DomainModel (`created_at_utc`, `updated_at_utc`, `created_uid`, `updated_uid`); zero backend touch | `src/frontend/src/managers/QuickMetaPopover.tsx` · `src/frontend/src/managers/quickMetaHelpers.ts` (+ `.test.ts`) · `src/frontend/src/theme/components/_quick-meta.scss` (semantic tokens; `--shadow-overlay` inherited via `.rdx-menu`) · `src/frontend/src/managers/FormControlPanel.tsx` (Breadcrumb sub-component renders the `(i)` trigger when `modelKey` + `recordUuid` present) · `src/frontend/src/managers/ActionManager.tsx` (threads `record` + `modelKey` + `recordUuid` + `isNewRecord` to FormControlPanel) · `src/frontend/src/theme/components/_breadcrumbs.scss` (title row becomes inline-flex; inner `<span>` carries the ellipsis so the (i) trigger sits inline without cropping) · theme.scss imports `_quick-meta` | [enhancements/05-quick-meta-view-popover.md](../roadmap/foundation/presentation/enhancements/05-quick-meta-view-popover.md) ✅ 2026-06-02 |
| Enhancement 08 — Client-Action Lifecycle + Registry (full SDK). `ClientActionDescriptor` shape (`{key, component, paramSchema?, requires?, onOpen?}`) replaces the bare-component map; `registerClientAction` overloaded for legacy `(key, component)` + descriptor signatures. **`<ClientActionPage>` unified host** (registry lookup + missing-key fallback + param validation + Suspense + lifecycle context + dual mount via `mountedAs="page" \| "dialog"`); replaces the two parallel mount sites in ActionManager + DialogActionHost with one. **Lifecycle**: descriptor-level `onOpen` (async pre-mount setup with render gate + "Failed to open" placeholder on throw) + component-level `useClientActionLifecycle({onClose, onSave})` (dynamic per-mount state). **Dialog X / Escape / outside-click veto** — `useImperativeHandle({tryClose: () => Promise<boolean>})` exposed from the page; DialogActionHost awaits the chain before popping; themed `confirm()` on veto. **Cmd/Ctrl + S** binds to registered `onSave`. **Runtime `requires` role gate** — `/api/auth/me` ships `roles: string[]` (mirror of `principal.role_codes`); `UserIdentity.roles` flows through; `ClientActionPage` enforces AND-semantics + renders Forbidden placeholder. **Zod-typed paramSchema** — `paramSchema` accepts `"uuid"\|"uuid?"\|"any"` (legacy) OR a zod schema; merged input `{...querystring, param: "<recordRef>"}` validated through `safeParse`; parsed result handed to component as `params: ClientActionParams` (kwargs-ready). **Backend introspection at `ede migrate upgrade`** — `scripts/dump_client_action_keys.py` grep-and-emit + `services/client_action_validator.py` + warning (non-fatal) on unregistered `ir.action.client_component`. **Admin debug UI** — `developer.client_actions` action (first runtime-role-gated registration with `requires: ["ir.role.admin"]`) backed by `GET /api/web/_client-action-registry`. Settings adopts the lifecycle via `SettingsPage` dirty-veto onClose + `paneSaveRef` onSave. **Layout-XML DSL `<ClientActionView>`** — new view type with three structural slots (`<header>` toolbar atoms + `<body>` imperative refs / component mount + `<footer>` status band) + 5 element types (button reusing form-button parser, `<container ref="X"/>` imperative DOM slot for plugins, `<component/>` placeholder for the descriptor's React component, `<hint>` muted text, `<ribbon label tone? visible-when?>` status pill). Composable across modules via the existing `<extend ref="…">` SDK. Backend: new define in `view.rng` + `_parse_clientaction_view()` walker + `view_registry._VIEW_TYPE_MAP` entry + field-name walker now iterates the `body` slot. Frontend: `mapClientActionPlan` mapper + `<ClientActionLayoutRenderer>` (walks parsed plan, emits ref-callbacks via state-backed context) + `ClientActionDescriptor.layout?: {modelKey, viewId?}` opt-in (loads the plan via `loadModelView(modelKey, "clientaction")` and wraps the component in the chrome). **Glob-based discovery** — `import.meta.glob` over `src/ede/foundation/*/frontend/client-actions/*.{ts,tsx}` + `src/domains/*/*/frontend/client-actions/*.{ts,tsx}` at app boot (`discoverClientActions()` from `src/main.tsx`); Vite path aliases `@foundation/*` and `@domains/*` + `server.fs.allow`; new `registerClientActionFromGlob(descriptor, sourcePath)` raises with both source paths named on duplicate-command collision (same loud-fail discipline as `register_model`); inline / bundled registrations stay replaceable; `getClientActionSourcePath(key)` exposes the source path to the admin debug page. **`useClientActionLayoutRefs()` hook** — surfaces the layout renderer's `<container>` DOM-handle map to plugin-style components (three.js, Leaflet, AG-Grid, …) via React context; state-backed so consumers re-render when handles come / go; returns `{}` outside a LayoutRenderer subtree so the hook is safe to use unconditionally; stable-identity contract per ref-name so `useEffect(..., [refs.viewer])` fires exactly once per mount. 70 new vitest cases total across the eight slices (308 → 377). | `ir.action.client_component` (consumed); registered keys = `settings.general`, `developer.client_actions` | `src/frontend/src/managers/ClientActionRegistry.tsx` (descriptor surface + zod type guard + source-path tracking + collision detection) · `src/frontend/src/managers/ClientActionPage.tsx` (host + lifecycle context + veto chain + onOpen gate + Cmd+S + layout-XML opt-in) · `src/frontend/src/managers/ClientActionPage.test.tsx` (31 cases) · `src/frontend/src/managers/ClientActionLayoutRenderer.tsx` (+ 23 vitest) · `src/frontend/src/managers/clientActionsDiscovery.ts` (+ 7 vitest) · `src/frontend/src/managers/DialogActionHost.tsx` (tryClose wiring) · `src/frontend/src/managers/ActionManager.tsx` (swap to ClientActionPage) · `src/frontend/src/settings/SettingsPage.tsx` + `SettingsPane.tsx` (first adopter) · `src/frontend/src/developer/ClientActionsDebugPage.tsx` (admin debug) · `src/frontend/src/react/runtime/buildRuntime.ts` (`runtime.http` exposed) · `src/frontend/src/core/contracts/domain.ts` (`UserIdentity.roles`, `ClientActionRenderPlan`, `ClientActionElement` union) · `src/frontend/src/core/services/mappers.ts` (`mapClientActionPlan` + `mapClientActionElement`) · `src/frontend/src/core/services/ActionService.ts` (`loadModelView` viewType union includes `"clientaction"`) · `src/frontend/src/theme/components/_client-action-layout.scss` · `src/frontend/vite.config.ts` (path aliases + `server.fs.allow`) · `src/frontend/tsconfig.app.json` (paths + include extension) · `src/frontend/src/main.tsx` (calls `discoverClientActions()` before `buildRuntime`) · `src/ede/foundation/auth/api/me.py` (`roles` on /api/auth/me) · `src/ede/foundation/presentation/api/controllers.py` (`GET /api/web/_client-action-registry`) · `src/ede/foundation/presentation/services/client_action_validator.py` · `src/ede/foundation/presentation/data/client_action_registry.json` · `src/ede/foundation/presentation/data/developer_actions.xml` · `src/ede/core/services/presentation/dsl/parser.py` (`_parse_clientaction_view`) · `src/ede/core/services/presentation/dsl/schemas/view.rng` (`ClientActionView` define + 6 children) · `src/ede/core/services/presentation/view_registry.py` (`_VIEW_TYPE_MAP`) · `src/ede/foundation/presentation/models/presentations.py` (field-name walker includes `body`) · `src/tests/foundation/test_dsl_parser_clientaction.py` (9 pytest) · `src/ede/cli/commands/migrate.py` (validate hook) · `scripts/dump_client_action_keys.py` | [enhancements/08-client-action-lifecycle-and-registry.md](../roadmap/foundation/presentation/enhancements/08-client-action-lifecycle-and-registry.md) ✅ 2026-06-05 |
| Enhancement 06 — `Related ▾` Dropdown — kernel substrate, DSL, two authoring modes, $active_record substitution, unified URL grammar, two-line dropdown UI. **Kernel primitive `env.ref(xml_id, *, raise_if_not_found=True)`** — Odoo-style external-id resolver via `ir.data.reference`; returns single-record `RecordSet` (attribute access; direct ORM, no dispatcher hop). Raises `XmlIdNotFoundError(reason="malformed"\|"unknown"\|"dangling")`. **`<related>` DSL element** in `<header>` with `<field name=… [action=…] [icon=…] [string=…] [hide-empty]/>` children — `action` is **optional**. **Two authoring modes**: Mode A (no action) — parser auto-derives `target_model_key` + `field_label` from the field-spec on the owning model; M2O chips drill straight to the linked record from the parent's value (no backend round-trip). Mode B (action="module.xml_key") — chip queries the bespoke `ir.action` for count + path + model_key via `presentation.related_counts`. **`$active_record.<field>` substitution** in domain templates (e.g. `[["record_uuid","in","$active_record.tag_ids"]]`) — backend resolves via parent-record attribute access; frontend mirrors via `substituteActiveRecord` in [`activeIdSubstitution.ts`](../src/frontend/src/core/navigation/activeIdSubstitution.ts). **Unified URL grammar** `<key>/<viewMode>/[recordRef]` repeating per frame — ListDrillFrame carries optional `recordRef`; M2O record drill emits `/<modelKey>/form/<recordRef>` so the triple repeats uniformly. **Drill row-click preserves the chain** — `OPEN_RECORD` handler in [`usePlatformCommands.ts`](../src/frontend/src/react/runtime/usePlatformCommands.ts) mutates the top list-drill's `recordRef` in place (no setActionRecord-collapse, no extra frame). **Two-line dropdown UI**: M2O marker uses a TINTED variant (transparent bg, palette-coloured icon) with TITLE = display_name + SUBTITLE = field label; multi marker uses FILLED (palette bg + white icon) with TITLE = bold count + SUBTITLE = field label. Same `colorForModel(modelKey) → 0..10` FNV-1a hash across both; palette reuses existing `--app-color-N` tokens (no duplicate palette). Default icons: `bookmark` (M2O), `git-branch` (multi). New `--fs-2xs` (11px) token for subtitle. **Trigger button** reuses `.btn.form-toolbar__actions` chrome + accent-orange tint via `.form-toolbar__related`. **First adopters**: `sales_crm.action_crm_leads_from_inquiry` (M2O via inverse FK `inquiry_id = active_id`) and `sales_crm.action_tags_from_inquiry` (M2M via `$active_record.tag_ids` — no inverse field on `logistics.tag`). New `logistics.tag` master in `logistics.masters` (name + color + active) with M2M `tag_ids` on `crm.inquiry`; demo seeds + `<ref op="link"/>` data-loader extension for XML-authored M2M assignments. 22+ pytest cases (`test_env_ref.py`), 10 (`test_dsl_parser_related.py`); presentation test stubs updated to intercept `env.ref()`. Developer guide: [foundation-presentation-related-dropdown.md](foundation-presentation-related-dropdown.md). | `ir.data.reference` (consumed) · `ir.action` (consumed) · `logistics.tag` (new master); M2M `tag_ids` on `crm.inquiry` | `src/ede/core/env.py` (`ref`) · `src/ede/core/errors.py` (`XmlIdNotFoundError`) · `src/ede/core/services/presentation/dsl/parser.py` (`_parse_related_element` — action optional) · `src/ede/core/services/presentation/dsl/schemas/view.rng` (form-related) · `src/ede/foundation/presentation/models/presentations.py` (`related_counts` + `_substitute_active_id` with `$active_record.<field>` + `_enrich_related_chips` post-parse pass filling `target_model_key` + `field_label`) · `src/ede/foundation/presentation/api/controllers.py` (`POST /api/web/_related-counts`) · `src/ede/core/services/data_loader/{xml_parser.py,loader.py,schemas/data.rng}` (`<ref op="link\|unlink"/>` M2M syntax) · `src/frontend/src/managers/RelatedDropdown.tsx` (two-row layout + tinted/filled marker variants) · `src/frontend/src/managers/colorForModel.ts` (FNV-1a hash) · `src/frontend/src/managers/icons.ts` (`bookmark`, `git-branch`, `link-2`, `layers` registrations) · `src/frontend/src/core/navigation/{activeIdSubstitution.ts,stack.ts,types.ts}` (`substituteActiveRecord` + unified URL grammar with `ListDrillFrame.recordRef?`) · `src/frontend/src/react/runtime/usePlatformCommands.ts` (`OPEN_RECORD` in-place mutation for list-drill top frame) · `src/frontend/src/theme/components/_related.scss` (tinted + filled marker variants) · `src/frontend/src/theme/abstracts/_tokens.scss` (`--fs-2xs`) · `src/domains/logistics/masters/models/tag.py` + migration + view · `src/domains/logistics/sales_crm/data/related_actions.xml` (two adopter actions) · `src/domains/logistics/sales_crm/views/crm_inquiry_views.xml` (Mode A + Mode B chips) | [enhancements/06-related-records-dropdown.md](../roadmap/foundation/presentation/enhancements/06-related-records-dropdown.md) ✅ 2026-06-02 |
| Enhancement 10 — Record-Bound Server Actions (Actions ▾). `ir.action` gains `action_type="server"` + `bind_model_action` + `bind_model_key` (the model whose Actions ▾ shows it; `model_key` stays the wizard the dialog renders). `PresentationKernel._resolve_bound_actions(model_key)` ships the model's bound actions on the `load_action` payload; `FormControlPanel`'s Actions ▾ loads them (**standard → separator → custom**) and opens the chosen one as a dialog with the active record substituted into the action's `context` (`{"default_user_id":"active_id"}` → `useFormState` draft seed). New kernel `TransientDataModel` base class. First consumer: `foundation.base` Enh 10 admin password-reset (replaced its global menu). | `ir.action` (consumed) | `action.py` (3 fields) · `kernel/model.py` (`TransientDataModel`) · Alembic `65d7f30df7d9` · `presentations.py` (`_resolve_bound_actions`) · `src/frontend/src/managers/{ActionManager,FormControlPanel,DialogActionHost,DialogActionStack}.tsx` · `core/{contracts/domain.ts,services/mappers.ts}` · `tests/foundation/test_password_reset.py` | [enhancements/10-record-bound-server-actions.md](../roadmap/foundation/presentation/enhancements/10-record-bound-server-actions.md) ✅ 2026-06-06 |
<!-- /SYNC-BLOCK -->

### Known Gaps
> Mirrored from gap tables in the roadmap (🔴/🟠/🟡 entries).

<!-- SYNC-BLOCK: gaps -->
| Gap | Severity | Roadmap Reference |
|---|---|---|
| Phase 2 power features (intra-column reorder, `<KanbanAggregate/>` header, per-column pagination, `<KanbanCover/>`, `<KanbanMenu>`, conditional ribbons/colors) | 🔴 | _scoped when Phase 2 starts_ |
| Phase 3 at-scale + future view types (virtualisation, swimlanes, saved filters, backend fold persistence, realtime, mobile gestures) | 🔴 | _scoped when Phase 2 ships_ |
| Render-plan caching for predicate-gated views (in-process LRU + `(view_id, principal_fingerprint, settings_version)` cache key + invalidation hooks on `ir.config` / `ir.rbac.role.binding` / `res.team.role.assignment` writes + Prometheus hit-rate metric) — deferred from Enhancement 04 per design Section G "stale-browser-after-write is acceptable"; lands as a follow-up perf enhancement when profiling shows the strip cost matters at scale | 🟢 | _follow-up to [Enhancement 04](../roadmap/foundation/presentation/enhancements/04-predicate-gated-view-rendering.md), scoped when a real perf signal surfaces_ |
| Embedded `RelationListView` popup forms (WS 4.3) render Many2One fields as raw UUIDs (`789d17d1…`) instead of the linked record's `display_name`. Root cause: child rows fetched through the kernel CRUD route (`POST /api/records/{model}/search`) bypass `presentation.get_record`'s `_resolve_reference_fields` / `_compute_display_name` / `_resolve_m2m_for_record` / `_resolve_o2m_for_record` pipeline. Fix: model-keyed sibling commands + routes (`presentation.read_record_by_model`, `presentation.search_records_by_model`) reusing the existing enrichment helpers, with frontend `ActionService` + `RelationListView` swapped to consume them. | 🔴 | [Enhancement 07](../roadmap/foundation/presentation/enhancements/07-model-keyed-record-enrichment.md) |
<!-- /SYNC-BLOCK -->

### Dynamic field-ref domain on Reference pickers
<!-- HAND-AUTHORED — preserved across syncs -->

Reference field `domain="..."` attributes support bare identifiers that resolve at render time against the current form record. This unblocks parent → child cascading pickers without a server-side endpoint per filter.

**Syntax** — author the domain as a string in your view XML. Quoted strings, numbers, JSON keywords (`true` / `false` / `null`), and list literals pass through verbatim. Any bare alphanumeric token in a value position is treated as a record field name.

```xml
<!-- Static (works on every release) -->
<field name="contact_id"
       domain='[["member_type","=","individual"]]' />

<!-- Dynamic — partner → contact cascade -->
<field name="contact_id"
       domain='[["member_type","=","individual"],["parent_partner_id","=",partner_id]]' />
```

**Resolution rules** (applied client-side, before the request leaves the browser):
- A bare identifier resolves to the matching field in the current record. Reference values (`{ id, display_name }` or `{ record_uuid, display_name }`) substitute to the UUID string; scalars substitute to their JSON-encoded value.
- If the referenced field is `null`, `undefined`, or a Reference with an empty id, the **whole tuple is dropped** from the domain — the picker stays usable while showing the looser candidate set. This is intentional: "no customer selected yet → show all individuals" beats "no customer selected yet → zero results".
- Field names (the left-hand operand of every tuple) must be quoted strings — only the value position is substituted.

**Out of scope** — boolean composition (`partner_id and [...] or []`), token interpolation inside string literals (`"%{partner_id}-suffix"`), traversal on the resolved value (`partner_id.parent_id`). If a picker on a child record needs to scope against a parent record's field, denormalise the field onto the child or expose it as a computed field.

**Implementation**: [`src/frontend/src/workspace/views/fields/substituteDomain.ts`](../src/frontend/src/workspace/views/fields/substituteDomain.ts) — pure utility called from `ReferenceField.renderEditMode` on every render of the edit widget. The substituted string is shipped to `/api/web/<action>/related/<field>?domain=...` exactly as a static domain would be — the backend search endpoint is unchanged and stateless.

### M2O templated display + multi-field name search
<!-- HAND-AUTHORED — preserved across syncs -->

Code-bearing master M2O pickers (currency, incoterms, commodity, lost-reason, quote-stage, document-type, …) declare two pieces of metadata on their `@api.model(...)` and every picker in every view automatically inherits the behaviour. No view-XML edits required.

**Model declaration** — author once on the target model:

```python
@api.model(
    "res.currency",
    record_name="name",
    name_search_fields=["code", "name"],          # search hits both fields with ilike
    display_name_format="[{code}] {name}",        # render label as "[USD] US Dollar"
)
class Currency(DomainModel):
    code = fields.Char(max_length=3, required=True, ...)
    name = fields.Char(max_length=100, required=True, ...)
```

Both kwargs are validated at decoration time — every `{key}` in the format and every entry in `name_search_fields` must resolve to a declared field on the model (auto-injected fields like `record_uuid` and `dbid` count). Unknown references raise `InvalidHandler` with the model key in the message; the boot does not start with a misconfigured model.

**Per-field XML override** — the rare case where one form needs different formatting:

```xml
<!-- Form-view chip: full label "[USD] US Dollar" (model default kicks in, no override needed) -->
<field name="currency_id"/>

<!-- Compact list cell: just the code "USD" -->
<field name="currency_id" display_format="{code}"/>

<!-- Customs filing form: format with extra fields -->
<field name="incoterms_id" display_format="[{term_code}] {name} ({version_year})"/>
```

`display_format` is a first-class attribute on `<field>` (sibling to `domain`, `widget`, `string`, `optional`) in form, list, and kanban view contexts.

**Wire format** — search response from `GET /api/web/<action>/related/<field>?q=USD`:

```json
[
  { "id": "uuid-1",
    "display_name": "[USD] US Dollar",
    "data": { "code": "USD", "name": "US Dollar" } },
  ...
]
```

- `display_name` — pre-rendered server-side using the model's `display_name_format` (or falls back to `record_name` field when the format is unset).
- `data` — present iff the model declared `display_name_format`; carries the union `name_search_fields ∪ {record_name_field} ∪ format-keys`. Used by the frontend when an XML `display_format` override needs to re-render with a different shape.
- The shape stays a strict superset of the legacy `{id, display_name}` — un-opted-in models keep the old behaviour exactly.

**Frontend rendering precedence** — both the dropdown options and the selected chip use the same path:

1. XML `display_format` (interpolated against `data` via [`formatDisplay`](../src/frontend/src/workspace/views/fields/formatDisplay.ts)) — wins when both are present.
2. Server-rendered `display_name` (already encodes the model's `display_name_format`).
3. `name` — last-ditch fallback for unusual payload shapes.

The chip rendering on form load and the dropdown items in the picker share `_resolve_reference_fields` on the backend, so the format applied to the dropdown is always the same as the one applied to the chip after a selection — no parity gap.

**Search behaviour** — when the target has `name_search_fields=[...]`, the `/related/<field>` endpoint builds a polish-prefix OR-domain across the listed fields with `ilike` semantics:

| `name_search_fields` | Domain shape |
|---|---|
| (unset) | `[["name", "ilike", "%q%"]]` |
| `["code"]` | `[["code", "ilike", "%q%"]]` |
| `["code", "name"]` | `["|", ["code", "ilike", "%q%"], ["name", "ilike", "%q%"]]` |
| `["code", "name", "hs_reference"]` | `["|", "|", ["code", …], ["name", …], ["hs_reference", …]]` |

User-supplied `domain="..."` filters merge with the search domain via implicit AND — the search clauses come first, followed by the user filter.

**Out of scope** (permanent — not even a Phase 3 item):

- **Per-locale formats** (e.g. `[USD] US Dollar` vs `[USD] Dollar américain`) — i18n of code-bearing master *names* lives in `foundation.i18n`. The format string is single-locale; the `name` field's translation handling is unchanged.
- **Conditional / computed formats** (e.g. `{code|upper}`, branching on length) — keeps the format trivially regex-replaceable on both Python and TypeScript sides. Sourcing data with the right casing is the model's responsibility.
- **Server-side rendering of XML overrides** — the override always renders client-side using `data`. Pushing the override into the HTTP request would require shipping a per-request format string to the search endpoint and would defeat HTTP-cache friendliness.
- **Format strings that traverse relations** (e.g. `{country_id.code}`) — only resolves keys in the immediate row's `data` payload. Authors who need a related field surface it in `name_search_fields` or as a stored compute on the model.

**Implementation map**:
- [`src/ede/core/kernel/decorators.py`](../src/ede/core/kernel/decorators.py) — `display_name_format` kwarg, validation, `_extract_format_keys`, `__ede_display_data_fields__` precompute.
- [`src/ede/core/services/presentation/display_format.py`](../src/ede/core/services/presentation/display_format.py) — shared `render_with_missing` (used by both the controller and the kernel for parity).
- [`src/ede/foundation/presentation/api/controllers.py`](../src/ede/foundation/presentation/api/controllers.py) — `/related/<field>` multi-field search + format rendering + `data` payload.
- [`src/ede/foundation/presentation/models/presentations.py`](../src/ede/foundation/presentation/models/presentations.py) — `_resolve_reference_fields` chip rendering with same precedence.
- [`src/ede/core/services/presentation/dsl/parser.py`](../src/ede/core/services/presentation/dsl/parser.py) — `display_format` attribute extraction on `<field>` in form/list/kanban contexts.
- [`src/frontend/src/workspace/views/fields/formatDisplay.ts`](../src/frontend/src/workspace/views/fields/formatDisplay.ts) — pure helper mirroring Python `format_map` semantics.
- [`src/frontend/src/workspace/views/fields/ReferenceField.tsx`](../src/frontend/src/workspace/views/fields/ReferenceField.tsx) — `renderLabel` precedence helper used by chip + option mapper.

### Things developers commonly get wrong
<!-- HAND-AUTHORED — preserved across syncs -->
- A Reference picker with a dynamic-domain bare identifier (e.g. `parent_partner_id=partner_id`) silently drops its tuple when the referenced form field is empty — that is intentional loose semantics, not a bug. If you want strict behaviour (empty input → zero options), gate the picker behind a guard rather than relying on the domain.
- Bare identifiers in `domain="..."` are resolved against the **current record** only, not via traversal. `partner_id.parent_id` will not work — denormalise or expose a computed field on the child record instead.
- `display_name_format` references field names — if you rename a field on a model that declares the format, the decorator validation raises at boot (with the model key in the message); rename the format key in the same diff. The format is not a free-form string; it is part of the model's declared interface.
- `display_format` on a `<field>` references fields the *target* model carries in its `data` payload, not the form's own fields. The payload includes the union `name_search_fields ∪ {record_name_field} ∪ format-keys` of the *target* model — references to fields outside that set silently render as empty string and the override falls back to the server `display_name`. If you need an extra field in the format, add it to the target model's `name_search_fields`.
- Migrating a model from `name`-only to multi-field search by adding `name_search_fields=["code", "name"]` does NOT change the existing on-disk data — it only changes how the picker queries. Existing chips re-render with the new format on the next form load (no migration needed).

### Migration / upgrade notes
<!-- SYNC-BLOCK: migration -->
- Phase 1 ships no Alembic migrations on its own — consumer modules adopting kanban add no schema changes for the MVP. Phase 2 introduces a `sequence Integer` field convention on opt-in models, requiring per-consumer Alembic migrations at that time.
<!-- /SYNC-BLOCK -->

### Permissions / RBAC
<!-- SYNC-BLOCK: rbac -->
| Role | Permissions |
|---|---|
| _none_ — kanban inherits the model's existing read/write/create/delete permissions; no kanban-specific RBAC keys | |
<!-- /SYNC-BLOCK -->

### Related modules
<!-- SYNC-BLOCK: related -->
- [Kanban Authoring Guide](foundation-presentation-kanban-guide.md) — **developer-facing** how-to for adding a kanban view to a model (DSL tag reference, drag modes, gotchas, file layout)
- [M2O Display & Search Guide](foundation-presentation-m2o-display-guide.md) — **developer-facing** how-to for `name_search_fields` + `display_name_format` (model-level default + per-`<field>` XML override) for code-bearing masters
- [`foundation.workflow`](foundation-workflow.md) — workflow drag path; `<statusbar/>` precedent for a workflow-aware DSL element
- [`foundation.customization`](foundation-customization.md) — `<DynamicProperties/>` precedent for adding new DSL elements
- [Foundation Apps overview](11-foundation-apps.md) — legacy hand-authored guide (mentions the presentation app at a high level)
<!-- /SYNC-BLOCK -->

---

*Last sync: 2026-06-06 (UI revamp Phase 5 banner re-sync — **WS 5.6 AIAssistantManager ✅ Delivered (user-confirmed browser smoke 2026-06-06)** + **WS 5.5 record search ✅ Delivered** (previously 🟡 in this doc but already flipped in roadmap.md when omni-search PC-10 shipped 2026-06-06). WS 5.6 implementation: the 4-slice port shipped 2026-06-04 (commits `45ae5ec` → `320fade` covering substrate + SSE streaming, bindings + view-intent dispatch, action buttons + slash palette + history popover, markdown + insights + nav guard + sparkles badge; 308 → 347 vitest cases) plus framework-level back-ref auto-skip + post-save record refetch (`f2f4d89`). Sync triggered by the legacy-frontend deprecation track ([`legacy-deprecation.md`](../roadmap/foundation/presentation/frontend-ui-revamp/legacy-deprecation.md) A5). Previous sync: 2026-06-05 (foundation.presentation enhancements — **Enhancement 08 (Client-Action Lifecycle + Registry) ✅ Delivered — full SDK in 8 slices: descriptor surface + lifecycle (onOpen/onClose/onSave) + dual mount (page/dialog) + dialog X/Escape veto via imperative tryClose + zod paramSchema + runtime `requires` role gate + backend introspection + admin debug UI + layout-XML DSL `<ClientActionView>` + glob-based discovery (foundation + domain trees with loud-fail duplicate-command detection) + `useClientActionLayoutRefs()` imperative-DOM-handle hook for plugin-style components**. Three commits added today (`7bcb650` layout-XML substrate, `9300b06` glob discovery, `3d098ec` refs hook); 308 → 377 vitest cases (+69 total across the eight slices) + 9 backend pytest on the parser. Adds: `view.rng` ClientActionView define + 6 children · `parser.py _parse_clientaction_view()` + dispatch · `view_registry._VIEW_TYPE_MAP["ClientActionView"]="clientaction"` · `presentations.py` field-name walker iterates `body` slot · `mapClientActionPlan` mapper + `<ClientActionLayoutRenderer>` (state-backed context for refs, stable-identity ref callbacks per ref-name via `_LayoutContainer` subcomponent so React doesn't infinite-loop on re-render) · `ClientActionDescriptor.layout?: {modelKey, viewId?}` opt-in (host fetches plan via `loadModelView(..., "clientaction")` + wraps the React component in the chrome) · `useClientActionLayoutRefs()` returns the live `<container ref="X"/>` DOM-handle map, empty when called outside a LayoutRenderer subtree so it's safe to use unconditionally · `clientActionsDiscovery.ts` two-glob discovery (foundation `*/frontend/client-actions/*.{ts,tsx}` + domain `*/*/frontend/client-actions/*.{ts,tsx}`) called from `main.tsx` before `buildRuntime` · `registerClientActionFromGlob(descriptor, sourcePath)` raises on duplicate-command with BOTH paths named (same loud-fail discipline as `register_model`) · `getClientActionSourcePath(key)` for the admin debug page · Vite path aliases `@foundation/*` + `@domains/*` + `server.fs.allow` for sibling source trees · tsconfig `paths` + extended `include`. Status snapshot row + Built Capabilities entry both flipped ✅; previous "Layout-XML DSL pending" Known Gaps row removed. Earlier today (same session, prior sync record): **Enhancement 08 SDK initial-shipment 🟡 In Progress** — The SDK that shipped (5 commits on `ui-revamp`): `ClientActionDescriptor` ({key, component, paramSchema?, requires?, onOpen?}) replaces the bare-component map + overloaded `registerClientAction` for legacy `(key, component)` + descriptor signatures; unified `<ClientActionPage>` host (registry lookup + missing-key fallback + param validation + Suspense + lifecycle context + dual mount via `mountedAs="page" | "dialog"`); ActionManager + DialogActionHost both swapped to this single host. **Lifecycle** = descriptor-level `onOpen` async pre-mount gate (render held on Loading until resolves; "Failed to open" placeholder on throw) + component-level `useClientActionLifecycle({onClose, onSave})` for per-mount state. **Dialog X / Escape / outside-click veto** — `useImperativeHandle({tryClose: () => Promise<boolean>})` from the page; DialogActionHost awaits the chain before popping; themed `confirm()` on veto. **Cmd/Ctrl + S** binds to registered onSave. **Runtime `requires` role gate** — `/api/auth/me` ships `roles: string[]` (mirror of `principal.role_codes`); `UserIdentity.roles` flows through `mapUserIdentity`; `ClientActionPage` checks `descriptor.requires` (AND-semantics) against `useSession().roles` → Forbidden placeholder when any role missing. **Zod-typed paramSchema** — `paramSchema` accepts `"uuid" | "uuid?" | "any"` (legacy) OR a zod schema (`ZodTypeAny`); merged input `{...querystring, param: "<recordRef>"}` validated through `safeParse`; parsed result handed to component as `params: ClientActionParams` (kwargs-ready). **Backend introspection at `ede migrate upgrade`** — `scripts/dump_client_action_keys.py` (regex-scans frontend2 source for `registerClientAction(...)` patterns + strips comments / template variables / test files), `src/ede/foundation/presentation/data/client_action_registry.json` source-of-truth, `services/client_action_validator.py` (validate + validate_and_log) + warning (non-fatal) on unregistered `ir.action.client_component`. **Admin debug UI** — `developer.client_actions` (first runtime-role-gated registration with `requires: ["ir.role.admin"]`) backed by `GET /api/web/_client-action-registry`; `Runtime.http` now exposed for ad-hoc consumers. **First adopter**: SettingsPage registers `onClose` dirty-veto + `onSave` via `paneSaveRef`. **Tests**: 308 → 347 vitest (+39 new — registry contract, ClientActionPage lifecycle, zod validation, role gate, dialog tryClose). **Still pending under Enhancement 08**: (1) layout-XML DSL (`<view type="clientaction">`) for declarative chrome + cross-module `<extend ref="…">` patches; (2) glob-based discovery across `src/ede/foundation/**/frontend/client-actions/` + `src/domains/**/frontend/client-actions/` (today's pattern is explicit `registerClientAction` calls in the bundled `ClientActionRegistry.tsx`); (3) Equipment 3D viewer port — the first-adopter case that will drive (1) + (2) forward; (4) `onResolve` / `onMount` / `onRefresh` separation with imperative refs (the simpler `onOpen` + `onClose` + `onSave` model covers every current consumer). Implementation will land in a future session when 2+ external adopters need the layout-XML composition path. Frontend-ui-revamp Phase 6 banner flips `6.1 🔴` → `6.1 🟡` to reflect the SDK partial. Previously: 2026-06-04 (foundation.presentation enhancements — **Enhancement 08 (Client-Action Lifecycle + Registry, with Equipment 3D Viewer as first adopter) 🔴 Not Started** authored at [`roadmap/foundation/presentation/enhancements/08-client-action-lifecycle-and-registry.md`](../roadmap/foundation/presentation/enhancements/08-client-action-lifecycle-and-registry.md). Closes WS 6.1's "Client-action registry parity" workstream and unblocks the platform-side delivery path for foundation.assistant / foundation.audit / foundation.storage / foundation.approval / foundation.communication + any future domain client action. **Pattern**: `ClientActionPlugin` lifecycle (onResolve → onMount → onCommand / onRefresh → onClose) + new `<view type="clientaction">` layout XML registered through the existing presentation view-registry (RNG schema gains `ClientAction` element with `<header>` / `<body>` / `<footer>` structural slots; `<container ref="...">` exposes platform-rendered DOM regions to plugins via `refs` in onMount; `<button name="...">` clicks route through `onCommand(name, ctx)`; `<extend ref="...">` lets other modules patch the layout cross-module). **Dual mount surfaces**, same plugin contract: (a) menu-attached `ir.action.client` rows route via TanStack to a `<ClientActionPage>` host (URL-pushed, refresh-safe, deep-linkable); (b) `<button command="open_X"/>` dispatches route to a `<ClientActionDialogHost>` Radix portal (single-instance per command, dispatching again swaps target on the existing host, no URL change). **Discovery**: Vite `import.meta.glob([foundation/**/frontend/client-actions, domains/**/frontend/client-actions])` at boot — TWO trees, ONE registry. Duplicate-command registration fails boot loudly with both source paths in the error (same discipline as `Registry.register_model`'s duplicate-key check). **First adopter**: equipment 3D viewer port — 6 legacy `src/frontend/src/workspace/views/equipment3d/` files → `src/domains/logistics/equipment_operations/frontend/{client-actions,components}/`; new `equipment_3d_layout.xml`; three.js + `@react-three/fiber` + `@react-three/drei` lazy-loaded via `React.lazy()` so the ~500KB chunk only fetches when the dialog actually opens (zero impact on first paint). **No backend change** for the first adopter — the existing `<button command="open_equipment_3d_viewer"/>` in `equipment_render_ext.xml` already dispatches the right command. Tests planned: 38 cases (28 vitest covering registry contract + lifecycle ordering + layout renderer + discovery; 10 pytest covering new `clientaction` DSL parser + RNG + HTTP integration via `loadModelView`). New developer guide planned at `docs/foundation-presentation-client-actions.md`. Implementation deferred to a dedicated session. Known Gaps gains the matching 🔴 row. Previous sync: 2026-06-04 (UI revamp **Phase 4 🔴 trio closeout — WS 4.1 + 4.2 + 4.3 flipped 🔴 → ✅**. Phase 4 banner now `4.1 ✅ · 4.2 ✅ · 4.3 ✅ · 4.4 ✅ · 4.5 ✅ · 4.6 🔴 · 4.7 ✅` — only **WS 4.6 FormAside (chatter)** remains for full Phase 4 ✅. The trio shipped the foundational form-side stack end-to-end: WS 4.1 `RecordProvider` + `useFormState` (draft / dirty / save state machine with dirty-filtered UPDATEs + workflow-field stripping + route-scoped lifetime via `key={recordRef ?? "new"}` + `initialIsEditing` first-render guarantee + invisible-aware required validation via new `formElements` option + `collectInvisibleFieldNames` helper); WS 4.2 `FormView` + `FormSheet` (recursive section / notebook walker, colspan / nolabel / required / `invisible` semantics, `record_title` hoist, per-section + form-wide label alignment, DynamicProperties placeholder); WS 4.3 edit field widgets (per-type editors for char / text / integer / decimal / date / datetime / enum / boolean, `ReferenceCombobox` with `displayFormat` + `domainTemplate` substitution including `$active_record.<field>`, `M2MCombobox` chip rail, `RelationListView` with full O2M / M2M tuple protocol — embedded inline-list resolution chain (inline `<ListView>` → `loadModelView(target, "list")` → synthetic 2-col fallback), Radix Dialog popup-form with its own RecordProvider + bufferer wiring, **baseline ids ref** so untouched rows survive the value→tuple shape flip after save, parent-context auto-fill of back-references on Add Line even when the parent itself is a new unsaved record). Backend siblings shipped: **Enhancement 07** model-keyed read / search (`presentation.read_record_by_model` + `search_records_by_model` + `/api/web/_model/{model_key}/records/...` routes — reuse the four enrichment helpers verbatim); **nested-tuple persistence** in the ORM relational engine — `_process_o2m` CREATE / UPDATE branches now `_split_target_values` to separate nested O2M / M2M tuples from scalars (`repo.create` / `repo.update` silently dropped non-column keys), recursively dispatch `process_relational_commands` against the just-created / -updated child, and CREATE branch applies scalar field defaults (mirrors `_ede_handle_create`) so `Boolean(default=True)` columns like `active` don't end up NULL and get hidden by `apply_active_filter`. Nested popup chrome: **DialogDepthContext** lifts each nested popup's overlay + content z-index by +20 per level so each nesting tier has a real backdrop blur over its parent; cmdk popovers bumped to z 9000 to top any nesting depth; `onPointerDownOutside` + `onInteractOutside` suppressed on `.relation-dialog` so a click inside a nested popup doesn't collapse the parent stack. Form-view field-name walker extended to pick up `<statusbar field="..."/>` so the status field isn't filtered out of the wire payload (was: workflow stepper highlighted nothing). Boolean read render: NULL / undefined → off-toggle (was: muted em-dash). Domain views: `crm.quote.line` / `route` / `package` / `communication` back-ref `quote_version_id` marked `invisible="1"` — field stays in the model (required FK), nested-tuple handler still wires it, popup no longer shows a redundant pick-parent affordance. New pytest: 8 cases for nested-tuple semantics (CREATE-in-CREATE, CREATE-in-UPDATE, UPDATE-in-UPDATE, DELETE-in-UPDATE, mixed-commands-in-one, defaults-applied, sanity, multiple-creates) + 12 cases for `read_record_by_model` / `search_records_by_model`. New vitest: bare-literal rowVisibility (×2), `collectInvisibleFieldNames` (×5), `validateRequired` with invisibleNames (×2), `buildAddLineDefaults` no-uuid path (×2), `buildSaveValues` shape changes. **240/240 vitest green**; relational + presentation pytest suites green. Previous sync: 2026-06-04 (foundation.presentation enhancements — **Enhancement 07 (Model-keyed Record Read / Search with Presentation Enrichment) 🔴 Not Started** authored at [`roadmap/foundation/presentation/enhancements/07-model-keyed-record-enrichment.md`](../roadmap/foundation/presentation/enhancements/07-model-keyed-record-enrichment.md). Surfaces the gap discovered during WS 4.3 smoke-testing: the embedded `RelationListView` popup form (Add Line / Edit row on an O2M / M2M field) renders Many2One fields as raw UUIDs (`789d17d1…`) instead of the linked record's `display_name`, because child rows are fetched through the kernel CRUD route (`POST /api/records/{model}/search` → `ede.search`) which bypasses `presentation.get_record`'s enrichment pipeline (`_compute_display_name` / `_resolve_reference_fields` / `_resolve_m2m_for_record` / `_resolve_o2m_for_record`). Fix design: two new model-keyed sibling commands + HTTP routes (`presentation.read_record_by_model` → `GET /api/web/_model/{model_key}/records/{record_id}` and `presentation.search_records_by_model` → `POST /api/web/_model/{model_key}/records/search`) that reuse the four enrichment helpers verbatim — no logic duplication; route namespace mirrors the `/_model/{model_key}/views/{view_type}` introduced in WS 4.3 for `load_model_view`. Frontend wiring: two new methods on `IActionService` (`readRecordByModel` / `searchRecordsByModel`) + RelationListView call-site swaps (table fetch + popup record open). Acceptance: M2O fields in embedded popups read as `display_name`, no regression on action-keyed paths. Tests planned: 2 new pytest files (read + search by model) + vitest cases on ActionService HTTP shape + RelationListView popup label rendering. Known Gaps gains a new 🔴 row pointing at the enhancement. Previous sync: 2026-06-02 (UI revamp **Phase 4 🟡 trio closeout** — WS 4.4 + 4.5 + 4.7 all flipped 🟡 → ✅. **WS 4.4**: workflow-stepper terminal stages render dim+italic (mapper wire-mismatch fix `is_final` → `is_terminal`); form pager lazy-fetches across pages via offset bump + pending-target effect (`canPrev/canNext` honor cross-page). **WS 4.5**: full button-attribute coverage — `confirm=` via themed `ConfirmDialog`, `context=` JSON on all three dispatch sites (form / list-row / workflow transitions), `special=` registry with `file_upload` built-in (ports legacy `FileUploadSpecial`), `label_field=` already shipped; result paths gain `ir.action.client` envelope + handler-attached `warning` payload toast variant. **WS 4.7**: unified URL grammar `<key>/<viewMode>/[recordRef]` repeating per frame — `ListDrillFrame.recordRef?` added so a row opened inside a drilled list updates THAT frame's recordRef in place (no setActionRecord-collapse); M2O drill emits `/<modelKey>/form/<recordRef>` so the triple repeats uniformly; drill title resolves to the drilled record's `display_name` (was action name) and an extra breadcrumb crumb pushes the drilled-list's action name (chain reads `Inquiries / INQ-… / Tags / <tag display_name>`); `OPEN_RECORD` handler in `usePlatformCommands` mutates the top list-drill's recordRef in place so the breadcrumb survives row clicks. Phase 4 banner now `4.1 🔴 · 4.2 🔴 · 4.3 🔴 · 4.4 ✅ · 4.5 ✅ · 4.6 🔴 · 4.7 ✅` — the 🔴 trio (RecordProvider+useFormState → FormSheet → edit field widgets) + 4.6 FormAside remain as the foundational form-side work before cutover. Previous sync earlier the same day: foundation.presentation enhancements — **Enhancement 06 (`Related ▾` Dropdown) ✅ Delivered — full end-to-end** — completes the design with Mode A (action-less, parser auto-derives `target_model_key` + `field_label` from the field-spec; M2O drills from parent's value with no backend round-trip) alongside Mode B (action-bound, reload-safe via action `path` slug). New `$active_record.<field>` substitution in domain templates ([presentations.py](../src/ede/foundation/presentation/models/presentations.py) `_substitute_active_id` + frontend mirror `substituteActiveRecord` in [activeIdSubstitution.ts](../src/frontend/src/core/navigation/activeIdSubstitution.ts)) — M2M chips work without an inverse field on the target (e.g. `logistics.tag` ← `crm.inquiry`). Backend reads parent's referenced fields via attribute access so M2M expands through `_get_many2many`; `_coerce_for_domain` mirror normalises ref shapes to UUIDs. Unified URL grammar `<key>/<viewMode>/[recordRef]` repeating per frame — `ListDrillFrame.recordRef?` added; M2O record drill now emits `/<modelKey>/form/<recordRef>` so the triple shape repeats uniformly. `OPEN_RECORD` mutates top list-drill's recordRef in place (no setActionRecord-collapse to root, no extra record frame pushed) so the breadcrumb chain survives row clicks inside drilled lists. Two-line dropdown UI: M2O marker uses TINTED variant (transparent bg, palette-coloured icon, default `bookmark`); multi marker uses FILLED variant (palette bg + white icon, default `git-branch`). Both share the same 22×22 footprint; same `colorForModel(modelKey) → 0..10` FNV-1a hash; reuses `--app-color-0..10` tokens (no duplicate palette). Title = display_name (M2O) or bare bold count (multi); subtitle = `chip.string ?? field_label` (uppercase, muted, new `--fs-2xs` 11px token). `load_action` post-parse pass walks every `<related>` block and fills `target_model_key` + `field_label` from the owning model's field-spec. New `<ref op="link\|unlink"/>` data-loader extension for XML-authored M2M assignments (RNG + xml_parser + loader; default op="link"; emits Odoo-style RelationalCommand tuples). First adopters: `sales_crm.action_crm_leads_from_inquiry` (M2O via inverse FK `inquiry_id = active_id`) and `sales_crm.action_tags_from_inquiry` (M2M via `$active_record.tag_ids`). New `logistics.tag` master + M2M `tag_ids` on `crm.inquiry`; 6 demo tags seeded; 3 demo inquiries pre-tagged. Built Capabilities row expanded; the unified-URL grammar and `$active_record` substitution become first-class doc surface. Previous sync earlier the same day: foundation.presentation enhancements — **Enhancement 06 (`Related ▾` Dropdown) ✅ Delivered — kernel + DSL substrate** — new kernel primitive `env.ref(xml_id, *, raise_if_not_found=True)` ([env.py](../src/ede/core/env.py)) as the canonical Odoo-style external-id resolver backed by `ir.data.reference`; returns single-record `RecordSet` with attribute access (no `.read()` round-trip); direct ORM read, no dispatcher hop; raises new `XmlIdNotFoundError(reason="malformed"|"unknown"|"dangling")` ([errors.py](../src/ede/core/errors.py)). 4 PresentationKernel consumers (`load_action`, `action_records`, `get_record`, `related_counts`) migrated from path-slug `ede.search` on `ir.action` to direct `env.ref(action_slug)` + attribute access — removes the per-caller `_resolve_action_by_key` wrapper entirely. New `<related>` DSL element ([parser.py](../src/ede/core/services/presentation/dsl/parser.py)) accepted as a child of `<header>` with `<field name="…" action="module.xml_key" [icon=…] [string=…] [hide-empty="0|1"]/>` children; RelaxNG ([view.rng](../src/ede/core/services/presentation/dsl/schemas/view.rng)) gains a `form-related` define + `<ref/>` in the `form-header` choice. New `presentation.related_counts` command + `POST /api/web/_related-counts` batch endpoint with `_substitute_active_id` template substitution and count=1 short-circuit so the form-toolbar chip can drill straight to a record. First adopter: `sales_crm.action_crm_leads_from_inquiry` registered in [`related_actions.xml`](../src/domains/logistics/sales_crm/data/related_actions.xml) + `<related>` block in the inquiry form ([crm_inquiry_views.xml](../src/domains/logistics/sales_crm/views/crm_inquiry_views.xml)). 22 new pytest cases (`test_env_ref.py` — happy path, nested-dot xml_keys, malformed input, dangling refs, raise/None semantics, exception shape); 10 new pytest cases (`test_dsl_parser_related.py`). Presentation test stubs (`test_presentation_record_api.py`, `test_presentation_search.py`, `test_registry_sync.py`) updated to intercept `env.ref()` directly via new `_StubRecordSet` + `_make_ref_stub` helpers — production logic was preserved, only test infrastructure adapted. **Worktree full suite: 3539 passed, 1 skipped + 620 vitest passed.** Built Capabilities gains Enhancement 06 row; Known Gaps loses the matching 🔴 entry; Commands, Services, REST endpoints SYNC-BLOCKs all updated. Earlier: foundation.presentation enhancements — **Enhancement 05 (Quick Meta View Popover) ✅ Delivered** — Radix DropdownMenu surface with uppercase `METADATA` label + History icon at the title-row end + 4-row key/value grid; locale-formatted timestamps via LocaleManager + `fmtDateTime`; History icon disabled with tooltip until `foundation.audit` ships, then auto-activates on detecting an `audit-history` action in `boot.menus`. Pure-frontend (no backend touch); 8 vitest cases for pure helpers. Moved from Known Gaps to Built Capabilities. Earlier: foundation.presentation enhancements — **Enhancement 05 + Enhancement 06 (`Related ▾` Dropdown) scoped** — both at 🔴 Not Started with fully-locked designs. Known Gaps gains two new rows pointing at the enhancement files; `ui-revamp-snapshot` Latest-push prose captures the design highlights (DSL grammar, URL grammar extension, backend `_related-counts` endpoint, count=1 short-circuit, `active_id` resolution from the closest upstream RecordFrame). Implementation deferred to a dedicated session per enhancement. Earlier: UI revamp sub-roadmap — **WS 4.7 RecordDrillStack minimum viable shipped** — paired with WS 4.5 so Convert-to-Lead drill end-to-end actually renders. New client-side helper `findActionForModel(boot, modelKey)` walks `boot.menus[*].action.modelKey` to resolve a drill record's owning action; ActionManager shadows `slug`/`viewMode`/`recordRef` so the existing FormView render path naturally targets the drilled record; breadcrumb chain collapses older crumbs into a Radix `…` overflow-dropdown via a new shared `BreadcrumbAncestry` component used by BOTH ActionControlPanel and FormControlPanel (single source of truth across multi-record + form modes). Pointer-close prevents focus-ring linger (`onCloseAutoFocus={(e) => e.preventDefault()}`); `:focus-visible` ring preserved for keyboard a11y. Orphan-drill fallback (no menu action for the drilled model) renders a clean placeholder + Back. Deferred to a follow-up: intermediate-frame display_names (currently uuid-prefix), true form-sheet render (WS 4.2 dummy form), dedicated `/api/records/*` direct-model endpoint. New vitest cases at `src/frontend/src/core/navigation/findActionForModel.test.ts` — 6 cases. WS 4.7 stays 🟡 with notes. Earlier: UI revamp sub-roadmap — **WS 4.5 DSL button dispatch → 🟡** — design plan signed off at [/tmp/central-command-handler-plan.md](/tmp/central-command-handler-plan.md). Engine-resolution-with-fallback registry; per-`command-type` engine probed via `canHandle`, falls back to default service-API engine if the named engine can't handle the dispatch — so a typo'd `command-type` or an offline engine degrades gracefully to the regular model-command path. Implementation in-flight: extends platform `RUN_BUTTON` payload with optional `commandType`, registers the handler in `usePlatformCommands`, builds `interpretServerAction()` as the single canonical adapter for `ir.action.url|navigate|window` and assorted handler-return-shape dialects, adds `pendingTrack` middleware so every dispatched command gets per-button disabled + inline spinner via a new `useCommandDispatch` hook. Wires a new `NotificationCenter` as the single SSE subscriber that fans `web.client.command.done` cross-user mutations into a new `REFETCH_RECORD` platform command (discreet "Updated by …" banner + refetch in place; toasts reserved for errors). DSL parser already accepts `command-type` attribute on `<button>` (form + list); TS mapper picks it up via `mapCommandType()`. Replaces the parallel `useButtonRunner.ts` (deleted) and the per-view inline `resolveNavigateHint`/toast wrappers that were briefly added earlier today. Phase 4 banner now `4.1 🔴 · 4.2 🔴 · 4.3 🔴 · 4.4 🟡 · 4.5 🟡 · 4.6 🔴 · 4.7 🟡`. Earlier: UI revamp sub-roadmap — **WS 3.4 ListView → ✅** (inline-edit double-click → `renderEdit` cell editors for char/text/integer/decimal/date/datetime/enum + Enter/Esc/blur commit/cancel semantics; new `evalRowInvisible` per-row predicate evaluator for row buttons with 12 vitest cases; `editable` DSL attribute plumbed end-to-end; add-line deferred to Phase 4.3). Phase 3 banner now `3.1 🟡 · 3.2 ✅ · 3.3 ✅ · 3.4 ✅ · 3.5 🟡 · 3.6 ✅ · 3.7 ✅ · 3.8 ✅` — 6/8 ✅, only 3.5 Duplicate (backend) and 3.1 (Phase 4 form integration) remain. Earlier today: UI revamp sub-roadmap — **WS 3.7 Field plugin registry → ✅**: central `WidgetRegistry` (`src/frontend/src/widgets/registry.tsx`, `types.ts`, `builtins.tsx`) with two-axis lookup, slot-expansion for multi-cell widgets (decoration kind), builtin registrations migrated from the old `Cell.tsx` switch + the inline `active_switch` / `avatar_title` hardcodes. `ListView` consults the registry per column; `Cell.tsx` reduced to a delegating shim so FormView / KanbanView continue working unchanged. Domain extension path: `widgets.registerWidget(key, descriptor)`. Banner now `3.1 🟡 · 3.2 ✅ · 3.3 ✅ · 3.4 🟡 · 3.5 🟡 · 3.6 ✅ · 3.7 ✅ · 3.8 ✅`. WS 3.4 stays 🟡 only because inline-edit is its own deliverable on top of the registry. Earlier today: UI revamp sub-roadmap — Phase 3 Group A close-out: shipped 5 sub-features (3.4a column resize, 3.4b row buttons, 3.5b Archive + custom-action prop, 3.6b quick-create inline mini-form, 3.6c dual drag mode with new `WorkflowService`); WS 3.6 KanbanView flips ✅; WS 3.4 + 3.5 stay 🟡 on remaining external dependencies (field plugin registry; backend bulk-duplicate). Banner now `3.1 🟡 · 3.2 ✅ · 3.3 ✅ · 3.4 🟡 · 3.5 🟡 · 3.6 ✅ · 3.7 🔴 · 3.8 ✅`. Earlier today: UI revamp sub-roadmap — Phase 3 Group A sub-feature audit: WS 3.2 ViewSwitcher flipped 🟡 → ✅ — view switcher in section nav was overlooked; WS 3.8 banner aligned 🔴 → ✅ since the feature shipped end-to-end this morning; sub-features 3.5a Print ▾ list-mode and 3.6a Kanban column fold confirmed already delivered inside still-🟡 parent WS rows; remaining real Group-A gaps narrowed to 5: 3.4a column-resize JS, 3.4b row buttons rendering, 3.5b Duplicate/Archive bulk + custom-action injection, 3.6b quick-create inline mini-form with group-value prefill, 3.6c dual drag mode workflow vs field-write branch. Banner reads `3.1 🟡 · 3.2 ✅ · 3.3 ✅ · 3.4 🟡 · 3.5 🟡 · 3.6 🟡 · 3.7 🔴 · 3.8 ✅`. Earlier today: UI revamp sub-roadmap — Phase 3 row gained WS 3.8 SearchPanel date-bucket group-by 🔴 — per-chip Year/Quarter/Month/Week/Day dropdown on date/datetime group-by chips with Month as default; backend `read_group` already supports the `field:granularity` wire format for year/month/week/day, only `quarter` needs adding. Same SYNC-BLOCK updated; status banner now reads `3.1 🟡 · 3.2 🟡 · 3.3 ✅ · 3.4 🟡 · 3.5 🟡 · 3.6 🟡 · 3.7 🔴 · 3.8 🔴`. Earlier today: UI revamp sub-roadmap — Phase 5 notifications row updated: WS 5.1 NotificationActionManager + WS 5.2 GeneralNotificationPane + WS 5.3 ApprovalNotificationPane flipped 🔴 → ✅ on branch `ui-revamp`; WS 5.4 ActivityNotificationPane reframed 🔴 → 🟡 — stub ported verbatim, waiting on a cross-record `/api/chatter` "activities-for-me" endpoint before the pane can be wired. Implementation surface: three sibling Radix Popovers in [`MainMenuManager`](../src/frontend/src/managers/MainMenuManager.tsx) — `ApprovalNotification` (always visible, no zero-pending gate) + `ActivityNotification` (stub) + `UserNotification` (SSE-live via `useEvent("web.client.notification")`). New runtime services `runtime.services.approval` ([ApprovalService](../src/frontend/src/core/services/ApprovalService.ts)) and `runtime.services.notification` ([NotificationService](../src/frontend/src/core/services/NotificationService.ts)) added to [`ServiceRegistry`](../src/frontend/src/core/contracts/services.ts) + wired in [`buildRuntime`](../src/frontend/src/react/runtime/buildRuntime.ts). SCSS at [`_notifications.scss`](../src/frontend/src/theme/components/_notifications.scss) — semantic tokens only, no Tailwind, no hardcoded colors. Same `ui-revamp-snapshot` SYNC-BLOCK updated; no change to Built Capabilities / Known Gaps on the platform surface — the revamp lives on a feature branch and has not landed on `main`. Earlier today: WS 5.5 Command Palette flipped 🔴 → 🟡 on commit `5a76bf5`; new `<!-- SYNC-BLOCK: ui-revamp-snapshot -->` region inserted between Status Snapshot and Built Capabilities so future revamp-banner updates have a stable target.) Previous sync: 2026-05-25 (Enhancement 04 ✅ Delivered — **all 3 slices landed same day**. **Slice C** (admin + settings + RBAC + guide): NEW `presentation.debug_view_strip` admin command in [`src/ede/foundation/presentation/models/presentations.py`](../src/ede/foundation/presentation/models/presentations.py) gated by `view.debug.read` RBAC permission (system_admin only or internal system principal); returns side-by-side `unstripped` vs `stripped` RenderPlans + the registered-predicate inventory (`view_names` + `env_methods`) so operators can answer "where did my field go?" — every gated element appears in unstripped but is missing from stripped. 2 new FoundationSettings (`PRESENTATION_PREDICATE_VALIDATION` default `strict` for boot-validation behavior on unknown predicates; `PRESENTATION_PREDICATE_DEBUG_LOG` default False for structured `[VIEW-STRIP]` WARNING log records) added at [`src/ede/foundation/settings.py`](../src/ede/foundation/settings.py). `_predicate_debug_log_enabled()` static helper threaded into all 4 production strip callsites (`get_view_plan` / `load_action` / `get_dsl_filters` / `_resolve_view_field_names`); admin debug command always forces `debug_log=True` regardless of the FoundationSettings flag. 1 new RBAC permission row `view.debug.read` in [`src/ede/foundation/presentation/data/ir.rbac.permission.csv`](../src/ede/foundation/presentation/data/ir.rbac.permission.csv) (system_admin only). NEW developer guide at [`docs/foundation-presentation-predicate-gates.md`](./foundation-presentation-predicate-gates.md) — comprehensive coverage of the two attributes, predicate registry, expression grammar (positional + keyword args, and/or/not, comparisons, literals; rejects bare identifiers / attribute access / lambdas / comprehensions), three first-party predicates with view-DSL + env-method tables, calling from Python, registering a new predicate (naming rules, signature contract, ownership rule), decision tree for visible-when vs invisible= vs RBAC vs readonly vs record-rule vs unit_scope, strip pipeline, fail-closed semantics, boot validation behavior, admin debug tool, settings + RBAC tables, common gotchas. 11 new pytest cases for the debug command (`src/tests/foundation/test_debug_view_strip.py` — TestRbacEnforcement × 4 (admin allowed / system principal allowed / non-admin forbidden / no-principal forbidden), TestPayloadValidation × 2 (missing view_id / unknown view_id), TestSideBySideOutput × 3 (unstripped keeps gated / stripped removes gated / unstripped is deep copy independent of stripped), TestRegisteredPredicateEnumeration × 2 (view_names + env_methods listed / registry without substrate returns empty lists)). **2791 pytest + 564 vitest** full suite green; zero regressions. **Deferred to a follow-up perf enhancement** (tracked as a 🟢 Known Gap when scale demands it): in-process render-plan LRU cache + `(view_id, principal_fingerprint, settings_version)` cache key + 3 invalidation hooks + Prometheus hit-rate metric. Per design Section G "Stale-browser-after-write is acceptable for admin-toggled keys (rare flips)" — strip pass runs per request and is correct without cache; the perf enhancement lands when profiling shows the strip cost matters at scale. **Three downstream consumers unblocked**: foundation.base Enh 04 Slice 3 (`base.allow_multiple_units` toggle hiding the unit pickers via `visible-when="config('base.allow_multiple_units')"`), foundation.base Enh 05 (reactive admin form for user-org-unit assignment), foundation.security Enh 01 (`BranchSwitcher.tsx` server-side visibility). Built Capabilities row added for Enhancement 04 with full file inventory; Known Gaps row replaced — the "Enhancement 04 ✅" gap entry from the prior sync was removed and replaced with a new 🟢 Low-backlog row tracking the deferred cache work. Prior in-session syncs: Slices A + B of Enhancement 04 (Python substrate + DSL wiring + 92 pytest cases). **Slice B** (DSL wiring): RelaxNG `gate-attrs` shared define + `<ref name="gate-attrs"/>` on 13 gateable elements (form-field / form-section / form-page / form-button / form-statusbar / DynamicProperties / list-field / search-field / search-filter / search-groupby / kanban-field / KanbanRibbon / kanban-button / settings-section) at `src/ede/core/services/presentation/dsl/schemas/view.rng`. `_extract_gate_attrs` helper + mutual-exclusion enforcement (`visible-when` + `hidden-when` on same element = `DslParseError`) in `src/ede/core/services/presentation/dsl/parser.py`. NEW `src/ede/core/services/presentation/predicate_expression.py` — dedicated safe AST evaluator (not reusing `formula/safe_eval` because predicates need keyword args; allows positional + keyword call args, `and`/`or`/`not`, comparisons `==`/`!=`/`<`/`<=`/`>`/`>=`, string + numeric + bool/None literals, parens; rejects bare identifiers / attribute access / lambdas / comprehensions / subscript / **kwargs splat). 3 dedicated exception types: `PredicateExpressionError` (syntax/disallowed-node), `UnknownPredicateError` (carries `available` list for helpful messages), `PredicateExecutionError` (wraps the cause). `collect_predicate_names(expr)` — syntax-only static walker that enumerates every referenced predicate name without executing — used by boot validator. NEW `src/ede/core/services/presentation/strip_pass.py` — form/list/search/kanban strip walkers; visible-when=False → strip; hidden-when=True → strip; fail-closed on syntax error / unknown predicate / predicate exception (better to lose a field from the UI than leak it); `_snapshot_predicates(env)` queries the registry once at entry so predicates registered later won't be picked up mid-pass; subtree-strip semantics (section/page with visible-when=False removes its entire descendant tree); optional `debug_log=True` emits structured `[VIEW-STRIP]` records at WARNING level; `validate_render_plan_predicates(plan, registered_names=...)` returns the list of validation errors for boot-time use. Wired into 4 parse sites in `src/ede/foundation/presentation/models/presentations.py` (`get_view_plan` / `load_action` / `get_dsl_filters` / `_resolve_view_field_names`) — strip runs AFTER `compose_view_xml`'s `<extend ref>` xpath patches are folded in, BEFORE the RenderPlan is shipped to React. **53 new pytest cases**: 25 expression evaluator (`src/tests/core/test_predicate_expression.py` — truthy/falsy calls, and/or/not, ==/!=, keyword default, literal True/False, unknown predicate, syntax error, empty expr, bare identifier rejected, attribute access rejected, lambda rejected, list-comprehension rejected, predicate exception wrapped, name collection across composed expressions, name collection on nested kwargs); 17 parser gate-attr extraction (`src/tests/foundation/test_dsl_parser_gate_attrs.py` — form-field / form-section / form-notebook-page / form-button / form-statusbar / form-DynamicProperties / list-field / search-field / search-filter / search-groupby / kanban-field / KanbanRibbon + mutual-exclusion rejection on form-field / list-field / search-filter + backward-compat None slots when no gate); 11 strip pass (`src/tests/foundation/test_strip_pass.py` — visible-when True keeps, False strips; hidden-when inverse; section subtree strip; notebook page strip; button strip; value comparison; composed and/or; list/search/kanban; fail-closed on unknown / exception / hidden-when error / env-without-registry; idempotence on no-gates and legacy views); 4 boot validation (all-known returns empty, unknown predicate reported with view-id + name, syntax error reported, walker reaches nested sections/pages). 5 pre-existing tests (statusbar / DynamicProperties / kanban) updated to absorb the new `visible_when`/`hidden_when: None` slots in parser output. **2780 pytest + 564 vitest** full suite green; zero regressions. Slice C still 🔴 (cache + admin debug + 2 settings + 1 RBAC perm + guide + final ✅ flip). Prior sync this session: Slice A landed (Python substrate — decorator + Registry + Env.__getattr__ + 3 first-party predicates + 39 pytest cases). `@api.view_predicate(view_name=, env_method=)` decorator at `src/ede/core/api.py` (mirrors `@api.extend_model` boot pattern — auto-registers via `runtime.ensure_registry()` when `auto_register_models=True`; tests register explicitly). Registry storage at `src/ede/core/registry.py` with 4 methods (`register_view_predicate` / `get_view_predicate_by_view_name` / `get_view_predicate_by_env_method` / `list_view_predicate_*`) + 2 new exception types (`DuplicateViewPredicate` / `InvalidViewPredicate`) at `src/ede/core/errors.py`. `Env.__getattr__` dynamic verb-prefixed method binding at `src/ede/core/env.py` — predicates registered via `env_method` become directly callable as `env.<method>(arg, **kwargs)`; pre-construction safety via `self.__dict__.get("registry")` avoids recursion; private names skip predicate lookup. 3 first-party predicate files in `src/ede/foundation/base/services/predicates/`: `config.py` (system/org/unit-scoped `ir.config` lookup with `defValue` fallback) / `roles.py` (CSV-OR check against `principal.role_codes`, defensive on empty/missing-principal) / `team_roles.py` (`<team_code>.<role_code>` pairs via `TeamRoleService.is_assigned` with per-call team-lookup cache + defensive try/except around every external call so the render never crashes on lookup failure). Auto-imported via `foundation.base/services/__init__.py` so decorators fire at boot. **39 new pytest cases**: 22 registry + decorator + Env binding (`src/tests/core/test_view_predicate_registry.py` — decorator metadata + validation, double-registration rejection by view_name AND env_method, idempotent re-registration of same fn, lookup, list sorted, Env getattr resolves registered methods, passes kwargs through, returns bound callable with descriptive `__name__`/`__qualname__`, unknown method raises with helpful "Registered view-predicate env methods: [...]" listing, private attributes skip predicate lookup); 17 first-party predicates with real DB integration for team_roles (`src/tests/foundation/test_view_predicates_builtin.py` — config returns defValue when settings_registry None, roles handles empty/missing-principal/whitespace gracefully, team_roles full DB integration with seeded Team + TeamRole + Assignment for assigned-true / unassigned-false / CSV-any-match / malformed-pair-skipped / unknown-team-false / no-principal-false / empty-input-false). **2719 pytest + 564 vitest** full suite green; zero regressions despite Env `__getattr__` addition (highest-risk change). **Remaining slices**: Slice B (DSL parser changes + RelaxNG `gate-attrs` shared include + strip-by-predicate RenderPlan compile pass + boot-time validation walking every view expression + runtime fail-closed) + Slice C (principal-fingerprint cache + cache invalidation hooks on `ir.config` / `ir.rbac.role.binding` / `res.team.role.assignment` writes + admin debug controller at Settings → Technical → Views → Debug + 2 new FoundationSettings + 1 new RBAC permission + developer guide at `docs/foundation-presentation-predicate-gates.md` + final ✅ flip across 4 sites). Hard consumers still blocked on Slice B+C: foundation.base Enh 04 Slice 3, Enh 05, foundation.security Enh 01 BranchSwitcher. Prior sync this session: Enhancement 04 🔴 Not Started authored — **Predicate-Gated View Rendering**. New `visible-when="..."` / `hidden-when="..."` DSL attributes uniform across every renderable element (form + list + search + kanban + settings). Function-call expression syntax via the existing safe AST evaluator. New `@api.view_predicate(view_name=..., env_method=...)` decorator with **modular per-module ownership** — each predicate registered by its owning module, dual-bound to view DSL AND a direct env method with verb-prefix encoding the return-shape (`get_*` returns value, `has_*` returns bool). 3 first-party predicates ship Phase 1 (`config`/`roles`/`team_roles`) all in `foundation.base/services/predicates/`. Engine: strip-by-predicate pass in RenderPlan compiler (AFTER `<extend ref>` inheritance composition, BEFORE serialization), principal-fingerprint cache key, cache invalidation on `ir.config` + `ir.rbac.role.binding` + `res.team.role.assignment` writes, boot-time validation (unknown predicate → fail-loud with file:line), runtime fail-closed (defense in depth). Admin debug tool at Settings → Technical → Views → Debug (gated by new `view.debug.read` RBAC permission). 2 new FoundationSettings (`PRESENTATION_PREDICATE_VALIDATION` default `strict`, `PRESENTATION_PREDICATE_DEBUG_LOG` default False). Reuses the safe AST evaluator from `foundation.base` / `foundation.dataset` Phase 2 — no new expression engine. Schema extension via shared `<define name="gate-attrs">` referenced from every gateable element — additive, back-compat preserved. Three downstream consumers blocked on this: `foundation.base` Enh 04 Slice 3 (hide unit pickers when `base.allow_multiple_units=False`), `foundation.base` Enh 05 (reactive admin form), `foundation.security` Enh 01 (BranchSwitcher visibility). Reusable skill drafted at `.claude/skills/using-predicate-view-gates/SKILL.md`. ≥25 pytest cases planned. Implementation pending HARD GATE approval. Prior sync: 2026-05-13 (Enhancement 03 🔴 Not Started authored — **Load-Time View Referential Validator**. Moves "missing `ir.action` for inline relational target" / "field-name typo" / "broken `ref=`" from runtime to `ede migrate upgrade` time. Motivated by the Phase 4 qa-automation rollout where operators hit `"No ir.action seeded for qa.usecase.step"` in the browser instead of at upgrade time. New `view_referential_validator.py` service under `src/ede/foundation/presentation/services/` runs after RNG; new `validate_all_views(env) -> ValidationReport` aggregates errors into one report; wired into `ede migrate upgrade` post-data-load step; new `PRESENTATION_VIEW_VALIDATION` foundation setting (`strict` blocks · `warn` logs · `off` skips) shipped to the foundation-settings table; new `ede presentation validate-views [--tenant <name>]` CLI subcommand for CI. RNG kept in place; runtime `presentation.load_action` keeps its existing fallback warning as a last-mile safety net. Implementation deferred to a dedicated session.

Earlier: 2026-05-12 — Enhancement 02 — M2O templated display + multi-field name search — flipped 🔴 → ✅ Delivered same day. Built Capabilities row added; Known Gaps row removed. Hand-authored prose section "M2O templated display + multi-field name search" added with model-declaration syntax, XML-override syntax, wire format, frontend precedence, search-domain shape table, out-of-scope list, implementation map. Three new "things developers commonly get wrong" entries added. 1589 pytest + 494 vitest green; bun run build clean.). To refresh, invoke the `syncing-roadmap-to-docs` skill.*
